# 仓库拓扑

> **活文档** · v1.5 · 2026-08-31（新增「动手前照做」一节：六条操作事实从七份日志的坑单归并到此，
> 那些日志已冻结、不再是入口，事实留在里面等于失传）。v1.4 于 2026-08-30 晚（**私有桶多了一个写的一侧**：channel-svc 把用户发来的图取下来落桶，
> 键 `uploads/{content_sha256}/original.{ext}`——起 channel-svc 从此要带 OSS 凭证。仓数不变仍十一仓。
> v1.3 于同日（**reportrender 建立服务**：Temporal worker，队列 `reportrender-activities`，
> activity `report-book-render` 已注册——裁决 2026-08-29「后续报告产出上线时建立服务」的触发条件到了；
> 新增**私有对象存储**：阿里云 OSS 私有桶，报告册与用户上传件的落点）。
> v1.2 于 2026-08-30（genpipe-svc 起 HTTP 入站面 :8104，报告两条线接通；
> v1.1 于 2026-08-29 晚新增 `ishome-reportrender`，十仓→十一仓；建档于 2026-08-28，内容承接自 CLAUDE.md 初版拓扑表）。
> 拓扑变更（新仓/新服务/新队列/端口变化）**就地更新本文并升版本行**，不发追记——本文是"仓清单 + 本地栈"的单一真源。
> 服务级架构口径以《方案/架构对齐-设计Agent×技术架构.md》V1.5 为准；本文只记事实清单。

## 十一仓清单（github.com/tommax-bai，全部私有）

| 仓 | 内容 |
|---|---|
| `ishome`（本仓） | 中控/文档 |
| `ishome-contracts` | 跨仓契约：proto/buf、注册表（activities、task_queues、artifacts、rulebook 契约面）。跨仓接口变更走此仓发版；**activity 注册名与事件 type 只增不改** |
| `ishome-backend` | Java Gradle 多模块 monorepo（estate/content/trade/identity/channel/c-bff/admin-bff/project-svc 等，ArchUnit 守门）；报告域种子与脚本在 `services/project-svc` 下 |
| `ishome-aipipe` | Python uv workspace：chat-svc、genpipe-svc、genpipe-worker（import-linter 守门）。genpipe-svc 两个进程入口：`genpipe-workflow-worker`（Temporal workflow，队列 `genpipe-workflows`）与 **`genpipe-http`（入站面 :8104，成文线派发入口 `POST /api/v1/genpipe/reports`）** |
| `ishome-render2d` | 绘图独立仓：确定性 2D（plan-2d-render），独立 Temporal worker |
| `ishome-imagegen` | 绘图独立仓：生成式（atmosphere-visual、realism-pass），独立 Temporal worker |
| `ishome-render3d` | 绘图独立仓：三维（scene-compile、base-render），独立 Temporal worker |
| `ishome-reportgen` | 报告成文线独立 Temporal worker（队列 `reportgen-activities`） |
| `ishome-reportrender` | 报告渲染层（pages + 数据包 → 单册自包含 HTML → 私有桶）：**确定性、零 LLM**。**已成服务**（2026-08-30 晚）：独立 Temporal worker，队列 `reportrender-activities`，activity `report-book-render`（contracts #14）。CLI 保留为本地迭代入口，与服务那条路由 import-linter 结构性分开 |
| `ishome-frontend` | 前端 TS |
| `ishome-infra` | 本地栈/LiteLLM 网关等基础设施 |

绘图三仓为 V1.4 物理拆分裁决产物（逻辑异质→物理隔离）；`ishome-reportrender` 同一判据的第三次应用（渲染确定性 vs 成文 LLM），裁决见《规范/落地假设与拍板清单.md》§〇。**"独立仓 ≠ 独立服务"这条仍成立，只是渲染层不再是它的例子**：仓在 2026-08-29 先立，服务在 2026-08-30 晚按写死的触发条件（报告产出上线）才建——两个判据分列、先后落地，正是当时那条裁决要的形态。绘图三仓仍在"有仓无服务"这一档。

## 质量门与本地栈

- 各代码仓质量门在本地 pre-push（新 clone 需 `git config core.hooksPath .githooks`）；GitHub CI 暂缓（Billing 未修）。
- 本地栈 `ishome-dev` 常驻：PG :15432（Flyway schema-per-service）、Temporal :7233 / UI :8233（namespace `genpipe`）、Redis :16379、LiteLLM 网关 :4000（infra/litellm，阿里云百炼 qwen）。
- 服务端口：channel-svc :8102（gRPC :9102）、project-svc :8103、**genpipe-svc 入站面 :8104**。报告一键出全册的进程组＝genpipe-http + genpipe-workflow-worker + reportgen-worker（队列 `reportgen-activities`）+ project-svc，缺一即整条线断在不同的地方。
- 凭证一律在 `~/.ishome/`（飞书 channel-feishu-local.yml、LLM llm-local.env、火山引擎 volcengine-local.env、**OSS oss-local.env**），不入库；服务器对应 `/opt/ishome/env/`。
- **私有对象存储**（2026-08-30 晚裁决）：阿里云 OSS 私有桶 `ishome`（华北2 北京，端点 `oss-cn-beijing.aliyuncs.com`）。用户上传的户型图、解析产物、报告册、为他生成的图**一律进这只桶**；公开内容另一只桶（红线：分桶不分前缀）。对外一律**签名链接**，由 OSS 域名直出、自带有效期——**本项目一个公网端口都不用开**。键的真源＝contracts `registries/object_keys.md`。
  **写的一侧现有两个**：reportrender（册，`reports/{report_id}/book.html`）与 channel-svc
  （用户上传件，`uploads/{content_sha256}/original.{ext}`）——起 channel-svc 要带 OSS 凭证，
  否则文字会话照常、收到图时响亮失败。
- **出全册的进程组因此多一个**：genpipe-http + genpipe-workflow-worker + reportgen-worker + **reportrender-worker** + project-svc。缺 reportrender-worker 时整条线**卡在重试直到超时**（不是当场报错），而两侧单测全绿——2026-08-30 晚真踩过。
- 本机网络坑（Clash 代理 7890、SSH 22 劫持走 443、镜像配置、`gh` 偶发 EOF 重试）见《交接/落地日志-2026-08-23.md》§四；中控与 contracts 仓已配仓库级 ssh:443 改写。

## 动手前照做（操作事实，2026-08-31 从七份日志的坑单归并到此）

不是规矩，是**下次照做才不会卡住**的操作事实；随环境变，就地改。

- **`./gradlew` 默认拿 Java 8**：起任何 Java 服务前 `export JAVA_HOME=/opt/homebrew/opt/openjdk@21`，否则 bootRun 起不来。
- **改 LiteLLM 网关配置要重启才认新逻辑名**，而 :4000 是常驻网关。新逻辑名的第一次真跑**另起临时端口**
  （`infra/litellm/run-dev.sh 4001`），跑完停掉，常驻的不动。
- **重启任何共用服务前先看有没有在途工作流**：`temporal workflow list --query 'ExecutionStatus="Running"'`。
- **`temporal workflow result` 把空列表打印成 `null`**：机器消费一律取 Temporal **历史事件 payload**，不取它的打印。
- **报告真跑前三项确认**：网关活着（对应逻辑名在不在）、台账环境变量设了没、release 是不是最新
  （改完种子必须 import + publish 才生效）。
- **dev 服务器同机跑着别人的生产服务**（121.89.85.150 上有 aidcp×3、isales×4）：动之前备份、动之后逐个核验邻居仍 active。
