# 仓库拓扑

> **活文档** · v1.1 · 2026-08-29 晚（新增 `ishome-reportrender`，十仓→十一仓；建档于 2026-08-28，内容承接自 CLAUDE.md 初版拓扑表）。
> 拓扑变更（新仓/新服务/新队列/端口变化）**就地更新本文并升版本行**，不发追记——本文是"仓清单 + 本地栈"的单一真源。
> 服务级架构口径以《方案/架构对齐-设计Agent×技术架构.md》V1.5 为准；本文只记事实清单。

## 十一仓清单（github.com/tommax-bai，全部私有）

| 仓 | 内容 |
|---|---|
| `ishome`（本仓） | 中控/文档 |
| `ishome-contracts` | 跨仓契约：proto/buf、注册表（activities、task_queues、artifacts、rulebook 契约面）。跨仓接口变更走此仓发版；**activity 注册名与事件 type 只增不改** |
| `ishome-backend` | Java Gradle 多模块 monorepo（estate/content/trade/identity/channel/c-bff/admin-bff/project-svc 等，ArchUnit 守门）；报告域种子与脚本在 `services/project-svc` 下 |
| `ishome-aipipe` | Python uv workspace：chat-svc、genpipe-svc、genpipe-worker（import-linter 守门） |
| `ishome-render2d` | 绘图独立仓：确定性 2D（plan-2d-render），独立 Temporal worker |
| `ishome-imagegen` | 绘图独立仓：生成式（atmosphere-visual、realism-pass），独立 Temporal worker |
| `ishome-render3d` | 绘图独立仓：三维（scene-compile、base-render），独立 Temporal worker |
| `ishome-reportgen` | 报告成文线独立 Temporal worker（队列 `reportgen-activities`） |
| `ishome-reportrender` | 报告渲染层（pages + 数据包 → 单册自包含 HTML）：**确定性、零 LLM**，首版**纯库 + CLI，不起服务**；将来接编排的 activity `report-book-render`、队列 `reportrender-activities`（未注册） |
| `ishome-frontend` | 前端 TS |
| `ishome-infra` | 本地栈/LiteLLM 网关等基础设施 |

绘图三仓为 V1.4 物理拆分裁决产物（逻辑异质→物理隔离）；`ishome-reportrender` 同一判据的第三次应用（渲染确定性 vs 成文 LLM），裁决见《规范/落地假设与拍板清单.md》§〇。**独立仓 ≠ 独立服务**：本仓与绘图三仓不同，暂不含 Temporal worker。

## 质量门与本地栈

- 各代码仓质量门在本地 pre-push（新 clone 需 `git config core.hooksPath .githooks`）；GitHub CI 暂缓（Billing 未修）。
- 本地栈 `ishome-dev` 常驻：PG :15432（Flyway schema-per-service）、LiteLLM 网关 :4000（infra/litellm，阿里云百炼 qwen）。
- 凭证一律在 `~/.ishome/`（飞书 channel-feishu-local.yml、LLM llm-local.env、火山引擎 volcengine-local.env），不入库。
- 本机网络坑（Clash 代理 7890、SSH 22 劫持走 443、镜像配置）见《交接/落地日志-2026-08-23.md》§四；中控与 contracts 仓已配仓库级 ssh:443 改写。
