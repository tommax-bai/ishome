# 交接文档 · 户型图解析与出图启动（"陌生人能拿自己的户型图换到三张图"）

> 本文是**获客线的专项入口**，与报告主线真并行（主线入口＝《交接文档-2026-08-30.md》）。
> 本线消费**户型图**产出**几何与户型特征**，主线消费户型特征产出**报告内容**——接口只有"特征"这一处，互不阻塞。
> 纪律：计划项一律用**事项名**，不用纯序号（CLAUDE.md 文档纪律，用户裁决 2026-08-29）。
> 启动裁决见《规范/落地假设与拍板清单.md》§〇 末条（2026-08-30：上传驱动 + 三步 + 户型库排最后）。

---

## 〇、一句话版

**产品的获客半边至今一行业务代码都没有：系统能给一个已经在对话里的人写报告，不能吸引一个陌生人进来。** 本线要做的：**用户上传户型图 → 解析成结构化户型 → 出三张图（免费，引流）→ 同一份解析结果喂进付费报告**。做完第一步的验收：**一个陌生人上传自己的户型图，换回三张能发朋友圈的图，并且他后面买的报告里写的是"你这套户型"，不是通用建议。**

用户裁决的三步顺序（顺序即先后关系，不得跳步）：

| 步 | 事项名 | 做完之后成立的事实 |
|---|---|---|
| 一 | **干净图能出图** | 质量说得过去的上传图，能稳定走完解析→三张图→喂报告全程 |
| 二 | **脏图也能出图** | 歪斜/水印/模糊/截图/低分辨率的图，门槛往下推一档 |
| 三 | **同户型能复用** | 脱敏回流户型库、同户型命中直接复用、排产预生成 |

**户型库不是起点是终点**——原口径"库命中优先、上传解析兜底"已由本次裁决改判为上传驱动为主（对照留在拍板清单）。

---

## 一、现状与输入

| 项 | 在哪 | 状态 |
|---|---|---|
| 解析任务的注册名与入口 | 非绘图任务侧（`floorplan-parse`，契约注册表已登记） | **存根**，无实现 |
| 三张绘图任务的注册名与队列 | 契约注册表（`plan-2d-render` / `atmosphere-visual` / `realism-pass`） | 名字与队列已定、**只增不改**；三个仓全是骨架（各约 130 行，无实现） |
| 任务编排底座 | 生成侧编排（Temporal） | **跑通**：任务路由、重试/超时、机检门禁状态机（多候选→打分→一致性→合规→发布，超限如实失败绝不假成功）；今日已起 HTTP 入站面，报告线由此一键出全册 |
| 户型特征进报告的通道 | 报告数据包的匿名画像（`layoutFeatures`，**契约必填**） | **口子留好了，一直传空** |
| 吃户型特征的规则 | 六域知识库 | **已有一批、从没触发过**：阳台带家政区讲清洁动线、卧室朝东讲遮光率、U 形厨房讲双人操作宽度、西晒房间避高光石材 |
| 三张图的定义与模板体系 | 《方案/第一阶段视觉提案与Prompt说明.md》＋《方案/架构对齐-设计Agent×技术架构.md》§七 | 默认组合＝**情绪图 → 功能说明图 → 彩铅生活草图**；模板＝风格参考图＋信息层级＋文案口吻＋负面约束，**顺序与组合是配置不是架构约束** |
| 几何唯一源的口径 | 同上 §7.1 | **母版**由确定性 2D 绘制产出，所有风格图**强制回读母版**，几何零漂移；确认底图与母版是同一管线的两次调用 |
| 户型图解析方法论 | 《方案/户型图解析方法论.md》 | 已成文，未验证 |

**本线不用等任何人**：上述输入全部就位或明确为空，第一步的验证不依赖主线任何在途工作。

---

## 二、接手第一件事：五个待拍板项

**① 图上文字怎么合成（原技术架构待拍板⑩，倾向已写在案）**。三张图上有 30 多条精确中文（标题、房间名、批注、贴士板）。让图像模型排版＝逐条乱码率相乘，整图通过率会很低，且**改一句文案就得重生成整图**，改图机制形同虚设。方案文档倾向：**图像模型只画图形层（场景、留白面板），文字由确定性排版层用手写风字体叠加**——通过率大幅提升、文案可独立修改，代价是"真实手写感"折损。**开工前必须拍**，它决定出图管线的形状。

**② 权益档不够用**。现在只有"公开内容免费 / 深化产物付费"两档，三张图是**私有但免费**（用户上传驱动＝私有产物，见红线三），需要补一档并进权益登记表。

**③ 上传资产的保留与授权**。用户上传的户型图、解析结果留多久，用户删号后怎么处理；**是否允许脱敏后回流户型库**（第三步"同户型能复用"的前置，不拍它第三步无从做）。属隐私口径，早定省返工。

**④ 图上没有的字段从哪来**。户型特征里有几项**图上读不出来**：朝向（决定遮光/西晒那批规则）、楼层、窗台高度。三条路：会话里问一句、楼盘数据带、先缺席。**建议**：朝向走会话问一句（一句话说得清的问题留 IM，符合采集分界），其余先缺席——缺席时相关规则不触发，是正确行为不是降级。

**⑤ 解析怎么实现**（视觉模型直出结构 / 传统视觉 + 模型混合 / 分步）。**建议不预拍**：第一个事项就是拿真实图验证，验证结果出来再定，比现在猜准。

---

## 三、本线的红线（违反即返工）

1. **入口判据必须确定性，不许模糊降级**：解析要么给出可用结构，要么**响亮失败并说明缺什么**；禁止把没把握的图硬解成低置信结构往下游传——下游是几何母版与付费报告的输入，一次静默降级污染整条链。"过拦与漏拦同样是失效"已在报告线踩过两次。第二步要做的是把门槛**往下推**，不是把门槛**拆掉**。
2. **公开与私有严格分轨**：用户上传的户型图、解析结果、三张图**一律私有产物**，不进公开内容库、不被其他用户复用；私有 OSS 桶 + 临时凭证，防盗链策略与公开内容不同。想要复用红利，只能走裁决过的脱敏回流路径（第三步）。
3. **几何零漂移**：所有风格图回读母版，图面表达不得修改设计层。母版同时输出机器可读层（房间遮罩、墙体图层），既做生成条件控制，也让"户型有没有漂"从模型判断变成可量化门槛。
4. **生成侧不知用户是谁**：图上、批注里不得出现任何用户标识；个性化只能来自结构化事实，不得凭空捏造（报告线已有真跑教训：模型给一个没有家庭构成字段的画像编出了"孩子"）。
5. **会话侧与绘图服务永不直接交互**：上传入口在会话侧，派发经项目侧铸任务，产物就绪再回到会话呈现。这条是既有边界，不因上传驱动而破。
6. **无人工介入**：不得靠人工标注户型、不得转人工兜底。判据是**人力会不会随用户量增长**——开发期录几套户型做验证不算（不随用户量增长），上线后每张图要人过一眼就算。
7. **失败要说人话**：解析不了的图，要告诉用户**为什么不行、怎么重拍**，一次为限的改期口径沿用既有失败路径；**禁止**"我去核实一下"这类假承诺话术。

---

## 四、事项序列

### 第一步 · 干净图能出图

| 事项 | 内容 | 做完之后成立的事实 |
|---|---|---|
| **解析能出结构** | 拿真实户型图（**先只取质量说得过去的**）验证解析：房间边界、墙体、门窗、面积。同批定入口判据（红线一） | 一张干净图稳定产出结构化户型；不合格的图响亮失败并说明缺什么 |
| **特征能被推出来** | 从几何推出触发字段：厨房形态、阳台类型、干湿分区可行性、玄关形态、床头墙候选、暗房间；图上读不出的按待拍板④处置 | **那批睡着的规则第一次被触发**；报告里出现"你这套户型"的内容 |
| **母版能画出来** | 确定性 2D 绘制：母版 + 机器可读层（房间遮罩、墙体图层） | 几何唯一源出现，后续所有图回读它 |
| **三张图能出来** | 按模板库组合出情绪图、功能说明图、彩铅生活草图；文字层按待拍板① 的形态合成 | **陌生人上传户型图能换回三张图** |
| **解析结果喂进报告** | 户型特征填进报告求值线入参（口子现成，一直传空） | 免费到付费的衔接第一次成立：他买的报告写的是他家 |

**这一步不需要**：户型库、排产、公开内容发布、三维与漫游、支付。

### 第二步 · 脏图也能出图

歪斜校正、水印与截图干扰、低分辨率、拍摄畸变——**逐类立项，每类要有真实样本**（同报告线"反例只收真跑样本"纪律）。验收＝入口判据的门槛下推一档，且**失败仍是响亮失败**。

### 第三步 · 同户型能复用

脱敏回流户型库、同户型命中直接复用、排产预生成、命中不了的回流排产信号。**前置＝待拍板③ 的授权口径**；这一步做完，边际成本才开始下降。

---

## 五、坑（给接手 session）

1. **绘图三仓是空壳但名字已定**：任务注册名与队列在契约注册表里**只增不改**，实现照名字填，别另起新名。
2. **解析任务不在绘图仓**：它是非绘图任务，归生成侧 worker——绘图三仓的拆分判据是绘图逻辑异质，解析不属于。
3. **图框免责文案的挂载在这条线收口**：规范 v2.6 把册内免责收敛进了报告册尾附录，**图纸图框侧的免责挂载明写"等绘图线出第一张图纸时另议"**——这条线出第一张图时要回去把它定掉（图框那条已有正文，可直接用）。
4. **文字层别顺手交给图像模型**：待拍板① 没拍之前不要开出图管线，两种形态的管线结构不一样，返工代价大。
5. **朝向不在图上**：遮光率、西晒那批规则依赖朝向，图上读不出。缺席时它们不触发是正确行为——**别为了让规则触发去猜朝向**（数字与关系都不由模型决定，同族纪律）。
6. **三张图的顺序与组合是配置**：默认组合来自真实生成实验（情绪图打头），不是架构约束；要调顺序改配置，别改管线。
7. **成本结构变了要心里有数**：上传驱动＝每来一个用户跑一次解析加三次出图，成本随用户数线性增长；预生成的复用红利要到第三步才兑现。这是裁决时已明示的代价，不是设计缺陷。
8. 本机环境坑（代理、镜像、SSH 改写）见《交接/落地日志-2026-08-23.md》§四；**push 时机由用户定**；中控仓只放文档，代码只进对应代码仓。

---

## 六、与主线的接口（谁等谁）

- **不等主线**：报告线今天已能一键出全册，本线第一步不碰它的任何在途工作。
- **主线等本线的**：户型特征。特征一进来，报告从"通用专业建议"变成"你这套户型的分析"——那批一直没触发过的规则是现成证据。
- **共用不冲突**：本线只往报告求值线的入参**填一个一直传空的字段**，不改内容大脑结构。
- **别动主线的东西**：报告域的种子、判据、写作与渲染本线只读。

*交接文档 · 户型图解析与出图启动 · 2026-08-30 · 执行者 Claude。正文定稿后不改，增量以文末"追记 N"追加。*

---

## 追记一（2026-08-30，执行者 Claude）：图上文字合成——倾向已定，待一次实测收口

正文 §二 待拍板项①（图上文字怎么合成）经与用户过一轮后**降级为"倾向已定、待实测收口"**，
不再是开工前必须拍的板。用户批准的是**这个处置**（把倾向写死、把要验的数写死），
**不是替他拍死技术方案**——实测结果与倾向相左时按结果改判。

### 一、倾向：图像模型只画图形层，文字确定性叠加

理由不变（乱码率相乘 + 改一句文案要重生成整图 = 改图机制形同虚设），新增的是**问题规模被拆小了**。

### 二、三类文字，风险不同（本轮新增的分类，实现按此分道）

| 类别 | 内容 | 定位来源 | 对齐风险 |
|---|---|---|---|
| **版面区文字** | 标题、情绪总结、三块专业小贴士 | 模板声明的固定版面槽位 | **零**——与渲染一张卡片无异 |
| **房间标注** | 房间名、房间批注 | **母版的房间遮罩**（质心算锚点，几何计算不是估计） | 取决于生成图对母版的几何跟随度，见下 |
| **引线批注** | 起点在版面区、终点指房间 | 同上，但容错更大 | 小偏移仍读得对 |

### 三、房间标注的三层保障（顺序即执行顺序）

1. **结构条件生成**：以母版的线稿/分割图作生成条件，几何跟随性远高于纯文生图；
2. **叠字前先过遮罩比对门禁**：生成图与母版逐房间比对重合度，不达标**重生成**——**不硬叠**
   （这正是既有设计"把户型有没有漂从模型主观判断变成可量化门槛"的用途，见《方案/架构对齐-设计Agent×技术架构.md》§7.3-2）；
3. **过门后仍有小偏移**：做一次配准把标注坐标微调过去，确定性可算。

**失败形态因此是"重生成一次"，不是"文字歪了发给用户"**——与渲染层 fail loud 同族。

### 四、真正要用数说话的那件事（挂在第一步验收上）

**待实测＝生成图对母版的几何跟随度**（逐房间重合度分布 + 需要重生成的比例）。这个数在第一步
"母版能画出来"与首轮出图时顺带量得到，**量完再决定房间标注叠在生成图上还是留在功能说明图上**。
在这个数出来之前，**不许**为"生成图上叠房间名"预先付出配准链路的复杂度。

### 五、一个待确认的有利线索（若成立，问题规模再降一档）

《方案/第一阶段视觉提案与Prompt说明.md》里，**两个风格模板（温暖奶油之家、彩铅生活草图）都给了完整
生成 prompt，功能说明图没有**——它更像**母版加批注的确定性绘制产物**，不是生成式产物。若第一步确认成立：
**十一个房间名加六条批注全在功能说明图上，是画上去的不是叠上去的，坐标天然准确、零对齐问题**；
需要叠在生成图上的只剩标题、情绪总结与三块贴士，而它们全在版面区（零风险）。

**第一步"三张图能出来"的事项因此多一条验收**：明确功能说明图是确定性产物还是生成式产物，写进追记。

### 六、待拍板项状态更新（覆盖正文 §二）

- ① 图上文字合成 → **降级**：倾向已定（图形层与文字层分离），待几何跟随度实测收口；
- ②③④⑤ 不变（权益档补私有免费一档／上传资产授权与脱敏回流／朝向等图上没有的字段／解析实现路径）。
  其中 ⑤ 照旧建议**不预拍**——第一个事项就是拿真实图验证，验完再定比现在猜准。

---

## 追记二（2026-08-30，执行者 Claude）：首个真实样本实测 + 尺寸推算裁决

### 一、样本与方法（如实标注）

用户在会话中提供一张**楼书级户型图**（92㎡ 三房两厅一卫，矢量渲染、印刷级、带指北针与卖点文案，
标"建筑面积约 92m²"，**无任何分房间面积与尺寸标注**）。同批另一张为无关截图，未采样。

**方法＝视觉模型直读，不是跑解析器**——这是待拍板⑤ 里"视觉模型直出结构"那条路的一次零成本预演，
不是产品实现，结论按预演的份量读。**样本落库时点**：解析实现落仓时另存进该仓验证目录（现无落点，
中控仓只放文档）。

### 二、读到了什么（按置信度三档）

| 档 | 内容 |
|---|---|
| **读得出、可直接用** | 三房两厅一卫／建筑面积约 92㎡；房间清单（厨房·餐厅·客厅·主卧·次卧·小孩房·卫生间·玄关·阳台）；**厨房形态＝L 形且开放式**；**阳台带设备位**（疑似洗衣机）；**玄关＝侧入**；**四处飘窗**（厨房·小孩房·主卧·次卧，卖点文案"全卧观景飘窗"互证）；**朝向**（图上有指北针 + "南北通透"卖点） |
| **读得出但要核** | 卫生间干湿分区可行性（洗手台/马桶/淋浴同空间，可行性取决于尺寸而尺寸没有）；床头墙候选（图上是效果摆法，不等于结构可行） |
| **读不出（图上根本没有）** | **任何精确尺寸**——无开间进深、无墙厚、无门窗宽、无分房间面积，全图只有一个总面积 |

**直接印证的一件事**：读出的特征立刻决定哪些规则活——L 形不是 U 形，那条双人操作宽度的规则
**不触发是正确行为**；阳台有设备位，家政动线那条触发；朝向读到，遮光与西晒两条能活。

### 三、尺寸推算裁决（2026-08-30，用户）

用户原话——*"我们就是要基于总面积，然后基于这个户型图里面的面积尺寸，去推算出来他整个一个尺寸信息，
因为用户有的户型图基本上就是这样子的，个别的会有标注尺寸，有标注的我们可以用，没有标注尺寸的话我们就要推算"*。

**裁读**：几何层走**比例标定**——以图形轮廓的像素度量配合图上给出的面积，标定出比例尺，反推整套尺寸；
**有标注尺寸时优先用标注**，无标注即推算。**推算是主路径不是降级路径**（用户上传图的常态如此）。

### 四、连带必须定的四件事（Claude 提出，非用户裁决，可推翻但要给替代）

1. **标定基准要先分清建筑面积与套内面积**：图上标的通常是**建筑面积（含公摊）**，而图形画的是套内。
   直接拿建筑面积除以像素面积会**系统性偏大约一到两成**（得房率常见 75~85%，而得房率**不在用户的图上**）。
   优先级写死：**分房间面积标注 > 任一段尺寸标注 > 建筑面积配得房率区间**；用最后一档标定时，
   **产出的是带区间的尺寸不是确定值**，区间随得房率区间走。
2. **推算尺寸的身份必须写死，且下游分用途**：
   - **出图（母版与三张图）**：够用——图要的是比例正确、相对位置对，绝对值差几个百分点不影响观感；
   - **进报告**：推算尺寸的校准状态天然是**未校准**，且按判据属**定位数字**（业主或工人会拿它现场画线）
     ——因此**必须**随页挂依据标注 + 现场复核提醒（正是保留下来的那两条提示话术的用武之地，规范 v2.6 §7）；
   - **禁止**把推算尺寸当实测写进正文（"你家主通道净宽 900"式陈述）——那是用估算冒充事实，
     与"数字不由 LLM 决定／说了就必须标"同族违规。
3. **算术不由模型做**：模型只负责**识别房间与边界**，像素度量、比例换算、面积反推**全部由确定性代码算**
   （红线"数字不由 LLM 决定"的直接落点）。模型给多边形，代码给数。
4. **免费自检当门禁**：各房间面积之和 + 墙体投影 ≈ 图内轮廓面积，对不上即边界提取有问题，
   **响亮失败**（入口判据的具体形态之一）。多来源并存（分房面积 + 尺寸标注 + 总面积）时交叉校验，
   冲突就报出来不静默取一个。

### 五、订正一处：朝向能读，看图源

正文 §二 待拍板④ 写"朝向图上读不出、建议会话问一句"——**样本证伪**：这张楼书图有指北针且卖点写"南北通透"。
正确口径：**能读则读、读不到再问**。楼书级图通常带指北针，手机拍的简图未必有——它是**图源问题不是能力问题**。

### 六、第一步内部顺序建议（三步大顺序不动）

实测暴露特征层与几何层难度差一个量级，建议第一步内部**特征层先行**：
**特征能被推出来 → 解析结果喂进报告**（这两件今天的能力就够，且能立刻让报告变成"你这套户型"）
→ **母版能画出来 → 三张图能出来**（要先解决标定与得房率口径）。**验收不变，只调先后。**

### 七、样本缺口（下一批要什么）

现有样本只有**最干净的那一档**（矢量渲染、印刷级）。还缺两类，各自决定一件事：
**手机翻拍图**（第二步主战场，决定门槛能推多低）、**带尺寸标注的图**（决定标定优先级第一档现实中占多大比例）。

---

## 追记三（2026-08-30，执行者 Claude）：标定优先级升为四级，得房率降为兜底并定取值策略

用户拍板（*"可以按照这个方式去做"*），裁决全文与判据见《规范/落地假设与拍板清单.md》§〇 末条。
**本追记覆盖追记二 §四-1 的三级优先级**（那一版没有门洞反标定这一档）。

### 一、标定优先级（四级，从高到低）

| 级 | 标定物 | 说明 |
|---|---|---|
| 一 | **分房间面积标注** | 最准，直接标定 |
| 二 | **任一段尺寸标注** | 一段即可定比例尺 |
| 三 | **门洞反标定**（新增） | 入户门 900~1000／卧室门 800~900／厨卫门 700~800，**结构件按图纸画**、分布窄、**不需任何图外信息** |
| 四 | **建筑面积配得房率** | 兜底，产出带区间的尺寸不是确定值 |

**家具禁作标定物**：户型图免责声明通行写明家具"仅为示意参考"（首个样本实证），尺寸不可信；
门洞与墙体不在此列。**得房率由此从主路径降为最后兜底**，被真正用到的概率大幅下降。

### 二、得房率来源与取值

- **优先让用户提供**；未提供则用 **78%~83%**。
- **必须取点值处取下限 78%**（母版几何、图上要标的分房面积）。判据＝**错误方向代价不对称**：
  取大→尺寸偏大→业主按报告买的柜子装不进、拆改按不存在的余量做；取小→尺寸偏小→只是留了余量。
  **买小了还能用，买大了放不下。**
- **进报告的数字带区间**：78~83 对应的尺寸区间原样下发，**不取假精确点值**。这与推算尺寸的身份一致
  （未校准的定位数字，须挂依据标注 + 现场复核提醒，规范 v2.6 §7），区间形态系统天然支持。

### 三、78~83 是暂定值，校准路径已写死

得房率真实分布与楼型强相关（塔楼／板楼／小高层／洋房差异大），当前区间是经验值。
**每收一张带尺寸或分房面积标注的图，即反算该图的真实得房率入库**——数据是解析的免费副产品。
**区间订正的时点＝样本量足以按楼型分档时**；分档口径与样本量门槛届时用数据定，**不预设阈值**
（同判官批量的定法，"阈值有数据再定"一贯口径）。

### 四、影响面明示（防止误判优先级）

得房率的不确定是**整体缩放**，不改变房间之间的相对大小——**三张图基本不受影响**
（图上不标绝对尺寸，比例对了就够；除非功能说明图要标分房面积）。这个问题真正影响的是
**报告里的数字**，不是图好不好看。做排期时别把它当出图的阻塞项。

---

## 追记四（2026-08-30，执行者 Claude）：首批输入形态锁死为一种，两类样本后置及其三条连带

用户裁决：*"手机拍的图和带尺寸标注的先不管，先用这个图去做后面的工作，需要补充的手机拍的图和带标注图
这部分工作作为后续功能再做"*。**首批输入形态因此锁死为一种：楼书级矢量渲染、印刷级、无任何尺寸与
分房面积标注**（首个样本的形态）。三条连带推论如下，**都不是新裁决，是这条裁决的必然结果**，落此备查。

### 一、标定四级里，首版只实现后两级

优先级口径（追记三）**不变**，但手上没有前两级的样本 → **首版只实现门洞反标定与得房率兜底**，
分房面积标注与尺寸标注两级**留接口位不实现**。

判据是本项目踩过的原坑：**入册不等于生效**（三十条判据里只有八条有执行器，"表里 active ≠ 有人在拦"）。
实现一个从未被任何样本验证过的分支，等于给后来人一个看着像能用、其实没人跑过的路径。
**实现时点写死＝拿到带标注样本的那一刻**（与下条同一批）。

### 二、得房率的自举校准暂时没有输入

追记三写的校准路径是"每收一张带标注的图即反算真实得房率入库"——**带标注样本被后置，这条路径现在没有输入**。
后果如实记：**78~83 在相当长时间里是唯一形态，且无法自证准不准**；首版推算尺寸的**绝对值精度无法验证**
（相对比例不受影响，见追记三 §四）。

这不阻塞，但把两件事的必要性抬高了：**进报告的推算尺寸必须带区间**、**必须挂依据标注与现场复核提醒**
——精度不可自证时，诚实呈现是唯一正确形态（同报告线"坦白缺口"同族）。

### 三、喂报告的第一批是特征，不含推算尺寸

第一步事项"解析结果喂进报告"因此**拆成两半，先后分明**：

| 半 | 内容 | 何时做 | 理由 |
|---|---|---|---|
| **特征**（厨房形态／朝向／阳台类型／玄关形态／飘窗／暗房间…） | 现在就做 | 不经比例标定，**完全不受得房率影响**；进去就能让那批睡着的规则活 |
| **推算尺寸**（开间进深、通道宽、柜体投影…） | **等带标注样本能校准之后** | 绝对值精度暂时不可自证，此时进正文＝拿不可验证的数当依据 |

**出图不受此限**：母版与三张图只要相对比例，可照常推进（追记三 §四）。

### 四、后续项归属（触发条件写死）

- **手机翻拍图** → 归**第二步"脏图也能出图"**（已有归属，不新增）；
- **带尺寸／分房面积标注的图** → 新增后续项**"标定前两级实现 + 得房率校准起跑"**，
  触发条件＝**拿到第一批带标注样本时**，两件事同批做（实现前两级标定 + 反算真实得房率入库）。

---

## 追记五（2026-08-30，执行者 Claude + 两个并行子代理）：户型特征从图到报告的链路打通

用户批准开工（*"开始做吧，然后可以一起多个任务并行也可以，也可以启动 subagent"*）。本轮做完
**第一步里的"特征能被推出来"与"解析结果喂进报告（特征那一半）"**，四段链路各自落地并有真跑：

| 段 | 落点 | 事实 |
|---|---|---|
| 公共契约 | contracts `2790ff0` | 户型特征标记闭集（首版四条，每条都有规则消费）＋ 报告数据包新增触发规则字段（**非必填**，消费侧先建） |
| 解析 | aipipe `33bca24` / infra `2b34e94` | 一张图 → 特征标记 + 依据 + 观察区 + 读不出区；纯库 + CLI，**接线时点写死＝上传入口就绪时** |
| 求值线 | backend `6cc2950` | 快照读取投影补 rules、`always` 与 `layout_feature` 两类触发判定、触发条目按域进包；核验侧加闭集校验 |
| 成文线 | reportgen `582585a` + 待并行线带出的分档改动 | 收下条目、接进**叙事推导步**（不是写作步）、防照抄确定性校验、按触发类型分档权重 |

**一句话**：**那批 layout_feature 规则从来没有执行器，现在有了**——它们在库里、在 release 快照里躺着，
但求值线的读取投影没取 rules、触发判定不存在。这不是"填个字段"，字段在、执行器不在。

### 一、真跑结果

**解析侧**（样本＝追记二那张楼书级图，同参三次）：

- **产物层三次逐字完全一致**：只出 `balcony_service` 一条，依据＝"阳台区域图中画有洗衣机与柜体示意
  虚线框，表明设家政功能位"；**叙述层每次都变**（观察区 5/4/5 条、措辞不同）——下游只吃标记，故对
  下游而言是"三次同"。
- **`kitchen_u_shape` 三次都没误报**（三次观察区都写"厨房为 L 形…未形成 U 形"，与实图一致）。
- **两条朝向标记三次都没出**，进读不出区——模型读到了指北针却把"知道朝向"与"判定西晒/东向"分开对待，
  **这是正确行为**。

**求值线侧**：给 `{"balcony_service": "阳台内有洗衣机设备位"}`，收纳域出
`rule-practice-storage-balcony-cleaning`，`triggeredBy.evidence` 逐字回带那句依据。测试 66→74 全绿
（含真库集成 8 条 0 跳过）。**成文线** 117→124 全绿。

### 二、订正一处（追记二的朝向口径再收窄）

追记二据样本图有指北针订正为"朝向能读则读"。**本轮实测再收窄**：**整体朝向可读**（指北针 + "南北通透"
卖点文案），**具体房间朝向不可信**——三次里有一次把主卧说成"朝西"（实图是南向飘窗）。它没进标记只进
观察区，未造成危害，但**闭集里那两条朝向标记短期拿不到可信输入**。待拍板④ 的答案因此再退半步：
朝向**问用户**是当前唯一可信来源，图上读到的只作交叉印证。

### 三、实现判断（非裁决，可推翻但要给替代）

**解析侧（子代理五条）**：①**换物理模型**——`qwen-vl-max` 同参两次把 L 形厨房误判成 U 形且漏掉阳台
设备位（**误报正好落在最贵的那条**），`qwen3-vl-235b` 不误报但理由是错的（"图上没写 U 形三个字"），
最终取 `qwen3-vl-plus`；②**一次调用读整张图**——两步读（先清点图例再判定）试过并撤回：清点那步照样
答"阳台内无任何图例"、还把指北针读错方向，两倍成本换负收益；③**加两道机检门禁**（真跑逼出来的，
不是设计时想到的）：依据里**禁量纲数字与标准号**（依据会随规则进报告，模型写的尺寸混进去就是"数字由
LLM 决定"）、依据**禁否定句**——模型两次把闭集当逐条打勾的清单填，四条标记全出现、依据写"未画出洗衣机
位"，在"键存在即触发、下游不读依据"的语义下**会让四条规则带着"它不成立"的理由全部触发，比误报更贵**；
④闭集在仓内持逐字副本（真源仍是契约仓）+ 守门测试比对，理由是解析件要能在没有契约工作副本的环境里跑；
⑤**prompt 里不放任何单条标记的判定口径**，闭集从契约数据生成——判定口径该长在契约里。

**求值线侧（子代理三条）**：①`trigger` 原样带走 jsonb 不投影成分类型（投影会在 answer 落地时静默丢掉
`question_id`）；②触发结果下发**不带 trigger 也不带 consumers**——把触发条件给成文线，它就会想自己再判
一遍；③标记名缺失的规则**判为未触发不在运行时抛**——承接它的是核验侧那道闸，运行时抛会让一条坏种子
拖垮整份报告。

**成文线侧（我）**：①**触发条目落推导步不落写作步**——它回答"这一章该讲什么"，正是推导步的题目；
②**依据同行下发**，`always` 条目不写空依据括号（写"因为：无"会诱出"根据通用规范"这类无依据背书）；
③**户型特征只下发依据文字不下发标记名**（内部标识符混进主张就会上卡片，客户语域禁内部编号——该行
此前 dump 整个 map，因一直传空未暴露）；④**按触发类型分档权重**：户型条目必讲、通行做法可讲。
依据是求值线真库实测——`always` 类 7 条分布不均（照明 3／用材 2／造价 1／收纳 1，人体工学与色彩软装 0），
**照明正是眼下 0/6 过检那章**，一律"必须讲到"等于给最难的一章再压三个通用话题，方向是反的。

### 四、登记不处置（下一个 session 别当已解决）

1. **图例级细节靠整图单次读读不准**：三个模型都漏过阳台设备位，只在被单独问阳台那一小块时才说得出
   那两个虚线框。本次救回靠的是一条**通用**读图约定（虚线框通常是设备示意位），不是针对这条标记的口径。
   要真正读准得**按房间分区放大读**，那需要先有房间定位（几何）——属解析实现路径选型，**时点建议＝
   拿到第二批样本那一批一起定**。
2. **朝向判读不可信**（见 §二）。
3. **prompt 措辞会连坐（第三次撞到）**：纪律从五条扩到六条、把"否定句"讲得更细的那一版，反而诱发模型
   写否定依据；改成短促的"只放成立的标记 + 一个用占位名写的对错形态对照"才稳。
4. **稳定性只测了同一张图**，跨图与脏图都没有数据。
5. **闭集反向没人查**：契约要求"新增标记必须与消费它的规则同批提交"，核验侧现在只查规则→闭集一个方向，
   反方向（闭集条目有没有规则消费）没实现。
6. **推导步下发行不带校准状态**：七条 `always` 里只有一条 calibrated，成文线看不出哪条能背书。
   要连着断言预算一起想，归收敛专项。
7. **成文线用不用得好这批条目未验证**：接线通了、包出得来，但"加了必讲条目之后过检率是升是降"没有数据。

### 五、并行协作（两条纪律，坑单归并行线写）

本轮 contracts/backend/reportgen 三仓有**两个会话同时在动**（另一条线在做规则 1.9 两层模型 v2.8）。
撞出两条纪律，均已与该线对齐、由其写入坑单：**三仓禁用 `git add -A`**（本会话已因此把对方在途的契约
改动扫进自己的提交，处置＝提交信息如实注明不拆历史，见 contracts `2790ff0`）；**自查命令与门禁命令
必须逐字相同**（本会话跑 `mypy src/` 传了路径，覆盖掉配置里的 `files=["src","tests"]`，把测试目录摘出
射程，本地绿而门禁红）。成文线那两处分档改动由该线收尾时一并带出并注明来源，本线不重复提交。

### 六、下一步

**本线**：出图那半（母版与三张图）仍待做；解析的分区读与朝向两条等第二批样本。
**主线可立即受益**：报告真跑时把特征传进去，收纳域那条规则就会带着依据出现在推导素材里——
这是"报告从通用建议变成你这套户型的分析"的第一个可观测证据，**建议并入并行线的灯光真跑那一批一起看**。

---

## 追记六（2026-08-30 晚，执行者 Claude·解析线）：判定结构改造 + 分区读与朝向换算落地

接追记五，解析侧又走了两轮，都由用户裁决驱动。**两轮的共同判据是同一条：结构性堵死 > 纪律禁止，
确定性计算 > 模型判断**——它已经是本项目第三、第四次用到（推导步看不见落点的值所以产不出数字；
`min`/`max` 不在项名空间所以"引单端"在语法上不存在）。

### 一、第一轮：让不成立的标记根本进不来（aipipe `b206679`，contracts `3524c00`）

**病**：解析产出过一份"四条标记名全在、依据全是否定句"的东西（依据写"非 U 形""无法判定"
"未画出洗衣机位"）。在"键存在即触发"的语义下，这份产物会让四条规则**带着"它不成立"的理由全部触发**。

**用户的根因判断**（本轮的关键，不是我提的）：**问题不在模型，在我们给它的结构**。
让模型直接产 `{标记名: 依据}` 时，输出里**"这条不成立"没有位置**；而模型面对一份候选清单的
自然做法是逐条作答（这本身是负责任的），"否"没地方放，只好写进依据栏。
靠"检查依据是不是否定句"去发现，是在下游堵一个上游本不该产生的东西——第二好的解法。

**改法**：模型输出层改成**逐条判定**（每条候选给成不成立 + 依据），代码只把判成立的投影成产物。
**下游契约一个字没动**（下发的仍是"只含成立的标记 + 依据"）。连带：名字越界校验前移到判定层
且不论判成立与否；否定句那道闸不删、语义收窄为**自相矛盾检查**（判成立却给否定依据）。
契约 `rulebook/layout_features.md` §四 补记了产出侧的这个实现形态。

**结果**：那一整类失效**不再产生**（不是被拦下来，是没地方产生了）——自相矛盾闸九次真跑一次没触发。
**代价如实记**：同一批里 `balcony_service` 从旧结构三次全中变成九次全丢。原因是**框架效应**——
旧提示把任务框成"从图上找出成立的标记"（搜索），新的框成"清单每条判一次"（核查），
搜索让模型去找证据，核查让它默认给否。**这不构成回退理由**：旧框架的代价是那一整类否定句产物。
同时泼一盆冷水：旧结构那三次"命中"到底是看见了还是猜的，从输出看不出来——
**对的答案配不确定的理由不算能力**。

### 二、第二轮：分区读 + 朝向由代码换算（aipipe 本轮提交）

用户批"把看得见做扎实"。**事实层那条（观察区结构化后下发）用户未表态，按未批准处理，未动**。

**分区读**（先定位、再裁剪放大、再逐块读；**按每个房间规划不按整体规划**）：
勘测一次整图问出每个房间占哪一块 → **代码裁剪并放大**（剪刀在代码手里）→ 每块单独读一次图例 →
汇总回判定层。判据是九次真跑：三种提示框架都救不回阳台设备位，而模型被单独问那一小块时逐个说得出来
——**不是提示问题，是分辨率问题**。

**朝向由代码换算**：模型只报指北针指向与窗开在哪面墙，代码按"**有指北针以指北针为准，
没有才用上北下南左西右东**"（用户给的通行约定，写成有出处的常量）换算。判据是模型自己推时
同一张图的主卧朝向出过"西南/南/西"三个答案——它每次都在现场心算，没有固定规则。
**朝向取决于窗所在墙面，不取决于房间在图上的位置**（样本主卧在左下角但飘窗在下墙，故朝南）。

### 三、这一轮的数（全部真跑，别当推理读）

| 项 | 结果 |
|---|---|
| **成本** | 调用 **1 次 → 11 次**（勘测 1 + 房间 9 + 判定 1），耗时约 20s → **35~39s** |
| **阳台设备位** | **恢复命中 2/3**，依据指得出图上的东西（"两个含黑点的虚线框及一个地漏小圆圈"），且与**独立留档的近景原文**对得上——现在能分辨"看见了"还是"猜的" |
| 漏的那次 | 勘测把阳台的框整体报低半个身位，裁出空白页。**瓶颈是框的坐标精度，不是模型看不清** |
| **误报** | `kitchen_u_shape` 三次全不成立且指得出 L 形；`west_facing` 三次全不成立 |
| **朝向可复现** | 主卧 **3/3 同一答案（南）**；次卧被改造**修正**（整图勘测原报"朝东"，近景读出下墙→南，与原图相符） |

### 四、中途引入过一次误报，已处置（这条最值得记）

先前把"窗开在哪面墙"放在**整图勘测**那一步问，模型给**没有窗的卫生间**报了一面西窗
（人工核对：卫生间左侧是整段粗黑实线墙，无开口），这条假事实经确定性换算变成"卫生间朝西"，
判定那步据此判 `west_facing` **成立**——一次货真价实的误报。
**处置＝把窗墙这件事挪到近景去定**（同一房间的放大图，看得最清），改完三次全干净。

**教训写死**：**确定性换算不会把输入变准，只会把不准的输入变得像个确定结论**，反而更容易被下游当真。
凡是"模型给事实、代码给结论"的地方，都要同时问一句"这个事实是在哪儿读到的、那儿看得清吗"。

### 五、试过并撤回：裁剪的"外扩下限"

针对"框偏了裁出空白页"，试过按整图尺寸给一个 5% 的外扩下限。实测阳台每次都裁对了、
近景每次都看见设备位——**代价是每块都带进邻居，窗墙归属跟着乱**（主卧飘窗三次里两次被报成
"没画窗"，阳台被报出左墙的窗、换算成"朝西"又催出一次误报）。**误报比漏报贵，故撤回**，
数与理由留在代码注释与仓内真跑存档里。

### 六、登记但不处置

1. **阳台设备位稳定性 2/3**，卡在勘测框的坐标精度。根治要换定位办法（非模型的版面定位，
   或按图幅切片），**时点＝拿到第二批样本（手机翻拍图与带尺寸标注图）那一批一起定**——与追记四
   已登记的那批同批，不新增项。
2. **`west_facing` 这条标记在解析侧不可判**：三次的判定理由都绕在"'该空间'指哪个房间"上。
   闭集里它写作"该空间西晒"，而解析侧拿到的是**逐房间**朝向，没有主体可对应。
   **属契约侧问题**——改口径要与消费它的规则同批（闭集扩充纪律"先有规则后有标记"的同一条），
   本轮只登记。**触发条件写死＝下次动 material 域那条西晒规则时一并定**。
3. **近景的窗墙识别仍不稳**：不再制造假朝向（没画窗就不给朝向，是正确行为），但漏报窗仍常见，
   厨房/小孩房/玄关三次之间有出入。同第 1 条一批处置。
4. **事实层下发（观察区结构化）未获批准，未动一字**。

*追记六完，2026-08-30 晚。代码在 aipipe，配置在 infra，契约在 contracts；本仓只放文档。*

---

## 追记七（2026-08-30 晚，执行者 Claude）：本线当前状态与接手入口

> **接手的 session 读这一条就能动手**，不必回看全部追记。追记一~六是执行细节留档，与本条冲突处以本条为准。
> 实现细节见追记六（判定结构改造、分区读、朝向换算、误报教训），本条只讲**状态、顺序、待办**。

### 一、一句话版

**户型图能读出特征、特征能一路走进报告、空间推理有了合法说法——但出图那半仍是零，第一道门是几何提取。**
用户已拍：**先出图**。

### 二、当前真实状态（按段）

| 段 | 状态 | 关键数（全部真跑，别当推理读） |
|---|---|---|
| **解析** | 可用 | 厨房形态 3/3 正确（L 形不误判成 U 形）；阳台设备位分区读后 **2/3**；朝向由代码换算 **3/3 一致**（主卧朝南，与原图相符）；调用 1→11 次（勘测 1 + 房间 9 + 判定 1），耗时 20s→35~39s（近景并发 4） |
| **特征进报告** | 链路通、产出少 | 闭集**只有四条**，其中两条朝向不可信输入、一条正确答案是"不成立"；求值线已能判 `always` 与 `layout_feature` 两类触发并随包下发 |
| **空间推理背书** | **机制定、实现零** | 裁决与写法都已收口（声明字段，不与占位符统一）；代码一行没写 |
| **事实层** | 已批准、未实现 | 双身份：给业主看的"你这套户型"那一节 + 空间推理的可引用锚点 |
| **出图** | **零** | 母版、三张图都没开始；第一道门＝几何提取验证 |

### 三、任务序列（按依赖排，顺序即先后关系）

**第一优先 · 几何提取验证**（用户拍的"先出图"的第一道门）
母版要的是**坐标**——房间边界、墙线、门窗位置。已知数：分区读实测**整图定位把阳台位置整体报低约半个身位**，裁出来是空白页——**让模型直接报坐标，精度到不了画图的要求**。
建议路径：模型定位 + 非模型的图像处理（找直线、找封闭区域、对齐墙线）。
**验收**：用它画出的母版与原图**叠合对得上**。
**不需要比例标定**（图上不标绝对尺寸，相对关系对即可）——得房率那套只服务报告里的数字。

**接着 · 母版绘制 → 三张图**（母版是所有风格图的唯一几何源，强制回读；文字层按追记一的倾向：图形层与文字层分离，几何跟随度实测后收口）

**可并行 · 空间推理落地**：三个待定项收口（事实字段闭集／事实 id 前缀入规范 §1.7 表／机检怎么分辨该引用事实还是走断言预算）→ 解析改产出结构化事实 → 契约加字段（**同批在渲染层声明，否则整包被拒**）→ 写作侧加声明字段 + 两条机检 → 打回话把可引用事实逐字列出。

**可并行 · 新规则起草 5~8 条**（定位见 §四-⑥）

**之后**：脏图也能出图（第二步，缺样本）→ 同户型能复用（第三步，回流授权已解除）

### 四、今晚六条裁决（全文见《规范/落地假设与拍板清单.md》§〇 末条）

① 户型图**可回流户型库**（第三步授权前置解除）；② **先出图**（排期改判）；③ **事实层给业主看**；
④ **权益不补档**——归属与权益是正交两维，要改的是规范"深化产物全量 PAID"把阶段与权益绑死那句，
改法＝**权益按产物逐个登记不按阶段推定**；⑤ **上传资产一直保留、删号不处理**（**登记为上线前重看项**：
有真实用户后个人信息相关法规通常要求提供删除途径）；⑥ **新规则先写 5~8 条**，定位＝**只写图上看不出来的
外部专业知识**，判据＝"这条知识能不能只看图就说出来？能＝不用写规则"；从**解析已稳定读出的事实**出发
（厨房形态/开放与否/飘窗分布/单卫/玄关形态/南北通透），**别按域铺开**——那会写出一批没有事实能触发的规则。

**同日更早的裁决**：空间推理另立背书通道（中控 `fafdf7f`）、引用写法＝声明字段（`7d18486`，评审文档追记一）。
两者全文见《评审/背书范围与空间推理-2026-08-30.md》。

### 五、技术债（八条，触发条件都写死了，不是"以后再看"）

| 条目 | 现状 | 何时处置 |
|---|---|---|
| 阳台命中率 2/3 | 卡在整图定位的**坐标精度**，不是模型看不清 | **并入几何提取验证**（同一个根因） |
| 西晒标记解析侧不可判 | 闭集写"该空间西晒"没有主体，解析拿到的是逐房间朝向 | 下次动用材域那条西晒规则时**同批改**（改集要与规则同批） |
| 窗户识别不稳 | 不再制造假朝向（没画窗就不给朝向），但漏报常见 | 并入第二批样本 |
| 只测过一张图 | 跨图与脏图无数据 | 拿到第二批样本 |
| 闭集反向没人查 | 只查了"规则引用的标记 ∈ 闭集"，反方向没查 | 工程项，随时可补 |
| 写作素材不带校准状态 | 写手看不出哪条规则能当判断依据（七条 `always` 里只有一条 calibrated） | 归收敛专项，连着断言预算一起想 |
| 触发条目对过检率的影响未验证 | 接线通了，"多了必讲条目之后过检率是升是降"没数据 | 等判官台账攒够 |
| 事实字段闭集未定 | 准入判据已定＝**同一张图多次读一致才能进** | 空间推理落地那一批 |

### 六、真跑逼出来的经验（比今天的功能更耐用）

1. **结构性堵死 > 纪律禁止**（今天第三次）：让模型"只输出成立的标记"，它会四条全填、在依据里写"其实不成立"，
   而下游只看键在不在——三条规则会带着否定的理由全部触发。改成"每条都给判定、代码只取成立的"之后，
   **这类产物没有地方产生**。
2. **确定性换算不会把输入变准**：把朝向从模型手里收回来交给代码算是对的，但模型给一间**没有窗的卫生间**
   报了一面西窗，经换算变成"卫生间朝西"——**看起来像硬结论，反而更容易被下游当真**。处置＝事实在**看得最清楚
   的地方**（近景）定。
3. **打回话必须"照做得到"**：重写只有两轮，做不到的打回就是在烧轮数。灯光章从六轮全败到过检，靠的不是
   加纪律，是**把合法写法逐字摆在模型眼前**。
4. **两个会话共用一份工作区会互相带走改动**：`git add -A` 扫走对方在途改动（今日两次）、本地自查命令与门禁
   命令不一致把测试目录摘出射程（本地绿、推送红）。**已成纪律：提交显式指定路径；自查跑与门禁逐字相同的命令。**

### 七、仓库状态（写本条时实测，**全部未推**，push 由用户定）

中控 6 / aipipe 2 / contracts 1 / reportgen 1 / backend 2 / infra 0。
**注意**：contracts、backend、reportgen 三仓**另一条线也在动**（规则 1.9 两层模型、叠字判据与出口过检）——
`git add` 一律显式路径。reportgen 那条线已收尾（其 HEAD `03719d1`），可以动了。

### 八、接手须知（三条）

1. **别把"链路通了"读成"产出够了"**：特征闭集只有四条，其中能真正工作的现在只有一条，而它的正确答案是"不成立"。
   要让报告真讲户型，靠的是**空间推理那条路**（引用事实直接写），不是等规则。
2. **事实层是两件事的公共前提**：给业主看的那一节、以及空间推理的可引用锚点——**先做它，两边都受益**。
3. **出图不等标定**：图不需要绝对尺寸。别被得房率那条卡住。
