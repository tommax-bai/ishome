# 交接文档 · 报告渲染启动（"报告能被人看到"）

> 性质：**专项交接**（同《交接文档-落地启动.md》形态），供独立 session 推进本线。
> 全局状态入口仍是《交接文档-2026-08-29晚.md》；本文与它并行不冲突——报告域主线（造价章出金额 → 判官分批
> → 先定主张再写卡片 → …）改的是**卡片内容**，本线消费的是**页结构契约**，两边真并行。
> 接手请先读：本文 → 《交接文档-2026-08-29晚.md》§三 阅读顺序 → 《规范/开发规范与代码分层.md》。

---

## 〇、一句话版

**成文线产出到 `pages` 就停了——项目至今没有任何东西把它变成人能读的报告，也没有任何人（包括用户）见过一份报告长什么样。** 本线要做的：一个**确定性、零 LLM** 的渲染层，把「pages + 报告数据包」变成一份能打开阅读的 HTML 册子。做完的验收：**打开一个文件，读到人体工学章的卡片、页脚的依据标注、锁定文案正文——第一份人能看到的报告。**

为什么现在做（三个都成立）：

1. **存量工作不可验证**：到今天所有质量判断都是从违规清单和 JSON 里推的，没人读过成品；
2. **两条已实装的纪律在纸面上不存在**：依据标注（规则 4.10c 标注必挂）与锁定文案（规则 2.4 gen-locked）的正文都约定"渲染层按 ID/结构化数据出"——渲染层不存在，这两样在业主面前就不存在；
3. **行为信号闭环的前提**：规则 4.17 的一切转正信号来自业主对内容的反应，前提是内容能被看到。

---

## 一、现状与输入（全部已就绪，本线不用等任何人）

| 输入 | 在哪 | 状态 |
|---|---|---|
| `Page` 契约（page_id/domain/cards/locked_text_ids/**provenance_notes**） | reportgen `src/reportgen_worker/models.py` | 冻结可用；`page_type` 恒 null（pt- 页型库未建，按域成页） |
| 报告数据包（anchors 含值与单位、provenance、锁定清单） | contracts `rulebook/report_data_package.schema.json` | v2.4 形态 |
| 锁定文案正文 | contracts `registries/locked_texts.md` | **3 条有正文**（DISCLAIM_P1 / GUIDE_SITE_CHECK / ACK_MANDATORY_WAIVER），5 条待补录（法务在途，另线） |
| 真跑样例数据 | 按 backend `_iteration/run-2026-08-29-v24-first-run.md` §一 的跑法重导（一次性只读测试导包 + reportgen 三 activity），ergonomics 单元 verdict=ok、22 卡 22 标注 | 可复现 |
| 最小合成夹具 | reportgen `tests/support.py` 的 PACKAGE_JSON | 现成 |

---

## 二、裁决与默认（接手第一件事：请用户拍板落档）

**待拍板 · 渲染层落仓**。推荐默认：**独立仓 `ishome-reportrender`，先以纯库 + CLI 形态存在，不起服务**。

两条既有裁决在此有张力，如实摆出：

- 支持独立仓：**"逻辑异质→物理隔离，不采信模块边界替代物理隔离"**（用户裁决，chat/project 与绘图三仓拆分同源）——渲染是确定性代码，成文是 LLM；更硬的一条：**锁定文案正文必须对 LLM 侧结构性不可见**（"写作层结构性拿不到"是现有保证的原话），正文数据放进 reportgen 仓，这道结构保证就退化成了口头纪律。
- 反对成服务：规范 §13.6 服务存在性判据（无独立伸缩轴/故障域→不成服务）。
- **化解**：独立仓 ≠ 独立服务。仓管物理隔离，服务等"报告一键触发"接编排时再起 worker——两条裁决都不违反。

其余默认（拍落仓时一并确认）：模块名 `reportrender`；输出**单册自包含 HTML**（可打印 A4；PDF 走浏览器打印，后续项）；将来接编排的 activity 注册名 `report-book-render`、队列 `reportrender-activities`（届时走 contracts PR，注册表只增不改）。

---

## 三、渲染层的纪律（红线，违反即返工）

1. **零 LLM**：整个渲染层没有一次模型调用；它是 gen-locked 的执行者，不是生成者。
2. **锁定文案禁编造、禁拼接**：正文只按 ID 从数据取，逐字输出；**"把免责接到句子后面"这种拼接也禁止**（注册表纪律原文）。缺正文（待补录）= 该句缺席，**缺是少说，编是替企业作错误承诺**。
3. **数字原样输出，禁换算折算**：`{lkp-*}` 占位符替换为落点值 + 单位，**一个占位符=整条落点**（区间渲染成区间）；mm 不折算成 m、0.03 不擅自变 3%——换算=渲染层自己造数字。百分比等表示问题遇到即上报待裁，不猜。
4. **fail loud**：占位符找不到落点、值形态不认识（如分档映射内联）→ 渲染失败并报明细，**不空替、不静默跳过**（过拦与漏拦同样是失效，静默丢内容是漏的一种）。
5. **不重判**：pages 已过册级校验（标注齐、锁定文案齐由 `report-book-check` 保证），渲染层只执行不复核——与"成文线不重判求值线"同一哲学。
6. **`lkp-` 标识名禁入输出**（客户语域禁内部编号）：依据标注展示用**落点名称**（从数据包 anchors 按 lkp_id join 出 name），lkp_id 本身绝不出现在 HTML 里。
7. **匿名**：输入即匿名（数据包结构性无用户标识），渲染层不得添加任何来源之外的信息。

---

## 四、任务序列（事项名指认，顺序即先后关系）

| 事项 | 内容 | 做完之后成立的事实 |
|---|---|---|
| **落仓拍板落档** | 用户确认 §二 默认（或改判）；裁决进《规范/落地假设与拍板清单.md》§〇；《仓库拓扑.md》就地更新升版本行（活文档，不发追记） | 落仓有裁决可引 |
| **建仓与骨架** | 照抄 reportgen 工程形态：uv + ruff/mypy(strict) + import-linter + pre-push（`git config core.hooksPath .githooks`）；依赖 contracts 走 git dep（参考 reportgen pyproject 的 tag 引用方式） | 质量门与兄弟仓同款 |
| **数字替换与册页模板** | Page/包 → HTML：占位符替换（首版支持 `{min,max}`/`{v}`/单边界三形态，其余 fail loud）、按域成页、封面/目录最简 | 合成夹具能渲出一册结构完整的 HTML |
| **锁定文案数据化与挂载** | contracts 增机器可读数据文件（如 `registries/locked_texts.json`，与 md 同源，加一致性校验测试；**md 仍是叙事权威**）；渲染按 `locked_text_ids` 取正文输出；待补录 ID 输出缺席并记日志。注：`ACK_MANDATORY_WAIVER` 的勾选留痕交互不在本线射程，先只渲文案 | 三条有正文的文案第一次出现在纸面上 |
| **依据标注渲染** | `provenance_notes` → 页脚"本页依据"块：落点名称 + 来源 + 取数时间 + 校准状态；`source=null` 按"经验判断、无外部标准背书"口径呈现——**这句说明文案建议注册为新锁定文案 ID**（与"现场复核话术"同批走法务，见主线），落表前用占位样式并标待补录 | 标注必挂纪律第一次对读者可见 |
| **第一份可见报告** | 按 §一 跑法重导真跑数据（ergonomics verdict=ok 那份），渲染成册 | **有人第一次看到报告**；把成品拿给用户读，读后反馈开新一轮（这正是 v2.4 裁决"发出去换信号"的第一次兑现） |
| 后续 · **接进编排** | contracts 注册 `report-book-render` + 队列；aipipe workflow 册检后追加；起 worker | 等"报告一键触发"（主线）就绪后再动 |
| 后续 · **PDF 与打印样式** | @media print 收尾、浏览器导出 | — |

---

## 五、坑（给接手 session，均有出处）

1. **`provenance_notes` 里只有 lkp_id 没有名称**——展示名必须从数据包 join，直接打印 lkp_id 即违规（红线 6）。若嫌 join 烦，正路是给 `ProvenanceNote` 加 name 字段（契约只增不删，两侧同步），不是打印 id。
2. **值是异构 map**（`{min,max}`/`{v}`/分档键/嵌套），形态清单见 backend `RulebookEvaluator` 与真跑数据。**首版只做三形态，其余 fail loud**——分档映射（如置信→宽度）的呈现是设计题不是替换题，别顺手内联。
3. **`withheldAnchors` 恒空**（v2.4 作废字段），渲染层不消费；生产方若发 `WITHHELD` 档位，reportgen 侧整包解析失败是**故意的严**，渲染层同理不做宽容解析。
4. **DISCLAIM_P1 属图框**：prec-schematic 图纸的免责由绘图服务挂（注册表"运行时消费口径"表），本线只管**报告册页面**上按 `locked_text_ids` 出现的文案，别去实现图框。
5. **中控仓不放代码**，渲染代码只进新仓；本文所在仓只收文档。
6. 本机环境坑（uv 用阿里镜像、代理 7890、gh 偶发 EOF 重试）见《交接/落地日志-2026-08-23.md》§四；建 GitHub 私有仓用 `gh`（账号 tommax-bai），SSH 走 443 改写见同文。
7. **push 时机由用户定**；提交信息中文、Conventional Commits。
8. 渲染样式别过度设计：本线验收是"能读"，不是"好看"——先让 22 张卡、标注、文案全部如实出现在纸面上，美化等页型库（pt-）落地再说。

---

## 六、与主线的接口（谁等谁）

- **不等主线**：页结构契约已冻结，主线改卡片内容不改 Page 形态；真跑数据可随时重导。
- **主线等本线的**：第一份可见报告出来后，"先定主张再写卡片"的效果评估才有人读得出来。
- **同批搭车**：依据标注的"经验判断"说明句 → 并入主线"现场复核话术"那批法务流程（两条新 ID 变三条）。
- **别动主线的东西**：reportgen/backend/contracts 的报告域代码本线只读；唯一例外是 contracts 加锁定文案数据文件（走 PR，只增）。

*交接文档 · 报告渲染启动 · 2026-08-29 晚 · 执行者 Claude。正文定稿后不改，增量以追记追加。*

---

## 追记一（2026-08-29 晚，执行者 Claude）：落仓已拍板落档，§四 第一个事项完成

**用户裁决原话**：*"落仓独立仓 ishome-reportrender，纯库+CLI 先行、不成服务。"* —— 即 §二 推荐默认**整体照准，无改判**，§二 的"待拍板"字样自此失效（正文不改，以本追记为准）。

**落档三处（已完成）**：

| 落点 | 动作 |
|---|---|
| 《规范/落地假设与拍板清单.md》§〇 | 新增裁决条目，含两条判据（物理隔离／§13.6 不成服务）与"独立仓 ≠ 独立服务"的化解，连带确认模块名、HTML 单册产物、`report-book-render` + `reportrender-activities` 的将来注册名 |
| 《仓库拓扑.md》 | 就地更新升 **v1.1**：十仓→**十一仓**，新增 `ishome-reportrender` 行并注明"暂不含 Temporal worker"；活文档纪律，不发追记 |
| 本文 | 本追记 |

**这条裁决在往后怎么用（省得下个 session 重推一遍）**：

1. **仓与服务是两件事**，别把它们绑一起复述。现在成立的事实是"独立仓、无服务"；起 worker 是**接编排时的执行动作，不需要新裁决**——§二 已把两条裁决的张力一次化解掉了，届时按 §四"后续 · 接进编排"直接做。
2. **物理隔离这条的真正承重点是锁定文案正文**，不是"代码风格不同"。判据可复述为：*正文数据进了 reportgen 仓，"写作层结构性拿不到锁定文案"就从结构保证降级成口头纪律。* 谁将来提议"渲染直接放 reportgen 里省事"，驳回理由是这句，不是"两种语言不一样"。
3. **`report-book-render` / `reportrender-activities` 是预留名，此刻不进 contracts**。注册表只增不改，没实现就注册＝占位污染注册表；等 worker 真起时走 PR。

**下一个事项：建仓与骨架**（§四 第二行）。已可开工，无前置阻塞——照抄 reportgen 工程形态（uv + ruff/mypy strict + import-linter + pre-push `git config core.hooksPath .githooks`，contracts 走 git dep 的 tag 引用）。两条本机操作纪律照旧：建 GitHub 私有仓用 `gh`（账号 tommax-bai，SSH 走 443 改写见《落地日志-2026-08-23.md》§四）；**push 时机由用户定**。**代码只进新仓，本仓仍只收文档。**
