# 交接文档 · 2026-08-27 晚（报告域裁决日：AI native 重写 + 知识资产链路贯通）

> 性质：换 session 交接。**新 session 读完本文即可接手报告生成域**；全局项目背景仍以《交接文档-2026-08-23晚.md》为入口（其裁决、红线、拓扑继续有效）。
> 截至：中控仓 HEAD `8b3fe19`、backend HEAD `ea0b22f`，**均未推远端**。
> 本日全部工作在报告生成域；chat/genpipe/绘图三仓/contracts 未动。

---

## 一、今天发生了什么（一句话版）

用户投来方法论侧的《Agent Graph 设计说明 v0.1》求落地评审，评审过程连环触发**五次裁决**，报告规范从 v1.3 升到 **v2.2**（AI native 重写为最大一次）；随后按序执行三件落地：**六域知识种子编译 → 获取回路首跑 → svc_rulebook 建表+核验+灌库链路贯通**。

## 二、五次裁决（按序，全部已落规范与拍板清单）

| # | 裁决 | 落点 |
|---|---|---|
| 1 | **执业者校准作废**（v1.4）：追问"专家起草的专家是谁"暴露空头支票——项目找不到六域执业者。`calibrated` 改由机检核验取得；条目分可核/经验两类，经验条目恒 draft、只能降档、唯一转正路径=埋点信号 | 规则 4.10a，§14.4 |
| 2 | **AI native 重写**（v2.0）：用户原话"**我们在做一个 AI native 的系统，不要依赖人，不要依赖运营，不要依赖人预设的模板**"。三条回路（获取/自迭代/冷启动）、取消运营编辑后台与一切验收台、template 改系统自产句式库、品味验证锚=转化信号。**同日订正（规则 4.19，重要）**：初稿把"人改手册"误归运营依赖——用户纠正：**"不依赖人"只管系统运行，不管系统迭代；人与 AI 共同迭代=产品/开发角色，判据=人力是否随用户量增长**。人的改动走"改源→AI 编译成数据→核验→release"，手册文本不进 prompt | 规则 4.16-4.19，§14.5 |
| 3 | **编号与命名规范**（v2.1）：语义代替序号、前缀即命名空间（`art-/dom-/pt-/cr-/lkp-/gap-/prec-/gen-/stage-/tier-/acc-`）、四条禁止。全域旧编号（D01-D18/C01-C04/P0-P2/G0-G4/S1-S2/L1-L3）一次性重命名 350+ 处，对照表 §14.6。时机判据：这批枚举尚未进 contracts，改动成本为零 | 规则 1.7/1.8 |
| 4 | **纪律与知识的边界**（v2.2）：用户对"纪律型条目豁免核验"的三分类方案表示犹豫→复核推翻——那是给放错表的条目打补丁。**纪律唯一形态=机检 check/锁定文案，不进 calibration 状态机，锚 `decided_by` 裁决记录；check 结构上放不了数字**（阈值只能引用 lkp- 参数），伪装无处落笔。原则一句话：**可核性门只问"这话对不对"；纪律没有对错，只有出处** | 规则 4.10b，§14.7 |
| 5 | Agent Graph 评审两轮：第一轮用 v1.2/v1.3 尺子（部分误判），v2.0 后**重评推翻第一轮**——图的判据回流方向是对的（被收编为规则 4.17），仍需四改两补 | 《Agent Graph 评审-2026-08-27.md》 |

## 三、执行成果（三件，全部验证过）

### 1. 六域知识种子（backend `b036bd4`→`a5e4eba`）
`ishome-backend/services/project-svc/src/main/resources/rulebook-seeds/`：照明/人体工学/用材/收纳/色彩软装/造价 + `_common/`。参数 49、规则 23、属性 13、机检 26（全部带 decided_by）、persona 6 份。**template/vocabulary 故意未编译**（规则 4.17：自迭代回路自产）。编译结论：**编译产出结构，不产出可信度**——可信度只能来自获取回路。

### 2. 获取回路首跑（backend `5d7c382`）
审计记录 `rulebook-seeds/_acquisition/run-2026-08-27.md`。四项工项单价接公开行情（各 2 源）；照度锚 GB 50034（两版同值，唯一严格一致）；**卧室双控查明非强制→降级 tier-practice**（种子预埋条件触发）。四条实况：宽松一致居多、**口径问题＞数字问题**（定制柜单位改"投影㎡"，禁换算推导）、城市档取不到、全部仍 draft。

### 3. 建表+核验+灌库（backend `ea0b22f`）
- Flyway `V2__init_svc_rulebook.sql`：八表（五形态+personas+**checks**+**releases**），checks 无 calibration 列、decided_by 非空——4.10b 长在 DDL 里。已对本地 PG 事务验证。application.yml flyway schemas 已增列。
- `scripts/rulebook/verify_seeds.py`：核验跑批（4.10a 四项+4.10b 结构检查），**已挂 pre-push**。首跑抓出自家正则误判（"内部规范 §x.x"被当外部源）——收紧后可转 calibrated 收敛到 5 条真外部锚。
- `scripts/rulebook/import_seeds.py`：幂等灌库，calibrated 由它授予（4.10a 执行点）。`--validate` 实测：121 条入库、5 条授予、回滚干净。

## 四、新 session 阅读顺序

1. 本文；
2. 《装修报告生成规则规范.md》**v2.2**——报告域宪法，重点 §1.3（命名）、规则 4.10a/4.10b、§4.7（三条回路+规则 4.19 人的位置）、§14.4-14.7（裁决审计）；
3. 《装修报告AgentGraph设计说明.md》v0.2——两条流水线（求值线/成文线）、dom- 注册表；
4. 《Agent Graph 评审-2026-08-27.md》——四改两补的依据（注意其第一轮结论已被自己推翻的部分有标注）；
5. backend `rulebook-seeds/README.md` + `_acquisition/run-2026-08-27.md`——种子现状与获取实况；
6. 全局背景需要时：《交接文档-2026-08-23晚.md》。

## 五、红线增补（叠加在 8-23 红线之上，报告域生效）

- **无编辑后台、无运营环节、无人预设模板**（v2.0）；知识资产写入方只有资产回路，admin 只读观测；
- 人的改动只走"改源→编译→核验→release"（规则 4.19），禁运行时热改、禁逐条 CRUD；
- 纪律禁以知识条目入库（规则 4.10b）；种子禁预置 calibrated（只能由核验跑批授予）；
- 命名禁纯序号（规则 1.7/1.8）；正文旧编号只允许出现在 §14.6 对照表；
- 数字不由 LLM 决定：落点在派发前求值完，随 input_snapshot 下发（图 v0.2 §0）。

## 六、下一步任务序列（按序）

1. **真灌库**：启动 project-svc 让 Flyway 应用 V2 → `scripts/rulebook/import_seeds.py`（不带 --validate）。本地 PG=ishome-dev :15432。
2. **contracts 回写**（一次 PR）：`art-*` 产物注册表（§3.1 全集）、domain/form/generation_mode(gen-*)/precision(prec-*) 枚举、各 entity_type 属性 JSONSchema、release 引用格式；同批回改 `registries/templates.md` 两处 v2.0 冲突表述（"admin 运营可调""模板验收台验收"→系统自产/自验，规范 §11.9）。
3. **reportgen 建仓**：`ishome-reportgen` 独立 Temporal worker、队列 `reportgen-activities`（task_queues 注册表只增）；成文线落此仓（图 v0.2 §8）。
4. **求值线首实装**：project-svc 规则引擎模块读 release 快照求值 lkp-*，产出报告数据包；先跑 dom-lighting + dom-ergonomics + dom-budget（种子密度最高/国标锚已接/机检纪律最全）。夹具用 estate 标注户型，**避开 floorplan-parse**（咽喉未验证，另有 8-24 审查 A1 升格 workflow 待做）。
5. **判官反例库**：13 轮附录 A 问题句/改后句转化为 release 数据（人驱动 AI 编译，同种子路径）——方法论 v4.0 与四分册按规则 4.19 处理：设计文档→编译成数据，文本不进 prompt。
6. 获取回路第二轮：GB 50034-2024 新版核对、厂商参数（需商品页级抓取）、城市档单价（需信息价类源）、lkp-budget-share 占比带。

## 七、遗留与注意

- **各仓提交均未 push**（中控 9 个 docs 提交、backend 5 个提交本地领先）；push 时机由用户定。
- 方法论 v4.0 与四分册**不在仓里**（用户侧文档）——落地按图 v0.2 §7 归宿表执行，无需等原文入仓。
- 收纳域 `lkp-storage-density-baseline` 无外部真源，是六域唯一天生只能靠埋点转正的核心参数（勿试图给它找标准号）。
- `Ra≥90` 是高于国标底线（Ra80）的品质档选择，性质已注在参数里，勿当国标引用。
- 造价域是唯一"单价过期即整章降档"的域（cr-budget-stale-price），effective 只有 3 个月（至 2026-11-27）——**这是第一个会自然到期的时效资产**，获取回路的月度刷新在此之前要能跑。
- 本 session 模型中途从 Opus 5 切至 Fable 5（用户 /model 操作），对内容无影响。

---

*2026-08-27 晚 · 执行者 Claude（用户逐项裁决，五裁决+三执行）。本文提交后，报告域工作入口 = 本文；全局入口不变。*

---

## 追记（同日深夜）：任务序列 1-4 执行完毕，求值线贯通

§六序列前四项已落地（本追记为唯一增量，正文不改）：

1. **真灌库 ✅**：Flyway V2 应用 + `import_seeds.py` 真灌（121 条、5 calibrated、二跑幂等验证）。执行中发现 **V2 写死 `svc_rulebook` 导致集成测试撞表**（IT clean 只管 `svc_project_it`，重迁移 CREATE TABLE 已存在，4 例挂）——已修（backend `1b012cc`）：V2 改 Flyway placeholder `${rulebook_schema}`，IT 注入 `svc_rulebook_it` 随 clean 重建；本地库已重置重迁移重灌，数据不变。
2. **contracts 回写 ✅**（`e1e55f4` + `779e2ad`）：`registries/artifacts.md`（art- 产物注册表 22 项首登）；新增 `rulebook/` 契约面——dom- 域枚举 13 项（单一命名空间双身份）、form 八表族（check 无 calibration）、gen- 五档、prec- 三级、release 引用格式 **`{domain}@v{n}`**（库表存去前缀形态）、四 entity_type props JSONSchema（对已灌 13 条 attributes 实测零违规）；`templates.md` 两处 v2.0 冲突表述回改；task_queues 注册表增 `reportgen-activities`。**注**：templates.md 尚有一处"默认交付组合与顺序（运营配置…）"措辞未动——裁决只点名两处，是否顺手改请下次定。
3. **reportgen 建仓 ✅**（新仓 `~/codes/ishome-reportgen`，`f2c5778`）：Temporal worker 骨架（namespace genpipe，队列 reportgen-activities），质量门同绘图三仓（ruff/mypy strict/import-linter/pytest 全绿）。**成文线 activity 未预造名字**——注册名只增不改，先过 contracts 评审再落实现（空注册表时 worker 守门退出）。GitHub 远端未建。
4. **求值线首实装 ✅**（backend `241df3b` + `05591b4`）：
   - `scripts/rulebook/publish_release.py` **发布动作**（此前缺失，运行时只读 release 无从谈起）：`{domain}@v{n}` 只 INSERT 不 UPDATE，`_common` 物化进各域快照，内容不变跳过；已切 **lighting@v1 / ergonomics@v1 / budget@v1**。快照**含 draft 条目并带 calibration 标记**（规则 4.10 降档呈现所需，PAID 门禁在消费侧）——如需严格"只含 calibrated"另裁。
   - project-svc **规则引擎模块**：ReleaseRepository（`${rulebookSchema}` 注入）→ ReleaseSnapshot 投影（首批 parameters/personas）→ `RulebookEvaluator` 纯函数求值（直取/公式代入/gap- 三路径；公式可执行形态按 assetId 显式登记，当前四条：台面高/挂杆/镜高/电视距离）→ **报告数据包**（落点对象+gap+persona 引用+匿名画像，全排序保证确定性）。
   - **真库首跑**（三域，夹具输入=匿名身高族+标注户型特征）：35 落点、1 缺口（lkp-budget-driver 公式未实现，符合预期）、**重复求值序列化字节级一致**——图 v0.2 §8 首批验证第一件事成立。project-svc 27 测试全绿（含 ArchUnit）。

**序列余项与新增遗留**：
- 任务 5（判官反例库）：**阻塞在用户侧材料**——13 轮附录 A 不在仓里，拿到即可按种子路径编译。
- 任务 6（获取回路第二轮）未动。
- 新增：成文线首批 activity 命名过 contracts 评审；报告数据包 schema 随成文线实装入 contracts；降档/PAID 门禁在消费侧待实装；lkp-budget-driver 等"判定类公式"的可执行形态待定；ishome-reportgen 远端仓待建（push 时机随全局由用户定）。
- 本地栈现状：三域 release@v1 在库；project-svc 未常驻（求值线纯库读，测试即可验证）。

*追记 2026-08-27 深夜 · 执行者 Claude（按 §六序列自主推进，无新裁决——两处口径按既有规则落地并在上文标注：release 含 draft、成文线 activity 缓注册）。*

## 追记二（2026-08-28）：任务 6 获取回路第二轮完毕，calibrated 5→11、冷启动首批可发内容出现

backend `4b102e6`，审计=`rulebook-seeds/_acquisition/run-2026-08-28.md`。要点：

- **GB 50034-2024 核对闭环**：①标准性质 **GB 强制 → GB/T 推荐性**（2024-08-01 实施）——照明"国标锚"的表述后续要跟着改口径；②起居室 100/300 三版同值，3× 下界锚不动；③**卧室床头阅读 150→200lx**（两源）→ 新增 `lkp-illuminance-bedroom`（留"原文直取"pending 故仍 draft——2024 全表被付费墙挡，转引只核到高频行；厨房操作台 300/走廊 100/镜前 300 三处单源不采）。2013 版报批稿 PDF 已本地解析原文直取，补上首跑"经搜索摘要"的缺口。
- **城市档首次取得**：水电人工费 元/㎡ 口径（一线 60-68/二线 35-58/三四线 25-50，两汇总源）→ `attr-price-hydro-labor-sqm`；与点位口径并存禁换算。
- **厂商参数分两层收口**：可得性层本轮闭环（3000K/4000K/Ra90/24° 市售两源）；商品级规格归货架打标回流（规则 5.3），不再挂获取回路。
- **占比带只到粗锚**：总预算口径 硬 50/软 30/家电 20（两源）进 source，七分项 value 未动——口径互斥禁换算；"水电:泥木:油漆=2:3:5"疑摘要错配，已记录不采。
- **链路联动验证**：重灌（123 条）→ 核验授予 **calibrated 11**（卧室照度因留 pending 被正确拒授——纪律在工作）→ 发布 **lighting@v2/budget@v2**（ergonomics 无变化正确跳过）→ 求值线自动消费新 release：36 落点/1 缺口/**非降档落点 0→5**（规则 4.18 冷启动的"第一批可发内容"出现），字节级可重放不变。
- 下轮硬目标：GB/T 50034-2024 原文直取（154 页 PDF 需登录）；七分项占比 ≥2 源；人因工程源；材质标准号。

*追记二 2026-08-28 · 执行者 Claude。任务 5（判官反例库）仍等用户侧 13 轮附录 A。*

## 追记三（2026-08-28）：全仓 push + 成文线命名评审与首实装，全链路首次产出真内容

**push（用户裁决）**：五仓全部上远端——中控/backend/contracts/infra 推平；**ishome-reportgen 远端新建**（github.com/tommax-bai/ishome-reportgen，private）。中控与 contracts 补了仓库级 ssh:443 改写（防 Clash 劫持 22）。

**成文线 activity 命名（contracts `95adf9c`）**：注册表 #11-13——`report-unit-compose` / `report-page-assemble` / `report-book-check`（dom- 为参数不拆，规则 5.0c）；task_queues reportgen 行落名；**报告数据包 JSONSchema 入 contracts**（`rulebook/report_data_package.schema.json`：自包含/匿名 additionalProperties:false/数字只经 {lkp-*} 引用）。

**前置补强（backend `052d9d1`）**：报告数据包改为**真正自包含**——persona 全文（identity/断言预算/域禁词）、cr- 判据（check_type/pattern/decided_by）、公共禁词平铺随包下发（此前只有 persona 引用，成文线无从拼 prompt）。

**成文线首实装（reportgen `508a5e2` + infra `a69405c`）**：models（camelCase 对齐 Java，extra=forbid 即匿名守卫）/ gate（正文禁裸数字、{lkp-*} 占位必须可解析且限本域、禁词合并扫描、**cr- 判据从包内 release 数据物化执行不硬编码**）/ writer（LiteLLM 逻辑名 `report-unit-compose.default`→qwen-plus，prompt 全部从 persona 载荷拼装）/ 三 activity（违规反馈重写 ≤2 轮→failed verdict 上抛）。质量门全绿，17 测试。

**全链路真跑（本地）**：Java 求值线导真实三域数据包（36 落点）→ Python 解析（跨语言契约实测通过）→ qwen 成文 12 卡 → 三页装配 → 册检通过。**门禁实测在拦**：budget 重写 2 轮、ergonomics 1 轮方过；正文零裸数字、禁词零命中（连"照度"域禁词都规避了）。

**如实记录的已知质量缺陷（判官层职责，正好论证任务 5 的必要性）**：①LLM 编造家庭成员（"你和你太太"——匿名画像无家庭构成信息，无中生有）；②lkp- 内部标识名泄入客户正文（"未背书的 lkp-budget-share"式表述，机检不拦——不含数字）。两者都是反例库/读者代理层的靶子。

**遗留**：本地 LiteLLM 网关（:4000，用户常驻进程）跑旧配置，**重启后才认 `report-unit-compose.default`**（冒烟用同底模 design-orchestrator.default 替身跑的）；genpipe 成文线 workflow 编排未动（activity 就绪，等编排接入）；判官层/pt- 页型库/册级 cr- 待编译。

*追记三 2026-08-28 · 执行者 Claude。序列余项：任务 5 等附录 A；消费侧 PAID/降档门禁待实装（degraded 标记已随包）。*

## 追记四（2026-08-28）：三路并行——编排接通、降档门禁强制、获取第三轮；端到端跑出四个真缺陷

三个 subagent 并行（按仓/目录切边界互不碰同一文件），全部质量门真绿，四仓已推远端。

### 一、genpipe 成文线编排接通（aipipe `226aef9`）

`ReportComposeWorkflow`：dom- 单元并行 fan-out → 页面装配 → 册级校验，三 activity 全派 `reportgen-activities`。**报告数据包对编排是不透明载荷**（aipipe 不建模包内字段）——事后验证这个选择是对的：同期另一路在改包 schema，编排代码一行未受影响。失败策略已写死并在代码注释留理由：**任一域失败即整册失败，不出"其余页"，失败册不回传 pages**（杜绝调用方拿页就发布）。入口 `POST /api/v1/genpipe/reports`。57 测试（含 5 条真连 Temporal）。
另做了跨仓真跑，验证 aipipe（pydantic converter）与 reportgen（默认 converter）载荷互通——**两侧 converter 不对称是隐性风险，记在案**。

### 二、消费侧降档门禁（backend `ebe9b43` + reportgen `847d7ca` + contracts `634774d`）

规则 4.10 只说了"draft 禁入 PAID，只能降档或隐藏"，没说何时降档何时隐藏。**本次补的就是这条判据**（`AnchorPresentationPolicy`）：
- calibrated → 可作判断句支点，与权益无关；FREE 只降档不隐藏（隐藏禁令的出处是 4.10 那句 PAID 限定，但断言预算是语言纪律不分权益）；
- PAID 侧判"能不能以参考形态说出口"：**定位数字一律隐藏**（参考口吻的定位数字不存在，规则 2.2/2.3）、**值不含 min/max 的一律隐藏**（把点值人为拓宽成区间＝引擎自己编数字）；判不准的一律隐藏（冷启动失效方向必须是少发）。
- 隐藏落点单列 `withheldAnchors` 不并进 gaps：gap- 是"求不出"（补公式/输入），withheld 是"求出了但纪律不许发"（补外部依据转正），两条回流信号动作不同。
- 成文线侧：主旨句支点必须过门、**断言预算改为声明制**（`Card.assertions`，因为"这句是不是判断句"无确定性判据——语义识别明确留给判官层，机检不假实现）、隐藏落点禁引用。

### 三、获取回路第三轮（backend `ce4fec5`）：calibrated 11→16

**GB/T 50034-2024 原文直取成功**（155 页全文），第二轮三处单源数值全部核实并新增四条照度落点；条文说明原文自证了变化清单。材质检测标准号全部定位。**本轮最重要的是一条口径发现：标准给的是"合格底线"，不是"材质属性"**——GB/T 3324 表4 说的是"任何木家具漆膜不得低于 3 级"，不是"白橡开放漆的耐磨等级=3 级"；把底线填进 attribute 属性位＝用行业下限冒充产品参数，比数值错误更难发现。故材质三条 source 已可定位仍留 pending、保持 draft。与规则 4.10b 同类：那条防"纪律伪装成知识"，这条防"底线伪装成属性"。

### 四、我在审阅中补的：conflict 条目真的不进 release（backend V3 迁移）

第三轮种子写下 `conflict: true` 时暴露：**规则 4.16① 只实现了前半句**——冲突标记被导入链路静默丢弃，条目照常进快照（实测该条确实在 lighting@v3 里）。V3 给 rules/parameters/attributes 增 conflict 列，publish 排除并打印被排除清单，verify 判其不够格转 calibrated。语义与 calibration 正交：**draft 是依据不足可降档，conflict 是两源互斥、降档也无从降起**（参考形态呈现的仍是没有共识的数）。实测排除 lighting 的吊灯下沿、storage 的吸尘器两条。顺带修好 `--validate`（Flyway 建表后原实现必然撞表，改走影子 schema）。

### 五、端到端真跑（三域 PAID，真实 release + 真 LLM）抓到四个缺陷

跑法：project-svc 真库 PAID 求值（28 落点/11 隐藏/10 可作支点）→ 导包 → reportgen 成文。

| # | 缺陷 | 处置 |
|---|---|---|
| 1 | **中文数字绕过数字门禁**——模型被禁裸数字后改写"亮三到五倍""不能低于九十" | 已修：`gate-chinese-numeral`（只在跟量词/单位时判违规，不误伤"一般活动"）。**重跑实测模型改回 `{lkp-*}` 占位**——门禁把模型逼回了正确形态 |
| 2 | **零卡片以 verdict=ok 溜过**——逐卡片过检，无卡片即无违规，"什么都不写"是绕过全部门禁最省事的路径 | 已修：`gate-empty-composition`，失败在源头报出不靠下游兜 |
| 3 | lkp- 内部名泄进客户正文（追记三记录的缺陷） | 已修：`gate-lkp-identifier-leak`，用缺陷原句做回归 |
| 4 | **造价域零落点**——单价是 attributes，求值线只投影 parameters，造价章拿不到任何数字 | **未修，是求值线扩展的活**（见余项） |

**最终跑法结果恰恰印证了冷启动纪律**：只有获取回路补上依据的 lighting（11 条 calibrated）发得出内容（2 卡，重写 1 轮后过）；ergonomics 全域 0 calibrated → 模型两轮交白卷 → 响亮 failed；册检如实报缺域、不放行半本报告。**"宁可章节少、页数薄，不可用无背书的判断句撑密度"（规则 4.18）在真实链路上第一次自己生效了。**

### 六、余项与新增遗留

- **仍等用户**：13 轮附录 A（判官层）；LiteLLM 网关重启才认 `report-unit-compose.default`（实测未热加载，冒烟用同底模替身跑的）。
- **新增待办**：①求值线投影 attributes（造价章的前提）；②aipipe/reportgen 两侧 Temporal converter 不对称；③reportgen 无心跳导致编排侧不敢开心跳窗口；④contracts `rulebook/README.md` §8 应补第四条（降档门禁）。
- **需要拍板的口径**（三路 agent 各自列出、我未替裁）：FREE 侧是否也隐藏定位数字；一次求值只服务一个权益档（混档产物需分两次求值）；包内枚举大小写两套并存是否统一；release 快照含 draft（**本次整套门禁建立在此前提上，若改成只含 calibrated 需重做**）；造价章两条并行降档路径（本次的未背书降档 vs 既有 cr-budget-stale-price 过期降档）是否统一；`report_id` 由谁铸造；派发通道走 HTTP 入口还是 Java Temporal SDK 直连。

*追记四 2026-08-28 · 执行者 Claude（三路并行 + 审阅补洞）。四仓已推远端：aipipe 226aef9 / backend ebe9b43 / reportgen 847d7ca / contracts 634774d。*
