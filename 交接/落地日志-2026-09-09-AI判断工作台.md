# 落地日志 · AI 判断工作台（2026-09-09）

> **写完不改**（《纪律·日志正文不改》）。记的是"用户说'落地吧'之后当天真做了什么、验到了什么、还差什么"。
> 口径与设计在《方案/大模型调用点检测工作台方案.md》v0.3 与《方案/判官台扩版设计稿.md》v0.2，本文不复述。
> 状态看《现在在哪儿.md》。

## 一、这一天做成的事

**判官台从"只判图"变成"管系统里全部 12 处 AI 判断"，页面能打开、能操作；每次调用记一条这件事在本机整条验通。**

四件并行，三件当天完成、一件只差最后一步（挂到常驻网关）。

### 1. 12 处 AI 判断都会报自己的名字了

三个仓五个客户端全部接线：每次调用在请求体 `metadata` 里带 `call_point`（哪一处判断）与 `run_ref`（哪次运行）。

| 仓 | 处数 | 运行编号取自 | 测试 |
|---|---|---|---|
| ishome-aipipe | 7（户型图 5 + 会话 2） | Temporal 的运行 id；会话是会话键 + 本轮消息 id | 284 绿 |
| ishome-imagegen | 2（风格图、写实图） | Temporal 的运行 id | 全绿 |
| ishome-reportgen | 3（推导、写作、判官） | 整册运行标识 + 章名 + 第几轮，判官再补批次 | 297 绿 |

三处共同的做法：**名字必填、没有默认值**——漏传的地方当场在类型检查与测试里露出来，不会悄悄放过。运行编号一律取现成的标识，取不到就是空，不编。**这次只加标记，提示词、业务逻辑、输出解析、自动检查一律没动。**

**中途订正一件**：任务书里写的键名带 `ishome_` 前缀，实际回调认的是 `call_point` / `run_ref`。第一路按代码改对了并回报，另两路当场统一。三仓核过，键名一致。

### 2. 回调代码从"照文档猜"变成"真跑验过"

在本机另起网关实例真打了四次（**常驻 :4000 全程没动**，仓里 `run-dev.sh` 的注释就写着它不该为一次验证被打断），照出并改掉三处猜错的：

- 逻辑模型名在 `litellm_params.metadata.model_group`，带厂商前缀的真实模型名在同一份 metadata 的 `deployment`；`litellm_params` 里**没有** `model` 这个键。
- 调用方带的标记被代理拷进 `requester_metadata` 并同时摊平进顶层，两处都要读。
- 上面两个由网关在调用方之后写入，所以**调用方冒充不了模型名**。

四次各验一件：带标记的记到名下、不带的标"未登记"且原名留在运行编号前缀、图片类走对分支、模型名不存在时错误也记一条。

### 3. 整条链跑通

新写 `ishome-infra/litellm/run-with-ledger.sh`，起 :4003 真打一次：

```
模型回复: 收到
AI 判断: chat-intent | 运行编号: e2e-verify-001 | 模型: design-intent.default -> openai/qwen-flash
发给模型的内容: [{"role":"user","content":"只回答两个字：收到"}]
耗时: 581 ms | token: 15 | 来源: local
```

再导进判官台库，查得到同一条。**真调用 → 落记录 → 导入 → 查得到，通了。**

脚本两处坑（都已修）：①变量名后紧跟中文标点，bash 把标点当成变量名的一部分，直接起不来；②临时配置放 `/tmp` 时回调导入不到——**网关只把「配置文件所在目录」加进模块搜索路径**（既有的万相插件能用 `custom.` 导入正是靠这条）。改成临时配置落在 `litellm/`、回调在 `custom/` 下做软链接，与既有插件同构，退出时都删。**回调代码不复制**，软链接指向判官台那份，一份代码只有一处。

### 4. 判官台页面做出来了

新页 `judgebench/static/callpoints.html`，五个视图，接真接口，18 次提交合进主干，85 测试全绿。

**为什么是新文件而不是改现有页**：仓里另两个 session 的分支（split-judge-ui、render3d-label-set）**都在改 `index.html`、`store.py`、`handlers.py`**。所以新页独立成文件，`app.py` 只加一条静态路由，那三个文件一行没碰——合并时零冲突，事后核过。

五个视图：①12 张卡按业务线分组，计数真实，没有规则检查检测器的三处显示"—"而不是 0，对不上名字的记录单列；②详情三栏，文字、看图、生图三种形态都渲染（含模型给的框叠回原图）；③测试用例页，来源三种标记、改了什么、跑几次过几次、多次回复并排标黄；④判定页，文字拖选、对话按轮、图上点位置三种都真跑通，键盘 P/F 与上下条，无分类选择器，AI 判官照实说"还没接"；⑤一致率页只出数量与列表，通过率块写了但默认不渲染。

**问号说明整套做进去了**（用户当天的要求）：每个功能区一个小圆圈，点开三段白话——这是什么 / 怎么用 / 什么时候用它；顶栏"第一次用？"五句总说明。

截图 17 张（五视图浅深各一、空态四种、问号一张），console 零报错。截图里的跑次来自本机替身网关，不是真模型。

## 二、还差什么

1. **常驻网关上还没挂**——挂了要重启网关，:4000 有别的活在用。**在那之前，线上调用一条都不会被记下来**，页面上"线上真实记录"全是 0。什么时候挂由用户定，脚本已备好。
2. **各仓已有的自动检查还没导出来**——报告线约 45 条、获客线十来条今天还散在流水线各步里，判官台只有格式检查与两条示例规则检查；页面上那三处显示"—"就是这个意思。
3. **图片类没真出过图**（一张两三毛钱），厂商真返回图之后抽成文件那段没跑过真数据。
4. **AI 判官对 AI 判断这条线没接**——判官台的判官今天仍只吃图；文字类只有人工判定。
5. 磁盘按天滚没写；基本信息表还在判官台自己那儿（方案 §六 第 5 条的落地默认）。

## 三、这一天的花费与影响面

- 真调用花费：两轮验证加起来不到一毛钱。
- **没碰任何别人在用的东西**：常驻网关 :4000/:4001/:4002 全程没动（验证用的 :4003 跑完即停）；三个仓里别人的工作区一个没碰；判官台那三个被别人改着的文件一行没动；全程没用 `git stash`，`git add` 全是显式路径。
- **一律未 push**（《纪律·push 由用户定》）。

## 四、当天的裁决

**不用造词，说这东西是什么**（用户原话与替换词表在《拍板清单》§〇 同日条，纪律 v1.7 新条）。起因是视觉稿里"调用点／回文／真跑／扰动／留底"这些词全是执行者与子代理当天新起的，用户说*"现在这些造的名词对沟通产生了非常大的影响，效率变得很低"*。方案、状态页、三份设计稿、界面文案、代码里给人看的中文，全部换过一遍。

## 五、被中断一次

三路子代理在同一时刻撞上会话额度上限被中断：报告线那路改完没提交、网关那路验证记录没写完、前端那路一行页面还没写。处置＝主 session 逐仓盘点残留（两处未提交改动都验过测试后补交）、验证记录由主 session 据留下的真实记录补写、前端重起一路从零做完。**没有一处半成品被当成完成品记账。**
