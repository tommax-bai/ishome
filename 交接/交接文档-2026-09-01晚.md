# 交接文档：2026-09-01 晚（换 session）

> 日志：写完不再改。本文只回答一个问题——**接手的 session 从哪儿继续**。
> 状态的唯一真源是《现在在哪儿.md》（本文建档时 v3.0），细节不在这儿复述。

## 接手顺序

1. **《现在在哪儿.md》**——先读这份，等业主的三件、四条线现状都在头部；
2. 本日三图侧的全部经过＝《交接/交接文档-三张图出得来.md》**追记三～五**（三轮定罪与修复、
   注释与槽位、蓝图精修、模型评估、真机发图流程）；
3. 合规＝《交接/交接文档-AI生成标识合规评估.md》（十个拍板问题在 §六）；
4. 口径权威＝《方案/第一阶段视觉提案与Prompt说明.md》v1.1（**§13 四条原则是 9-01 刚补的**：
   三张同底稿、三层分离、心理所有权、透视图第四阶段）；裁决全部在《规范/落地假设与拍板清单.md》§〇。

## 等业主拍的（新三件 + 旧账）

1. **蓝图挑版**：墙体（淡芯/剖面纹）× 版面（排底竖版/排侧横版），三张样张已在业主飞书；
   执行者推荐淡芯×排底（推荐不是裁决）；
2. **注释版 v2 认不认**（业主飞书最后一张手账图；三跑排序 v2 > v2-2 > v2-3）；
3. **合规十问**（核心：显式角标自家叠印 vs 火山水印；字高照不照国标 5%）；
4. 旧账：生成图对象键形态进 contracts 表、扩展名跟不跟字节走、生图尺寸降档减半价
   （出处：《交接文档-三张图出得来.md》追记二、追记三）。

## 在途物（接手要知道的悬空件）

- **render2d 分支 `brief-blueprint-sheet`（提交 3bb1ea9）未合并**——蓝图整页版式在这上面。
  合并时点＝业主挑定版式；合并要做三件：去页脚（免责文案，旧裁决赢）、按 564badd 之后的默认
  brief 字节（117939B）重钉该分支的 sha256 测试、切默认输出。工作树在
  `~/codes/ishome-render2d.wt/brief-blueprint-sheet`（若被清理，分支还在仓里）；
- **挑图门禁未立项但已"必须做"**：imagegen 三跑出过人脸涂鸦、三维那边同日量出写实化方差极大
  （凭空加房间），两条线同日各自撞到"单张生成不能直发业主"。可自动：长宽比/留白带/几何跟随度；
  现阶段只能多跑挑优＋人眼：错字、涂鸦、箭头。三维侧的保真度门禁是同类，设计时对齐；
- **小房间槽位挤不下待议**（卫生间三跑各缺一样，挑图救不了）；
- **桶现状**（前缀 `uploads/e2a41bfe…4514/`）：plan-* 是按段厚度版（master 5944B）；
  atmosphere 注释版 v2 三张在桶，但它们生成时吃的是上一版母版（6023B）——下次真跑自动吃新母版。

## 本机操作要点（真机发图，追记二坑单的浓缩）

- channel-svc 短起：`JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`、
  `--spring.config.import=file:$HOME/.ishome/channel-feishu-local.yml`（`~` 在引号里不展开；
  `optional:` 会吞凭证缺失）。**本机实例抢 dev 服务器的入站事件，发完立即 pkill**；
- 发送脚本形态：aipipe venv + `PYTHONPATH=~/codes/ishome-contracts/gen/python`，gRPC 到
  `localhost:9102` `ChannelService/SendMessage`，`external_user_id=ou_03e9db45c786e33eadbdf038e570dd9a`，
  图走 `image.object_key`；
- 样张类候选图**借桶传输**（传→发→删键），不算产物；
- OSS 凭证 `~/.ishome/oss-local.env`（注意变量名是 `ISHOME_OSS_BUCKET_PRIVATE`）。

## 本次推送

六仓全部推平（ishome / ishome-imagegen / ishome-render2d 主线＋`brief-blueprint-sheet` 分支 /
ishome-aipipe；backend 与 contracts 此前已平）。服务器上跑的仍是旧形态，**上服务器另拍**。

*建档 2026-09-01 晚 · 执行者 Claude。*
