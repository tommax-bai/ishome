# 交接文档 · 2026-08-29（午）

> 本文接替《交接文档-2026-08-27晚.md》成为**当前状态唯一入口**。那份的正文与追记一~六仍然有效，
> 作为 8-27/8-28 两天的执行细节留档，但**状态口径以本文为准**（本文与它冲突处，本文赢）。
> 命名去掉了"晚"字：这是一次午间交接，按文件名日期取最新的规则不受影响。

---

## 〇、一句话版

**规范 v2.4 推翻了立了三个版本的"未校准资产禁止进入 PAID"，报告域的纪律形态从"没依据就别说"改为"说了就必须标"；同批把积压的十六条待裁一次性拍完。**

**⚠️ 最重要的一件事：规范已经是 v2.4，代码还停在 v2.3 的实现。** 求值线现在仍在隐藏落点（`AnchorPresentationPolicy` 的 `WITHHELD` 分支还在跑），成文线仍有 `gate-withheld-*` 系列。**下一个 session 的首要任务就是把实现追上规范，且必须按规定顺序走（见 §四）。**

---

## 一、本次裁决（全部已落《落地假设与拍板清单》§〇）

### 1. 隐藏档取消，纪律形态改为标注（规范 v2.3 → **v2.4**，§14.9）

**用户原话**：*"未校准资产禁止进入 PAID 产物，这条限制我认为应该去掉，我们的整个产品策略就是给客户建议，如果建议有问题，我们后续应该调整，而不是隐藏掉，这样对客户也没有价值，我们也没法迭代；过期的降级方式是给标记，数据来源和时间，但是还是使用这份数据。"*

**推翻的六处**：

| 原条文 | v2.4 |
|---|---|
| 规则 4.10「未校准资产禁止进入 PAID」（v1.2 立） | **作废**——照常进，随标注 |
| 规则 4.10c 三条隐藏判据（定位数字／无区间形态／判不准） | **全部作废**（昨天刚写进去） |
| 规则 4.10a「经验条目只能降档或不收录」 | 受限的是**语域**不是**存在** |
| 规则 4.18「经验判断整批降档或缺席」 | **照发并标注** |
| 规则 5.15「单价过期 → 只出占比不出金额」 | **照常出金额 + 标取数时间** |
| §12 禁止项四条 | 两条作废、两条改射程 |

**裁决逻辑（要记住，因为它推翻的是立了三个版本的条文）**：隐藏的成本一直被低估——它同时拿掉了**业主的价值**（藏起来的建议等于没有）和**系统的信号**（规则 4.17 的行为信号全部来自业主对内容的反应）。于是规则 4.10a 的转正路径在冷启动期成了**死循环**：藏起来 → 没信号 → 转不了正 → 继续藏，而冷启动期正是最需要信号的时候。真正要防的风险不是"业主看到没依据的数"，是"业主**误以为**这个数有依据"——标注解决它，成本低得多，业主还拿回了判断权。

**新纪律（替代隐藏档）**：**标注必挂**——`draft` 或已过期落点进正文的，同一页必须出现它的依据标注（来源 + 取数时间），**未标注即违规**。拦截点从"说出来"移到了"不标就说"。这是确定性判据、机检可判。

**没被推翻的两条**：
- **断言预算（规则 5.8/4.13）原样保留**——它管"以什么底气说"，不管"能不能说"。calibrated 仍是判断句背书的唯一来源，只是不再兼任"能否出现"的开关。draft 的数照出，语域限"我们建议"，不能写成"国标要求"。
- **`conflict` 仍不进 release**——标注解决不了两源互斥，标哪个都是替业主选边。

### 2. 十六条待裁一次性拍完

| # | 待裁 | 裁决 |
|---|---|---|
| ① | 造价章城市档 | **匿名画像加 `cityTier` 字段**（市场参数不是身份） |
| ② | 五条锁定文案正文 | 手册找不到，**改走起草 + 法务过目**路径 |
| ③ | `report_id` 谁铸造 | **project-svc**（幂等键在重试的那一侧） |
| ④ | 派发通道 | **HTTP 入口**（不用 Java Temporal SDK 直连） |
| ⑤ | 每轮入册上限 | **固化算法不固化数字**：≤ min(bad+fixed 双逐字有据的类数, 有产出跑数 ÷ 3)，每条每轮新增样例 ≤2 |
| ⑥ | "一条 cr- 只能属一层" | **升明文，作规则 4.10d 挂 4.10b 之下** |
| ⑦ | 弱词漏网词表 | **拆两批**：词面遗漏补既有判据，语境依赖建新判据 |
| ⑧ | 伪因果破格收 | **先查后裁**，不占入册名额 |
| ⑨ | 判官触发率过高 | **先建计数载体**，N 与阈值等有数据再定 |
| ⑩ | `fixed` 当写作侧正例 | **不给**（A/B 已证正例会被逐字照抄，`fixed` 更甚） |
| ⑪⑬ | FREE 隐藏／两条降档路径 | **被裁决 1 吸收** |
| ⑫ | 一包一档 | **维持**（混档产物求两次；一包两档会把档位从数据挪进控制流） |
| ⑭ | 包内枚举大小写 | **维持两套**（前缀是命名空间，枚举值是常量） |
| ⑮ | `GUIDE_SITE_CHECK` 推广 | **新增一条 ID**（注册表只增不改，不得改正文） |
| ⑯ | 锁定文案要求的口径 | **以数据包清单为唯一口径**（不从 check 的自然语言 requirement 抠 ID） |

---

## 二、当前真实状态（哪些建成了、哪些没有）

### 数据面（backend `4a6d710`，已推）

- `svc_rulebook` 七表族 + releases 表；**127 条资产、16 calibrated**（截至 8-28 获取第三轮）。
- release 已切：`lighting@v3` / `budget@v2` / `ergonomics@v1` 等，格式 `{domain}@v{n}`，快照**含 draft**、排除 `conflict`。
- **30 条 `cr-` 判据**：26 条规则层（`active`，gate 执行 pattern）+ **4 条判官层**（`semantic_judge` + `observing`：`cr-fabricated-fact` / `cr-undeclared-assertion` / `cr-anchor-out-of-scope` / `cr-self-endorsement`）。
- 种子链路全通：`verify_seeds.py` → `import_seeds.py` → `publish_release.py`。审计留档在 `_acquisition/` 与 `_iteration/`。

### 求值线（project-svc，**仍是 v2.3 实现**）

- 规则引擎读 release 求 `lkp-` → 报告数据包，字节级可重放。
- PAID 三域实测：**31 落点 / 8 隐藏**。⚠️ 这个"8 隐藏"就是 v2.4 要拆掉的东西。
- **造价章仍零单价落点**——单价是 `attributes`，求值线只投影 `parameters`（裁决①的 `cityTier` 也在这条上）。

### 成文线（reportgen `416f435`，已推）

- 三 activity：`report-unit-compose` / `report-page-assemble` / `report-book-check`。
- 出口过检两道：规则层 gate（机检）+ 判官层（**观察态，只记录不拦截**）。
- **锁定文案挂载链路已建**：要求随包 → 单元透传 → 装配层挂上页 → 册级验齐（`gate-locked-text-missing`）。写作器结构性产不出它。
- persona 四件消费面已齐（身份／判断句样例／断言预算／禁词表）。

### 编排（aipipe `226aef9`）

- `ReportComposeWorkflow` 已建、已跑通，入口 `POST /api/v1/genpipe/reports`。**至今没有调用方**——裁决③④就是为接这一条准备的。

### 契约（contracts `765a014`，已推）

- 报告数据包 JSONSchema、产物注册表（22 个 `art-`）、**锁定文案 ID 注册表**（8 条，其中 **5 条正文待补录**）、activity 注册名 #11-13。

### 本机环境

LiteLLM 网关（用户常驻，**改配置必须重启，不热加载**）已有六个逻辑名，含 `report-unit-compose.default` / `report-unit-judge.default`。凭证在 `~/.ishome/`（`LITELLM_MASTER_KEY` 在 `llm-local.env`），**永不入仓**。

---

## 三、新 session 阅读顺序

1. **本文**（当前状态唯一入口）；
2. 《规范/装修报告生成规则规范.md》**v2.4** —— 报告域宪法。**重点读规则 4.10 / 4.10a / 4.10c / 4.18 / 5.15 与 §14.9**，这五处是 8-29 刚改的；
3. 《规范/落地假设与拍板清单.md》§〇 —— 全部裁决记录，末两条是本次的；
4. 《方案/装修报告AgentGraph设计说明.md》v0.2 —— 两条线的分工（求值线 / 成文线）；
5. 《规范/开发规范与代码分层.md》—— 写代码前必读；
6. 《交接文档-2026-08-27晚.md》追记一~六 —— 8-27/8-28 的执行细节与踩坑记录。

---

## 四、下一步任务序列（**顺序是纪律，不得反序**）

### 序列 1 · 标注链路（v2.4 实现，最高优先）

**规范里写死了实现纪律：先建标注链路，再拆隐藏分支。** 反序会有空窗期——旧门禁拆了、新门禁没建好，没依据的数不带标注就发出去了。

1. **落点加 `provenance`**：`source` + `effective_from/to` + `calibration` 随落点下发（contracts schema 先改，字段只增不删）。
2. **成文线页级标注比对门禁**：`draft` 或已过期落点进正文 → 同页必须有对应标注，否则违规。这是新的执行器。
3. **然后**才删 `AnchorPresentationPolicy` 的 `WITHHELD` 分支、`gate-withheld-*` 系列；`withheldAnchors` 字段**保留恒空**（契约只增不删），description 写明作废日期与裁决。
4. 定位数字配套：标注 + `GUIDE_SITE_CHECK` / `DISCLAIM_P1` 现场复核话术一起挂。

### 序列 2 · 造价章补齐（裁决①）

求值线投影 `attributes`（`work_item` 单价）；匿名画像加 `cityTier`；单价过期改为标注取数时间（规则 5.15 新口径）。做完造价章才第一次有金额。

### 序列 3 · 两条线接通（裁决③④）

project-svc 铸 `report_id` → `POST /api/v1/genpipe/reports`。注意 aipipe（pydantic converter）与 reportgen（默认 converter）**两侧 Temporal converter 不对称**，已跨仓真跑验证互通，但仍是隐性风险。

### 序列 4 · 三条纯文档裁决落规范

⑥ 规则 4.10d（一条 cr- 只能属一层）；⑤ 入册上限的算法式口径 + "排序判据=能不能溜进成品，非频次"进规则 4.17；⑦ 弱词词表前半批（`建议先`/`大致是`/`可以考虑` 补进 `cr-weak-word` pattern，重新 release）。

### 序列 5 · 观察期计数载体（裁决⑨）

统计"某条 observing 判据在多少份里触发了多少次"。**有了计数才谈得上定 N 和阈值**，现在没有任何地方在数。

### 序列 6 · 挂账项

- ⑧ 查 lighting `accent-ratio` 落点的 `triggered_by` 有没有那三个户型字段（先查后裁，不占入册名额）；
- ② 五条锁定文案起草 + 法务过目；⑮ `GUIDE_SITE_CHECK` 推广新 ID；
- 获取回路第四轮：七分项占比（**连续三轮未取到**——公开源对"硬装"定义不唯一，需要报价单类数据源）；
- persona 判断句样例**改源重编译**（19 对里 15 对自己违反数字纪律或带禁词，两域归零；且示范不能写成能独立成立的结论句，否则修好数字仍会被抄）。

---

## 五、红线与纪律速查（叠加在 8-23 红线之上）

- **AI native**：无人审、无转人工、无运营编辑后台、无人预设模板。"不依赖人"只管系统**运行**不管**迭代**——人的改动走"改源 → AI 编译成数据 → 核验 → release"（规则 4.19）。
- **数字不由 LLM 决定**：落点派发前求值完，正文数字只经 `{lkp-*}` 引用。**一个占位符 = 整条落点**（区间不许拆成 `-min`/`-max`，拆开会丢掉另一端）。
- **中文数字同样算数**，列举计数不算（"四个区域"合法）。
- **命名禁纯序号**：前缀即命名空间（`art-/dom-/pt-/cr-/lkp-/gap-/prec-/gen-/stage-/tier-/acc-`）。
- **纪律型条目禁以知识条目入库**（规则 4.10b）；种子禁预置 `calibrated`、禁预置 `status=active`——拦截权只能由观察期授予。
- **禁以想象填充判官反例库**：反例只收真跑观察到的样本。**一条真样本胜过十条编的。**
- **锁定文案禁编造正文**：法律性质话术，缺一句只是少说，编一句是替企业作错误承诺。
- **v2.4 新增**：`draft` 或已过期落点进正文**必须随页标注**（未标注即违规）；标注不是可选的礼貌，它是隐藏禁令的等价物。

---

## 六、给下一个 session 的三个坑（都是真踩过的）

1. **"一条 `cr-` 只能属一层"是结构约束，不是风格偏好**。`verify_seeds` 写死"带 `examples` 者必须 `observing`"，而 reportgen `gate` 执行 pattern 时**不看 status**。所以规则层（恒 `active`）与判官层（`semantic_judge` + `observing`）互斥——给既有 regex 判据挂样例会让**表说假话**。**"gate 忽略 status"是正确实现，不是 bug，别顺手修。**（裁决⑥要把它升成规则 4.10d，升之前尤其别动。）

2. **过拦与漏拦同样是失效**。两次栽在这上面：`gate-chinese-numeral` 收紧后漏掉了它自己的立案样本，同时把"四个区域"误判；`AnchorPresentationPolicy` 只看顶层键，把造价章整章判没了。**改判据必须回归立案样本。**

3. **persona 判断句样例：示范句越多，过检率越低**。四轮 A/B 实证，机制是模型把 ✓ 逐字抄进卡片当结论（已加 `gate-sample-verbatim-copy`）。为保样本量试的非对称规则 **0/6，比对称规则更差**——样本量不是稀缺资源，**可抄性才是代价**。

---

## 七、仓库状态（全部已推远端）

| 仓 | HEAD | 状态 |
|---|---|---|
| ishome（中控） | `4958f51` | 规范 v2.4 + 拍板清单 |
| ishome-contracts | `765a014` | 锁定文案注册表 + 数据包锁定清单 |
| ishome-backend | `4a6d710` | 判官库首批入册 + 降档判据递归订正 |
| ishome-reportgen | `416f435` | 正例通道 + 锁定文案挂载链路 |
| ishome-aipipe | `226aef9` | 成文线编排 |
| ishome-infra | `21b2c16` | 网关六个逻辑名 |

*交接文档 2026-08-29 午 · 执行者 Claude。本文正文定稿后不改，增量以文末"追记 N"追加。*

---

## 追记一 · 2026-08-29（下午）· 执行者 Claude —— 序列 1 做完：实现追上 v2.4

**一句话**：**§四 序列 1（标注链路）四步全部落地并端到端真跑**——落点带 `provenance` 下发、成文线页级标注比对门禁建成、隐藏分支拆除、定位数字配套挂载就位。规范与代码的版本差消失：现在两边都是 v2.4。

### 一、按规范写死的顺序执行（先建后拆，四个提交分两步）

| 步 | 提交 | 内容 |
|---|---|---|
| 建 1 | contracts `672a145` | `anchors[].provenance`（source/effectiveFrom/effectiveTo/calibration/**annotationRequired**）+ 顶层 `evaluatedOn`；`withheldAnchors` 标作废恒空；`presentation` 描述收窄射程；锁定文案注册表补"派生必挂"一行 |
| 建 2 | backend `a07540d` | `AnchorProvenance` + `AnchorProvenancePolicy`（两条确定性判据：未过门／时效越界）；求值基准日提为入参；未过门定位数字 → 派生 `GUIDE_SITE_CHECK` |
| 建 3 | reportgen `d2bb771` | 页级标注比对门禁：单元投影 → 装配挂载 → **册级拿数据包重算比对**（`gate-provenance-annotation-missing` / `-note-mismatch`）；生产方守卫改判 `gate-provenance-inconsistent` |
| 拆 | backend `ff30df6` + reportgen `8313388` | `WITHHELD` 分支、`gate-withheld-*` 两道、`hasRangeForm` 递归判据、**`gate-thesis-degraded-anchor`** 全删 |

**三个设计判断，理由记在此**（都不是裁决，是实现选择，认为不妥可直接推翻）：

1. **标注的载体是"页"上的结构化字段（`ProvenanceNote`），不是卡片、不是文案**。理由与锁定文案同源：标注里全是日期，而卡片正文禁裸数字——写作器**结构性地**产不出合规标注；正文由渲染层按结构化数据出（规则 2.4 零生成）。prompt 里相应加了一条"来源和日期由系统挂，你别写"，省掉必然的一轮重写。
2. **求值基准日 `evaluatedOn` 是入参不是时钟**。时效越界若读运行时时钟，同一份 release 同一份输入在两天里会算出两个包，规则 8.2 的字节级可重放当场失效。线上调用走三参数重载取当天（UTC），重放走四参数重载传回原包的日期。
3. **`GUIDE_SITE_CHECK` 的挂载条件由求值线派生**（未过门 × 定位数字 → 该域并入锁定清单），而不是在成文线判。派生依据是落点的结构化属性，不是从 check 的自然语言 requirement 抠 ID（裁决⑯ 的禁令射程），成文线仍只搬运。

### 二、`gate-thesis-degraded-anchor` 一并拆掉（规范明文，非自作主张）

规则 4.10c v2.4 修订段原话：*"原文'主旨句的数字支点必须全部是可作支点档'随隐藏档取消而失效（未过门落点已可进主旨句，条件是随页标注）"*。这条门禁拦了三个版本，代价是未过门的建议进不了主旨句、也就进不了业主眼里，规则 4.10a 的转正路径拿不到行为信号。**断言预算那条原样不动**——它管"以什么底气说"不管"能不能说"，`calibrated` 仍是判断句背书的唯一来源。prompt 侧同步改档位标签：`【降档·只可参考口吻，禁进主旨句】` → `【未过门·建议口吻】`，并明说"可以进主旨句，但语域限'我们建议'，不许写成'国标要求'"。

### 三、端到端真跑（真库 + 真模型），裁决效果核对

存档见 backend `_iteration/run-2026-08-29-v24-first-run.md`（`634f29a`）。

| 口径 | v2.3（8-28） | v2.4（本次） |
|---|---|---|
| PAID 三域 | 31 落点 / **8 隐藏** | **39 落点 / 0 隐藏**，29 条需随页标注 |
| ergonomics 单元 | — | `verdict=ok`，22 张卡，22 条标注全部挂上页，册级比对通过 |
| 派生现场复核话术 | — | 空——真库**当前无 `number_class=locating` 参数**，机制就位待数据 |

八条回来的落点正是"点值降不成参考区间故隐藏"拦掉的那一类（造价分档值、色温点值、挂杆高度）。budget/lighting 两域未通过，栽的全部是 8-28 已登记的老形态（示范句照抄、拆 `min`/`max`、自造 lkp-），与本轮改动无关。

### 四、真跑采到的两条新东西（都**未处置**，请下一轮裁）

1. **一数一卡、正文与主旨句逐字重复 22/22，`assertions` 声明 0/22**。落点从 3 条变 22 条后模型转入"一个数配一句话"的机械模式——**这是取消隐藏档的伴生现象**，隐藏档存在时落点太少，没机会暴露。加一条"正文不得与主旨句逐字相同"的机检拦得住形态、拦不住成因（模型会补一句填充话），真正的处置在卡片编排（pt- 页型库）与 prompt，故本轮不动。
2. **判官灵敏度随卡片数塌陷**：同一份文稿，22 张卡 → `observations=0`；取前 6 张再问一次 → 6 条 `cr-anchor-out-of-scope`。同模型同判据 temperature=0，**无报错、无解析失败，就是空数组**。要紧处：观察态的全部价值是计数（裁决⑨"先建计数载体"），而 0 命中会被当成"这一份很干净"记进统计。**建计数载体之前先撞上这条，等于先救回了那份统计的可信度**——分批送审是显然方向，但 N 与"分批是否改判官口径"要有数据，与裁决⑨ 是同一件事的两面。
3. 附带存疑一条：`gate-chinese-numeral` 命中 `半小时`。时长不属规则 2.3 的三类数字，疑似过拦；但它确实是一句没有落点背书的数字表述。**登记不处置**——改判据必须回归立案样本（`不能低于九十` 不能被放过），等同类样本攒够再议。

### 五、需要用户拍的一条

`GUIDE_SITE_CHECK` 正文写死"请在**水电交底当天**，携带本清单…"，而 v2.4 把它的挂载条件扩展到了"任何含未过门定位数字的页"（规则 4.10c 原文点名它作配套话术）。**用在非水电章节时语境偏窄**。它与既有待裁项①（`GUIDE_SITE_CHECK` 推广到验收/报价清单需不需要新 ID，即裁决⑮）是同一件事：**是否为"正文里的未过门定位数字"另立一条 ID**。注册表已就地登记，新 ID 落表前按规范原文用这一条。真库当前无 locating 落点，故此事**暂无实际产出影响**。

### 六、序列表的当前状态

- **序列 1**：✅ 四步全done（标注链路 → 门禁 → 拆隐藏 → 定位数字配套）。
- **序列 2（造价章补齐）**：未动，现在是最高优先——`attributes` 投影 + `cityTier` + 单价过期标注，`provenance` 的 `effectiveFrom/To` 就是为它留的（parameters 表无此二列，恒 null；不为参数表预造列）。
- **序列 3（两条线接通）**：未动。`lockedTextsByDomain` 目前只含求值线**派生**的那部分，调用方按 art- 传入的那部分在接通时并入同一 map（并集去重），届时记录形态不变。
- **序列 4/5/6**：未动。序列 5 请连同上面第四节第 2 条一起考虑。

### 七、仓库状态（本轮四提交，均**未推**，push 时机由用户定）

| 仓 | HEAD | 增量 |
|---|---|---|
| ishome-contracts | `672a145` | provenance 契约 + 隐藏档作废 |
| ishome-backend | `634f29a` | 求值线 provenance + 拆隐藏分支 + 真跑存档 |
| ishome-reportgen | `8313388` | 页级标注门禁 + 拆隐藏与主旨句限制 |
| ishome（本仓） | 本追记 | — |

质量门全绿：project-svc 41 条（含真库集成 5 条）+ spotless/checkstyle；reportgen 86 条 + ruff/mypy/import-linter。

---

## 追记二 · 2026-08-29（晚）· 执行者 Claude —— 三问题分析归档 + 一处订正

**一、订正追记一 §四.1 的错误归因**：那里写"落点从 3 条变 22 条后模型转入念数字模式"，**数字错、归因也站不住**。
按同一份包复算：ergonomics 域 23 条落点，v2.3 口径下会被隐藏的只有 6 条——该域**本来就有 17 条可见**，不是 3 条。
真实情况是**这个域此前从未跑出过成品**（8-28 三跑全白卷/failed），22/22 是它第一次有产出就暴露的形态，
与"落点变多"**没有因果证据**。backend `_iteration/run-2026-08-29-v24-first-run.md` 已就地订正留痕（`492a0f0`）。
（各域实测：lighting 13 / ergonomics 23 / budget 3；v2.3 口径下会被隐藏 1/6/1，合计 8。）

**二、三问题的背景/成因/目标/现状已摊开归档**：《评审/v2.4落地遗留三问题-2026-08-29.md》（`16488e2`）。要点：

- **共同根源＝"设计里有、实现里没有"**：①图 v0.2 §3 的"叙事推导 → 卡片写作"两步**塌成一步**；
  ②库里 30 条 `cr-` **只有 8 条有执行器**（4 条 pattern 在规则层 + 4 条判官层），页级三条判据
  （`takeaway`/`first-visual`/**`one-thesis-per-page`**）一条都没物化；③观察态**没有任何地方在计数**（裁决⑨）。
  隐藏档在的时候这三条缝被"内容本来就发不出去"盖住了，v2.4 把内容放出来才第一次可见。
- 问题一建议：补叙事推导节点为主、物化页级判据跟上；**只加"正文≠主旨句"机检不可取**——会把看得见的退化变成看不见的填充。
- 问题二建议：先做"6/11/22 分批对照跑"量灵敏度曲线，再定分批 N，并**与裁决⑨ 计数载体合并为一项**
  （计数要带"送审卡片数"维度，否则将来无法回溯解释触发率）。
- 问题三：真正的阻塞不是文案 ID，而是**分类口径没落到种子里**——按规范 §2.3 定义应属定位数字的
  `lkp-switch-height`/`lkp-socket-height-common`/`lkp-mirror-height`/`lkp-toilet-center-offset`/
  `lkp-pendant-drop-dining` 全标成 `selection`/`analysis`，真库 `locating` 为 **0 条**；且 V2 列注释写
  `position` 而契约与代码判 `locating`、`verify_seeds` 不校验取值——**词表两套且无校验**，将来会静默失效。

**三、新增两条待裁（并入挂账）**：①是否为"正文里的未过门定位数字"另立锁定文案 ID（与裁决⑮ 同源，可一并拍）；
②`number_class` 是否重标（影响标注与话术挂不挂、图纸侧"零定位数字"机检的射程），建议同批给 `verify_seeds` 加取值校验。

**四、附带查出两条 v2.4 数据残留**（均无执行器，当前无产出影响）：`cr-budget-stale-price` 的 requirement
（"过期单价只出占比不出金额"）**与 v2.4 规则 5.15 新口径直接冲突**，随序列 2 改源重发；
`cr-draft-not-in-paid` 的 message 仍是作废口径（"未校准资产进入 PAID"），改文案不改 `asset_id`。
