# 《是我的家》落地交接文档

> 日期：2026-08-22
> 用途：设计阶段已收敛，新 session 从本文件开始进入**落地**。本文只做导航与裁决汇总，细节以五份基线文档为准。
> 铁律：**"已裁决事项"一节列出的结论不再重新讨论**；如落地中发现与文档冲突，以后修订者为准并回改另一处（回改纪律）。

---

## 一、项目一句话

"是我的家"：AI 家装设计产品。IM 会话式设计 Agent（首发飞书）+ 内容工厂双引擎——初步设计（确认闭环 + 母版 + 模板化交付图集）建立信任，用户认可后进入付费深度设计（3D 场景 + 全屋一致效果图），家具/定制柜体经联盟与量房闭环变现。

## 二、文档集（建议阅读顺序）

均在本目录 `/Users/baitianxing/codes/ishome/`：

| 顺序 | 文件 | 角色 | 版本 |
|---|---|---|---|
| 1 | 架构对齐-设计Agent×技术架构.md | 拼接层：六大决策、服务落点、契约映射、待拍板⑤-⑩ | V1.3 |
| 2 | AI装修效果图产品-技术架构方案.md | 11 服务、中间件、仓库拆分、待拍板①-④ | 修订二 |
| 3 | 装修设计Agent架构方案.md | 产品与 Agent 行为基线：确认闭环、认知状态、母版+模板库、Workflow | V1.3 |
| 4 | 开发规范与代码分层.md | 分层、类/函数/变量命名、领域术语表、变化轴七规则 | V1.1 |
| 5 | 第一阶段视觉提案与Prompt说明.md | 视觉模板素材与两个完整 Prompt、QA 清单、§12 产品化改造要点 | 含 §12 |

## 三、已裁决事项（不再重新讨论）

**最高优先级两条（用户反复强调，V1.3 双裁决）：**

1. **系统中不存在"人工介入"概念**。无人审、无人工复核、无抽检队列、无转人工。用户侧收集并确认信息（确认闭环），其余全部靠迭代系统解决。人的判断只允许出现在设计时（模板上线一次性验收）。
2. **设计只面向"把系统做好用"，不面向责任划分**。禁止"责任归属/免责/声明"类论证；产出性质由入口服务声明一次性说明，交互与交付物中零免责文案。

**产品与架构裁决：**

3. 核心保证 = 确认闭环：提取 → 确认清单 → 看图点错 → `user_confirmed` 后才进入设计。烂图也要支持，不做解析准确率前置测试。
4. 数据信任分轨：尺寸类确认后即为设计依据（算术交叉校验兜可检出的矛盾，毫米级交付下单前量房升级为 `measured`）；结构类永不采信口述，只走"默认不动结构 / 硬证据"两条路径，**结构数据的系统性正解是在户型库侧沉淀做准**。
5. 户型获取主路径 = 户型库（小区名检索），上传解析是降级路径；比例锚点校准全屋推算。
6. 双引擎：内容工厂（批量预生成，机检门禁后发布）× 交互设计引擎（私有 ProjectState），在户型库"认领即分叉"处咬合。
7. 交付形态 = 确定性母版（plan-2d-render，几何唯一真源）+ 模板库组合交付图集（默认：温暖奶油之家→功能说明图→彩铅生活草图；运营可配；每张独立回读母版）。
8. 11 个服务：c-bff / admin-bff / identity / estate(+catalog) / content / genpipe-svc / genpipe-worker / **design-svc** / trade / shelf / **channel**。业务域 Java 21+Spring Boot 3，生成域 Python（Temporal 双 namespace），前端 TS（uni-app 一码双端）。
9. IM 为主通道、可插拔（能力声明分支，禁渠道身份分支），**首发飞书**；H5 只承接"指图时刻"，每次 H5 结果回显进聊天线程；小程序=分享卡片+支付容器。
10. 中间件定案：Temporal、RocketMQ 5（只跑业务事件，编排留 Temporal）、Postgres schema-per-service（含 svc_design、svc_catalog）、LiteLLM+Langfuse、RuoYi-Vue-Plus、Meilisearch、PostHog+ClickHouse+Metabase。
11. 质量门 = 机检门禁（consistency/compliance/scorer，全自动）+ 用户确认门 + 硬证据机检；运行时质量信号产品内嵌采集（"换一张"点击、确认修正率、投诉回流）作为迭代地面真值。
12. 工程规范：DDD-lite 四层（Java）/ 纵切+workflow 无 IO（Python）；变化轴七规则 R1-R7 与 10 条轴盘点；量纲入名（`_mm/_sqm/_cents`）；领域术语表进 contracts 仓做唯一真源。

## 四、待拍板项现状（落地前需用户拍板的标 ★）

| # | 事项 | 状态 |
|---|---|---|
| ① | 团队画像 8-15 人、Java+Python 双班底 | ★ 未拍，**优先级最高**——不成立则语言收敛单栈、裁剪节奏 |
| ② | Postgres vs MySQL | ★ 未拍（文档倾向 PG：PostGIS/JSONB） |
| ③ | 模板验收台第一版范围（原审核工作台，已缩编） | 未拍，可后置 |
| ④ | 网关：云托管 vs APISIX | ★ 未拍 |
| ⑤ | 微信系渠道接入时机 | 未拍，第二批 adapter 时决 |
| ⑥ | 渲染算力：外部 API vs 自建 GPU | 未拍，靠 Langfuse 成本数据决 |
| ⑦ | 三维资产库来源（自建/采购/合作） | 未拍，深度设计链路最大外部依赖 |
| ⑧⑨ | ~~结构复核 SLA / 真人接管~~ | **已作废**（V1.3 无人工介入） |
| ⑩ | 风格图文字合成：模型直出 vs 确定性叠加 | 未拍，建议两种各生成一批实测对比 |
| — | `{code}` 公司/项目代号（包名、域名、proto 包前缀处处引用） | ★ 未定，落地第一天就要 |
| — | 模板库物理存储归属（svc_design vs genpipe schema） | 未定，实现期决策 |

## 五、落地第一步（新 session 的任务清单）

技术架构定的顺序是"拍板后：contracts 骨架 + backend 模块结构"。建议执行序：

1. **先请用户拍板 ★ 项**（①②④ + `{code}` 代号）；用户未拍时可按推荐默认（PG、云托管网关）搭骨架，但需显式声明假设。
2. **contracts 仓骨架**（唯一真源，先行）：
   - proto 包 `{code}.{domain}.v1`；`channel.v1` 统一消息模型（text/image/card/quick_reply/audio + 能力声明）
   - glossary.md + 枚举 proto（`CognitiveState` 六值：observed/inferred/proposed/user_confirmed/measured/verified；`ChannelType`：feishu 起）——词表见规范文档 §4.2
   - CloudEvents 事件清单（`{domain}.{entity}.{past-verb}`，design.* 见对齐文档 §5）
   - Temporal activity 注册名清单（10 个，只增不改）
   - 错误码注册表 `{DOMAIN}_{3位}`
   - CI：发布 Java/TS/Python 三语言 SDK
3. **backend 仓**：Gradle 多模块骨架（8 个 Java 服务 + shared/{kernel,starter}）、ArchUnit 基础规则集（分层依赖、层内禁令，见规范文档 §1.1）、Checkstyle 类后缀正则、Flyway 目录、服务脚手架模板。
4. **aipipe 仓**：uv workspace（genpipe-svc / genpipe-worker / design-svc + packages/{scoring,adapters,patch-engine}）、import-linter 契约（workflows 无 IO 等，见规范文档 §1.3）、copier 脚手架模板。
5. frontend / infra 仓可随后。

## 六、给下个 session 的工作约定

- **红线**：任何设计产出不得引入人工介入环节或责任划分论证（见裁决 1、2）。发现自己在写"兜底""免责""人审"时停下来重想——正确出路永远是约束、机检、当事人校验或迭代。
- **回改纪律**：后定的决策必须回改所有早期文档，冲突以后修订者为准；改词表需三处同步（规范文档 §八）。
- **规范即工具链**：规范不进 CI 等于不存在；分层靠脚手架模板生成，不靠人记。
- 用户偏好：说人话、批判视角欢迎但别绕、拍板后直接执行不反复确认。
