# 落地日志 · 2026-08-23

> **同日追记（白天）**：★ 项全部拍板（见拍板清单 §〇）；SDK 消费管道打通（contracts v0.1.2 基线 → v0.2.0 追加 mock 渠道；backend 源码纳入 `shared/contracts` 模块，aipipe git 依赖锁 tag；CI 用只读 deploy key）；**第一条纵切完成并 E2E 实跑通过**——mock 渠道注入用户消息 → channel-svc（gRPC ChannelService、mock/飞书双 adapter、幂等去重）→ design-svc（DesignService.IngestMessage、骨架回话）→ 回发捕获断言通过（`ishome-backend/scripts/e2e-mock-smoke.sh`）。飞书 adapter 已实装长连接与消息翻译，凭证门控。**同日下午：飞书真机双向收发打通**——用户在飞书发"你好"，收到 design-svc 回话（凭证在本机 `~/.ishome/channel-feishu-local.yml`，不入库；平台侧曾漏订阅 `im.message.receive_v1` 事件，补订后通）。真机首跑暴露"成功路径零日志"，已补三处观测（channel 入站转发/飞书发送、design 入站与回话）。本地起服务：design `DESIGN_GRPC_PORT=19101 uv run design-grpc`；channel `bootRun --args="--spring.profiles.active=local --ishome.channel.design-target=localhost:19101 --spring.config.import=optional:file:~/.ishome/channel-feishu-local.yml"`。**CI 口径（同日用户裁决）**：GitHub Billing 暂不修——代码目前全部从本机出去，五仓已配本地 pre-push 质量门（`.githooks/pre-push`，push 前自动跑本仓全套检查，实跑验证过；新 clone 需 `git config core.hooksPath .githooks` 启用）。云端 Actions 恢复的触发条件（届时再修 Billing，配置现成）：云端 session 直接改仓 / 多人多机协作 / 向 GitHub Packages 正式发布 SDK。Orchestrator/LLM、Temporal workflow、事件总线、identity 归一均为骨架 TODO（代码内已标注接入位）。

> **同日晚追记二（架构裁决）**：**绘图能力物理拆分**——经服务边界讨论，用户裁决推翻对齐文档"不新建渲染服务"口径：绘图 activity 按绘图逻辑拆为三个独立服务 + 独立仓库（`ishome-render2d` / `ishome-imagegen` / `ishome-render3d`），各自为独立部署的 Temporal worker、监听专属 task queue；genpipe 保留编排与非绘图 activity（解析/求解/校验/门禁）。裁决依据：无物理隔离的模块边界必然腐烂（用户历史经验）。明细见对齐文档 V1.4 §三、拍板清单 §〇。**待执行**：三仓建仓 + ishome-aipipe 中 5 个绘图 activity 存根迁出（见路线图第 5 项）。

> **同日晚追记三（架构裁决）**：**design-svc 一拆为二 + 里程碑引擎事件驱动 + 沟通助手 v0.3 收录**——①design-svc 拆为 chat-svc（Python 会话引擎，Orchestrator v1 代码保留）+ project-svc（Java，入 backend monorepo，项目唯一真相），判据同绘图拆分（逻辑异质→物理隔离），此刻拆零迁移成本（ProjectState 未落库）；②里程碑引擎改事件驱动状态机（真相在表 + checkCompletion），长周期 Temporal workflow 作废，Temporal 收缩至任务层；③《沟通助手架构方案.md》v0.3 收录为会话域基线（记忆/画像/承诺区/封闭动作集/里程碑 M0-M6），五处归位修订见其收录说明头。对齐文档升 V1.5（决策一重写、§5.1 表结构拆 svc_project/svc_chat）。**待执行**：aipipe design-svc 目录更名 chat-svc；backend 新增 project-svc 模块（见路线图 3、4、5 项）。

> **同日深夜追记四（拆分执行 + Temporal + 数据层，多 agent 并行）**：交接文档 §六 任务序列 1-2 与路线图 3/4/5 项主体执行完毕，全部提交并推远端。①**V1.5 拆分执行**：aipipe `design-svc` 更名 `chat-svc`（aipipe `9566b81`：包/入口/import-linter 同步，DesignProjectWorkflow 删除、temporalio 依赖移除；design.v1 契约入口与 LiteLLM 逻辑模型名未动）；backend 新增 `project-svc`（backend `085069d`：里程碑引擎事件驱动 checkCompletion——判据为结构化谓词纯数据（SlotCriterion/ArtifactCriterion，全 AND，零表达式语法）、on_enter 数据声明、`GET /api/v1/process-definitions/{version}` 单点分发 chat/project 双切片、修订预算判定；17 测试）。②**Temporal 落地**：infra 新增 `compose/dev-local`（infra `84050d0`：Temporal 1.29.7 + UI :8233 + PG16 :15432 + Redis :16379，compose 项目名 ishome-dev，**栈保持运行**）与 `compose/dev-server` 变体；genpipe 任务层落真实（aipipe `bbf1dc3`：namespace `genpipe`、GenBatchWorkflow 机检门禁状态机（重生成上限、超限 failed 绝不假成功）+ GenerationTaskWorkflow 任务路由、5 个绘图 activity 按注册名字符串 + task_queue 跨服务派发、workflow 专属队列 `genpipe-workflows`、真连集成测试 + 失败路径冒烟实跑）。③**V1.4 三绘图仓建成**（github.com/tommax-bai 私有：ishome-render2d/imagegen/render3d，worker 骨架 + activity 存根 + pre-push 门禁 + CI 占位；contracts `2b27f7a` 新增 `registries/task_queues.md`：`genpipe-activities`/`render2d-activities`/`imagegen-activities`/`render3d-activities`，绘图存根已自 genpipe-worker 迁出、注册名未动）。④**数据层首批**：backend `f35dff1`（MyBatis-Plus + Flyway 按仓内先例；`svc_project` 九表 + `svc_channel` 入站去重/出站幂等两表，仓储全落 PG，集成测试实连 + 无 PG 跳过双路径）；aipipe `c487a47`（`svc_chat` conversations/messages/user_profiles/commitments 四表 + 极小 SQL 迁移器 `chat-migrate`；repo 双实现——`CHAT_DATABASE_URL` 设置走 PG、未设内存，e2e 裸起不破；episodic_memories 待 pgvector 镜像后 V2）。⑤**E2E 回归通过**：mock 纵切在更名+落库后全绿，且真 Orchestrator 回话验证（正确进入 M0 槽位收集）；**两个坑**：起 chat-grpc 走真 LLM 需 `export LITELLM_API_KEY=$LITELLM_MASTER_KEY`（source ~/.ishome/llm-local.env），否则走兜底话术；e2e 现依赖本地 PG（channel-svc Flyway）。⑥**dev 服务器**（用户本日授权部署）：只读勘察完成——root@121.89.85.150 无任何容器运行时（全裸机 systemd）、2C/7.3G（余 5.5G）、根分区仅剩 12G（第一瓶颈）、目标端口 15432/16379/7233/8233/19101 全空闲、/opt/ishome 干净、Docker Hub DNS 污染须走镜像源；**装 Docker 被 Claude Code 权限分类器拦截，待用户批准后执行**（安全序列已备：网段预检→daemon.json 先行（mirror/live-restore/日志上限/10.200 地址池）→装后生产核验 aidcp/isales）。

> **同日深夜追记五（dev 服务器全栈部署，用户当场授权并放行权限）**：**部署形态经用户裁决改为裸机 systemd + 复用机上 PG**（推翻装 Docker 预案：PG 13.23 建 `ishome`/`temporal`/`temporal_visibility` 三库 + `ishome` 角色（scram、CONNECTION LIMIT 60——初设 20 被 Temporal 常驻池 15 连接挤爆，已扩）+ pg_hba scoped 行；Redis 暂不部署，chat 会话缓存本就未接）。落位 `/opt/ishome`（bin/python/aipipe/contracts/litellm/channel-svc/project-svc/env），运行时全部本机中转上传（Temporal 1.29.7 server+CLI、CPython 3.13.15 独立构建、uv；Docker Hub 在该机被 DNS 污染）。五个 systemd 服务：`ishome-temporal`（:7233，schema 1.9，namespace genpipe）、`ishome-litellm`（:4000，独立 venv——首装曾因 cwd 在 aipipe 项目下被 uv 装错 venv，已用 --python 显式指定修复并 uv sync 修剪 aipipe venv）、`ishome-chat`（:19101，CHAT_DATABASE_URL 落 svc_chat）、`ishome-channel`（:8102/:9102，**飞书长连接已切至服务器**）、`ishome-project`（:8103）。**全部端口只绑 127.0.0.1**（为此给两处 gRPC 增加绑定配置：backend `ishome.channel.grpc-bind`（5671639）、aipipe `CHAT_GRPC_BIND`（cf8d35d），默认行为不变）。**服务器上全链路实测通过**：mock 注入 → channel（svc_channel 幂等落库）→ chat（svc_chat 落库 2 条）→ LiteLLM/qwen 真实回话（正确记录"89平两室"槽位并追问小区名）。生产邻居（aidcp×3/isales×4/nginx/PG/redis）全程 active；余量：内存 available 4.7G、磁盘 9.9G。部署细节：aipipe 服务器副本的 contracts 依赖由 git tag 改为本地路径（`/opt/ishome/contracts/gen/python`，部署时 sed，仓库不动）；凭证在 `/opt/ishome/env/`（600，飞书/LLM/DB）；DB 口令本机留档 `~/.ishome/dev-server.env`。**待真机回归**：用户在飞书发一条消息即可确认收发已由服务器承接。

> **同日深夜追记六（图像模型接入）**：用户开通火山方舟即梦 Seedream 并给出 Ark key，遗留项"火山引擎凭证用途"就此落定（详见拍板清单 §〇）。实测四发：直连文生图/图生图各一、经 LiteLLM 网关文生图/图生图各一，全部成功（`usage.input_images=1` 证实 image 参数穿透网关）；服务器网关重启后同样出图。接入只改配置不改代码——`atmosphere-visual.default` / `realism-pass.default` 两个逻辑名进 `infra/litellm/config.yaml`（`3bb3a57`），**单模型关闭 drop_params**（否则 image/watermark/size:"2K" 这些非 OpenAI 标准参数会被丢掉、图生图静默退化为文生图，这是个易踩的坑）。质量结论：室内场景图生图能保住墙体/家具/软装位置，但会有细节漂移（实测吊顶多出木饰边、取景位移）——**consistency-check 因此是必需门禁**。待验证：遮罩局部重绘、以及 imagegen 的成本/并发画像。

> **追记七（2026-08-24：Web 3D 承载验证，用户裁决方向）**：用户提出"不做 3D 漫游视频，改做 HTML 三维预览"，并质疑手机端承载。已建**空间预览验证台**（artifact `a419fb24-106b-4336-9302-08fbf035e8b3`，源码留档 `ishome-frontend/experiments/space-preview-testbed.html`，未提交）：three.js r128 + 程序化房间壳体（墙/地/顶/窗洞全部代码生成，零素材）+ 三件 Poly Haven CC0 家具，页面自带帧率/装载耗时/设备与 WebGL 信息/资产包围盒读数。**实测结论**：①**飞书内嵌浏览器、手机浏览器、PC 三端均可渲染**（WebGL 可用，用户实机确认）——web 3D 承载可行性成立；②承载不是 PC/H5 二选一，**同一份页面响应式跨设备**（桌面右侧仪表栏、手机底部抽屉），分工按场景而非设备：手机=随时看/转发家人，大屏=认真研究与决策，手机端靠预设机位而非自由漫游把空间感说清；③**场景负载极轻**：三角面 20,143、绘制批次 9、贴图 11——**Seed3D 的 3 万面档位完全够用**，不必为高面数付溢价；④**资产尺寸偏差实测**（本次最有价值的数据）：CC0 家具包围盒 vs 真实规格——沙发 1571→1800mm（×1.15）、茶几 1540→1200mm（×0.78）、扶手椅 848→780mm（×0.92），**偏差 -22%~+15% 且方向不一致，不能用全局系数糊弄，必须逐件校准**；页面内置校准开关可直观对比（校准前茶几压过沙发）。**架构影响**：render3d 服务端**不需要 GPU**——渲染转嫁用户设备，base-render 保留但用途收窄为"给 realism-pass 出控制信号"（光栅化，CPU 即可）；`scene-compile` 产出契约应明确为 web 可直接消费（glTF 2.0 + Draco/meshopt + KTX2，多边形与纹理预算作为配置数据）；`3D 资产加工` activity 补两项入库门禁：**尺寸校准归一化 + 授权校验**；《装修报告生成规则规范》D13 漫游视频建议降级或替换为交互式 web 预览（视频在体验上输给可交互 3D、在质感传播上输给精修静态图，却最贵）。**工程坑记录**：①artifact 沙箱 CSP 放行 `img-src data:` 但不放行 `connect-src data:`，而 three.js GLTFLoader 在 Chrome 下默认用 `ImageBitmapLoader`（内部 fetch），导致贴图加载失败并连带整个模型解析失败——解法是给 `data:` URI 注册 `TextureLoader`（走 img.src）；几何另经 GLB 二进制块装载彻底免除请求。**此限制为 artifact 沙箱特有，自有服务器无此坎**。②教训：资产装载失败原本静默留下空房间，必须让失败显式可见——已在页面上把错误顶到常驻区。③**场景包不可内联进 HTML**：4.2MB 页面 + 同步 base64 解码阻塞主线程，首屏等待约 30 秒；真实做法是场景包独立文件异步加载 + 进度条。**待办**：真机帧率数值、遮罩局部重绘验证、以及 plan-2d-render 首实装时一并验的 Seed3D 闭环（见追记六）。

> 执行者：Claude（用户授权夜间自主推进）。本文是交接文档 §五 任务清单的执行结果，**新 session 接手落地工作从本文开始**。

## 一、结果一览：六仓全部建成、验证通过、已推 GitHub 私有仓

| 仓库 | 内容 | 验证结果 |
|---|---|---|
| [ishome](https://github.com/tommax-bai/ishome) | 中控/文档仓（五份基线 + 交接文档 + 假设清单 + 本日志） | — |
| [ishome-contracts](https://github.com/tommax-bai/ishome-contracts) | proto 三域（common/channel/design）、glossary、CloudEvents/activity/错误码/渠道/模板五类注册表、OpenAPI 两骨架、buf 三语言生成（gen/ 入库）、CI | `buf lint / build / generate` 全过，Java gRPC stub、TS(protobuf-es)、Python(pb2+grpc) 共 125 文件 |
| [ishome-backend](https://github.com/tommax-bai/ishome-backend) | Gradle 8.14.5 多模块：8 Java 服务四层/三层骨架 + shared/{kernel,starter}、ArchUnit 规则集（testFixtures）、Checkstyle 正则、脚手架 new-service.sh、CI | `./gradlew build` 全绿（JDK 21 toolchain）；9 个测试类过，规则集自检含"违规样例被拦截"验证 |
| [ishome-aipipe](https://github.com/tommax-bai/ishome-aipipe) | uv workspace：3 服务纵切 + 3 包、10 个 activity 存根、两个 Workflow 骨架、import-linter 7 契约、copier 模板、CI | `uv sync / ruff / lint-imports(7/7 KEPT) / mypy strict / pytest(7/7)` 全绿 |
| [ishome-frontend](https://github.com/tommax-bai/ishome-frontend) | pnpm workspace 骨架 + 禁手写 fetch/axios 的 CI 纪律检查 | 纪律脚本自检过 |
| [ishome-infra](https://github.com/tommax-bai/ishome-infra) | terraform/helm/argocd/gateway 占位、网关入口清单、中间件清单、环境事实 | 占位仓 |

**交叉验收（主会话独立复核）**：activity 注册名 contracts↔aipipe 10/10 逐字一致；认知状态六值 proto↔pydantic 一致；ChannelType 五值一致；backend 渠道 adapter 落位（domain 端口 + infrastructure/adapter/feishu|mock）正确；五仓红线 grep（人审/免责/转人工类措辞）干净。

## 二、执行中的既定假设

见《落地假设与拍板清单.md》：`{code}=ishome`、Postgres、云托管网关、团队画像①暂按成立、GitHub 替代 GitLab、Python SDK 走生成代码入库+git 依赖。**dev/ol 服务器今晚未动**（骨架不需要；机上有 aidcp/isales 生产服务）。

## 三、落地时新产生的决策点（进拍板清单附录）

1. **模板 id 首定**：`functional-annotation`（功能说明图）、`pencil-life-sketch`（彩铅生活草图）——视觉提案只定了 `cream-journal`，另两个为本次按 kebab-case 风格首注册。改名要趁 contracts 首个 tag 前。
2. **ChannelCapability 增加 `channel_grade` 字段**（会话级/触达级）——依据对齐文档 §6.3"渠道分级靠能力声明区分"，非新概念。
3. **PatchOpType 预留 `UPDATE`**（对齐 §11 示例只有 add/remove），语义随 patch 引擎细化。
4. `Fact.value` 用 oneof(number/string/bool)；`ChannelService.SendMessage` 带幂等键（IM 不可撤回，防重发）。
5. aipipe 的 `PatchOp` pydantic 模型暂定义在 packages/patch-engine 并由 design.models re-export；跨语言真源后续归 contracts design.v1（仓内已注明）。

以上均标注在对应仓内，属 v0 草案期可调项，无需逐条拍板——过目即可，有异议改哪条说哪条。

## 四、本机环境备忘（换机/新 session 有用）

- 外网 HTTPS（gh/buf/brew）需带代理：`export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890`；Clash fake-IP 偶发劫持 github.com:22，仓库局部配 `url."ssh://git@ssh.github.com:443/".insteadOf "git@github.com:"` 可解（frontend/backend 两仓已配）。
- Gradle：依赖走代理（`~/.gradle/gradle.properties` 已配，未提交）、wrapper 下载须直连；本地 JDK=brew openjdk@21，构建可依赖 foojay toolchain 自动供给。
- aipipe：本机 `UV_DEFAULT_INDEX=https://mirrors.aliyun.com/pypi/simple` 加速；`.python-version`=3.13（CI 验 3.12 下限）。

## 五、路线图（2026-08-23 晚更新；原 1-3、5 项已完成，见顶部追记）

**近期（可立即开工）：**

1. ~~design-svc 接真 Orchestrator~~ **已完成（同日晚）**：LiteLLM 网关本地起（infra/litellm，逻辑模型名 design-orchestrator.default→qwen-plus、design-intent.default→qwen-flash，阿里云百炼 key 在 `~/.ishome/llm-local.env`）；Orchestrator v1 上线——意图路由、五槽位最小必要输入收集（一次问一件事）、认知状态钳制（抽取只给 observed/inferred，user_confirmed 仅确认闭环授予）、结构类口述永不采信（§8.3 红线实测）、文本版确认清单（`【确认清单】`前缀、inferred 带"约"、quick_reply 能力降级）、LLM 故障兜底。22/22 测试 + 真网络冒烟 + mock 全链路实测通过（复杂首条消息全接住、只追问缺项）。**Langfuse 成本追踪仍为 TODO**（自托管部署后接 callback）。E2E 断言已随之改为"每入站至少一条文本出站"不变量。
2. ~~飞书真机接入~~ **已完成（同日下午）**：真机双向收发验证通过，见顶部追记。
3. ~~Temporal 接入 + V1.5 拆分执行~~ **已完成（同日深夜，见追记四）**：本地栈运行中、genpipe 任务层落真实、chat-svc 更名、project-svc 建成。
4. ~~数据层首批~~ **已完成（同日深夜，见追记四）**：svc_project / svc_channel / svc_chat 首批表落地，channel 幂等与 chat 会话存储已落库。**遗留**：`svc_chat.episodic_memories` 待 pgvector 镜像；`scenes/open_questions/outbox` 只建表未建仓储；Redis 会话态缓存未接（接入位已注释）。
5. **绘图三仓建仓部分完成（同日深夜，见追记四）**：三仓已建成推 GitHub、存根迁出完毕。**剩余**：estate-svc 户型库最小实现（小区/户型检索、认领即分叉的数据面）+ 首条绘图 activity 实装（plan-2d-render，落 `ishome-render2d`：确认底图与母版，衔接视觉提案）。
6. 事件总线：RocketMQ + outbox，首批 design.* / channel.message.* 事件按 contracts 注册表接线。
7. c-bff + H5 指图时刻第一页（看图点错确认），结果回写聊天线程。

**基础设施（按需触发）：**

8. ~~dev 服务器部署~~ **已完成（同日深夜，见追记五）**：裸机 systemd + 复用机上 PG（用户裁决，Docker 方案作废），五服务上线、飞书长连接已切服务器、全链路实测通过。infra `compose/dev-server` 变体留作历史参考不再是部署真相。**遗留**：genpipe-svc/worker 未上服务器（等首条绘图 activity 实装后一并）；服务器无 Temporal UI（需要时 SSH 隧道接本地 UI 或补装 ui-server）。
9. GitHub Actions 解封（封存中，触发条件见顶部 CI 口径）。

**未决拍板项（不阻塞，届时再拍）**：③模板验收台首版范围、⑤微信系渠道时机、⑥渲染算力（V1.4 拆分后可按 imagegen/render3d 分别拍）、⑦三维资产来源、⑩风格图文字合成方式（建议两方案各生成一批实测对比）；v0.3 遗留：M1/M2 里程碑边界是否合并（对话样例验证）、CAD 导出 activity 归属（先归 render2d-svc，DXF 栈若异质再独立）、project 域 proto 是否独立包。
