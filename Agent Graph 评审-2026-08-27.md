# 装修报告生成 Agent Graph 评审 · 2026-08-27（AI native 尺度重评）

> 对象：《装修报告生成 Agent Graph 设计说明》v0.1（承《装修报告生成方法论》v4.0 及四分册）
> 性质：**推翻同日早前的一版评审**——那一版用"人的判断在设计时"的尺子（规范 v1.2/v1.3 口径）去评这张图，尺子已随规范 v2.0 作废。
> 对照基线：《装修报告生成规则规范》**v2.0**、《架构对齐》V1.5、《架构审查-2026-08-24》。

---

## 一、判定

**图的方向与 v2.0 一致，可以作为 reportgen 的设计基线。** 它的 §6 判据回流子图已被 v2.0 收编为规则 4.17（自迭代回路）——那部分不是需要被批评的东西，是我们缺的东西。

剩余问题四条冲突 + 四条图内不自洽 + 两条新增要求，**没有一条是路线之争**，全部是"与已拍板裁决冲突"或"图自己不自洽"。

---

## 二、撤回的三条（上一版评审的错误）

| 原判 | 处置 |
|---|---|
| C1「图按全 LLM 生成，违反 G0–G4 分档」 | **撤回一半**。图不必改造成"填人写的模板句"。但数字仍不能由 LLM 决定——理由不是分档教条，是规则 8.2 的可重放与版本锁定（用户裁决）。见 R1。 |
| C4「图把人删干净了，删的位置与项目裁决不同」 | **整条撤回**。图对，规范错，规范已改（v2.0 §4.7）。 |
| 「图重新发明了我们已有的机制（复审队列/验收台）」 | **撤回**。是我们那些机制该被它替换，不是反过来。 |

---

## 三、仍然成立的冲突（四条）

### R1 · 落点取值必须下沉为确定性求值

- **图**：LLM 决定查什么表、取哪个值（`落点取值 ⇄ R-1 查表工具`）。
- **冲突**：规则 8.2 版本锁定与可重放（用户裁决）；G1 要求同输入同输出。LLM 发起的运行时查表两条都保不住。
- **v2.0 之后改法变了**：不是"查人维护的表"——表由系统自动获取并过核验（规则 4.16），作者是系统。变的是作者，不变的是**消费方式必须是确定性求值 + 快照下发**。
- **落点**：求值在 project-svc 规则引擎（归属仍为待拍板项 §13.6），结果随 `input_snapshot` 下发；R-3 缺口反向作为 miss 记录回流（规则 4.5）。

### R2 · 共享状态与 checkpoint 不能实现为长周期 workflow

- **图**：§1.2 累加式共享状态 + §1.3「改一条决策只重跑下游 2–4 段，其余从 checkpoint 恢复」。
- **冲突**：规则 8.1 禁第二台状态机；V1.5 裁决作废长周期 workflow、Temporal 收缩至任务层、真相在表。
- **落点**：状态真相 = `svc_project.slots/artifacts`；一次生成 = 一次短 workflow run（输入 = 上次产物快照 + 变更集）；"局部重跑"由里程碑引擎判据决定这次派哪些任务。

### R3 · 生成侧不知用户是谁

- **图**：D-2 产出家庭画像；读者代理设定为具体家庭成员。
- **冲突**：红线（交接文档 §七、规则 4.12）。
- **落点**：D-2 产出物定义为 slots 派生的匿名结构（无 user_id/姓名/联系方式）。**v2.0 之后读者代理升格为规则 4.17 的正式信号源，更要界定输入**：它读产物 + 匿名画像，不读用户档案；它属评测回路，不属生成链路。

### R4 · 命名空间冲突（机械但必须先改）

| 图里的符号 | 图里的含义 | 已被占用 |
|---|---|---|
| S-0～S-5 | 调度与校验环节 | S1/S2 = 两个生成阶段 |
| D-1～D-12 | 领域单元 | D01–D18 = 产物编号（图 D-6 照明 vs D06 比选图；图 D-10 预算 vs D10 灯光） |
| P0–P6 | 页型库 | P0/P1/P2 = 图纸精度等级（P1 带"禁定位数字"硬规则） |
| L1–L5 | 推导层级 | L1/L2/L3 = 规则库三层（L1 带豁免留痕硬规则） |
| C0/C3 | 校验判据 | C01–C04 = 清单产物编号 |
| R-1/R-3 | 查表/缺口登记 | R-x.x = v0.1 规则编号 |

建议前缀 `dom-*` / `PT-*` / `CR-*` / `LKP`·`GAP`。这些编号在本项目是 contracts 枚举、只增不改，现在改成本为零。

**比改名更要紧**：图的十二领域 ≠ 规范六专章。**domain 是 release 的发布粒度**（规则 4.12，一域一版），两套域枚举 = 两套发布粒度。须先出 dom → D-nn 产物映射表。

---

## 四、图内不自洽（四条，与本项目无关）

| # | 问题 |
|---|---|
| D1 | 第 3 节五种取值类型：前三种是确定性查表、后两种是带判断的筛选，塞进一个节点，落地必然退化为"全交给 LLM 按类型行事" |
| D2 | 判官与写手同源、无 oracle。**v2.0 之后这条更要紧**——没有任何人做兜底，规则 4.17 的三道门（种子集回归/观察态/入册上限）是唯一防漂移机制；图 §6.3 已有，但须前移为判官层的准入条件，不能只作回流子图的内部细节 |
| D3 | 「≤2 轮重写」×「删减不算通过」会死锁，缺 failed verdict 出口（genpipe 现有纪律：超限返回 failed，绝不静默假成功） |
| D4 | 第一层是假并行：D-12 消费 D-1、同刻扫描依赖 D-2 的时刻表，落地会成竞态 |
| D5 | 无成本/时延模型。12 单元 × 5 节点 × ≤2 轮 ≈ 单份上百次调用，直接决定 §7 评测夹具跑得起几轮 |

---

## 五、v2.0 之后新增的两条要求（图里没有）

### N1 · 冷启动条款（规则 4.18）

图默认判据体系已经跑过 13 轮、信号已有。**真实起点是零信号**：第一份报告发出前没有追问、没有换款率、没有跳读数据。

图必须补一段"信号为零时发什么"。v2.0 的答案写死了：只发可核条目支撑的内容，经验性判断整批降档或缺席，**主动收窄产物范围**——宁可章节少、页数薄，不用无背书的判断句撑密度。

### N2 · 手册的位置：设计文档，不是运行时输入

图 §8 待定项 3 问「离线方法论修订是否也视为人工节点」。**答案：不是人工节点，但形态要改。**

按规则 4.19，人与 AI 共同迭代系统属开发/产品角色，本就该有——红线是系统**运行**不依赖人（判据：人力是否随用户量增长）。所以"人驱动 AI 改手册"完全合规。

要改的是**手册怎么被系统消费**：

| | 现在（图的形态） | 应该 |
|---|---|---|
| 手册 3 领域段 | 直接挂在写作节点 prompt 上 | 编译成 persona 资产包（身份语域/判断句样例/断言预算/禁词表） |
| 手册 2 判据 | prompt 里的条文 + 判官读的规则 | 编译成 schema 字段 + 机检规则 + 判官反例库 |
| 手册 1 装配规则 | 装配层的说明文字 | 编译成页型声明与校验规则 |
| 手册 0 | 父图说明 | workflow 定义本身 |

**手册文本不进 prompt**，系统消费的是它编译出的数据。手册之于知识资产 = 种子文件之于库（规则 4.12）：可审计、可评审、可 diff，编译入库后 DB 为唯一真相。人改手册 → AI 重编译 → 过核验与种子集回归 → 发 release，与回路自产的资产共用同一套门禁。

这样两件事同时成立：人保留方向权与重写权（开发形态），运行时不读任何人写的文本（无运行时人依赖）。

---

## 六、落点建议（沿用上一版，未受尺子变化影响）

- **报告文本生成不进 genpipe-worker**：它已装解析/求解/校验/门禁，8-24 审查 P1 已指出内部异质；再塞 12 单元 LLM 写作是重复同一错误。→ 新仓 `ishome-reportgen` + 独立 Temporal worker + 队列 `reportgen-activities`。伸缩轴（LLM 并发）、故障域（模型供应商）、迭代节奏（自迭代回路）三项全异质。
- **编排仍在 genpipe workflow**，形态照 8-24 审查 A1 处理 `FloorplanParseWorkflow` 的方式（升格 workflow，内部拆 activity）。
- **确定性求值留 project-svc 规则引擎**。这同时回答待拍板项 §13.6：规则求值不异质（留 project-svc），文本生成异质（拆 reportgen）。
- **冲突仲裁不是二选一**：尺寸/规范/净距类是 G1 可判定的，落规则引擎；只有叙事语域冲突才用 LLM 仲裁节点。
- **评测夹具避开 floorplan-parse**（刚判定要升格 workflow、脏数据验证未做），用 estate 库已录入的标注户型起步，先做 attribute 密度最高的域。

---

## 七、结论

图可作为 reportgen 设计基线，前置 **R1–R4 四项改造 + N1/N2 两条补充**。

《方法论 v4.0》与四分册的位置已落定（规则 4.19）：**设计文档 → 编译成数据 → 系统消费**，手册文本不进 prompt。人保留方向权与重写权（开发/产品角色，合规），运行时不读任何人写的文本。图需要相应改造的只有一处：写作节点的输入由"手册 3 段落"换成"该域 persona 资产包（release 版本）"。

*执行者 Claude · 承规范 v2.0（§14.5）。*
