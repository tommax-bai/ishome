# 架构审查 · 2026-08-24

> 触发：连续多轮单点决策（web 3D、Seedream 接入、Seed3D、户型解析方法论）后，需检验整体是否仍自洽，还是已沦为补丁堆。
> 检验标准：**每个新增能力必须先回答"它属于哪个既有概念"；答不出来，才允许新增概念。**
> 结论：**骨架承得住**。8 项发现中 7 项可归位既有机制，1 项需新增架构原则。核心结构（chat/project 分立、事件驱动里程碑、真相在表、绘图物理拆分、变化轴纪律）未被动摇。

---

## 一、系统全景（当前口径）

```
用户（飞书 IM）
   │
   ├─ 消息 ──▶ channel-svc ──▶ chat-svc ────┐
   │           (渠道可插拔)   (听懂/说话)    │ 结构化业务事实
   │                                        │ slot_filled / artifact_confirmed
   └─ 指图 ──▶ c-bff (H5) ─────────────────┤ feedback_received
              · 看图点错                    ▼
              · 三维预览（web 3D）      project-svc
              · 材质风格选择            (事实与规则 · 项目唯一真相)
                                        · 里程碑引擎 checkCompletion
                                        · slot/artifact/task/revision
                                        · 流程定义分发
                                             │ 创建任务
                                             ▼
                                        genpipe-svc
                                        (Temporal 编排 · 机检门禁)
                                             │
                    ┌────────────┬───────────┼────────────┬─────────────┐
                    ▼            ▼           ▼            ▼             ▼
              genpipe-worker  render2d   imagegen     render3d    （门禁）
              解析/求解/校验  确定性2D   生成式出图   场景编译     一致性/合规
              floorplan-parse plan-2d-   atmosphere-  scene-       consistency-
              plan-layout-    render     visual       compile      check
              solve                      realism-pass base-render  compliance-
                                                      (控制信号)    check
                    │            │           │            │
                    └────────────┴─────┬─────┴────────────┘
                                       ▼
                              产物写 OSS + 注册 ArtifactRegistry
                                       │ 事件
                                       ▼
                        chat 主动消息推图 / c-bff 签发 URL
```

**供给侧**（内容工厂）与需求侧共用上述管线；estate-svc 持户型库与 catalog（三维资产），identity/trade/shelf/content 各司其职，本次审查未涉及。

---

## 二、逐项审查

### ✅ 归位项（7 项，无需新概念）

#### R1 · render3d-svc 的拆分理由已失效，但拆分本身应保留

- **现状**：V1.4 判据写的是「伸缩轴各自独立（CPU 批量 / API 配额与 GPU 推理 / **GPU 渲染**）」。web 3D 裁决后 render3d 不再有 GPU 渲染——`base-render` 降为 CPU 光栅出控制信号，`scene-compile` 是 CPU 几何处理。
- **判定**：判据中"伸缩轴独立"对 render3d **已不成立**（与 render2d 同为 CPU 几何）。
- **但拆分应保留**，理由更换为**技术栈异质**：2D 绘图栈 vs 三维几何/glTF 工具链，符合用户信条「逻辑异质 → 物理隔离」。
- **动作**：修订对齐文档 §3.0 表格中 render3d 的"算力形态"与 §3.1 判据表述，把 GPU 依据换成技术栈依据。**不改拓扑**。

#### R2 · imagegen-svc 判据同样需更新表述

- 原判据「API 配额与 GPU 推理」，现实为纯外部模型 API 客户端（方舟 Seedream）。
- **故障域独立**（模型 API 挂不影响 2D 制图）与**伸缩轴独立**（外部 API 配额与限流）仍然成立，服务保留。
- **动作**：判据表述由"GPU 推理"改为"外部模型 API 配额与限流"。

#### R3 · floorplan-parse 不得膨胀成含用户交互的管线

- **风险**：《户型图解析方法论》定义 P1–P6，其中 P5（尺度锚定，需用户申报面积）与 P6（当事人校验）**不是 activity 能做的**——生成侧不知道用户是谁，这是既有硬边界。
- **归位**：`floorplan-parse` 收窄为**纯函数式**：`(图 + 可选比例锚点) → 带置信度的几何与语义初稿`。P5/P6 归 chat-svc（收集面积、呈现待确认项）与 project-svc（落 slot、判定认知状态）。
- **动作**：在方法论 §四 明确标注 P1–P4 属 activity、P5–P6 属会话与项目域；activity 契约不含任何用户标识。

#### R4 · 解析校验必须复用既有确认机制，禁止另造

- **风险**：容易新造一套"户型确认"流程。
- **归位**（零新增概念，全部映射到既有物）：
  | 解析产物 | 既有机制 |
  |---|---|
  | 解析初稿 | `artifact`（type=floorplan_draft），登记 ArtifactRegistry |
  | 初稿的认知状态 | `inferred`（六值词表既有） |
  | 用户确认后 | `artifact_confirmed` 事件 → 认知状态升 `user_confirmed` |
  | 低置信度待确认项 | `svc_project.open_questions`（确认清单既有表） |
  | 确认交互 | c-bff「指图时刻」+ chat 确认闭环（既有） |
- **动作**：无需新表、新事件、新状态。仅需在方法论中写死这套映射。

#### R5 · 户型库回流走事件，不得跨域直写

- **需求**：用户校验后的户型应沉淀进 estate-svc 户型库，供同小区同户型复用（物理依据：同楼各层厨卫垂直对齐，见方法论 §3.4 E3）。
- **边界**：project-svc 是项目真相，estate-svc 是供给侧主数据，chat 不得直写 estate。
- **归位**：project-svc 经 **outbox + 事件总线**（既有规划）发 `design.floorplan.confirmed` → estate-svc 消费入库（去重/合并）。复用既有事件机制，零新通道。
- **动作**：事件注册表新增一条 type（只增不改，走 contracts 发版）。

#### R6 · 精度分层用既有认知状态承载，不新增字段体系

- **缺口**：预览级几何（拓扑对、面积对、边界差 5% 可接受）与出量级几何（毫米级）要求差一个量级，当前 activity 参数只有 `preview/final`（**渲染质量档**），没有**几何精度档**。
- **归位**：认知状态六值里的 `measured` / `verified` 本就是为此准备的。几何真相带认知状态即可区分来源：`inferred`（解析）/ `user_confirmed`（当事人校验）/ `measured`（实测量房）/ `verified`（图纸导入核验）。
- **纪律**：出量类用途必须校验几何的认知状态 ≥ `measured`；**解析结果不得直接进出量链路**。
- **动作**：不新增字段体系，把既有词表用起来；在契约中补该前置校验。

#### R7 · 住宅规则库必须单一真源，跨语言消费

- **风险**：方法论 P3（约束求解推语义）与 `plan-layout-solve`（求解排布局）用**同一套住宅规则**；若各写一份必然漂移。另《装修报告生成规则规范》已有 `svc_rulebook` 六表族落 project-svc（Java），而生成侧是 Python——跨语言共享是真问题。
- **归位**：遵循既有纪律「**配置只放数据，逻辑归服务**」——规则以**版本化数据制品**形式单一真源，两侧各自实现消费逻辑。与流程定义（project-svc 权威分发、chat/project 双切片消费）是同一模式。
- **待拍板**：住宅布局规则是并入 `svc_rulebook` 六表族，还是作为独立版本化配置进 contracts。**倾向后者**——生成侧要用，contracts 是唯一跨语言真源，且 rulebook 面向报告域求值、语义不同。

---

### ⚠️ 需新增的架构原则（1 项）

#### R8 · 单一几何真相，三种呈现

- **现象**：现在有三条出图链路，职责不重叠但**依赖同一份几何**：
  | 链路 | 产物 | 职责 |
  |---|---|---|
  | `plan-2d-render` | 2D 母版 / 确认底图 / 遮罩 | 几何真相的可视化 + 生成式的控制底图 |
  | `atmosphere-visual` / `realism-pass` | 静态效果图 | 情绪与传播（照片级质感） |
  | `scene-compile` | glTF 场景包 | 空间感与交互（web 3D 预览） |
- **风险**：三者若各自从不同数据派生，必然漂移，出现「2D 图上是三室、3D 里变成两室」「效果图的窗在东、场景包的窗在西」这类**用户一眼可见的事故**，且极难排查。
- **原则**：**Scene Graph / BaseFacts 是唯一几何真源，三条链路都只是它的投影。** 任何链路不得自行推断或修补几何；发现几何有问题应回改真源并触发全链路失效传播（既有 §13 机制）。
- **为什么必须显式写下**：这是唯一一条无法归位到既有概念的新增约束，且违反的代价高、发现晚。

---

## 三、总体判定

**架构未被这几轮决策破坏。** 依据：

1. **服务拓扑零变动**——所有新能力都落进了既有服务，没有新增服务，也没有出现"不知道该放哪"的能力。
2. **新概念增量为 1**——8 项中 7 项归位既有机制（artifact / 认知状态 / open_questions / 事件总线 / 配置数据纪律 / 变化轴），仅 R8 需新增原则。
3. **边界未被侵蚀**——生成侧仍不知道用户是谁（R3 守住）、chat 仍不判里程碑、跨域仍走事件（R5 守住）。
4. **变化轴纪律仍成立**——图像模型接入只改 LiteLLM 配置、业务代码零修改（变化轴 3 的实际兑现，实测验证过）。

**需要修订的是文档而非架构**：R1/R2 是"理由已变、结论不变"，属于**判据表述过期**，不改会误导后来者按 GPU 去规划算力。

---

## 四、待拍板

| # | 事项 | 建议 |
|---|---|---|
| 1 | 住宅布局规则库归属：并入 `svc_rulebook` 还是独立进 contracts | 倾向 contracts（跨语言真源，语义与报告域求值不同） |
| 2 | 户型解析是否现在投入 | 倾向暂缓，先走「AI 初稿 + 当事人校验 + 结果入库」自举，不做全自动解析 |
| 3 | D13 漫游视频降级/替换为交互式 web 3D 预览 | 建议降级（视频在体验上输给可交互 3D、在质感传播上输给精修静态图，却最贵） |
