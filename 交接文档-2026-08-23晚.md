# 交接文档 · 2026-08-23 晚（架构裁决日收尾）

> 性质：换 session 交接。**新 session 读完本文即可接手**，细节按文内指针下钻。
> 前序：《交接文档-落地启动.md》（8-22 夜，已完成使命，作历史保留）→ 本文。
> 截至：2026-08-23 晚，中控仓 HEAD 含 V1.5 裁决（commit `9bea933`），**均未推远端**（本地领先 origin/main 若干 docs 提交，接手后可直接 `git push`）。

---

## 一、项目与阶段一句话

"是我的家"：AI 家装设计产品——IM 会话式设计 Agent（首发飞书）+ 内容工厂双引擎，初步设计建立信任 → 付费深度设计。当前阶段：**骨架全部建成、第一条纵切 E2E 通过（飞书真机 + Orchestrator v1 真实对话）、今日完成三轮服务边界裁决**，下一步是执行拆分与 Temporal/数据层落地。

## 二、阅读顺序（新 session 按序）

1. 本文（总览与任务序列）；
2. 《落地日志-2026-08-23.md》——执行现状、三条追记、路线图（**执行细节的唯一入口**）；
3. 《架构对齐-设计Agent×技术架构.md》**V1.5**——架构现行口径的唯一权威（版本行有 V1.3/V1.4/V1.5 三次裁决摘要；文中残留"design-svc"字样按版本行的对照规则理解）；
4. 《沟通助手架构方案.md》v0.3——会话域机制基线，**先读收录说明头**（五处归位修订，正文原样未改）；
5. 《落地假设与拍板清单.md》§〇——全部裁决记录；
6. 写代码前：《开发规范与代码分层.md》。

## 三、今日三轮架构裁决（已定案，非用户主动重开不再议）

| 裁决 | 内容 | 落点 |
|---|---|---|
| **V1.4 绘图物理拆分** | 绘图 activity 按绘图逻辑拆三个独立服务+独立仓库：`render2d-svc`/`ishome-render2d`（plan-2d-render，确定性 2D）、`imagegen-svc`/`ishome-imagegen`（atmosphere-visual、realism-pass，生成式）、`render3d-svc`/`ishome-render3d`（scene-compile、base-render，三维）。各自为独立部署 Temporal worker、专属 task queue、无 RPC 无 schema 无状态；genpipe 保留编排与非绘图 activity（解析/求解/校验/门禁） | 对齐文档 §三 |
| **V1.5 design-svc 一拆为二** | `chat-svc`（Python，管听懂和说话：理解/情绪/封闭动作集/记忆画像/承诺区/主动消息；现 Orchestrator v1 代码保留演进）+ `project-svc`（Java，入 backend monorepo，管事实和规则：里程碑引擎/slot/artifact/task/revision 唯一真相/修订预算/流程定义分发）。支付权益仍归 trade-svc。链路单向：chat 发结构化业务事实 → project 判定建任务 → genpipe workflow 派发 worker+绘图服务 → 产物登记 → 事件 → chat 呈现。**chat 与绘图服务永不直接交互；chat 永不判里程碑** | 对齐文档 §二 |
| **里程碑引擎事件驱动** | 真相在表 + 事件触发 checkCompletion；原"设计项目=长周期 Temporal workflow（continue-as-new）"作废；**Temporal 收缩至任务层**（生成管线，重试/心跳/超时原生语义） | 对齐文档 §2.2 |

**贯穿判据（用户设计信条，后续一切边界讨论以此为准）**：逻辑异质 → 物理隔离（独立仓库+独立服务）；不采信"构建级门禁/模块边界可替代物理隔离"。

另：v0.3 收录时五处归位——任务队列→Temporal（D11 三原则保留但只用于事件通知，走既定 outbox+RocketMQ 规划）、LangSmith→Langfuse、渠道→channel-svc（chat 定投递策略、channel 按能力声明执行）、支付→trade-svc、M0 户型获取=库命中优先/上传解析兜底。

## 四、当前服务拓扑（15 服务）

- **Java（backend monorepo，9）**：estate、content、trade、identity、channel、c-bff、admin-bff、shelf 相关既有服务 + **project-svc（新增，待建）**；
- **Python（6）**：**chat-svc（由 design-svc 更名，待执行）**、genpipe-svc、genpipe-worker（以上在 aipipe 仓）+ **render2d / imagegen / render3d（三独立仓，待建）**；
- 数据：schema-per-service；`svc_design` 设计已作废，拆为 `svc_project` + `svc_chat`（表结构见对齐文档 §5.1，slot 表带认知状态六值）。

## 五、仓库与运行现状

- 六仓已建并验证（github.com/tommax-bai 私有）：ishome（中控/文档）、contracts（v0.2.0 基线，buf 三语言生成入库）、backend（Gradle 8 多模块+ArchUnit）、aipipe（uv workspace+import-linter）、frontend、infra。三绘图仓**待建**。
- **已跑通**：mock E2E（`ishome-backend/scripts/e2e-mock-smoke.sh`）；飞书真机双向收发；Orchestrator v1（22/22 测试，意图路由/五槽位/认知状态钳制/确认清单/LLM 兜底）。
- 本地起服务：design（即未来 chat）`DESIGN_GRPC_PORT=19101 uv run design-grpc`；channel `bootRun --args="--spring.profiles.active=local --ishome.channel.design-target=localhost:19101 --spring.config.import=optional:file:~/.ishome/channel-feishu-local.yml"`；LiteLLM 网关 :4000（infra/litellm/run-dev.sh，阿里云百炼 qwen，key 在 `~/.ishome/llm-local.env`）。
- 凭证均在 `~/.ishome/`（不入库）：飞书 channel-feishu-local.yml、LLM llm-local.env、火山引擎 volcengine-local.env（鉴权有效、即梦未开通，用途待用户确认）。
- **CI 口径**：GitHub Billing 暂不修，五仓本地 pre-push 质量门（`.githooks/pre-push`，新 clone 需 `git config core.hooksPath .githooks`）；云端改仓/多机/发 SDK 时再修。
- 本机坑（代理/SSH 443/Gradle/uv 镜像）见落地日志 §四。

## 六、下一步任务序列（接手即可开工，按序）

1. **Temporal 本地 + V1.5 拆分执行**（路线图第 3 项）：
   - docker compose 起 Temporal（不动 dev 服务器）；genpipe 生成 workflow/activity 从骨架落真实（只做任务层）；
   - aipipe：`services/design-svc` 目录更名 `chat-svc`（Orchestrator 代码保留；design.v1 契约入口与 channel 转发目标**不变**；import-linter 契约同步改名）；
   - backend：用 `new-service.sh` 脚手架新增 `project-svc`（里程碑引擎 checkCompletion + 流程定义分发接口）。
2. **数据层**（第 4 项）：`svc_project` / `svc_chat` / `svc_channel` 首批表 + Flyway（本地 docker PG）；channel 幂等去重与 chat 会话存储换落库。
3. **绘图三仓建仓**（第 5 项前置）：uv 骨架 + pre-push 门禁 + CI 占位，aipipe 中 5 个绘图 activity 存根迁出（**注册名不改**，契约仍在 contracts）；随后 estate 户型库最小实现 + plan-2d-render 首实装（落 ishome-render2d）。
4. **户型解析技术验证**（v0.3 §14 定为全产品咽喉）：floorplan-parse 用真实脏数据（手机拍/水印/歪斜）验证，解析置信度字段贯穿全链路。
5. 事件总线（RocketMQ+outbox，首批 design.*/channel.message.*）→ c-bff 指图时刻第一页。

## 七、纪律红线速查（违反即返工）

- **无人工介入**：全系统无人审/转人工/兜底人力概念；措辞红线 grep 已在五仓生效；话术永不说"转人工/我去核实"；
- **契约**：activity 注册名与事件 type **只增不改**；跨仓接口变更走 contracts 发版（tag 锁定）；
- **配置只放数据，逻辑归服务**：completion_criteria 仅简单谓词，不发明表达式语法；不建通用平台；
- **边界**：chat 不判里程碑不建任务；project 不理解自然语言；生成侧不知用户是谁；支付只在 trade-svc；
- **承诺兑现率恒 100%**：只承诺机器可自兑现事项，白名单拦截；挂任务承诺以任务事件核销，不自扫 deadline；
- 设计讨论只用能力视角，责任/合规入 backlog（V1.3 裁决）。

## 八、未决拍板项（不阻塞，届时找用户拍）

③模板验收台首版范围；⑤微信系渠道时机；⑥渲染算力（可按 imagegen/render3d 分别拍）；⑦三维资产来源；⑩风格图文字合成方式；M1/M2 里程碑边界是否合并（对话样例验证）；CAD 导出归属（默认 render2d-svc，DXF 栈异质再独立）；project 域 proto 是否独立包；火山引擎凭证用途确认。

---

*2026-08-23 晚 · 执行者 Claude（用户授权自主推进）。本文提交后，落地工作入口 = 本文 → 落地日志。*
