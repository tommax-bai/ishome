# CLAUDE.md

本文件给在本仓工作的 Claude Code 用。**它只做路由与速查**：每条只有一行，全文在它指向的地方——
不在这儿复述（《纪律·同一条规矩只写一处》）。

## 本仓性质

"是我的家"（AI 家装设计产品）多仓项目的**中控/文档仓**：只放方案、规范、裁决、日志，**不含代码，无构建/测试命令**。
所有代码在 `~/codes/` 下的兄弟仓中。

## 文档分四类，一份文档只回答一个问题

| 回答的问题 | 是谁 | 怎么改 |
|---|---|---|
| **从哪儿开始读** | 本文件 | 权威处改了，跟着改这里的一行 |
| **现在怎样** | 《现在在哪儿.md》（进度）、《仓库拓扑.md》（仓/服务/端口/凭证） | 就地改、升版本行、**不发追记** |
| **应该怎样** | `规范/`、`方案/` | 用户拍板才变；就地改、升版本号、被推翻留对照 |
| **当时发生了什么** | `交接/` 日志、`评审/` | **写完不再改**——它是裁决的证据 |

一份文档里同时出现"当前状态"和"规则口径"＝该拆（《纪律·一份文档只回答一个问题》）。

## 接手入口（按序读，勿全仓通读）

1. **《现在在哪儿.md》**——现在到哪儿了、下一件做什么。**接手先读这一份。**
2. **《规范/协作与文档纪律.md》**——协作怎么进行（文档、裁决、对话、做事、仓库五组）。
3. 下一件那条线的日志（由第 1 份指名），要来路和判据时读它。
4. 讨论架构或改规则前，读对应权威篇：
   - 《方案/架构对齐-设计Agent×技术架构.md》V1.5——全局架构唯一权威；
   - 《规范/装修报告生成规则规范.md》——报告生成域"宪法"；
   - 《规范/落地假设与拍板清单.md》——全部裁决记录；
   - 《规范/开发规范与代码分层.md》——写代码前必读（分层、命名、量纲入名）。
5. 全局背景（长期有效）：《交接/交接文档-2026-08-23晚.md》→《交接/落地日志-2026-08-23.md》（本机环境坑）。

`交接/` 日志 · `方案/` 架构与设计 · `评审/` 评审记录 · `规范/` 规范与裁决。既有文档互相以《文件名》指认，按文件名搜索即可。

## 红线速查（违反即返工，全文见《规范/落地假设与拍板清单.md》与《规范/装修报告生成规则规范.md》）

- **AI native / 无人工介入**：无人审、无转人工、无运营编辑后台、无人预设模板。
- **逻辑异质→物理隔离**：独立仓；是否起服务另过服务存在性判据（两判据分列）。
- **边界**：chat 不判里程碑不建任务；project 不理解自然语言；chat 与绘图服务永不直接交互；支付只在 trade-svc；生成侧不知用户是谁。
- **数字不由 LLM 决定**：派发前求值完，正文数字只经 `{lkp-*}` 引用；关系也不由 LLM 决定。
- **命名禁纯序号**：语义命名、前缀即命名空间。
- **标注必挂**：draft 或已过期落点进正文，同页必须挂依据标注。
- 纪律型条目禁以知识条目入库；种子禁预置 calibrated；反例只收真跑样本。
- 配置只放数据，逻辑归服务；不发明表达式语法，不建通用平台。

## 纪律速查（全文与判据见《规范/协作与文档纪律.md》）

- **文档**：一份文档只回答一个问题 · 状态写在状态页 · 日志正文不改 · 活文档就地改 · 改口径要在版本行留对照 · 文件名不改 · 新裁决要回改旧文档 · 同一条规矩只写一处。
- **裁决**：裁决必落档 · 裁决要带用户原话 · 已定不重开 · 自己提的要标明不是裁决 · 阈值有数据才定。
- **对话**：计划与对话用事项名 · 顺序不写进名字 · 编号只用来定位 · 不以标识符为沟通依据 · 不许只说不做什么 · 说人话别绕 · 拍板后直接执行。
- **做事**：按定错了会波及什么取舍 · 能自动拦住的不写成纪律 · 动工理由必须落在用户身上 · 没有证据不主动做 · 拿不到就说没有，不许填猜的值 · 只收真跑样本。
- **仓库**：提交信息用中文写清增量 · push 由用户定（勿主动 push）· 显式 add 路径（禁 `git add -A`）。
