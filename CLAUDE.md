# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 本仓性质

这是"是我的家"（AI 家装设计产品）多仓项目的**中控/文档仓**：只放架构方案、裁决记录、交接文档与落地日志，**不含代码，无构建/测试命令**。所有代码在 `~/codes/` 下的兄弟仓中；仓清单、本地栈与凭证位置见根目录《仓库拓扑.md》（活文档，单一真源）。

## 目录分组

文档按性质分组存放，新文档入对应组，**文件名（含日期）一经引用不再改**：

- `交接/` —— 交接文档与落地日志（时间性，按日期累积）
- `方案/` —— 架构与设计方案（版本化权威口径）
- `评审/` —— 评审与审查记录（时点性）
- `规范/` —— 规范与裁决记录（版本化权威口径）

既有文档互相以《文件名》指认（非路径链接），找文档按文件名搜索即可。

## 接手入口（按序读，勿全仓通读）

1. **`交接/` 下最新一份《交接文档-YYYY-MM-DD.md》**（按文件名日期取最新，部分带"晚"字后缀）——当前状态唯一入口；文末的"追记"是最新增量，**追记比正文新**。
2. 全局背景：《交接文档-2026-08-23晚.md》（裁决、红线、拓扑长期有效）→《落地日志-2026-08-23.md》（执行细节与本机环境坑）。
3. 权威口径文档（讨论架构前必读对应篇）：
   - 《方案/架构对齐-设计Agent×技术架构.md》V1.5 —— 全局架构唯一权威；
   - 《规范/装修报告生成规则规范.md》v2.2 —— 报告生成域"宪法"；
   - 《规范/落地假设与拍板清单.md》—— 全部裁决记录；
   - 《规范/开发规范与代码分层.md》—— 写代码前必读（分层、类/函数/变量命名、量纲入名、Temporal activity 命名）。

## 文档纪律（本仓的"开发规范"）

- **交接文档正文不改，只加追记**：正文定稿后一切增量以文末"追记 N"追加，并注明日期与执行者。
- **裁决（用户拍板）必须落档**：进《落地假设与拍板清单.md》，涉及规范的同步升版本号（如 v1.3→v2.2），并在规范内留裁决审计节。已定裁决非用户主动重开不再议。
- 权威文档带版本行；旧表述被推翻时在版本行留对照说明，不悄改正文。
- 《仓库拓扑.md》为活文档：拓扑事实变更时就地更新并升版本行，不发追记。
- **计划项与对话一律用事项名，不用纯序号**（规则 1.7 的同一条纪律）：名字说"做完之后成立的事实"（如"造价章出金额"），顺序用"排在 X 之后"表述。编号只在定位时进括号——规范条文号／裁决编号／release tag／提交号是文档地址，保留；`序列 N`／`步 N`／`轨 X` 这类纯排序编号废除（用户裁决 2026-08-29）。
- **对用户沟通不以标识符为依据**（同日扩展，同一条纪律）：变量名/文件名/仓名/字段名是 Claude 改文件的依据，不是与用户沟通的依据——对话与汇报说机制、说事项，标识符只在它本身是被裁决对象或用户问到具体位置时出现。中文《文件名》指认与交接/执行文档内部不受此限。
- **描述逻辑完整闭环**（同日扩展）：否定式口径必须同句给出替代形态与后续路径，范式="不成服务，以工具形式存在，后续报告产出上线时，建立服务"；后续路径的触发条件用事件名写死，禁止悬空的"不做 X"与"后续再看"式开放结尾。判据（与上两条同）：读者读完不需要回查、不需要追问。
- 提交信息用中文、`docs:` 前缀、概括当日增量（见 git log 风格）。
- **push 时机由用户定**，本地提交领先远端是常态，勿主动 push。

## 红线速查（违反即返工，全文见交接文档）

- **AI native / 无人工介入**：无人审、无转人工、无运营编辑后台、无人预设模板；"不依赖人"只管系统运行不管迭代——人的改动只走"改源→AI 编译成数据→核验→release"（规则 4.19）。
- **逻辑异质→物理隔离**（独立仓+独立服务），不采信"模块边界可替代物理隔离"。仓与服务两判据分列：隔离先给独立仓，是否起服务另过服务存在性判据（规范 §13.6）——渲染层"独立仓、纯库+CLI 不成服务"即此例（2026-08-29）。
- **边界**：chat 不判里程碑不建任务；project 不理解自然语言；chat 与绘图服务永不直接交互；支付只在 trade-svc；生成侧不知用户是谁。
- **数字不由 LLM 决定**：落点派发前求值完，随 input_snapshot 下发；正文数字只经 `{lkp-*}` 引用。
- **命名禁纯序号**：语义命名、前缀即命名空间（`art-/dom-/pt-/cr-/lkp-/gap-/prec-/gen-/stage-/tier-/acc-`）；旧编号只允许出现在规范 §14.6 对照表。
- 纪律型条目禁以知识条目入库（规则 4.10b）；种子禁预置 calibrated（只能由核验跑批授予）。
- 配置只放数据，逻辑归服务；不发明表达式语法，不建通用平台。
