# 落地假设与拍板清单

> 日期：2026-08-22 深夜（落地第一晚）
> 性质：落地执行时对 ★ 未拍板项采用的**工作默认值**记录。交接文档授权"未拍时可按推荐默认搭骨架，但需显式声明假设"，本文即该声明。
> 用法：明天逐项拍板。**拍板结果与默认一致 → 划掉该行即可，零改动；不一致 → 按"改判成本"列执行回改。**

---

## 〇、拍板结果（2026-08-23，用户裁决）

- **`{code}` = `ishome`，定死。**
- **① 团队画像 → 开发完全由 Claude 承担，不考虑开发语言约束。** 架构不裁剪：Java+Python 双栈、11 服务维持。原"8-15 人双班底"变量不再是前提；开发规范与 CI 门禁的作用相应转为约束 Claude 产出的一致性（"规范即工具链"纪律照旧，价值不降）。
- **② 存储 = Postgres**（PostGIS/JSONB，schema-per-service）。
- **④ 网关 = 云托管；部署平台 = 阿里云**（按技术架构既定：ACK/RDS/OSS + MSE 云原生网关或 Higress）。
- 《落地日志-2026-08-23.md》§三 的 5 个小决策点：**已过目通过（同日）**——模板 id `functional-annotation`/`pencil-life-sketch` 定死，`channel_grade`、UPDATE 预留、幂等键等保持。contracts 打首个 tag v0.1.0。
- **绘图能力物理拆分（2026-08-23 晚，用户裁决；推翻对齐文档 V1.3 及以前"不新建渲染服务"口径）**：绘图 activity 按绘图逻辑拆为三个独立服务 + 独立仓库——`render2d-svc`/`ishome-render2d`（plan-2d-render，确定性 2D）、`imagegen-svc`/`ishome-imagegen`（atmosphere-visual、realism-pass，生成式）、`render3d-svc`/`ishome-render3d`（scene-compile、base-render，三维）。genpipe 保留编排与非绘图 activity；集成形态 = 各服务独立部署 Temporal worker、监听专属 task queue，activity 契约走 contracts 注册表发版。**裁决依据（用户历史经验）：无物理隔离的模块边界在长期开发中必然腐烂，不采信"构建级门禁可替代物理隔离"。** 落点见对齐文档 V1.4 §三；建仓与 aipipe 存根迁移已排入落地日志路线图第 5 项前置。
- **design-svc 一拆为二（2026-08-23 晚，用户裁决）**：chat-svc（Python 会话引擎，现 Orchestrator v1 代码保留演进）+ project-svc（Java 项目状态机与项目唯一真相，入 ishome-backend monorepo）。判据与绘图拆分同源：LLM 会话逻辑与确定性状态机逻辑异质 → 物理隔离。拆分时机零迁移成本（ProjectState 尚未落库）。支付与权益仍归 trade-svc。落点见对齐文档 V1.5 §二。
- **里程碑引擎 = 事件驱动状态机（2026-08-23 晚，用户裁决；推翻对齐文档 V1.0-V1.4"设计项目=长周期 Temporal workflow（continue-as-new）"方案）**：真相在表，事件触发 checkCompletion（读判据配置→查表→布尔求值→迁移+on_enter 编排）；**Temporal 收缩至任务层**（生成管线 workflow/activity，重试/心跳/超时用原生语义）。对话与项目连续性由外置状态承载，不用框架挂起。
- **生成式图像供应商 = 火山引擎方舟 / 即梦 Seedream（2026-08-23 深夜，用户开通并实测确认）**：解决遗留的"火山引擎凭证用途待确认"。物理模型 `doubao-seedream-5-0-pro-260628`（Ark `/api/v3/images/generations`，key 在 `~/.ishome/llm-local.env` 与服务器 `/opt/ishome/env/llm.env` 的 `ARK_API_KEY`）；**接入形态严格按变化轴 3**：只在 LiteLLM 配置登记逻辑名 `atmosphere-visual.default` / `realism-pass.default`，业务代码不感知厂商（infra `3bb3a57`）。实测结论：文生图与**图生图均可用**（本地与 dev 网关双跑通），图生图能保住构图与家具位置——满足 atmosphere-visual"母版几何不变"、realism-pass"底渲结构不变"的形态要求；**但非像素级保真**（重生成会带来吊顶饰边、取景位移等细节漂移），故 `consistency-check` 机检门禁是必需项而非可选项。**尚未验证**：遮罩/局部重绘（架构 §4 的"固定遮罩"路径）、视频能力（即梦含视频，当前架构无位置，不接）。渲染算力未决项⑥的 imagegen 一半由此落定，render3d 仍未决。
- **编号与命名规范（2026-08-27，用户裁决）**：口径——**"命名规范作为规则记录下来，要避免无意义的命名方式；用语义代替数字，命名应有命名空间，通过名字能推出意图"**。落规范 **v2.1** 规则 1.7/1.8：标识符须带命名空间前缀 + 语义主体 + kebab-case 只增不改；前缀全集 `art-`/`dom-`/`pt-`/`cr-`/`lkp-`/`gap-`/`prec-`/`gen-`/`stage-`/`tier-`/`acc-`（新增前缀先进表）；四条禁止（禁纯序号、禁前缀语义重复、禁自造缩写、禁同概念两套名）。**全域标识符一次性重命名 350 余处**（D01–D18/C01–C04/P0–P2/G0–G4/S1–S2/L1–L3 → 语义名，对照表 §14.6）；时机判据=产物与等级枚举尚未进 contracts，此刻成本为零。连带：Agent Graph 的编号按同一规则换名，且其领域枚举须与专章域收敛为同一套 `dom-`（否则 release 发布粒度分裂）。
- **AI native 重写（2026-08-27，用户裁决；推翻规范 v1.2/v1.3 的"人的判断移到设计时"整条路线）**：用户口径——**"我们在做一个 AI native 的系统，不要依赖人，不要依赖运营，不要依赖人预设的模板。"** 落《装修报告生成规则规范》**v2.0**（§14.5）：①新增三条回路（获取回路=外部事实自动进库+交叉验证/时效/可定位三道自动校验；自迭代回路=句式/判据/禁词由系统自产，信号=用户行为+读者代理+失败聚类，入册过种子集回归与观察态；冷启动条款=只发可核内容、主动收窄范围）；②**知识资产取消编辑后台**（原"admin 第三个重投入界面"作废，只留只读观测），待定项8 作废；③persona 维护由"模板验收台+人工验收"改为自动回归门禁；④template 由"专家起草模板句库"改为**系统自产句式库**，G2/G3/规则 8.2 相应改写；⑤规则 4.9"主维护方"整列重写为"产出方式"，无"运营"；⑥禁止项 +4（禁人工编辑入口、禁人写模板作生成前提、禁内部审美作品味锚点、禁一切验收台）。**未推翻的一条**：规则 8.2 版本锁定与可重放——AI native 改的是"谁产出资产"，不改"资产必须是可版本化的确定性数据"，系统自产物仍须固化 release 后消费。**连带作废**：未决拍板项③（模板验收台首版范围）。**同日重评**《装修报告生成 Agent Graph 设计说明 v0.1》，落《Agent Graph 评审-2026-08-27.md》（早前一版评审因尺子错误被推翻）。
- **知识资产校准门作废（2026-08-27，用户裁决）**：《装修报告生成规则规范》v1.2/v1.3 遗留的"六域知识资产上线前各过一次执业者校准"（原待定项5）**删除**——用户确认无可依托的执业者资源（灯光设计师/造价员等六域），该门在此前提下即空门，与运行时人审是同一失效形态（V1.3 判据）。替代机制=**可核性门**（规范 v1.4 规则 4.10a）：条目分可核/经验两类，`calibrated` 只能由机检核验跑批取得（source 可定位/区间自洽/量纲合法/consumers 双向校验），无人工签字环节；经验条目恒为 draft，只能降档呈现，唯一升级路径是埋点信号累积。**纪律一句话：收窄收录范围优先于增加审核环节。** 代价已记录在案（经验性判断初版进不了 PAID 判断句）。落点见规范 §14.4。
- **《沟通助手架构方案》v0.3 收录为会话域基线文档（同日）**：三层架构/封闭动作集/产物换反馈/修订收敛/记忆承诺体系/指标函数整体采纳；服务拓扑按对齐文档 V1.5 归位，五处照改（任务队列→Temporal、LangSmith→Langfuse、渠道→channel-svc、支付→trade-svc、户型获取补库命中路径），明细见该文档收录说明头。
- **应用内依赖单向总纲（2026-08-28，用户拍板）**：口径——每个应用内部依赖整体沿 Types→Repo→Service→Runtime→UI 趋势单向流动，**不存在反向依赖**；非固定层数，卡方向不卡格子。落《开发规范与代码分层.md》**V1.2** §1.0，既有三套形态分层（Java 四层/BFF 三层/Python 纵切）自此为总纲实例；附四判据（DAG 非线性/依赖倒置不算反向/向上通信只走事件/必须进 ArchUnit·import-linter 门禁）。Config 不占层位（承"配置只放数据"红线）。

- **release 快照含 draft 条目（2026-08-28，用户裁决）**：问法——"没有外部依据背书的知识，要不要下发到报告生成环节"。裁决**维持"含 draft、带 calibration 标记、降档门禁在消费侧强制"**，否决"快照只含 calibrated"的严格版。判据：①严格版会砍掉规范 4.10 自己规定的"降档呈现（如参考级区间）"路径——造价章的参考级区间正是 draft 单价的降档形态，不含 draft 该章直接消失；②当前库存 16/127 calibrated，严格版下六专章五个当场全空；③真正的风险"拿没依据的话当断言"已由消费侧门禁强制（规范 v2.3 规则 4.10c）。落规范 **v2.3**：规则 4.12 存储段措辞订正（原"发布动作把该域全部 calibrated 资产固化"与本裁决冲突）+ 新增规则 4.10c 降档判据 + §14.8 审计。**代价明示**：降档纪律的正确性从此依赖消费侧执行，执行错＝没依据的话被当有依据说出去，故 4.10c 的判据与机检是这条路线的承重墙。
- **判官反例库来源 = 冷启动真跑样本，不用旧材料（2026-08-28，用户裁决）**：原计划"13 轮附录 A（方法论 v4.0 四分册）问题句/改后句转化"**作废**——用户选择不去翻旧材料，改用**真实链路真实产出的问题样本**自建。判据：规则 4.17 本就写明"种子集初版来自冷启动期问题样本"，且规则 4.17 同时写明**种子集的真实度决定系统能发现的问题维度**（系统已知天花板）——真跑样本的真实度高于旧文档转写。**纪律**：反例样例只收真跑观察到的，**禁止凭想象填充**；一条真样本胜过十条编的（承规则 4.18 宁薄勿撑）。落点：反例样例挂在 cr- 判据之下（checks 表，规则 4.10b：正当性锚仍是 decided_by，不新建第三类命名空间）；首批入册一律观察态（规则 4.17 入册门禁第二道）。

- **隐藏档取消，纪律形态改为标注（2026-08-29，用户裁决；推翻 v1.2 立、v1.4/v2.3 沿用的"未校准资产禁止进入 PAID 产物"）**：用户原话——*"未校准资产禁止进入 PAID 产物，这条限制我认为应该去掉，我们的整个产品策略就是给客户建议，如果建议有问题，我们后续应该调整，而不是隐藏掉，这样对客户也没有价值，我们也没法迭代；过期的降级方式是给标记，数据来源和时间，但是还是使用这份数据。"* 落规范 **v2.4**（§14.9）：①draft 与已过期资产**照常进产物**，随呈现标注来源与取数时间；②规则 4.10c 由"降档与隐藏的判据"整条重写为"标注判据"，v2.3 的三条隐藏判据全部作废，新增**标注必挂**（未标注即违规——原隐藏禁令的等价物，拦截点从"说出来"移到"不标就说"）；③规则 4.10a/4.18/5.15 连带重写（经验条目受限的是**语域**不是**存在**；造价章单价过期照常出金额+标注取数时间）。**判据**：隐藏同时拿掉了业主的价值与系统的信号，使规则 4.10a 的转正路径在冷启动期成为死循环（藏→无信号→转不了正→继续藏）；真正的风险是"业主误以为这个数有依据"，标注解决它且成本低得多。**未推翻**：断言预算（规则 5.8）——calibrated 仍是判断句背书唯一来源，只是不再兼任"能否出现"的开关；`conflict` 不进 release 维持（标注解决不了两源互斥）。**实现纪律**：先建标注链路，再拆隐藏分支，不得反序（空窗期＝没依据的数不带标注发出去）。
- **报告域十六条待裁一次性拍板（2026-08-29，用户裁决）**：①造价章城市档——**匿名画像加 `cityTier` 字段**（城市档是市场参数不是身份，一个城市几十万住户不具标识性；缺它则造价章要么失真要么哑火）；②五条锁定文案正文——**《页面级操作手册》原文找不到，改走起草 + 法务过目路径**（草案单独标状态，过目后才进 release）；③`report_id` **由 project-svc 铸造**（幂等键必须在重试的那一侧）；⑤每轮入册上限**固化算法不固化数字**（≤ min(bad+fixed 双逐字有据的类数, 有产出跑数 ÷ 3)，每条每轮新增样例 ≤2）；⑥**"一条 cr- 只能属一层"升明文**，作规则 4.10d 挂 4.10b 之下；⑦弱词词表**拆两批**（词面遗漏补既有判据，语境依赖建新判据）；⑧伪因果**先查后裁**，不占入册名额；⑨判官触发率**先建计数载体，N 与阈值等有数据再定**；⑩`fixed` **不给写作侧当正例**（四轮 A/B 已证正例会被逐字照抄，`fixed` 更甚）；⑭包内枚举大小写**维持两套**（前缀是标识符命名空间，枚举值是常量，硬统一会造出第三套写法）；⑮`GUIDE_SITE_CHECK` 推广**新增一条 ID**（注册表只增不改，不得改正文）；⑯锁定文案要求**以数据包清单为唯一口径**（不从 check 的自然语言 requirement 抠 ID＝发明表达式语法）。⑪⑬ 被上一条 v2.4 裁决吸收。④**派发通道走 HTTP 入口**（project-svc → `POST /api/v1/genpipe/reports`，不用 Java Temporal SDK 直连——直连会把里程碑引擎裁决刚收缩掉的 Temporal 依赖放回 Java 服务）；⑫**一次求值只服务一个权益档**维持（`entitlement` 是包级字段，混档产物求两次；一包两档会把档位从数据挪进控制流，下游每个消费方都要先知道"我在哪个档"才能读 `presentation`）。**十六条至此全部拍完。**
- **v2.4 遗留三问题处置（2026-08-29 晚，用户裁决："好，按照你的建议"，对经复核订正后的建议整体放行）**：①**新工作项插队顺序**——"判官分批送审+观察计数载体"（原序列 5 提前并入）与"叙事推导拆两步"插在序列 2（造价章）之后、序列 3（两条线接通）之前；判官分批先行、批大小 6 为**临时值**（唯一有实测支撑的值），对照跑作分批后的第一次校准而非前置，判官 prompt 不动（"宁可漏报"句撤回删除建议——前 6 张带同句指令判出 6 命中，删句证据不足）。②**正文用现场复核话术另立新 ID**（与裁决⑮ 推广 ID 同批注册，语义命名注册时定），正文走裁决② 起草+法务过目路径，落表前状态"待补录"；`GUIDE_SITE_CHECK` 正文与原射程不动。③**`number_class` 重标判据＝"业主或工人会拿它现场画线定位的，算定位数字"**；`lkp-pendant-drop-dining` 从重标名单除名（规范 §2.3 示例明文钦定为选型数字）；§2.3"定位数字仅 prec-exact 图纸"与 §12 v2.4 替代条文的矛盾**按 v2.4 口径收口**（定位数字可进正文，条件=随页标注+挂现场复核话术；v2.4 裁决 §12 替代条文已含此义，本条是收口订正不是新裁），§2.3 修订时留对照说明；`verify_seeds` 同批加 `number_class` 取值校验（`locating|selection|analysis` 闭集）。**连带订正两条判据数据**：`cr-budget-stale-price` requirement 改 v2.4 规则 5.15 口径（随序列 2 改源重发）；`cr-draft-not-in-paid` **置 retired**（名字即作废口径，改 message 留名＝名字说假话），其剩余有效语义由 `cr-assertion-backed` 承接（requirement 补"且为 calibrated"，升 version）。**撤回一条建议**（记此供审计）：每单元卡片数上限"先设 6~8"不采纳——阈值无数据依据（违反裁决⑤⑨"有数据再定"的一贯口径），且叙事推导落地后自然消解。

- **报告渲染层落仓 = 独立仓 `ishome-reportrender`，纯库 + CLI 先行、不成服务（2026-08-29 晚，用户裁决）**：口径全句（用户定形，同晚）——**"不成服务，以工具（纯库+CLI）形式存在，后续报告产出上线（'报告一键触发'接进编排）时，建立服务。"** 确认《交接文档-报告渲染启动.md》§二 推荐默认，两条有张力的既有裁决按"独立仓 ≠ 独立服务"合取化解。**判据**：①**物理隔离**——渲染是确定性代码、成文是 LLM，逻辑异质（与绘图三仓、chat/project 拆分同源判据）。*订正（同晚，执行者 Claude）：初记曾以"锁定文案正文对写作层结构性不可见，正文进成文仓则保证退化"作加固理由，经核不成立——该保证的承重件是成文侧数据模型不含锁定文案位（Card 无此字段）与依赖面只含契约生成代码、够不到注册表数据，两者均与渲染代码落仓无关；判据收口为物理隔离一条，结论不变*；②**不成服务**——规范 §13.6 服务存在性判据（无独立伸缩轴、无独立故障域→不起服务），故首版只做纯库 + CLI，Temporal worker 等"报告一键触发"接编排时再起，届时不再需要新裁决。**连带确认的默认**：模块名 `reportrender`；产物形态=**单册自包含 HTML**（可打印 A4；PDF 走浏览器打印，属后续项）；将来接编排的 activity 注册名 `report-book-render`、队列 `reportrender-activities`（届时走 contracts PR，**注册表只增不改**纪律照旧）。**唯一的跨仓写入例外**：contracts 增锁定文案机器可读数据文件（只增，走 PR，md 仍是叙事权威）；reportgen/backend/contracts 的报告域代码本线其余部分只读。落点：《仓库拓扑.md》升 v1.1（十仓→十一仓）；渲染层七条红线见《交接文档-报告渲染启动.md》§三。

- **渲染首册三案一次性拍板（2026-08-29 晚，用户裁决："可以，修改吧"，对三条建议整体放行）**：①**依据来源拆两字段**——provenance 的来源信息拆为客户可见引文（citation）与内部审计叙事（audit_note），获取回路改源、契约只增字段、求值线随包下发，渲染层届时页脚上纸 citation。判据：现状 source 字段是获取过程工作笔记（含内部资产编号与推理过程），渲染层禁改写/禁静默取舍/内部编号禁上纸三条红线下无出路，页脚来源栏被迫整栏空置；拆字段一次解决语域、编号泄漏、方法论语言三问题且渲染层零改动。**待实施**（归获取回路/主线，涉契约与求值引擎）。②**单边界措辞归渲染层**——写手文句禁在占位符前写边界词（不少于/至少等），措辞由渲染层按值形态出（"不低于/不超过"）；判据：边界语义存在值里、值的执行者是渲染层，与"数字不由 LLM 决定"同向；把措辞交还写手＝数字语义回流大模型，方向反。否决甲案（渲裸值+机检句子须带边界词：句子漏词时裸数被误读成点值）与丙案（容忍叠字）。**已实施**：跨域判据 `cr-bound-word-before-placeholder`（规则层 regex，恒 active）入 `_common/checks.yaml`，随下次各域 release 重发生效。③**量纲必填入核验门**——数值/公式资产缺 unit 即核验 ERROR，真无量纲显式 `dimensionless: true` 豁免（豁免＝声明"无单位是事实"，非"忘了填"）。**已实施**：核验规则 + 六条占比类标豁免 + 两条补单位（电视观看距离 mm——立案条目、全屋收纳总延米 延米）。三案立案出处均为渲染专项首册真跑（《交接文档-报告渲染启动.md》追记四 + 渲染仓运行记录）。

- **无背书题目呈现形态＝坦白缺口 + 落点归组与耦合边界（2026-08-29 晚，用户裁决，规范升 v2.5）**：判定材料=《评审/造价灯光失败归因与待裁点-2026-08-29.md》例证节（真跑逐字）。①待裁一用户选 **B（坦白）**：无背书题目只许坦白——算不出来、取决于什么、等什么，依据限包内可知事实不编原因；"描述现象不下判断"作废（实测＝元语言与伪因果填空）。连带"固定四节"改判**满配四节**（节的存在由数据背书决定，规则 4.18 方向收口）。②待裁二用户原话拍出更准的口径——*"两个事情同一个落点没问题，但是问题是我觉得落点不一定是因果……'因为床太高或太低都会让你起身时费力，而床边留得太窄会让你下床时碰腿、留太宽又浪费空间'这句我觉得没问题，但是床面高度和床侧净距得一起定，不应该是需要一起定，他俩都是床的人体工学设计，这是不是已经算是一个落点了？"* 裁读：**多落点同主题并列＝合法归组**（"床"这个整体的容器是主张不是落点——落点是带各自来源/校准的单个数，合并砸掉标注粒度）；**落点间因果/耦合不得编造**（"得一起定/互相让/配着调"在独立参数间造不存在的约束，与造价"拆除量挤压投影面积"同病，归伪因果族⑧）——"数字不由 LLM 决定"的同族纪律：**关系也不由 LLM 决定**，真实联动的载体是 parameters.linked；`cr-anchor-out-of-scope` 收窄回立案形态（数字按到错误对象头上才算外推），升 version 回归立案样本。判官 5/5 全拦嗅到真病但归错判据——一条判据管一件事。落规范 v2.5（§14.10）。

- **册内免责收敛为附录法务说明，现场复核提示保留（2026-08-30，用户裁决，规范升 v2.6）**：用户原话——*"法务话术落表，这个放到最后，最后同意由人员出一个法务说明，放在附录里面就行了，不排在前面"*；就两条现场复核话术的性质追问后——*"不是法务性质……保留吧 这个算提醒"*。裁读两半：①**册内免责/安全话术不再逐条起草嵌页**，改由人一次性出具的**一份法务说明**整体承接，位置＝报告册尾附录；本事项排在全部代码事项之后，不再作为并行轨占射程。落机制：新注册 `DISCLAIM_APPENDIX`（contracts 锁定文案注册表，只增），正文待补录、由人出稿，仍走 gen-locked 零生成／禁编造纪律（附录是一整段人写文本，不是模型产物，机制不变、数量从七条收敛为一条、位置从页内移到册尾）；原五条册内免责（`DISCLAIM_PRICE`／`DISCLAIM_RENDER`／`SAFE_WALL_GENERAL`／`SAFE_WALL_SUSPECT`／`SAFE_LAYOUT_CHANGE`）**ID 与正文不动**（只增不改），改的是**挂载条件**——报告册正文页不再逐条必挂；图纸图框侧的挂载**等绘图线出第一张 prec-schematic／prec-concept 图纸时另议**（`DISCLAIM_P1` 已有正文，图框侧不受影响）。②**现场复核两条是提醒不是免责**——`GUIDE_NUMBER_SITE_CHECK`（正文页未过门定位数字旁）与 `GUIDE_CHECKLIST_SITE_CHECK`（验收／报价清单页头）**保留在原挂载位置**，正文**不进法务批次**，由产品侧定稿走常规改源路径（人改源→AI 编译成数据→核验→release，规则 4.19），随**定位数字归类重标**同批落表。**连带**：规则 5.15"DISCLAIM_PRICE 必挂"改判为"造价章免责由附录法务说明承接"（规范 v2.6 §7 留对照）；`cr-budget-disclaimer` 判据语义随之变更，**升 version 的时点＝附录形态实施时**（该判据属 presence_require 类型、当前无执行器，不提前动数据）。**交付前置（写死）**：报告册对外交付业主的前置条件＝**附录法务说明有正文**——在它落表前，报告只作内部验证与产品迭代用。这也是造价章"严格说不能交付"的解法：卡点从"七条 ID 各自落表 + 法务逐条过目"收敛为"一份附录到位"。

- **规范 v2.6 → v2.7 订正落档（2026-08-30，执行者 Claude；本次无新裁决）**：把已拍裁决补进规范条文、并消除规范内自相矛盾条文，**裁决射程一字未扩**。①**报告域十六条裁决⑥**"一条 cr- 只能属一层"升明文 → 规范新增**规则 4.10d**（按裁决要求挂 4.10b 之下）：规则层恒 `active`／判官层 `semantic_judge` + `observing`，同一条判据不得同时在两层出现；配套写明"**gate 忽略 status 是正确实现不是 bug**"——规则层机检不看状态、观察态只作用于判官层，否则规则 4.17 门禁二的触发率统计当场失真。②**裁决⑤"固化算法不固化数字"** → 每轮入册上限的算式进**规则 4.17**：≤ min(bad+fixed 双逐字有据的类数, 有产出跑数 ÷ 3)，每条判据每轮新增样例 ≤2；条文同时点明"固化的是算法不是数字"这条口径本身（与裁决⑤⑨"阈值有数据再定"同源），防后来人把它当一个可调数字去调。③**v2.4 遗留三问题处置③** → **§2.3 数字三分法收口订正**：原表"定位数字仅 prec-exact 图纸"与 v2.4 之后的 §12 替代条文自相矛盾，**按 v2.4 口径收口**——定位数字**可进正文**，条件＝随页依据标注 + 挂现场复核话术（`GUIDE_NUMBER_SITE_CHECK` 正文页数字旁／`GUIDE_CHECKLIST_SITE_CHECK` 清单页头，两条正文待补录、由产品侧定稿）；同批把**分类判据**写进 §2.3——"业主或工人会拿它现场画线定位的"，**看用途不看它是不是一个高度/距离**，`lkp-pendant-drop-dining` 明文钦定为选型数字；原表述**留对照不悄改**，规则 2.2 图纸侧禁令未动。④**连带事实补录**（非裁决）：§1.7 前缀表 `lkp-` 落点列补第二个来源——`entity_type=work_item` 单价资产投影成 `lkp-price-*`、单位派生"元/计价单位"，并注明投影规则在求值线与核验两处各实现一遍、改投影两处都要动。⑤**规则 4.10c 说理句的话术 ID 订正**（同批消自相矛盾）：该句原文点名 `GUIDE_SITE_CHECK` 作配套话术，而该 ID 正文写死"水电交底当天…逐项现场确认并勾选"、射程是清单页头，用在正文页数字旁语境错；按 v2.6 已注册的两条新 ID 按页型分派，并写明**求值线派生常量仍指旧 ID**、改指的触发条件＝两条提醒正文落表（随"定位数字归类重标"同批）。**落点**：规范升 **v2.7**（h1 + 版本行；版本行开头滞留的"v2.2"一并订正），审计节 **§14.12**；§12 禁止项汇总同步（现场复核话术 ID 订正 + 4.10d 自查项）。

- **获客线走上传驱动，分三步，户型库排最后（2026-08-30，用户裁决）**：用户原话——*"我们是通过用户上传的内容，然后去生成后续的数据……用户上传内容的质量的话呢，我们先按照质量还说得过去的情况来看，然后质量有问题的事情我们再单独优化，先不去考虑这个问题；然后关于户型的问题的话也是后续再做"*，并明确*"分为几步来看"*。裁读三条：①**户型获取主路径改判**——原口径"库命中优先、上传解析兜底"（8-23 收录《沟通助手架构方案》v0.3 时的五处归位之一）改判为**上传驱动为主**：不预先囤户型，用户带图进来才生成；户型库、排产日历与批量预生成**移出首发射程**（原内容工厂"一次生成全小区复用"的边际成本优势随之延后兑现，代价已明示：成本随用户数线性增长）。②**三步顺序写死**（事项名，顺序即先后关系）：**干净图能出图**（假定上传质量说得过去：解析→三张图→解析结果喂付费报告）→ **脏图也能出图**（歪斜/水印/模糊/截图/低分辨率单独立项优化）→ **同户型能复用**（脱敏回流户型库、同户型命中直接复用、排产预生成）。③本裁决**只定顺序与射程，不定实现**——每步的技术选型与验收在《交接文档-户型图解析与出图启动.md》逐项拍。
  **同批定的实现纪律（非用户裁决，Claude 提出，可推翻但要给替代）**：**"质量说得过去"必须是确定性判据不是模糊感觉**——解析要么给出可用结构，要么**响亮失败并说明缺什么**，禁止把没把握的图硬解成低置信结构往下游传（下游是几何母版与付费报告的输入，一次静默降级污染整条链）。判据同渲染层 fail loud 与"过拦与漏拦同样是失效"。第二步"脏图也能出图"要做的正是把这条判据的门槛往下推，不是把门槛拆掉。

- **户型尺寸标定四级优先 + 得房率取值策略（2026-08-30，用户裁决）**：承同日"尺寸推算"裁决（几何层走比例标定）。用户原话——*"得房率的话我觉得我们可以让用户提供，如果用户没有提供的话呢，我们就按照比如说 78% 到 83% 之间去做一个估算"*，并就取值策略问询后拍板*"可以按照这个方式去做"*。定四条：①**标定优先级四级**——**分房间面积标注 > 任一段尺寸标注 > 门洞反标定 > 建筑面积配得房率**：门洞（入户门 900~1000／卧室门 800~900／厨卫门 700~800）是按图纸画的**结构件**，宽度分布窄、不需图外信息，标定精度高于"建筑面积×猜得房率"；**家具禁作标定物**——户型图免责声明通行写明家具"仅为示意参考"（样本实证），尺寸不可信。得房率由此**从主路径降为最后兜底**。②**得房率来源**：优先**让用户提供**；未提供则用 **78%~83%** 区间。③**必须取点值处取下限 78%**（母版几何、图上要标的面积等）——判据是**错误方向代价不对称**：得房率取大→尺寸偏大→业主按报告买的柜子装不进、拆改按不存在的余量做；取小→尺寸偏小→只是留了余量。买小了还能用，买大了放不下。④**进报告的数字带区间**（78~83 对应的尺寸区间原样下发，不取假精确点值）——与推算尺寸的身份一致：它本就是未校准的定位数字，须挂依据标注与现场复核提醒（规范 v2.6 §7）；区间形态系统天然支持（渲染层"一个占位符＝整条落点，区间渲染成区间"）。
  **区间自身标"暂定，随样本校准"**（同"阈值有数据再定"一贯口径）：得房率真实分布与楼型强相关（塔楼／板楼／小高层／洋房差异大），78~83 现为经验值。**校准路径免费且写死**：每收一张带尺寸或分房面积标注的图即**反算该图真实得房率**入库；**区间订正的时点＝样本量足以按楼型分档时**，分档口径与门槛届时用数据定，**不预设阈值**（同判官批量 N 的定法）。
  **影响面明示**：得房率的不确定是**整体缩放**，不改变房间相对大小——**三张图基本不受影响**（图上不标绝对尺寸；除非功能说明图要标分房面积），真正受影响的是**报告里的数字**。

- **正文可引用落点的某一项 + 项名命名空间（2026-08-30，用户裁决，规范升 v2.8）**：判定材料＝灯光六轮
  0/6 的真跑证据——六轮全部 27 种自造占位符，**27/27 逐字等于「真实落点 id」＋「该落点 value 字典里
  一个真实的键」**；即模型不是不守规矩，是**想说的那句话没有合法写法**（多键落点只能整条引用，而报告
  要说的恰是其中一项）。用户三句原话：分场景要不要——*"我觉得是需要的，因为卧室的灯光和客厅的灯光
  肯定会不一样"*；形态——*"我觉得需要加上标记能指定带哪一项，但这里面应该有一个命名空间的规则，
  这个命名空间的规则我们需要去制定一下"*；范围——*"我觉得修改的话就一起改吧，然后这样的话，那我们
  后面的命名规则可以保证统一性，避免以后再做调整的时候，我们的注意力分散改错了"*。
  **落规则 1.9**：①**两层模型**——一条落点＝若干项，一项的值＝一个数或一个区间，`valueKind` 七值闭集
  随落点下发；②**`min`/`max` 不是项**是项的值形态，故引单端的写法在语法上不存在——"引一端丢另一端"
  由**结构堵死不靠纪律禁止**；③**元信息出 `value`**（单位、参考平面各归各位，同层就管不住）；
  ④**项名命名空间**——闭集两类（档位 low/medium/high、维度 depth/width/height）、形态受控一类
  （档位比较 `{高档}-vs-{低档}`）、受控词表两类（分场景、分项，词表登记项名+中文语义+首次出处），
  ASCII 小写 kebab-case 与落点标识同一套、**只增不改**、核验强制。
  **被承接而非推翻**："一个占位符＝整条落点"是 2026-08-27 晚定的**实现层契约**（非规范明文、非用户
  裁决），其理由"拆开会丢掉另一端"只对区间成立——定它时数据里的多值落点恰好全是区间；两层模型把
  该理由原封保留在区间一档上，其余五类松绑。**改动范围按裁决＝一起改**，含已挂账的造价七分项占比
  （该条数据本身仍缺报价单类源、内容挂账不变，但**名字这一轮就改到位**，不留半套旧名）。审计节 §14.13。

- **不确定性由标注承担，禁自造精度声明（2026-08-30，用户裁决，规范升 v2.9）**：规则 1.9 实施时清点
  值形态，发现 `lkp-storage-closed-ratio` 是"点值 + 容差"（`0.8` 带 `tolerance: 0.1`），是两层模型
  之外的第三种形态。追查出处——**它自种子首版 `b036bd4`（系统批量编译六域那一轮）就在**，而该条
  `source` 只写"行业通行（二八原则）"：**二八原则给的是 0.8，没给 ±0.1**，同条自己还写着"无外部标准，
  属经验条目"。即 ±0.1 是**编译时自造的精度声明，没有源**。用户裁决（"OK，按建议处理吧"）：**删，
  不为它扩契约**。**裁决逻辑**：删的理由不是没用，是它用一个编造的数字重复表达"这条不确定"——
  而这件事系统本就有专门机制（规则 4.10c 标注必挂 + 语域限建议口吻）；再加一个自造的 ± 不增加信息，
  反而给业主假的精确感（"80% ± 10%"读起来像做过测量，实际只是一句行业说法）。**落规则 4.10e**：
  禁 `tolerance`/`approx`/"约"一类自造精度声明；真有区间用 `value_kind=range` 且**两端各自有源**、
  不许由点值折算（折算＝凭空造源里没有的数字）。核验加拒灌 + 三条回归（含"有源的真区间照常放行"——
  禁的是自造精度声明不是禁表达区间）。删后第三种值形态消失，规则 1.9 两层模型不必扩。审计节 §14.14。
- **同批收回一条"待裁"（无需裁决，记此供审计）**：`lkp-budget-driver`（总价主驱动项判定）此前列为
  "答案是项名、两层模型里没有它的位置，待裁"。复核后**不需裁决**——落点这套机制管的是**数字的身份**
  （来源/时效/可核性/语域/标注），对"一个分项的名字"全部无效；且它本质是**判断**不是**事实**，
  该由断言预算背书走结论那条路。**它现在的形态已经是对的**：求不出 → 走 `gap-` → 报告坦白
  "哪一项最终决定总花费，这轮给不出可靠判断"（正是坦白缺口裁决要的结果）。真正缺的是数据——
  要说"哪一项最大"得从七分项占比推，而那七个范围互相重叠（下限和 79%、上限和 148%），推不出来；
  该数据已挂账（要报价单类源）。**后续路径写死**：数据到位后它以**结论**形态出现，不会变成落点。

- **叠字判据换载体 + 单位那一半补齐（2026-08-30，执行者 Claude；本条不是新裁决，是既有裁决的实现
  形态变更，另有一条越出裁决字面的延伸，用户可推翻）**。触发＝当日"唯一读者可见的成品缺陷"：
  灯光章成品逐字 `全屋灯光颜色种类不能多于 不超过 3 种 种。`（边界词与单位各叠一次）。
  ①**裁决未变**——2026-08-29 晚"单边界措辞归渲染层"仍然有效，一个字没动；变的是执行它的载体：
  `cr-bound-word-before-placeholder` **retire**（version 1→2，pattern 摘进注释留档），语义由 reportgen
  两条代码判据承接（`gate-bound-word-before-ref` 记号前的边界词、`gate-unit-after-ref` 记号后的单位）。
  **换载体的理由是它漏了自己那条纪律**：原 pattern 靠"固定词表 + 紧邻占位符"匹配，成品那两句全漏
  （「不能多于」不在表里、「上限」不是它认的边界词）——**词面枚举是形态错误，不是词表不够长**。
  摘掉 pattern 是必须的：规则层 gate 按设计不看 status（规则 4.10d 明文禁"修"），留着 pattern 的
  retired 行会继续拦截，等于没退役。②**单位那一半＝用户裁决 2026-08-30（原话："单位裁定这件事情，我觉得没有问题，我觉得是对的"）**：
  8-29 晚那条裁决只覆盖边界措辞，**单位是本次新形态**，Claude 按同一条道理先行处置、当场提请过目，
  用户确认成立。口径落定：**单位与边界措辞同族，都由渲染层随值出，正文写到记号为止不再补**；
  正文在记号后面再写一遍单位即违规（机检 `gate-unit-after-ref`）。射程＝全部章节，不再逐章问。③release 重发六域
  （`budget@v11 / ergonomics@v10 / lighting@v11 / material@v9 / softdeco@v9 / storage@v9`）。
  ④**新立一条待裁**：改后十二跑取证，**单位那一半 0/12 复发、边界那一半 9/12 复发**，机制不是模型
  不守纪律，是**它想写的那句中文里天然带边界词**（"显色指数不低于 90"）——三条处置路（维持现状／
  写作侧看不见 min/max 键面／重开 2026-08-29 晚被否的甲案）都动到已定裁决或跨仓结构，**不自行选**，
  例证与逐字样本见 backend `_iteration/run-2026-08-30-wording-around-ref.md` §四。

- **空间推理另立背书通道，与数字落点同构（2026-08-30，用户裁决）**：用户对"背书判断的范围是不是有问题"提出质疑并认可解法（原话*"我觉得这个机制可以"*）。**判据**：现有断言预算只按"依据是不是外部知识"一个维度分类，把**依据是图本身**的空间推理（"你家厨房只有两面操作面，两人同时用容易在转角撞上"——两面操作面是图上画的，读者看图即可自验）一并卡住；代价正落在获客线上——**户型信息读得再准，进了报告也说不出什么**。**风险已认不因裁决消失**：伪因果最擅长伪装成空间推理（真跑样本"拆除量直接挤压定制柜投影面积的可布设范围"长得就像空间关系、机制全是编的，规范 v2.5 §14.10 立案），**模型会声称依据在图上，而声称是免费的**。**解法＝把"依据在图上"从自我声明变成可机检的事实**：①解析产出**带 id 的结构化户型事实**（非自由文本）；②随包下发，与落点同一位置同一性质；③空间推理句**必须声明引用了哪几条事实**；④机检两条——引用的事实必须在包内存在、**一条都没引用的空间推理句即打回**。与数字机制**完全同构**：不判断数字对不对只保证它来自求值线，不判断推理对不对只保证依据来自解析产出；模型想编，得先编一个包里不存在的 id——**不是禁止它编，是编了引不到**（灯光六轮 27 种自造占位符全被拦即此机制的证据）。
  **定位写死**：**管编造不管错误**——引真事实但推理错（引"厨房是 L 形"却说"三人一起做饭很宽敞"）挡不住，那一层归判官抽检与业主反馈信号，同数字侧"数字为真但用错对象"的处置。
  **连带**：同日"让下游消费观察区明确信息"那条提议**升格**——事实层有双重身份，除"事实级+零生成的户型档案复述"外，它更是**空间推理的可引用锚点**；没有它，判断无物可引，"依据在图上"永远只能是声明，于是只能一刀切。两件事是同一件。
  **待收口后才写规范条文**（不落马上要改的条文）：引用写法是否与规则 1.9 单项引用统一为一套语法（**须与并行线协调，两套引用机制打架代价高**）、事实字段闭集（判据＝同一张图多次读一致才能进：实测房间清单与厨房形态三次一致、朝向三次三种答案）、事实 id 新前缀须先进规范 §1.7 前缀表、**"什么算空间推理句"的机检判定（本机制最难的一处，过拦与漏拦各有代价）**。全文与例证见《评审/背书范围与空间推理-2026-08-30.md》。

- **获客线收尾六裁（2026-08-30 晚，用户裁决）**：同日空间推理背书通道（见上条）之外，用户就获客线一次性拍定六条。①**户型图可回流户型库**——原话*"用户上传的户型图可以回流到户型库，这个地方不用纠结"*：第三步"同户型能复用"的授权前置**解除**（脱敏口径与实现形态待做时定）。②**排期改判＝先出图**——原话*"先把图初弄出来吧"*：出图那半（母版与三张图）提到特征链路完善之前；**第一道门＝几何提取验证**（母版要坐标，而分区读实测整图定位把阳台位置整体报低约半个身位，让模型直接报坐标精度到不了画图要求），**不需要比例标定**（图上不标绝对尺寸，相对关系对即可，得房率那套只服务报告数字）。③**事实层给业主看**——报告里那一节"你这套户型"照出（事实级 + 零生成、模板渲染不经写作步）；事实层的另一身份（空间推理的可引用锚点）不受影响。④**权益不补档，是规范表述要改**——用户订正*"私有和免费没有关系，免费就是免费，付费就是付费，私有和免费之间不存在关联性"*：**归属（私有/公开）与权益（免费/付费）是正交两维**，权益枚举本就够用；真正的问题是规范"stage-project 深化产物全量 PAID"把**阶段与权益绑死**——改法＝**权益按产物逐个登记，不按阶段整批推定**（待动规范时落条文）。⑤**上传资产一直保留，删号不处理**——原话如此；**登记为上线前重看项**（有真实用户后个人信息相关法规通常要求提供删除途径，现无真实用户不影响任何事）。⑥**起一批新规则，先写 5~8 条**——定位写死：**空间推理那条路通了之后，"从图上看得出来的"不需要规则**，这批只写**图上看不出来的外部专业知识**（开放式厨房的油烟外溢与风量取值、南向飘窗夏季得热、单卫家庭早晚高峰与干湿分离价值这类）；判据一句话——**这条知识能不能只看图就说出来？能＝不用写规则，不能＝才是这批要写的**。规模先 5~8 条且**从解析已稳定读出的事实出发**（厨房形态/开放与否/飘窗分布/单卫/玄关形态/南北通透），写完立刻能验证"规则→触发→进报告"在新事实上走得通；反过来先按域铺开会写出一批**没有事实能触发它们**的规则，那是又一次"入册不等于生效"。

- **在空上给出值的特征，检测时检测内容合不合特征（2026-08-30，用户裁决）**：用户原话——
  *"在输入的地方给出需要填的值的特征，然后在检测的时候检测内容是否符合这个特征"*，并先行确认
  *"单位裁定这件事情，我觉得没有问题"*、*"点值这个词不好…它应该是个固定值"*。本条**覆盖 8-29 晚
  裁决②的一半**：单边界措辞不再由渲染层出，交还给写手的句子；**单位那一半不动**，仍由渲染层随值出。
  判定材料＝十二跑真跑（backend `_iteration/run-2026-08-30-wording-around-ref.md` §四）：边界词违规
  **9/12 跑**，而模型自发写的 17 处边界词**方向 16 处正确**——**能力不是问题，是那句中文的语法主干
  正好落在洞里**（要说一个上界，顺口就是"最多 3 种"，而旧形态要求它写"…种类▢。"）。
  ①**四种特征各配一句要求与一道机检**：固定值（不许加边界词）／只给下限、只给上限（必须写那一族的
  词，可用词逐字列出）／两端齐的区间（不许加单侧词）；②**分界线只有一条＝句子够不够得着**——一个
  记号只渲一个值，说法归句子；一个记号并列多项（淋浴净尺寸宽/深），句子够不着每一项，说法仍归渲染
  层。**表格与图框同理，那里"旁边"指列头，触发条件写死：清单页族落地时同批做那道机检**；
  ③**词表是正面清单不是禁止清单**，理由是失败方向不对称——正面清单漏一个词只多烧一轮重写，禁止
  清单漏一个词缺陷就进成品（当日上午那句叠字正是后者）；④**术语订正**："点值"改称**固定值**
  （用户："点值这个词不好"），数据字段名不动（闭集只增不改），只登记中文说法。
  ⑤裁后六跑：边界词违规 **9/12 → 1/6**、方向写反 0/6，出册成品逐字 `全屋灯光颜色种类最多 3 种。`
  ⑥**新立待裁（本条的连带）**：单位违规 **0/12 → 2/6**——"不超过三种"在中文里是一个词组，
  让写手写前半、后半仍归代码，它顺手写完整句。三条路见
  backend `_iteration/run-2026-08-30-slot-characteristics.md` §四（维持／单位也交给句子＋登记展示单位／
  连边界词一起收回——第三条已被本轮真跑否掉）。

- **收费入口留形态、不接资金路径（2026-08-30 晚，用户裁决）**：用户原话*"收钱的部分先不用做，我们先是自己在做测试，所以说前期我们可以把收费的入口留着，但是不用真收钱"*。**关键区分（落档要点）**：**权益判定 ≠ 支付**——①**权益机制照常工作**：哪些产物免费、哪些付费的**内容分界真实存在**（报告数据包的权益字段已在用，与同日"权益按产物逐个登记不按阶段推定"那条配套）；②**收费入口以形态存在**：业主看得到"这部分是付费内容"的分界，但**不接资金路径**；③**交易侧仍是空骨架，不动**；④**测试阶段的权益授予走开关**（自测阶段付费内容对测试对象全开，实现形态待做时定，**不得**为省事把权益分界一并取消——分界没了，将来接支付时无从判"这份该不该给"）。**真收钱的时点写死＝要验证付费转化时**（非"以后再看"）。**连带**：本裁决把"能不能交付给用户"三缺口里最大的一件移出当前射程，余下两件（**上传入口接线**、**报告能在线打开**）合起来即"免费闭环"——真实用户发图、拿到三张图、拿到一份能打开的报告，足以验证最要紧的那个问题：**东西做出来了，人愿不愿意看、看了觉不觉得懂他家**。

- **重写要看得见上一稿；打回一律带原文与原因，且简洁（2026-08-30，用户裁决，射程＝所有裁判场）**：
  用户原话——*"重写的时候把原稿带回去，这条没问题，我们加上吧"*；*"在做裁判的地方，我们都应该
  将打回的原文和错误原因进行返回，便于后续的修改。然后这里注意返回信息的简洁性，避免给重写的
  模型造成过多负担。然后这个规则的话应该是适用于所有裁判的场景"*。
  ①**上一稿原样带回**——改前重写只递一张违规清单、原稿不回传，等于让模型"逐条修正"一份它已经
  看不见的稿子（它实际是从头再写一章）；违规按卡**归位挂到那张卡下面**，并明说"没被点到的卡片
  原样抄回"。②**打回三件按序写死**：`「原文片段」+ 哪儿不对 + → 怎么改`，**且短**——打回话逐字
  进下一稿的 prompt，说理与背景在那里只是噪音（改前最长一条 150 字，其中 120 字在解释这条纪律
  为什么存在）。③**射程是所有裁判场**：规则层逐条压缩；**`cr-` 判据补原文**（它的 message 是
  release 数据只说"哪儿不对"，`cr-weak-word` 只说"分析级结论句禁弱词"，模型不知道是哪个词）；
  候选词只给最顺的三个而机检仍认全表；判官层本就是"判官命中「原句」：为什么"，形态已合。
  ④真跑六跑：**出册 1/6 → 3/6**，平均重写 2.0 → 1.7 轮——**两项同批下去，N=6 分不出各自贡献，
  不当单项证据**；机制是清楚的：模型现在能改稿，不是重写。

- **单位也交给句子，但由我们预制、模型照抄（2026-08-30 晚，用户裁决；覆盖同日"单位跟着值走"）**：
  用户原话——*"单位不用大模型写，我们给它预制好…那个'种'字应该加在花括号变量的后面，这样大模型
  往上填的时候就知道只需填数、不用填单位了"*。**与当日下午那条不矛盾**：模型仍然不"想"单位，
  它抄我们逐字递过去的那个字，机检逐字比对该落点的 `unit`——单位依旧由数据决定。
  ①**为什么要挪**：上一版把边界说法给句子、单位留给渲染层，单位违规 0/12 → 2/6——
  **`不超过三种` 在中文里是一个词组，所有权的线划在词组中间，接缝处必漏**；这条裁决把线挪到
  词组外面，代码只拥有"数"本身。②**连带逼出一个更早的洞**：单位既然要写进正文，数据里就不能
  放"要求写、写了就违规"的单位——扫六域两处撞禁词（lighting `×环境照度` 含「照度」、storage
  `延米` 就是该域禁词），而**前者已经逐字印在出过的成品里**。根因是**禁词检查跑在替换之前的
  文本上**，渲染层再把数据里的词面替进去，禁词从数据侧绕过机检上纸；唯一堵得住的地方是生产侧
  （新增 `gate-unit-collides-banned-term`，整包拦下并指名改源）。**lighting 已改源**
  （`×环境照度`→`倍`，发 `lighting@v12`）；**storage 的「延米」未改**——延米既是真单位又是该域
  禁词，换成什么是**产品措辞判断**，且该域从未跑过，守卫会在它第一次跑时响亮拦下。
  ③同批按用户"这句话写的有问题…信息很杂很乱"重排落点下发行：四行封顶＝**你写／读者看到／
  这是什么／（撞词提醒）**。两行并排的用处不止好读——**单位该不该写、写在哪儿，一对照就看见了**，
  那句"别写单位"的叮嘱因此整句删掉（它讲了两版都没拦住）。
  ④真跑六跑：**出册 3/6 → 4/6**，平均重写 1.7 → 1.5 轮，**单位与边界词违规双双 0/6**，
  记号旁边那一族判据整族归零；剩余失败只剩禁词与弱词两类老形态。

- **私有产物放阿里云 OSS 私有桶，先做"报告能打开"那一半（2026-08-30 晚，用户裁决）**：用户定的目标是
  *"尽快让真人用上"*，随后就两个分岔各选一项——**私有产物存放＝阿里云 OSS 私有桶**（另两个候选：
  火山引擎 TOS、dev 服务器自建），**先后＝先让报告能打开**（另一项：先接上传入口）。
  桶＝`ishome`，华北2（北京）。**判据（问题里已写明、用户据此选择）**：签名链接由 OSS 域名直接对外、
  自带有效期，**本项目一个公网端口都不用开**——而部署那台机器上跑着 aidcp/isales 的生产服务，
  多开一个公网面就是多一份风险；自建则没有厂商那套签名机制，"只有拿到链接的人能打开"要自己实现。
  **连带落地（Claude 提出，非用户裁决，可推翻但要给替代）**：
  ①**分桶不分前缀**是红线的执行点——用户私有产物与公开内容各一只桶；同一只桶里靠前缀分公私，
  改错一次 ACL 就全公开了（获客线红线二"公开与私有严格分轨"唯一靠得住的落点）。
  ②**对象键由 `report_id` 确定性派生**（`reports/{report_id}/book.html`，真源＝contracts
  `registries/object_keys.md`）：写的一侧是 Python、签的一侧是 Java，谁也不能 import 谁，
  只能靠同一条键接头；派生而非分配的直接后果是**"这份报告出没出册"问存储即知，不必另立台账**——
  台账会与真相漂移（写成功了没记上、记上了其实没写成），派生不会。这与"里程碑真相在表、
  不建第二台状态机"（规则 8.1）是同一条纪律。
  ③**生成侧只写不签，签名归业务侧**：签名是"给谁看、看多久"的事，属业务侧（project-svc）——
  生成侧不知用户是谁（图 v0.2 §0）。
  ④**链接有效期七天，且口径如实**：现在**没有"认人"这一步**（触发条件写死＝开始接外部真实用户时），
  所以当前成立的说法是**"拿到链接的人就能打开，且会过期"**，不是"只有他能打开"。
  **认人上线后改短、每次现签**，那时后半句才成立——这条不许含糊，它是安全口径不是措辞。

- **渲染层建立服务：触发条件已满足（2026-08-30 晚，既有裁决的兑现，非新裁决）**：2026-08-29 的原话是
  *"不成服务，以工具形式存在，后续报告产出上线时，建立服务"*——它是 CLAUDE.md"描述逻辑完整闭环"
  那条纪律的范式句。**报告要交到业主手上＝报告产出上线**，触发条件到了，服务本次建立：
  独立 Temporal worker，队列 `reportrender-activities`，activity `report-book-render`（contracts #14）。
  **与成文线分队列**：渲染是 CPU + IO、成文是 LLM 推理，伸缩轴不同——"逻辑异质→物理隔离"的第四次应用。
  **"独立仓 ≠ 独立服务"这条口径不受影响**，只是渲染层不再是它的例子：仓先立、服务按写死的事件后建，
  两个判据分列、先后落地，正是当时那条裁决要的形态（绘图三仓仍在"有仓无服务"这一档）。
  **CLI 不废**且与服务那条路**结构性分开**（依赖契约里 CLI 看不见 activity 层）——从它能看见起，
  "本地改样式不需要凭证"就只是一句承诺而不是结构。

- **失败摆在那儿就直接修，不为测量而测量（2026-08-30 晚，用户裁决）**：用户原话——*"如果能过就是能过，
  如果不能过就是不能过……我们现在验证这个改动是否有效的价值是什么呢？如果不能过，那我们就看它如何能过
  不就好了吗？"*。**射程**：驳回了"逐轮落档以算改动有效率"那个提法——**出册率是业务要的数，有效率是
  开发给自己的改动打分**，后者不构成产品需求。**判据**：一件工程要做，理由必须落在"用户拿不到东西"
  或"拿到的东西不对"上；"我想知道我上一个改动有没有用"不是理由。**连带（同日即兑现，见下一条）**：
  逐轮记录这件事本身没有废——它换了用途，从**记给开发看的统计**变成**喂回给模型的输入**，
  那一刻它才有产品价值。

- **判据只判不写；替代说法由写手自己想（2026-08-30 晚，用户裁决）**：用户原话——*"改说叉叉叉这件事情
  的话，我觉得是不是应该在生成的模型去判断，而不是我们在裁判模型去给出建议？这个不是裁判模型应该
  做的事情"*。**背景**：当时提的改法是给禁词表每条加一个"替代说法"字段，打回话从「禁词『照度』→
  换人话说」变成「→ 改说『××』」。**两条驳回理由**（用户点的是第一条，第二条是落档时补的连带）：
  ①**判和写不能同层**——判据一旦给出该写什么，它就在写字，出文的责任散成两处；②**替代说法固定在
  release 数据里，每份报告都说同一个词＝人预设模板**，撞 AI native 红线。**现形态**：打回话只说
  "这个词不行"，换成什么由写作步自己判断；**它判不出来时的后续路径写死＝把它已经试过的写法告诉它**
  （下一条），不是给它答案。

- **每一轮的打回原因都喂回给模型（2026-08-30 晚，用户裁决）**：用户原话——*"我们其实应该在每次重新
  产出内容的时候，将上一轮、上上轮、上上上轮三次被打回的原因都给到模型"*。**背景**：当晚六跑失败的
  两跑，形态完全一样——**同一个词连写三稿**（一跑「照度」、一跑「宜」），打回了两轮一个字没改。
  成因是模型每一轮只看得见"这一稿哪儿错"，看不见"上一轮我也是这么写的"。**现形态（三条，缺一不可）**：
  ①最新一稿**带全文**（2026-08-30 早先裁决"重写要看得见上一稿"）；②更早各轮**只带打回原因不带原稿**
  ——原稿摆在眼前会把改动退化成在旧句子上挪字，被打回的词反复进 prompt 也容易把它钉死在那个词上；
  ③同一条判据跨轮再犯的，**直接标在当条违规后面**（"第 1、2 稿也栽在这条"），按判据归并不按卡片
  归并——措辞挪到另一张卡不算改掉。**归并口径的理由**写死在这里：卡片序号每轮会变，判据 id 不变。

- **prompt 自己不许写它禁的词（2026-08-30 晚，立案+实装，非裁决）**：查证的三个事实——①灯光域
  13 条落点里**7 条题名带「照度」**逐行递进 prompt，纪律条文举例又写一次，禁词表再写一次，模型全篇
  看见「照度」**9 次、8 次是我们递的**，然后被要求"一个都不能出现"；②那条纪律的原文是"落点的名字里
  **可能**就带着禁词（如「**照度**标准值」）"——**一句话踩两个禁词**；③「依据」在写作 prompt 里 4 次、
  推导 prompt 里 3 次，它本身在公共禁词表内。**落点名不改**（坑单四已裁：「照度」在题名里是准确的，
  改成「亮度」是另一个物理量），**改的是 prompt 自己的措辞**（依据→背书／理由，举例整句撤掉）。
  **长效拦法＝两步各一条守卫测试**：把禁词表本身那一处剔除后，prompt 里再出现任何禁词即测试失败。
  **与铁律一的关系**：题名撞词的**逐行点名**不违铁律一——它不是把新词面塞进 prompt，是给一个已经
  不得不出现在输入里的词贴内部标签；该形态 2026-08-29 已在推导步实装并实测有效，出文步一直漏着，
  本次补齐。

- **禁词按「为什么禁」分组（2026-08-31 凌晨，用户裁决）**：用户原话——*"OK，增加上分类吧。"*
  **背景**：25 个禁词共用一句打回「换人话说」。那句话对「照度」成立（换个业主读得懂的说法），
  对「可能」是**错的指令**——软话不是换个近义词能救的：换「或许」还是禁词，换「大概」不在表里
  但立刻被弱词判据打（真跑 v5 就是这么烧掉两轮重写的，同一个「宜」两条判据一起中）。
  **关键事实：分类本来就写在种子里**（`weak`/`methodology`/`overreach`/`responsibility` 四组），
  一路进了库，**只在装包那一步被拍平丢掉**——这件事不是"要去分类"，是"别再把已有的分类扔了"。
  **形态**：①包里加分组字段，**平表不删**（扫描与守卫要平表，平表恒为分组的并集）；
  ②prompt 按组分行，每行给这一类的改法；③打回话按组给路——**与 prompt 共用同一份路由表**，
  两处分家正是本轮在修的那类毛病；④**组名不认得就退回通用那句**，加组不破消费；
  ⑤**路里不给替代词**（判据只判不写那条裁决的直接延续）。
  **射程**：灯光域四个工程词的组名由 `domain_extra` 改为 `jargon`（`lighting@v13`），
  **其余五域不动**——同「延米」那条先例：没跑过的域不预先分类，等它第一次跑再说。
  **第二个（更大的）用处**：平表只禁**表上这些字符串**，带理由的分行能让模型避开**表上没有的近邻**
  ——「大概」不在表里但同样是软话；当初那个叠字缺陷的「不能多于」也是这样漏的。
  **落档时说清的一条代价**：禁词违规当时已经是 0/6，这件事量不出来——它是**便宜的保险**，
  不是在修一个正在犯的病。做，是因为分类现成、且判据只判不写那条裁决要求打回话必须说得准。

- **比例锚点不再是必答题：拿不到就推算，要问只问得房率（2026-08-31，用户裁决）**：用户原话——
  *"我们之前的设计应该是优先推算的，但是我们的 chat 服务似乎一直在让客户去进行测量，这个是为难客户的
  一个行为，我们不应该这么做"*；就"拿不到锚点时还问不问"拍板**只问得房率、不问实测**。
  **背景**：会话侧把"比例锚点（用户实测/确认的已知尺寸）"硬编码成五个最小必要输入之一，缺口判定只看
  这条事实在不在、不看用户说过什么，所以业主答"不方便测量"下一轮照样再问——真机对话里连问三轮。
  **射程**：这条与 2026-08-30「户型尺寸标定四级优先」是同一条的会话侧形态——那条已写死"**推算是主路径
  不是降级路径**"，本条把它落到追问行为上：①**四级标定自己走完**，会话侧不为标定发起任何追问；
  ②**四级全落空时只问得房率**——业主看一眼合同就知道，不用弯腰拿卷尺；
  ③**用户主动给的实测尺寸照收**（它比图上标注更好），只是系统不主动逼问。
  **同日补充（用户）**：得房率**问的时候就要把台阶给够**——同一句话里明说"不知道的话告诉我们、
  我们自己推断就好"；业主答不知道或不回答，**就用 78~83% 自己推，不再追第二次**。理由与本条主体
  同源：问一个他可能没有的数，如果不同时给出"不知道也行"的出路，问句本身仍然是压力。
  **触发条件写死＝会话侧下一次动工时**（上传入口那条线的"会话收到图、上报业务侧"那一段）——
  那一次连这条一起改，不另开一轮。当前形态先留着，不改代码是用户同日的另一处决定（先出诊断）。

- **排期改判：先做三张图那条腿，飞书直接把图回给业主（2026-08-31，用户裁决）**：用户原话——
  *"我觉得我们需要把这部分先做出来，然后让那个飞书能直接回这 3 张图，这个应该是我们的一个目的。
  通过 Chat 收集用户信息，然后生成 3 张免费的图，这个先搞定"*。
  **射程**：免费闭环本有两样东西（8-30 裁决原话"真实用户发图、**拿到三张图**、拿到一份能打开的
  报告"），此前的执行线只排了报告那半；本条把**三张图那半提到报告链路完善之前**——与 8-30
  "排期改判＝先出图"同向，那条当时被"目标改判为尽快让真人用上"暂时压过，现在归位。
  **两条腿的共用段不变**：图进桶（已完成）、会话侧上报业务侧、业务侧铸任务派发解析——
  这三件两条腿都要，排在最前不因先做哪条腿而变；分岔在其后（解析产物是先去画母版，还是先去喂报告）。
  **连带触发条件到期**：渠道出站发我们自己桶里的图（此前写死＝"生成的图要送回会话时"）**现在到期**。
  **报告那半不废**：已通的出口（出册、私有桶、签名链接、手机上能读）原样保留，接线时点＝
  三张图这条腿走通之后。

- **模块各自保证自己的质量，上游数据可信；上游错了去改上游（2026-08-31，用户裁决）**：用户原话——
  *"我觉得每一个模块保证自己模块内部的质量，上游模块产生的数据对下游来说的话是可以信任的，
  上游如果做的不对的话，我们应该是去找上游的服务去修改。而不是我们自己在这边去补，能理解吗？
  系统应该是一个松藕合的状态。"*
  **判据**：下游发现上游违约 → **响亮失败**（整包解析失败那一档），不做容错回退、不静默补齐。
  **当场执行的一处**：缺口的 `basisTag` 消费侧原写了"缺了就全发"的兜底，已改为必填、缺了整包失败——
  兜底的坏处是上游哪天不发这个字段会**静默**回到同一个 bug，而契约里它本就是 required，
  消费侧再容错等于两处口径打架。
  **不违反本条的一件**：拟真上游包（fixture）是**测试替身不是生产补丁**——松耦合的模块本来就该能拿
  一份桩上游数据独立验收。硬约束＝fixture 禁止成为生产回退路径（只在 tests/ 下，打包只收 src/，
  另有一条测试逐字扫 src/ 禁止生产代码 import 它）。

- **报告不许有"以后再算给你"这个态（2026-08-31，用户裁决）**：用户原话两句——
  ①*"地毯尺寸和收纳走长应该是我们给出的建议，对吗？而不应该是客户给出来的。客户怎么知道地毯
  应该用多长的呢？这应该是我们告诉他的，而不是他告诉我们的。"*
  ②*"不要在报告里写我们后面算给他，这报告都给客户了，我们后面怎么算给他？"*
  *"我们不应该有后面算给他这个概念。"*
  **射程**：报告是**一次性交付物**——交付那一刻要么有这个数，要么这件事根本不进报告
  （规则 4.18 宁薄勿撑）。两个方向都堵死：既不许写成"等你确认/等你提供"（把我们没做完的活说成
  业主没交作业），也不许写成"我们下一步补给你"（承诺没有送达渠道）。
  **成因**：求值线的缺口 reason 只有三种（`missing_input`/`formula_not_implemented`/`empty_definition`），
  **没有一种的意思是"等客户告诉我们"**；而写作 prompt 原文写着"可坦白留待现场确认"，四章都照它
  写成了"等你确认沙发落位"。现口径＝缺口只作为"别编它"的禁令下发，不作为可写的题材。
  **顺带划清的一条边界**：**他知道的事实该问**（套内面积、几口人、有没有宠物），
  **他不知道的设计判断不许问**（地毯该多大、要多少延米收纳）。
  **未处理的邻接项**：8-29 拍的"无背书题目＝坦白（选项 B）"话术里也有"等什么才算得出来"这类朝前
  指的说法，与本条同族但**射程不同**（那是"有落点但没过可核性门"，不是"根本没有值"）。
  按《纪律·已定不重开》未动，等用户说。

- **通用知识不必事事与这户强关联，能给获得感即可（2026-08-31，用户裁决）**：用户原话——
  *"我觉得没必要呀，因为装修的人的话，他也不见得对这些知识都了解，对不对？我们其实能给他提供
  获得感就行了，不见得一定要跟他的家人有强关系吧？"*
  **推翻的对象**：这条推翻了执行者拟提的"卡级抠数字判据"（一张卡没有本户数字就不许出册），
  也**推翻了《D-10 造价单元规格说明》第 1 节的全章判据与第 7 节禁止项第一条**
  （该文档原话：*"把一段话里属于这一家的具体数字全部抠掉。如果句子仍然完整、仍然成立，这段就该删"*、
  *"行业计价规则……通用知识，抠掉数字后句子仍成立"*）。**同一件事两份文档说法相反，
  后来者按本条**。
  **同时立的核心目标（用户原话）**：*"我们的最终目标其实是把这一页比较好的呈现，并且让用户有
  获得感，这是我们的核心目标。"*——获得感成为呈现层的第一判据。
  **执行者提议、未拍**：通用知识该讲到"这对你意味着什么"那一层（对照：「柜子按投影面积算」
  vs「柜子按投影面积算，所以做深做浅不涨价、做长了才涨——想省钱先砍长度不是砍深度」）。
  标明不是裁决。

- **对话与文档三条纪律（2026-08-29，用户裁决；2026-08-31 补档，原话未留档）**：
  ①**计划项与对话一律用事项名，不用纯序号**——名字说"做完之后成立的事实"，顺序用"排在 X 之后"表述；
  编号只在定位时进括号（规范条文号／裁决编号／release tag／提交号是文档地址，保留；
  `序列 N`／`步 N`／`轨 X` 这类纯排序编号废除）。
  ②**对用户沟通不以标识符为依据**——变量名／文件名／仓名／字段名是改文件的依据，不是与用户沟通的依据；
  对话与汇报说机制、说事项，标识符只在它本身是被裁决对象或用户问到具体位置时出现；
  中文《文件名》指认与交接/执行文档内部不受此限。
  ③**描述逻辑完整闭环**——否定式口径必须同句给出替代形态与后续路径，范式＝*"不成服务，以工具形式存在，
  后续报告产出上线时，建立服务"*；后续路径的触发条件用事件名写死，禁止悬空的"不做 X"与"后续再看"式开放结尾。
  三条共用判据：**读者读完不需要回查、不需要追问**。
  **补档说明**：本裁决当日只写进 CLAUDE.md 的文档纪律节，**未进本清单**，违反《纪律·裁决必落档》；
  2026-08-31 用户确认**原话未留档**（用户原话："我现在也不记得当时说什么了"），按转述补录，
  **引文缺失已知**——《纪律·裁决要带用户原话》在本条上是欠着的，不因补录而抹平。
  **现行权威处**＝《规范/协作与文档纪律.md》§三 对话（《纪律·计划与对话用事项名》《纪律·顺序不写进名字》
  《纪律·编号只用来定位》《纪律·不以标识符为沟通依据》《纪律·不许只说不做什么》）。
  **与 8-27「编号与命名规范」的分工**：那条管产品数据里的标识符怎么起名（规范规则 1.7/1.8 与前缀注册表），
  本条管**对话与计划怎么表述**，两个域各管各的、名字不进同一张表。

- **能自动拦住的不写成纪律；纪律名字改用平白动作词（2026-08-31 晚，用户裁决，纪律文档升 v1.3）**：
  **裁决一**——用户原话：*"这个是测试的问题……这个不是开发规范，这个是开发的问题，这是 bug……
  应该是通过架构去解决，而不应该通过规范去解决"*。执行者当轮把七份日志坑单里的九条判为"够格升格为规则"，
  经此判据重判，**八条是工程缺口不是规范**：全链冒烟缺失（跨仓改动两侧单测全绿却卡到超时，踩过三次）、
  契约变更没跑消费侧（`extra="forbid"` 拒收，一天两次，第二次成文已过死在出册）、本地自查与门禁不是
  同一条命令、历史迁移文件可被改动（Flyway checksum）、改判据语义未强制升版本、改判据未回归立案样本、
  改 prompt 未跑两侧回归；另一条（禁词词面不进 prompt）**结构上已经拦住了**（守卫测试已存在）。
  **只剩一条是真规范**：示范句越多过检率越低——可抄性才是代价不是样本量（四轮 A/B 实证，
  非对称规则 0/6 更差）；它拦不住，因为放几条示范句是设计取舍，没有测试能替人决定。
  **落纪律**：新增《纪律·能自动拦住的不写成纪律》，判据＝提出新纪律时答不出"为什么结构拦不住"
  即为缺陷、登记到代码仓；承 2026-08-30 规则 1.9 正文"结构堵死不靠纪律禁止"，本次升为通则。
  **裁决二**——用户原话：*"这个标题是不是有问题……在模型看的时候能完全 get 到它是什么意思吗？
  还是需要结合规则才能理解？我觉得标题是不是应该简洁清晰一些，而不是用一些比较夸张的词语"*。
  **落名字四条判据**（入纪律文档§命名与引用）：单独读出来就知道要做什么／用动作词不用比喻口号／
  带上对象／不用内部行话。**据此改名 13 条**：正文冻结→日志正文不改、回改→新裁决要回改旧文档、
  口径只在一处成文→同一条规矩只写一处、描述闭环→不许只说不做什么、事项名→计划与对话用事项名、
  顺序不入名→顺序不写进名字、编号只作地址→编号只用来定位、执行者自提须标注→自己提的要标明不是裁决、
  原话逐字→裁决要带用户原话、版本行留对照→改口径要在版本行留对照、不为测量而测量→动工理由必须落在
  用户身上、待办不主动做→没有证据不主动做、失败响亮→拿不到就说没有不许填猜的值、
  按波及面取舍→按定错了会波及什么取舍、提交信息→提交信息用中文写清增量。
  **改名时机判据**：名字是主键，一经被引用即按《纪律·文件名不改》冻结；建档次日引用面只有 CLAUDE.md
  速查五行与两条裁决条目，此刻成本最低（同"标识符重命名时机"先例）。

- **规则按波及面取舍，两条纪律判据订正（2026-08-31 晚，用户裁决，纪律文档升 v1.1）**：用户原话——
  *"那如果一个规则我们定的不好，后面我们所有的开发都会受到影响，这才是核心问题。我觉得你关注错了
  关注点"*。**背景**：执行者拿"这只是一分钟的事"给一条管住所有未来文档的规则排序，用成本代替了波及面。
  **裁决三条**：①**新增《纪律·按定错了会波及什么取舍》**——做不做、怎么定，按"定错了会波及什么"判，不按工时判；
  判据＝理由里只有成本或整洁度即不成立。②**《纪律·同一条规矩只写一处》判据改判**——原判据"同一句话
  逐字出现在两份文档＝违规"作废，改为**援引可以、复述不可以**（援引＝名字＋一句话主旨＋指回权威处；
  复述＝出现改判时会跟着变的数值／字段名／路径／判据细节）；连带**冻结日志不在射程内**，
  原先打算加的"射程豁免"补丁随之取消——它是在给一条定歪的规则打补丁。**驳回理由**：原判据往严了
  会让每份跑法文档退化成指针迷宫，"读一份就能动手"是交接文档唯一还健康的部分，不能被规则毁掉。
  ③**《纪律·拍板后直接执行》判据改判**——原判据"拍过的事再问一遍＝违规"过严（按它算，同日那几次
  有价值的追问全属违规），改为**只禁空确认**（"这个方案可以吗／要不要继续"），
  **带着已完成部分来问剩下那一处合规**。**同轮另两条经重述后裁定（用户："按照这个方式优化吧，可以"），纪律文档升 v1.2**：
  ④**《纪律·拿不到就说没有，不许填猜的值》门槛定形并升格为用户裁决**——原表述"没把握就响亮失败"**没有门槛**，往严了读
  ＝整图打回，会把模糊一点的户型图全挡在门外（这条线的目的恰是让真人用上），往松了读＝低置信结果照传
  （业主拿到讲错自己家的报告）。**现形态三种结果写死**：全读出来即全用；主干读出来、个别项缺则收下并把
  那项标成"没有"，下游不讲它或坦白说缺；**只有主干都没有**（图幅／墙线闭合／房间可分，缺一即算）
  **才打回业主重拍**，且必须说明为什么不行、怎么重拍。**最严重的违规是产物里出现不是从输入得来的值**
  （默认值／猜测／低置信填充）——报告照常出得来但讲的是一套不存在的户型，无人察觉。
  ⑤**《纪律·没有证据不主动做》加边界**——禁的是**没有证据的加码**（往已经够用的东西上再加字、再加判据），
  **不禁已知会伤到业主的缺口**（那类缺口本身就是证据）。**成因**：原写法与《纪律·动工理由必须落在用户身上》叠加
  会变成"只修已经坏掉的，永不预防"，而这条线马上要放真人进来——典型例子是图解析失败时业主收不到任何
  说明，这件事等到"有数据"时，意味着已经有真人发了图、石沉大海。

- **文档按"回答哪个问题"分四类，协作纪律成文并命名（2026-08-31，用户裁决）**：用户原话——
  *"这个文档的纪律，我觉得有些零散，没有层次感，不成结构，并且有一些描述的话呢，不够清晰"*；
  *"文档内的内容的话呢，也没有类似于命名空间、命名规则"*；执行口径——*"进行修改吧，注意描述的结构
  应该是清晰且相对简洁的。我们的目标是，在工具去读取文档的时候，能明确的知道需要去怎么做，
  便于工具去理解这个规则的意义"*。
  **裁读三条**：①**文档按"回答哪个问题"分四类**——从哪儿开始读（CLAUDE.md）／现在怎样
  （《现在在哪儿.md》《仓库拓扑.md》，就地改不发追记）／应该怎样（`规范/`、`方案/`）／
  当时发生了什么（`交接/` 日志、`评审/`，写完冻结）；**一份文档只回答一个问题，兼两个就拆**。
  ②**协作纪律成文并给名字**——新建《规范/协作与文档纪律.md》v1.0，五组（文档／裁决／对话／做事／仓库）
  共 27 条，每条四件套（**名字·规则·判据·出处**），引用写《纪律·名字》，不复述规则全文。
  ③**CLAUDE.md 收成路由与速查**——每条一行并指回权威处，不再承载全文。
  **判定材料（真跑实测，非推理）**：《交接文档-2026-08-30.md》一份里 **9 处互相覆盖的状态更新**、
  追记编号撞号 4 次（两个"追记一/二/四"）；"数字不由 LLM 决定"复述在 **10 份**文档、
  "计划项用事项名"被逐字抄进 **4 份**；有编号地址的规范条文被引用 **504 次／74 条**，
  而文档纪律 9 条**无一有名字**、5 处引用全靠整句复述——**没有名字就只能复述，复述出来的副本不跟着改判走**。
  **连带四条**：①"现在怎样"此前只有《仓库拓扑.md》一个实例且未成通则，本次升为通则；
  ②进度状态从四份交接文档抽出、合并进《现在在哪儿.md》，日志自此不写状态、不自称入口
  （原"按文件名日期取最新"的接手规则同时作废——当时四个活跃入口里三个文件名无日期，规则已失效）；
  ③补齐 6 条此前只散在日志里、新 session 读不到的纪律：不为测量而测量、待办不主动做、失败响亮、
  执行者自提须标注、原话逐字、显式 add 路径；
  ④新纪律的中文事项名与报告域标识符前缀表（`art-`/`dom-`/`lkp-` 等）**显式分家**，不进规范 §14 注册表。

- **获客图上的情绪文案不走强制引用（2026-08-31，用户裁决）**：用户原话——*"不用跟批注一样，
  这里我们直接推导就可以了，这个是一个获客的工具，它不是严谨的报告数据，我们的东西只要在
  合理范围内就可以接受"*。**射程**：三张免费图上的**标题、情绪总结、专业小贴士**这一类文案，
  **不逐句要求声明引用、不因为没引就打回**——空间推理背书通道（裁决 2026-08-30）仍然管
  **报告正文与功能说明图的房间批注**，那两处是要被当依据用的，这一处不是。
  **仍然守住的两条（执行者收窄，同日）**：①**数字不许编**——"数字不由 LLM 决定"是全项目红线
  不是报告域专有，文案里出现的数字必须在事实清单里出现过，机检可判（数字串是可枚举的）；
  ②禁自造精度声明、禁施工与报价承诺。**事实清单照样递给写文案那一步当素材**——
  "直接推导"要有可推的东西，不给素材的"直接推导"就是编。
  **判据分界**：一句话该不该被拴住，看**它是不是要被人当依据用**——报告里的话业主会照着买东西、
  照着画线，获客图上的话是让他愿意点开。

- **业主开头只给两样：面积 + 户型图；其余一律推断，产出后摊开说、他要改再改（2026-08-31，用户裁决）**：
  用户原话——*"用户需要给我们东西只有两个，在最开始的时候，第一个是面积，第二个是户型图"*；
  *"基于户型的尺寸来进行推断人数、推断得房率……产出结果之后也告诉用户，我们现在是基于您的房屋面积，
  比如说 138 平方米，一般情况下会住 4~5 个人，我们现在按照 4 个人数做，然后得房率平均一般是 80。
  你如果要修改的话可以告诉我你家里的居住人口、得房率是什么，如果需要也可以告诉我你的小区名字……
  **如果用户填写就填写，不填写就不填写**"*。
  **这是形态改变不是少问两句**：从"问齐最小必要输入再做"改成"**先按推断做出来，把假设摊开说，
  他要改再改**"。**射程**：①开头的缺口只剩**面积**与**户型图**两项，家庭结构、核心诉求、
  不可接受项、小区名一概不问；②人数与得房率由**代码从面积推**（数字不由 LLM 决定），
  不问用户；③产出之后**如实说明按什么假设做的**，并给出改的入口——**给了就用，不给就算**，
  不追问不阻塞。（**口径澄清（2026-08-31 晚）：这里的"产出之后"＝三张图发到业主手里之后，
  不是两样输入齐了之后**——时点见同章《假设说明的时点＝图发到业主手里之后；发给业主的文案
  一律分条，不论谁写的》那条。）
  **收窄了同日两条**：「拿不到锚点只问得房率」——现在**连得房率也不问**了，直接按均值推，
  它降为"产出后可改项"；「问得房率同句给台阶」随之作废（问都不问了，无所谓台阶）。
  **与 2026-08-30「必须取点值处取下限 78%」的关系**：那条的理由（错误方向代价不对称——
  取大则业主按报告买的柜子装不进）**未被推翻**。现形态＝**对业主说 80%**（好懂的整数、
  也是用户给的口径），**进几何与报告数字仍取下限**；两者不同不是打架，一个是沟通口径、
  一个是计算口径。真要统一由用户再拍。

- **回话按思维停顿分条，断点由写的那一步自己标（2026-08-31，用户裁决）**：
  用户原话——*"人分条是因为有思维停顿，是否要先写完整段、再由另一个模型判断在哪断"*。
  **拍板＝写的那个模型自己标断点，不另起切分模型**。理由：**思维停顿是写的时候产生的，
  不是文本的属性**——写的一方知道"这句是回应、下句是另一件事"，停顿正从这儿来；整段写完再交给
  第二个模型，它只能从字面反推意图结构，信息更少，还多一次调用的延迟与失败面。
  **判据是"换一件事说就另起一条"**（回应是一件事、追问是另一件事），**字数只当兜底护栏**
  （一条超 60 字＝这条里塞了不止一件事），不是主判据——按长度切会把同一件事从中间断开，比不分更难读。
  **它标不好时的后续路径写死**（《纪律·不许只说不做什么》）：真机上出现"总是不分"或"在同一件事
  中间断开"时，再加一步切分——先用便宜的假设，被证伪了再上贵的。
  多条之间按上一条长度歇 0.4~1.5 秒，不模拟打字速度。**经过见《交接文档-用户发图到拿到报告.md》§二**。
  **本条落档补于 2026-08-31 晚**（此前只写在日志里，按《纪律·裁决必落档》补进本章）；
  它的射程后被同日晚那条扩到"发给业主的文案一律分条，不论谁写的"，见下条。

- **假设说明的时点＝图发到业主手里之后；发给业主的文案一律分条，不论谁写的（2026-08-31 晚，用户裁决）**：
  用户原话两条——
  ①*"应该说收到户型图之后就说是开始设计，设计完之后并且把图片发出来给用户以后才说我们这个设计是
  按照什么东西预估的，如果他想修改可以再进行修改。现在图没发出来就先说那个预估的东西了，这个是不对的。"*
  ②*"而且和客户说怎么做的那个内容不应该是一段很长的内容，这段也触发了之前设置的规则的问题，对吧？
  之前说过的，如果一段对话的话，我们应该拆成多个信息，一条一条发出去，而不是一起发出去。"*
  **成因是真机对话（当场纠正）**：业主发来户型图后，系统先回一条"收到，建筑面积 138㎡ 已记录……
  您方便说说家里常住几口人、主要生活需求吗"，**紧接着又发一整段长文**把 4 人、得房率 80%、
  "改善讲究"档连同改的入口一次性摊开。图一张都还没有。
  **第一件不是新规矩，是执行走样**：同章《业主开头只给两样：面积 + 户型图……》那条的用户原话本来
  就写着"**产出结果之后**也告诉用户"，而代码（`ishome-aipipe/services/chat-svc/src/chat/service.py`
  的 `_converse`）把它落成了"`missing_slots` 空了（＝面积与户型图两样齐了）就立刻说"。
  **本条把"产出后"钉死**：①收到户型图那一刻**只说开始设计**，不摊假设；②假设说明的时点＝
  **三张图已经发到业主手里之后**，在那时说按什么假设做的并给改的入口。
  **不变的那半**：给了就用、不给就算、不追问不阻塞、只说一次。
  **第二件是射程扩大**：「回话按思维停顿分条」（2026-08-31，用户裁决；经过见
  《交接文档-用户发图到拿到报告.md》§二与《交接文档-三张图出得来.md》§八）此前只管住了**模型写的
  那一格回复**，**系统自己写死的固定文案不在射程内**——真机那段长文正是系统写死的。
  本条把这条规矩扩到**所有发给业主的文案，不论出自模型还是出自代码**；断点判据与节奏照原条不变。
  **今天的处境（《纪律·不许只说不做什么》，不留悬空否定）**：图现在还送不到业主手里——
  《现在在哪儿.md》里「渠道出站发我们自己桶里的图」那一段未做，所以**假设说明今天没有调用方**，
  这一段文案现在不该发出去；**接线时点＝那一段接通时**。这与同章《业主开头只给两样……》落地时
  "确认闭环那套机件没删、时点挪到真有产出可确认时"是同一个先例。

- **服务之间只有数据通信：先定需要什么数据，开发测试期 mock 它，让这条服务独立闭环（2026-08-31 夜，用户裁决）**：
  用户原话——*"服务之间的话，其实应该是相对解耦的，只有数据的通信。那我们其实可以去构建一下我们需要
  什么数据，然后呢，在测开发测试的过程中呢，去 mock 这份数据，然后来生成我们自己 3D 的这一部分，
  让我们这东西的服务可以独立闭环。上下服务的话呢，是一个通信关系，不应该是紧耦合的。"*
  **成因**：三维这条线要动工时，它的上游（定稿平面 DeepDesign、三维资产库）**一样都不存在**，
  按"等上游"排，这条线永远开不了工。
  **拍板＝不等上游**：把"这一步需要什么数据"写成一份输入包契约，开发期喂拟真包，
  真派发时同一份包由上游填，**本仓代码一行不改**。
  **三条判据**（照这三条才算"独立闭环"，缺一条就是把耦合藏起来了）：
  ①**契约是服务的边界**——入参出参写死在本仓的模型里，不 import 上游任何内部模块
  （两个仓两种语言谁也不能 import 谁，几何那一族就是产出侧那份的**逐字对面**，对不上就是接不上头）；
  ②**拟真包要标明哪一块是真跑、哪一块是补的**——补出来的不许说成真跑样本
  （《纪律·只收真跑样本》《纪律·拿不到就说没有，不许填猜的值》）；
  ③**缺的那一块要能看得见**——上游没做出来的字段留空、失败响亮，不许在本仓编一个默认值填上。
  **本条不只管三维**：它是所有"上游还没有、本仓要先动工"的场合的通则；
  已有先例＝报告线的三档拟真上游包（卷子固定、跨天可比）。
  **首个落地见《交接文档-三维这条线出得来.md》**（同日夜，render3d 一轮做完）。

- **三维这条线按架构实装，力量都投它（2026-08-31 夜，用户裁决；排期改判）**：
  用户原话——*"我觉得我们可以考虑把 3D 绘图和 2D 绘图那部分做起来。"* 摊开两条路后选定
  **「按架构实装 render3d-svc（scene-compile + base-render）」**，并在排期上选定 **「力量都投 3D」**
  （另两段——会话侧上报业务侧、业务侧铸任务派发——本 session 因此撤下不做）。
  **2D 那半不归本条**：`render2d` / `imagegen` 的服务化（CLI 接进 activity、产物落桶、worker 起起来）
  同时由另一个 session 在做，本条只管三维。
  **摊开时如实说过的代价**：三维的上游（定稿平面、三维资产库）都不存在，架构文档 §16 待定项
  ⑥（GPU 自建 vs 外部 API）⑦（三维资产库来源）都没拍，做出来**短期喂不到业主手里**——
  用户在知道这些的前提下选的这条。做法随即由上一条裁决（服务只靠数据通信、mock 上游）解掉了"等上游"这个死结。
  **执行中产生、需要你过目的两个判断（《纪律·自己提的要标明不是裁决》，这两条不是裁决）**：
  ①**底渲这一步不需要 GPU 与三维引擎**——`base-render` 的产物是几何/深度/线稿/遮罩四路，
  它们是**给写实化那一步当条件图的**（`realism-pass` 在 imagegen 仓走生成模型），四路都是几何缓冲，
  纯 numpy 软光栅就能出，还能做到"同一份场景包渲两次逐字节相同"。**§16 ⑥ 卡的是写实化那一步，
  卡不住底渲**——架构文档 §三"GPU + 三维引擎重依赖"那句说的是本服务将来的伸缩轴，不是今天这一步的门槛。
  ②**尺子的锚点取外轮廓围合面积**（＝套内建筑面积的几何定义），不取外接框、也不取房间遮罩总面积——
  后者要乘一个"房间净面积占套内多少"的系数，那个数今天没有依据。
  两条的经过、真跑数字与仍未解的四件见《交接文档-三维这条线出得来.md》。

- **比率印给业主用百分数，走改源不动渲染层（2026-08-31，用户裁决）**：
  用户原话——*"那个改动我理解了，我觉得没问题，可以做。"*（先经一轮评估：用户前一日裁定
  *"先让另一个 session 评估这个做法合不合理，合理再做；不合理回来讨论"*。）
  **立案**＝整册真跑成品逐字印出裸小数：*"主卧窗帘按不低于 0.9 比率做"*、*"我们建议封闭收纳占比
  就用 0.8"*、*"悬挂 0.5、叠放 0.3、抽屉 0.2"*——正文十处出自六条落点，业主读不出。
  **做法**：值 ×100、补 `unit: "%"`，核验白名单撤「比率」加「%」。
  **改说法不改量**（同 2026-08-30「延米改米」乙案的处置）：0.8 就是 80%，数一个字没变。
  - **为什么不撞渲染层禁换算红线**（《交接文档-报告渲染启动.md》§三 第 3 条）：那条约束的是
    **渲染层不许自己造数字**（原文*"换算=渲染层自己造数字"*），本次渲染与成文的逻辑一行没动，
    改的是数据里那个值与那个单位。全仓「换算/折算」一律指*"从一个数造出源里没有的另一个数"*
    （规则 4.10e 原文），80% 与 0.8 是同一个数。**红线自己写着"百分比等表示问题遇到即上报待裁"**
    ——那次评估就是这次上报。**否决甲案**（数据声明 `display_form`、渲染层照它渲）：它要在渲染层
    拿 0.9 算出 90，是四条候选里**唯一真撞红线**的，且要同步三个真源。
  - **形态定 `%` 的判据（不是偏好）**：**表示形态必须无损覆盖该量在库里的全集，且不引入新的小数**。
    「成」在 0.15、0.03 上就破（只能造词或四舍五入，而四舍五入是改值）；`%` 换完全集恰好都是整数。
    两条旁证：系统里唯一已在用的比率形态就是百分数（contracts `floorAreaRatioPercent` 逐字
    *"取百分数而非小数……少一层换算就少一处会漂移的口径"*）；这几条的 `source` 自己写的也是百分数
    （60-30-10、二八原则、硬装≈50%）——**存成小数本就是编译时折算过一次**。
  - **改到了一条既有裁决的实施动作、裁决本身未动**：2026-08-29 晚「量纲必填入核验门」中
    *"真无量纲显式 `dimensionless: true` 豁免"* 一字未改；当时执行者把这六条判成"无单位是事实"，
    本次改判其分类——**无量纲不等于没有记数形态**（同为无量纲的 `lkp-accent-ratio` 写「倍」，
    印出「3–5 倍」就读得懂）。按《纪律·已定不重开》，动的是分类不是裁决。
  - **射程边界**：`lkp-budget-confidence-width`（`±比例`）**不在本条内**——它讲的是我们内部怎么定
    区间宽度，**本就不该给业主看**，属"该不该引"不属"怎么印"，**另立**；
    `lkp-storage-density-baseline`（`0.25–0.35 米/㎡`）真有量纲，本来就读得懂，不是这一类。
  - **同批的排版一条（执行者判断，非裁决，用户可推翻）**：符号类单位紧排（`80%` 不是 `80 %`），
    闭集登记、两仓各一处（渲染层与下发行），**射程只到 `%`**；`°` 同型但未动，
    **触发条件写死＝下次真有落点用 `°` 印进正文时一并改**。
  - **落**：release `budget@v14` / `material@v10` / `softdeco@v10` / `storage@v12`；
    三档考卷 `full` 两跑均一跑成册、六章零失败；**全书裸小数 10 处 → 4 处**，剩下 4 处即上面射程
    边界那两条。评估、执行与两处踩坑见《交接文档-比率怎么印给业主.md》追记一、追记二。

- **上游没有的那一段，按常规档位 mock 着先跑；产出上要看得出它是 mock（2026-08-31 夜，用户裁决）**：
  用户原话——*"高度的话，我觉得我们可以按照常规住宅的高度做一个 mock，然后先用来做测试，
  后续的话我们要求上游给发过来。"* 说的是三维的竖向那一维（层高、门高、窗台高）。
  **成因**：户型图上量不到高度——那是立面的事，平面图一个字都不说；上游给出这户实测高度之前，
  三维要么按常规档位起体，要么整条线停在这儿。
  **两半都要**：①按常规住宅档位 mock 着跑；②**触发条件写死＝楼盘资料或业主自己报出层高时，
  以它为准**（《纪律·不许只说不做什么》，不留"后续再看"）。
  **执行时加的一条（执行者提出，不是裁决）**：mock 与真值**要在产出上分得开**——场景包带
  `heights_source`，判据是**上游填包时写没写这一段、不看值**（值碰巧等于默认值不代表它是猜的）。
  口径照报告线那条既有的"draft 或已过期落点进正文，同页必须挂依据标注"。
  这条与本章上一条（服务只靠数据通信、mock 上游）是同一件事的两面：**mock 是为了不等上游，
  标注是为了别把 mock 当成真的**。

- **免费第三张风格图＝手账·写字版；图上要有注释（2026-09-01 上午，用户裁决）**：
  用户原话——*"手账·写字版，推荐写字板吧。然后注释呢？注释为什么没写啊？这个我没看明白，
  我理解应该是有注释才对的。注释注解。"*
  **两半**：①三版对比（奶油水彩/彩铅/手账写字/手账零字四张真机发飞书后）拍定**手账·写字版**；
  ②**注释要有**——执行者原本推荐"模型自造注释先关"（那一版四条注释是模型自己编的，两处箭头
  指错对象），被这半推翻的是"先关"，**没被推翻的是"不许模型自造"**：注释要有，且内容不能由
  模型临时编。
  **执行形态（执行者提出，不是裁决）**：注释**内容从户型事实与假设来**（同批注那条线的口径：
  每句引得到事实）、**位置用模型原生的归一化坐标钉在房间上**（出图模型评估实测 pro 支持
  `<bbox>`/`<point>`，房间质心现成）、**注释文本不含数字**——数字上图走叠印那条线（未建），
  不让模型画数字（红线"数字不由 LLM 决定"的精神，见《交接文档-三张图出得来.md》追记三）。

- **功能说明图走制图蓝图风；墙体表现要改、左右要留白、图上要补信息层（2026-09-01 下午，用户裁决）**：
  用户原话——*"我觉得制图蓝图那个效果好一点。然后墙体的表现的话，我觉得需要调整一下。墙体现在
  这种黑色粗线的这种质感不太好。然后包括左右留白，我们需要有，最好有些空间。给这张蓝图的话呢，
  一些其他的信息，让用户觉得这张图上是有一些内容的，能理解吗？这张图我们是要给用户的，对吧？"*
  三张质感样张（制图蓝图/精密制图/科技暗调）比对后拍定**制图蓝图**为功能说明图（免费第二张，
  "这是依据"那张）的方向，附三条改进要求：①**墙体不要实心粗带**（黑色粗线质感不好）；
  ②**版面左右要留白**；③**补信息层**，让业主觉得图上有内容。
  **执行形态（执行者理解，不是裁决）**：墙体改工程画法（双线描边＋墙芯淡填充，借精密制图那版的
  墙体处理放进蓝图配色）；信息层用确定性元素（标题栏、图例、房间占比小表），**数字全部走既有
  求值路径**，几何/朝向拿不到的元素（如指北针）宁缺不编；
  默认输出的切换时点＝改进版样张业主看过点头时，在那之前默认线稿不变。
  **执行时订正一处（2026-09-01 下午）**：执行形态初稿列了页脚"概念方案·尺寸以实测为准"，
  与既有裁决相撞——《方案/装修设计Agent架构方案.md》"其二"第 5 条已明令交付图不携带该落款
  （免责文案，交付物中不出现）。**旧裁决赢，页脚不画**；样张里已画的一版合并时去掉。

- **真户型（测试户型图）基准数据＝建筑面积 138 ㎡、得房率 80%（2026-09-02，用户拍板）**：
  用户原话——*"真户型的面积是138，得房率是80%。把这个数据记下来吧，我们用的这个泰斯的这个
  户型图就是基于这个数据来的。"*（执行者注："泰斯"按上下文读作"测试"，语音转写。）
  **套内＝138 × 80% ＝ 110.4 ㎡**（几何口径＝外轮廓围合面积，承本章"尺子的锚点取外轮廓
  围合面积"条）。
  **了结一件挂账**：三维线"最窄内墙洞按常规内门 0.80 m 反推约 114 ㎡、这个锚值得用户确认"
  （render3d `_iteration/run-2026-09-01-realism/run.md`）就此收口——反推偏小约 17%
  （套内 91.0 对 110.4），落在它自报的敏感度射程内（0.75 m→100 ㎡、0.90 m→144 ㎡）。
  **此后尺子一律以本条真值为准，门宽/墙厚锚降为交叉验证不作主锚**；render3d 入包值与
  各锚点相对关系按真值重算，归三维线，动那仓时带进去。
  **连带印证两处**：会话侧按面积推的默认得房率恰是 80%，与真值一致；138 ㎡ 档注释假设
  （4 人、改善讲究）继续成立。**红线不破**："比例尺不许由模型给"原样成立——真值由用户给，
  不是模型编的。

- **AI 生成标识先不打；法律问题统一收敛到最后交法务（2026-09-02，用户裁决）**：
  用户原话——*"AI生图的标我们先不打，我们最后直接在那个报告里面加一个说明就行了，包括用户的
  协议上我们加说明就行，这个最后交给法务的问题再处理，法律的问题我们都统一收敛到最后，先不做，
  这会不是关键。"*
  **处置**：《交接文档-AI生成标识合规评估.md》§六的十个拍板问题**整批冻结、不逐问拍**；
  替代形态＝报告内加说明＋用户协议加说明，处理路径＝交法务统一处理。评估里的调研结论
  （义务清单带条文出处、显式标识字高国标、火山 C2PA 实测）继续有效，届时作法务的输入。
  **执行者注（不是裁决）**：收敛时点原话是"最后"，未钉到事件名——按《纪律·描述逻辑要完整闭环》
  建议钉"正式对外投放前"，钉的时候回改本条。

- **生成图扩展名跟字节走（2026-09-02，用户裁决）**：用户原话——*"标签跟着内容走吧。"*
  （语境＝执行者摆了三个选项：①键随字节 ②键写死 `.png` ③落桶前转码成真 PNG；拍的是①，
  执行者推荐的③未采纳。）含义：风格图网关返回 JPEG 字节 → 键叫 `.jpg`，与 registry
  `uploads/{sha}/original.{ext}` 先例（扩展名由字节首部判定）合流；推翻现行"键写死 `.png`、
  Content-Type 按字节写"的处理。三维底渲深度/遮罩不受影响——那属"格式由我们定"（我们真写
  16 位 PNG 字节，名实本来就符）。**对象键前缀形态（`uploads/{sha}/` 同前缀那套执行者默认）
  仍未拍**，进 contracts 表等那半点头后一起。执行落点＝imagegen 写桶时按字节首部定扩展名。

- **生图尺寸先不降档（2026-09-02，用户裁决）**：用户原话——*"降档的事情先不降档了，
  后面的话我们再拍。"* 维持 1584x2816 档（单价 0.60 元/张，召回 97.2%）。**再拍时点未钉
  事件名**——执行者建议（不是裁决）钉"挑图门禁观察态跑出真实重画率后"，届时净节省
  （省一半 − 重画增量）有真数可算。显式像素尺寸与降档无关、已在用（长宽比 0.5625 钉死那条）。

- **禁词自检开关＝开，存量一起改（2026-09-02，用户裁决）**：用户原话——*"近词开关的话，
  这样的话我理解是可以开的了，对吧？"*（语境＝执行者澄清"开关只有一道、在 `verify_seeds.py`
  一处；三个域是存量违规不是三个开关"之后，用户确认开；"近词"为语音转写，读作"禁词"。）
  处置＝「派发数据不许含本域禁词」检查从测试夹具进 `verify_seeds.py` 正式核验；存量 6 处
  （ergonomics 净宽 3 / lighting 照度 2 / material 优异 1）改写、三域重发 release、
  相关 worker 重启。此前挂着的"现在一起改 vs 各域下次动工顺手改"就此收口＝现在一起改。

- **去掉修饰词（2026-09-03，用户裁决）**：用户原话——*"去掉矫情的修饰词，再说一下。"*、
  *"后续把这个去掉矫情的修饰记到记忆里，这是个重要的沟通策略。"*（语境＝执行者对控制图通路架构做
  反推批判，第一版夹自评与加重语；改成只留事实、判断、处置的版本后用户原样认可。）
  处置＝进《规范/协作与文档纪律.md》对话组《纪律·去掉修饰词》（v1.4），CLAUDE.md 速查行同步；
  判据＝删掉后意思不变的词就删，删的是包装不是内容。

- **渲染两档合为一档（2026-09-04，用户裁决）**：用户原话——*"两档位合成一档没有问题。"*（语境＝执行者说明
  preview/final 两档是为 Seedream 单价 0.60 元、两分半设的，万相 0.14 元、20–50 s 后动机消失。）
  处置＝《架构对齐》§3.1 两档段落作废留对照（V1.6），realism-pass 不再带档位参数，每张都过门禁。

- **门禁重推的顺序＝先列失效清单，再定检测器（2026-09-04，用户裁决）**：用户原话——*"我们先列失效清单，
  然后看眼情况，再考虑一下失效的检测器如何来做。"* 处置＝清单只收真跑样本（万相揭顶 6 张 + 室内机位补跑），
  检测器与阈值等清单出来再议；《门禁方案》升 v0.3 记此顺序。

- **陈设的有无、位置、尺寸算设计；样式、材质算观感（2026-09-04，用户裁决）**：用户原话——*"我觉得陈设算设计。
  陈设的尺寸也算设计。比如床的大小、桌子的大小。然后陈设的样式、材质算观感。"* 后果＝模型不得自行决定家具
  有无、位置、尺寸——这三样必须来自设计步骤并画进控制稿；模型只决定样式与材质。设计上游今日不存在，
  故近期写实化出**不带陈设**的图，模型自行摆的家具记为失效。与 9-01"家具 mock 不进生产路径"同向。

- **标注人＝用户自己，判"错没错"（2026-09-04，用户裁决）**：用户从执行者给的选项中选定，选项原文——*"你自己标"*
  （问法＝"标注集里 pass/fail 由谁来标？判'错没错'，对照物是派发数据与户型事实；'好不好看'按规则 4.17 只认转化信号，不进标注集"；
  备选＝业主标／分工：用户判错没错、业主判认不认／先不标只用代码尺子）。处置＝三图线失效清单由用户逐张标 pass/fail + 一句批评，
  样本＝业主已收真机图 + 三跑记录，只收真跑；检测器验收＝对该集真阳率/真阴率分开报，阈值等数据；样本集即《现在在哪儿》缺口表
  "改判据没回归立案样本""改 prompt 没跑两侧回归"两条的载体（回归集）；业主反馈只当信号回证不当真值。标注是迭代期人力，
  不随用户量增长，不撞 AI native（规则 4.19 判据）。《门禁方案》升 v0.4 记此；来路《评审/场景库与看板讨论-2026-09-04.md》。

- **看板＝自己迭代用，不做网页（2026-09-04，用户裁决）**：用户从执行者给的选项中选定，选项原文——*"自己迭代用"*
  （备选＝给外人展示（业主/合伙人/投资人）／先不做）。处置＝挂裁决⑨"先建计数载体"（已拍未做，至今没有任何地方在计数）；
  考卷跑完自动产出一页汇总进中控仓，替掉状态页里手抄的数字；放业务要的数（出册率/出图率、按户型×功能通过率、失效分布、
  判官一致率、跨 release 趋势），不放模型自评分与 1–5 分；判官一致率量出之前不展示通过率。升网页的触发事件＝出现第二个看的人。
  既有口径不变：验收台禁、只读观测许（规范 §11.4）。动工理由＝生图降档再拍时点与重出上限 K 两件都卡在"真实重画率"这个数上。
  来路同上。

## 一、★ 待拍板项：今晚采用的默认值（已全部拍板，见 §〇；本节保留作历史记录）

| # | 事项 | 今晚采用的默认 | 改判成本 |
|---|---|---|---|
| ① | 团队画像（8-15 人、Java+Python 双班底） | **假定成立**（全部基线文档的前提，骨架按 11 服务双语言搭） | 若不成立：语言收敛单栈，backend/aipipe 两仓合并重构——成本最高，**建议最先拍** |
| ② | Postgres vs MySQL | **Postgres**（文档倾向：PostGIS + JSONB；schema-per-service） | 骨架层面只涉及 Flyway 方言与依赖声明，当前无真实迁移脚本，改判成本≈0 |
| ④ | 网关：云托管 vs APISIX | **云托管网关**（阿里云 MSE / Higress，文档首选） | 骨架层面只出现在 infra 仓占位目录与 README，改判成本≈0 |
| — | `{code}` 公司/项目代号 | **`ishome`**（取自产品名"是我的家"，与本目录名一致） | 见下节"ishome 出现的全部位置"；当前无业务代码，全局 grep 替换即可，**越晚改成本越高**，建议明天定死 |

## 二、`{code}=ishome` 的全部落点（改代号时的 grep 清单）

- GitHub 仓名：`ishome`（中控/文档）、`ishome-contracts`、`ishome-backend`、`ishome-aipipe`、`ishome-frontend`、`ishome-infra`
- Java 包/Gradle group：`com.ishome`
- proto 包：`ishome.{domain}.v1`
- CloudEvents type 前缀：`com.ishome.{domain}.{entity}.{past-verb}`
- SDK 坐标：Maven `com.ishome:contracts-*`、npm `@tommax-bai/ishome-contracts`（GitHub Packages 强制 scope=账号名，与 code 解耦）、Python 包 `ishome-contracts`
- 渠道实例示例值：`feishu:ishome-prod`
- K8s 服务名：`ishome-{domain}-svc`（infra 仓占位）

## 三、执行层假设（非架构决策，但偏离了文档字面，需要知悉）

| 事项 | 文档口径 | 实际执行 | 原因 |
|---|---|---|---|
| 代码托管 | `gitlab.{code}.com` | **github.com/tommax-bai/ishome-\*（私有仓）** | 用户 2026-08-22 夜授权"访问 github 参考 aidcp"；aidcp 家族即此账号 |
| CI | GitLab CI | **GitHub Actions** | 随托管走 |
| 仓库布局 | monorepo 按语言域切 | 不变，但各仓在本机为 `~/codes/` 平级目录（沿用 aidcp 家族习惯），`~/codes/ishome` 为中控/文档仓 | 与既有工作方式一致 |
| SDK 分发 | CI 发布三语言 SDK | Java→GitHub Packages Maven；TS→GitHub Packages npm（scope `@tommax-bai`）；**Python→生成代码入库（gen/python），消费方用 git 依赖**（GitHub Packages 不支持 PyPI）；三语言生成代码全部提交进 contracts 仓，registry 未定也能立即消费 | 不阻塞落地；私有 PyPI 是否另建，后置 |
| dev 服务器 | — | **今晚未动**。dev=121.89.85.150（key `~/codes/dev-0722.pem`），机上跑着 aidcp 与 isales 生产服务；contracts/backend/aipipe 骨架不需要服务器。中间件部署（PG/Temporal/RocketMQ…）属 infra 阶段，等 ①②④ 拍板后规划，且需先确认与同机既有服务的端口/资源隔离 | 不在跑生产的机器上做无谓操作 |
| 本机工具链 | Java 21 / uv | 本机原装 Java 最高 19、无 uv、Gradle 5.6.4——落地时按需补装（brew），Gradle 用 wrapper 8.x + toolchain 自动下载 JDK 21 | 环境事实 |

## 四、明天拍板动作清单（按优先级）

1. ~~①团队画像~~ **已拍**：Claude 开发，架构不裁剪。
2. ~~`{code}` 代号~~ **已拍**：`ishome`。
3. ~~② Postgres~~ **已拍**：Postgres。
4. ~~④ 网关~~ **已拍**：云托管，部署平台=阿里云。
5. 顺带可拍（非阻塞）：③模板验收台首版范围、⑩文字合成两方案实测安排。
6. **过目落地新产生的 5 个小决策点**（模板 id 首定、channel_grade 字段等）：清单见《落地日志-2026-08-23.md》§三，有异议改哪条说哪条，趁 contracts 首个 tag 前。
