# 现在在哪儿

> **活文档** · v2.60 · 2026-09-11（**看板上了服务器，两个仓推了**。端点 `https://121.89.85.150:8106/`（自签证书 + 账号 `harness`，口令问执行者）。形态＝**nginx 托管静态快照，看板自己没有应用进程、不读任何仓、不连库，不成服务**；服务器上只加了一个 nginx 站点文件，同机其他站点一个字没动，reload 前后十二个 ishome 服务与别人的 aidcp×3／isales×4 全部 active。**要记住的一点＝服务器上那份是快照不是实时**：各仓状态文件不进版本库、只在开发机上，服务器读不到，页脚已写明是哪台机器什么时候生成的；状态变了要在开发机重跑 `./deploy.sh`。**踩了一个坑**：原打算放 `/opt/ishome/harness`，那个目录属主 ishome、其他用户没权限，nginx 进不去——表现是账号口令过了却 500，改放 `/var/www/ishome-harness` 解决。**另拍一条**：看板第二版的验收＝人连着一段时间真的靠它做判断、不再回头翻原始记录（第一版仍按信息契约那张表核对值）。**还差**：第一件真业务的改动还没定——执行者已给建议（把会话与生图两仓的接线代码同步到服务器，让真业主的调用归到正确的判断名下），等用户点头。）
> **活文档** · v2.59 · 2026-09-11（**看板第一版跑起来了**（用户"开工吧"）。`ishome-harness` 里一条命令 `uv run python build.py` 生成四个静态页——「现在怎么了」「证据」两屏 + 「谁负责什么」「架构入口」两个静态页；**不起服务、不依赖外部资源、离线能打开**，数据现读现拼不留副本。**第一份真数据是它自己**（建看板这件事本身），自举。**它替人看住三件事**，都有测试守着：①某个仓报的"手上是第几版说明"和派工记录对不上就点出来，已经报完成的明说"按规矩这不算最终完成"——规范那条硬规矩从此不靠人记；②测试跑的代码和当前代码不是同一版，点出"这个测试结果是旧代码的"；③状态文件校验不过（写完没记 commit、卡住没说卡在什么上、没卡住却留着旧值）一律按"取不回来"显示，**不显示猜的值**。空值按规范分三种显示，页面上没有"没接"这类混词。聚合连 worktree 一起扫（`git worktree list`），同一个仓多工作区读到两份状态就报冲突不挑一份；没有状态文件＝这个仓还没开工，是真实状态不是数据缺失。**分层按《开发规范与代码分层》单向**：models→repo（读盘跑 git 收口）→service（判断收口）→render→build.py。**16 条测试全过**，含用真 git worktree 造的两个场景。浏览器里看过第一页（深色模式正常、手机宽度有样式），其余三页用文字内容核对。两份数据格式各有文档（《一件事的记录格式.md》进版本库、《状态文件格式.md》不进版本库），规范里只留指路。**还差**：第一件真业务的改动没定是什么——看板的验收标准就是"跟第一件真事一起跑、挑三个时刻核对页面的值等于原始记录的值"，所以它现在还没验过。**下一件**：定第一件真事。三个仓的提交都没 push。）
> **活文档** · v2.58 · 2026-09-11（**各仓状态文件的格式定了，建看板的开工前依赖清掉**。GPT 先写了一版草案，审出三条必改，改完落 `ishome-harness/状态文件格式.md` v1（那个仓是使用方——聚合命令按它解析，格式契约就放那儿；规范里只留一行指路，不复述）。**最要紧的一条＝加"报这个状态时手上那份说明是第几版"**：不记这个，某个仓按旧版说明干完报完成，看板会显示成真完成，规范刚拍的"收到通知前干完的不算最终完成"就照不出来。另两条＝状态文件不进版本库（一天变好几次，进了会混进业务仓历史和代码评审）、聚合时要连 worktree 一起扫（你同一个仓开两个工作区是常态，只扫主目录会漏掉一半）。**还改了三处 GPT 的设计**：删掉"还没开始"这个状态（助手没启动就写不了文件，要别人代写就成了跨仓写，改成"文件存在＝已开工"）；卡住时不填升级编号（没有发号的地方、记录也没地方存，填了就是查不到的值，撞《纪律·拿不到就说没有》），改成直接写卡在什么上；本地测试加记"跑的是哪版代码"（否则跑完又改两行，看板一直显示通过）。**顺手照出一件**：GPT 草案把三条标成"用户拍过"，其中"记录单位＝仓 × 那件事"你没拍过——规范昨天刚立的出处制度，第一份下游文档就犯了它要防的事；已在格式契约文末按四类重标。**还差**：第一件真业务的改动还没定是什么。**下一件**：开建看板（占独立会话，开工前依赖已清）；或先定第一件真事。两个仓的提交都没 push。）
> **活文档** · v2.57 · 2026-09-11（**多仓流程的待裁项一次清空，看板单独建仓 `ishome-harness`**（用户*"单独建一个仓库。Git上单独建一个仓库。3条待拍和7件待改的采纳你的建议"*）。三条待裁：①后面几档归谁做——接通与否由跨仓只读的检查者宣布，运行版本与真跑由**新增的验证角色**取值与执行，**最终业务确认由人**；两条界限＝验证角色交证据不交结论、不许改代码（它够得到运行环境又能跨仓读，能改代码就会顺手改到跑通，这档就废了）。②看板放哪——**推翻了我的建议**（我建议第一版放中控仓），用户裁决从一开始就独立建仓：中控仓不含代码，看板页面化就带代码；和判官台也是两件事（看开发流程状态 vs 看生成图好不好）。③评审提的三条规则采纳，出处归位。七件待改一并改完（检查表加问清单交没交、验收那节删掉"尽可能"、统筹角色不得自己取值又自己宣布、分开架构性与技术性两种做不了、上线次数改计尝试次数含没成立那几次、`PENDING` 一词拆成"看过说再想"与"还没问到"两个、总规则逐条标出处）。规范升 **v0.6.0**，总规则 33→39 条。**这轮最值钱的发现＝三十九条里十七条标着"来源未追溯"**——它们在入仓前就在外部模型的初稿里，本仓没有裁决记录，已写进规范：不得因为写在那儿就当拍过，补上出处之前只能当默认做法用、不能当红线引用。新仓已建并推 GitHub（私有，只有 README：第一版两屏加两个静态页、每栏数据从哪来、验收判据）。仓库拓扑 v2.0，十二仓→十三仓。**还差**：各仓状态文件长什么样没定（不定格式聚合命令读不了，「每个仓走到哪一步」那栏就没数据），这是建看板开工前要先定的；第一件真业务的改动还没定是什么。**下一件**：定各仓状态文件格式 → 开建看板（占独立会话）；或先定第一件真事。经过在本次会话，未单独写日志。）
> **活文档** · v2.56 · 2026-09-11（**多仓怎么协作这套规矩定下来并进了仓**。外部模型起草了一套跨多仓开发流程，在本仓评审三轮（阻塞项两轮清空），用户拍了六条，落《规范/多仓开发流程与完成判据.md》v0.5.3，用户原话*"规范进仓库"*——**入仓后这份是唯一权威处，以后就地改这一份**。它管的是：一件要同时改几个仓的事，怎么从需求走到真正完成，谁能决定什么、谁能写哪里、什么时候必须停下来问人。**最值钱的一条＝"环境上真跑着"和"验过了"这两档从"有人觉得跑通了"改成"取得到值才算成立"**：运行版本必须从跑着的服务自己报出来（不是仓里的 commit），真跑必须留下编号、产物在哪、发给谁、谁确认、谁宣布，取不回来就是没成立——这正是本项目已经踩过的形态（服务器跑上一版镜像、常驻网关认旧逻辑名、走通的是 mock 那半，却已经宣布跑通、业主什么也没收到）。另外五条：等拍的问题要分"开工前必须确认／可以边做边确认"两段并按依赖顺序一条条问；口径一改就直接通知正在干活的会话、收到之前写的不算写完；建看板算独立一件事占一个会话；看板第一版按两屏加两个静态页（按视图布局、按那张十栏表供数）；同一个仓被两件事同时写不新增裁决、继续用 worktree 那套。**入仓时做了一件原稿没做的事**：开头加《哪些是拍过的》按来源分五组，三十三条总规则里**十六条是入仓前就存在、来源查不到的**，明确不得因为写在那儿就当拍过的。**还差**：三条等你拍——后面几档（接上了／真跑着／验过了）归谁做、看板的东西写在哪个仓、评审提的三条规则采纳否；七件文档要改（三十三条逐条标出处、"待定"一词三处三义、开工前检查表不问队列交没交等），列在那份规范文末。**下一件**：建看板这件事卡在"写哪个仓"上开不了工；第一件真业务的改动还没定是什么。经过在本次会话，未单独写日志。）
> **活文档** · v2.55 · 2026-09-11（**整条线在服务器上打通**：业主真实用系统产生的每一次模型调用，都会带着"这是哪一处 AI 判断"被记下来、判官台读得到（用户授权直接操作服务器）。装了三样：网关补丁、判官台（自动导入 + 登录跳转）、三处服务的接线代码（chat/genpipe-worker/imagegen 重启后近一分钟零报错）。**用服务器上的业务代码自己跑了一次**，判官台 API 读到 5 条、分到两处判断名下、对不上名字的为 0。**这天最值钱的发现＝出图本来是丢的**：用户问"适合也部署上去么"，唯一没验过的是生图那条，花两三毛钱真打一次——好消息是带标记没被厂商拒、真出了图；坏消息是 **Seedream 返回的是一条一天就过期的签名链接、不是图本身**，回调只认内嵌的图，那次只留下入图。当天修掉：遇到链接真去取回来，下载放后台线程（回调跑在网关事件循环上，一张图十几秒会把网关拖住），图落盘后再写记录；取不回来就留链接不留空文件。两条测试守着。**只有真跑能照出来**——本机那次图片验证是连接失败的，"厂商给的是链接不是图"这个事实一直没露面。**还差**：没有真业主跑过（今天的记录都是手动触发的，真实一次"发户型图→出三张图"会点亮五六处判断）；各仓已有的自动检查没导出来，页面上三处仍显示"—"。经过《交接/落地日志-2026-09-09-AI判断工作台.md》追记三。）
> **活文档** · v2.54 · 2026-09-11（**六仓全推 GitHub；服务器网关开始记每次调用、判官台新页面上线**（用户"推送，部署"）。**服务器验了四项**：网关活着、11 个模型名一个没少、真打一次成功、记录字段全对且来源标 ecs。服务器上的写操作与重启由用户执行（安全策略拦执行者那侧），故整套动作写成 `ishome-infra/litellm/deploy-ledger-to-dev.sh`，带验证与回滚。**当天照出并修掉三件**：①部署脚本报假失败——网关冷启 11 秒而脚本只等 6 秒，改成轮询健康检查；②浏览器打开新页面看到一行 JSON 报错——登录跳转只认首页，改用 Accept 判断（人看到登录框、代码拿到 401），并把"本来要去哪"带进登录页；③网关一直记但页面显示 0 条——中间的导入只能手动跑，改成打开总览时顺手导一次（幂等、失败不带塌页面，两条测试守着）。**还差**：会话与生图两仓的接线代码**没同步到服务器**，所以真实业主的调用现在会记成"对不上名字"；换生产运行代码风险大于只加网关配置，等网关这半稳一两天再说。报告线服务器上没部署不涉及。服务器网关配置比本机旧（写实图那条仍是 9-05 老路子），本次只加记录一行没碰它。经过《交接/落地日志-2026-09-09-AI判断工作台.md》追记二。）
> **活文档** · v2.53 · 2026-09-10（**页面上的行话全换成人话；纪律补一条：现成的专业术语与机器的值同罪**。用户看页面点名*"闭集这种词是自己造的吧，不利于阅读"*，又问*"模型回复怎么只有一个标记，没有内容呢"*。**两件都是同一个毛病**：把机器的词原样摆给人看。改法＝①行话逐条换（闭集→固定清单、要模型答的那份清单→只能从这五个里选一个、归一化框→位置按 0 到 1 的比例算不是像素、检测命中→抓到问题等约 160 处，页面/判断说明表/两份方案/视觉稿全改，改完全文扫为 0）；②**机器的值一律"中文在前、原值小字在后"**（五个意图分类给中文、规则名给中文、角色给中文）；③回复很短时页面写明"这处判断要模型回答的就是……，回复短是它本来的样子，没有截断"，**不伪造内容**。**纪律 v1.8**：判据从"是不是自己造的"改成**不看词的出身、只看读的人懂不懂**——数学词、编程词、行业黑话与自造词同等对待，枚举值直接摆出来也算没翻译。**顺手修一件**：地址认不出来时整屏空白（手输错、旧链接、页名改过都会撞上），现在退回第一页并改正地址栏。**判官台的自我介绍也改了**：从"生成图对不对"改成"每一处 AI 判断做得对不对"——图那两处只是 12 处之一，旧的是新的一个特例；两个入口两套数据并存、长期收敛。**架构问题答过一次**（用户问要不要新建仓）：不新建，判官台就是干这件事的，拆开等于把同一套标注与一致率写两遍。**部署状态没变**：六仓全部未 push、服务器上一条记录也没开始记。）
> **活文档** · v2.52 · 2026-09-09 晚（**常驻网关挂上调用记录并重启，业务代码打的第一条记录已经进判官台页面**（用户"挂上重启吧"）。做法＝**不改 config.yaml**，用 `run-with-ledger.sh 4000` 重起（那份配置将来要上服务器，永久带"挂回调"会在回调文件不在时起不来；回滚＝`run-dev.sh 4000` 一条命令）。旧进程已连跑 3 天 9 小时且当时零活跃连接，停它没打断在途的活。**验了四件**：11 个模型名一个没少、手动带标记的调用记到名下、**用真实业务代码（会话服务的意图判断）打通一次——这是这条线上第一条由业务代码自己产生的记录**、导进判官台后页面上两张卡显示出真实条数。**注意**：网关是手动起的、没做开机自启；磁盘按天滚仍未写；**服务器那边的网关没动、一条也没开始记**。经过《交接/落地日志-2026-09-09-AI判断工作台.md》追记一。）
> **活文档** · v2.51 · 2026-09-09（**AI 判断工作台落地：判官台页面做出来了、12 处判断全接上标记、调用记录整条验通；只差挂到常驻网关**。用户"落地吧"后四路并行：①**三仓五个客户端接线完成**——12 处 AI 判断每次调用都在 metadata 里报 `call_point` 与 `run_ref`（aipipe 7 处 284 测试、imagegen 2 处、reportgen 3 处 297 测试），名字必填无默认值、运行编号只取现成标识、只加标记不动提示词与业务逻辑；②**回调代码从照文档猜改成真跑验过**——本机另起网关打四次，照出三处猜错的（模型名与调用方标记各在哪个字段、调用方冒充不了模型名），常驻 :4000 全程没动；③**整条链跑通**——新脚本 `run-with-ledger.sh` 起 :4003 真打一次，落记录→导入判官台→查得到；脚本两处坑已修（中文标点吃掉变量名、网关只把配置所在目录加进搜索路径）；④**判官台新页 `callpoints.html` 五视图接真接口**，18 提交合进主干 85 测试全绿，问号说明整套做进去；**新文件独立成页是因为另两个 session 的分支都在改 index.html/store.py/handlers.py**，那三个文件一行没碰。**还差五件**：常驻网关没挂（挂前线上一条都不记）、各仓已有自动检查没导出（页面上三处显示"—"）、图片类没真出过图、AI 判官对文字这条线没接、磁盘按天滚没写。**方案五条待你决定按推荐当默认值走了**（v0.3 §六 各写了落地默认与改判成本，全做成开关），唯一与推荐不同的是判断名字表先放判官台不进契约仓。当天另拍一条**不用造词**（纪律 v1.7）。全程未 push、没碰别人的工作区、没用 stash。经过《交接/落地日志-2026-09-09-AI判断工作台.md》。）
> **活文档** · v2.52 · 2026-09-09 下午（**报告线：占比由算得不由搜得，造价章不再是瓶颈**。用户拍：占比＝金额÷总价、金额＝单价×量、三档＝三档单价×量；搜来的三条占比临时锚退役（规范 v2.13）。起因＝执行者翻来源后收回"占比类允许判断"的建议，那三条自陈口径不唯一。落地：业务侧退役四条（含置信宽度内部映射，它被印给了业主）、求值线派生水电占比 2–7% 与三档总价、造价域 v18；成文线造价章只讲三件事（眼下能算的钱 / 四项等平面 / 单价口径）、主张上限 3。**成的那册造价章三张卡，无搜来的占比、无内部参数、无套话**。四跑两成，造价章四跑四过；两次失败＝写桶代理断连（环境）、人体工学推导两轮重开都写「可能」。**未处置**：金额条目名该叫花费不叫单价、大数印元不印万（等拍）、推导重开不带上一稿。经过《评审/报告读后感三路与迭代倒推-2026-09-08.md》追记二。全部未 push（9-09 凌晨推过一次，此后新增未推）。本条只记报告线。）
> **活文档** · v2.51 · 2026-09-09（**检测规则清了个底：204 条里 27 条是假门；用户拍板退役 17 条，已发版**。起因＝9-08 撤掉业主对布局的确认后，执行者提出「撤门之前先得知道机检门有几道是真的」。**底数**（《评审/检测规则清单-哪些真在跑-2026-09-09.md》，每条给了管什么／反例／在不在跑）：生产链路 204 条，**真在跑 163、只记录不拦 9、标着启用却没有执行路径 27、已退役 5**；报告线 82（纸上 21）、出图线 48（纸上 2）、**布局线 4 条全是纸上的**、三维 70 条全在跑。**用户拍板两条**（全文《拍板清单》§〇）：①报告册那 14 条真空档 + 3 条重复声明的**先去掉**（原话——*不是明确不做了，是先把它去掉……以后如果需要的时候我们再补*；判据＝*后续要看实际例子的情况*，与《只收真跑样本》同向）；②**布局那 4 条要加上**（执行者已说明分量＝`plan-layout-solve` 是深度设计核心算法本身、几周量级，且与 `plan-rule-check` 是上下游；顺序未表态）。**已执行**：17 条标 retired 并六域全部重新发版——**budget@v17 / ergonomics@v13 / lighting@v16 / material@v13 / softdeco@v12 / storage@v14**（版本号未能写进提交信息，补记于此）；数据现状＝启用 7（3 条真带匹配式 + 4 条未表态的部分承接）、只记录不拦 4、已退役 20。**同批照出一件机制缺陷，用户已拍「修吧」、正在改**：**规则层根本不读状态字段**——退役只有判官层认，规则层照挑带匹配式的跑。今天没出事纯属侥幸（那 17 条本就无执行路径），但机制是坏的：**将来谁退役一条带匹配式的规则，表上写着已退役、它照样在拦**。这是「以为有门」的镜像——**以为拆了门，门还在**。**等你拍**：另 4 条「只兜住一半」的怎么办、布局那 4 条从哪条先动（执行者建议 `compliance-check` 先做，它射程独立于布局，且契约注册表今天写着「两条路径都强制、机检全覆盖」而实际一次都没跑过）。**顺带记一件**：本轮撞车第二次——另一个 session 用会扫走在途改动的方式提交，把本轮 7 份规则文件卷进他们的提交，已按《多 session 共用工作区》未推时改提交信息处置、双方署名都留。本条只记检测规则这条线。）
> v2.50 · 2026-09-09（**判官台扩版：三路设计稿落档、后端在独立分支做出一版、用词裁决落档**。用户指派三个子代理（交互设计师、视觉设计师、后端研发）对判官台"扩到管全部 12 处 AI 判断"各出一稿：交互稿＝主轴从"样本集"换成"AI 判断"，六条操作流（存为测试用例 / 把输入改坏 / 再跑 N 次 / 对一次回复判错没错 / 看一致率 / 从检查结果跳回代码），最难的是给 JSON 回复标问题，解法＝按字段路径 + 原文片段 + 一句问题，不存分类；视觉稿＝五页可点网页（链接在设计稿头部），模型回复最大最亮、结果只用 ✓✕—、三种来源用形状+边框+底纹区分、通过率默认藏着等你定；后端＝判官台仓独立工作区分支 `callpoint-workbench` 8 次提交 78 测试全绿（基本信息表 12 行 + 调用记录契约与导入 + 网关 callback 骨架 + 测试用例与四种改坏算子 + 再跑与格式/规则检查 + 一致率 JSON + API），未 push 未合，网关 callback 没在真网关跑过，各仓自动检查只留插件点。**同日用户裁决一条**：不用造词、说这东西是什么（视觉稿里"调用点/回文/真跑/扰动/留底"等全是执行者当天新起的，用户："现在这些造的名词对沟通产生了非常大的影响，效率变得很低"）——纪律 v1.7 新条、拍板清单 §〇 带替换词表、CLAUDE.md 速查加一行；方案、状态页、三份稿子已全部换词。**下一件等你定**：方案 §六 五条（汇总页上不上网页 / 调用记录截在网关还是各仓 / 只留服务器本机 / 改坏的输入算不算违反只收真跑 / 基本信息表放哪）+ 交互稿 G-2 九条 + 视觉稿五条；定了就能把网关 callback 挂上真网关开始记。设计稿《方案/判官台扩版设计稿.md》。）
> **活文档** · v2.49 · 2026-09-09（**大模型 AI 判断检测工作台：盘点做完、方案落档、待你决定**。用户 9-09 提"梳理系统中所有大模型判断是否合适的检测工作台"并给七步（盘点/描述/看输入输出/mock 正确与错误输入/预判/检测/展示）。五路子代理分仓盘出**12 处业务 AI 判断 + 判官台 1 个 AI 判官**（aipipe 7：判断业主这句话想干什么、从业主的话里抽事实写回复、看整张户型图找房间、逐间放大看细节、判定户型特征、每间房写一句怎么用、手账版的标题和贴士；reportgen 3：定这一章讲什么、写报告卡片含重写、报告 AI 判官；imagegen 2：母版画成风格图、线稿画成写实图；backend / render2d / render3d / reportrender 零模型），对照模型网关 11 个模型名（`floorplan-parse` 一个名字三处用）。**头号事实＝12 处都没有调用记录**：没有一处存发给模型的内容与模型回复，七步里唯独第三步"看输入输出"没有真东西可看，其余各步各线都有底子（报告线约 45 条确定性自动检查全拦、判官台骨架、考卷与测试数据）。**主张**＝在模型网关 callback 一处记调用记录（四个客户端各加"AI 判断名 + 运行编号"一个参数）→ 接进判官台按 AI 判断看 → 各仓自动检查导出成可单独跑的检测器 → 测试用例三种来源（线上真实记录/测试数据/改坏的输入）再跑 N 次做三种检查（格式/规则/人工判定）；不建第二个后台、不写标准答案逐字比。**待你决定五条**（方案 §六）：汇总页上不上网页（碰 9-04"看板不做网页"）、调用记录截模型网关还是各仓、调用记录只留服务器、改坏的输入算不算违反"只收真跑样本"、基本信息表放 contracts 还是判官台。顺手照出七件：判断业主意图那步答错了悄悄当成"提供信息"、抽事实那步转录数字无比对、房间批注那步禁数字没有自动检查、风格图出图后零判据、写实图保真度及格线未配、报告 AI 判官四条全只记录、判官台 AI 判官未接出图路径。方案《方案/大模型调用点检测工作台方案.md》v0.2（执行者提案、待你决定；9-09 按用户裁决换词）；底数《评审/大模型调用点盘点-2026-09-09.md》。）
> **活文档** · v2.48 · 2026-09-09 凌晨（**报告线续：星号、硬装总价进册、造价章成了瓶颈**。拍了：未校准＝正文数字后一个星号不解释、页脚依据块撤下（规范 v2.12）；先修册子再接线；ECS 授权（未用）。**业主问的"15 万还是 30 万"有了粗算路**：硬装行情单价（土巴兔/网易带 URL）进种子，110 ㎡ 一线 11–33 万，求值线零改动派生，六域 release 本机跳版、project-svc 重建重启。**钱进正文压了三步**：写作步判据放错层 → 推导步打回模型仍不挂 → 改成系统追加一条固定主张挂上金额条目，成的那册末卡写出「硬装全包…110000–330000 元…水电…6600–7500 元」。**成册率掉到一半，分母全在造价章**（新旧判据叠加、它是唯一挂 12 条参数还要下判断的章）。新照出：金额条目名该叫总价不叫单价（派生名规则）、大数印元不印万（单位在数据里声明，等拍）、记号被引到不相干意思上无判据。8 跑成 3。**下一件等你定**：造价章怎么减负（判据合并 / 重写轮数 / 拆卡）还是先接派报告让业主拿到册。经过《评审/报告读后感三路与迭代倒推-2026-09-08.md》追记一。）
> **活文档** · v2.47 · 2026-09-08（**报告线：读后感三路照出的问题当日改了四条口径 + 六件修 bug，考卷两跑两成**。起因＝用户要看报告产出与质量效果，三个只读子代理分别查质量检测怎么做、评易读性与价值感、代入第一次装修的普通人读一遍；三边一致＝每句像对的，业主读完不知道要花多少钱、不知道下一步、被"老房改造/下沉卫生间"吓到。**查实的根因多半不在模型**：喂给写手的东西不干净（推导示范句"你家阳台带家政位"被当事实写进三章、写手把户型特征 map 原样直译成"侧边户型"、考卷没同步金额条目、灯光题名照抄国标"走廊与楼梯间"）＋该拦的没拦（判官只记录不拦、标题正文复读只判整句相等、术语判据跑在记号替换前从未命中、脚注不扫禁词、禁词不跨章）。**四条裁决**（《拍板清单》§〇 末条，规范升 v2.11）：标题一句一个数按场景归组 · 术语解释放页脚一排 · 公式取整按数据逐条声明 · 行话全册扫含脚注。**四个子代理分四仓并行改完**（reportgen `82dbbc1`/`c6be02d`/`41c70d7`/`fd20977`、contracts `1da67a4`、reportrender `731ae8c`、backend `ac7cbd3`，全部未 push），两条 worker 重启到新代码并打开判官台账落盘。三轮真跑：前两轮各照出一件当场修掉（推导提示词把"数"引进推导步；册级行话扫命中考卷旧题名），第三轮 **2/2 成册**。新册对 9-07 册：编造事实 15 处→0、造价章第一次出现"元"、三页有「本页术语」、页脚行话 0、区间套区间 0。**新照出未处置**：11 条参数一卡（机检只管标题几个数）、"放弃…的做法"套话每卡一句、"起居 and 卧室"拉丁词无判据、收纳章抽象词与编造关系、判官台账只记命中数不记原句。**等你定**：三条未拍口径（"未校准"怎么说 / 册首摘要与册尾下一步 / 量缺时造价章怎么写）、查"全屋硬装单价带"能否进种子（总价可能不等布局求解）、六域 release 跳版（先 Flyway V8 再 import）、先修册还是先接派报告与链接回会话。全部经过与计数在《评审/报告读后感三路与迭代倒推-2026-09-08.md》。顺带：对话口径一条——仓内造词（"落点"一类）与标识符同罪，《纪律》v1.6。）
> **活文档** · v2.46 · 2026-09-08（**叠字白边改成自己造，已推已部署，等真机验**。承 v2.45 欠的三件之一——第一张情绪图的版面此前是 imagegen 模板里一句英文向模型讨的留白，**模型给多少是它的事**：9-07 两跑一跑三次全判不过整跑失败（业主一张图没收到）、一跑第三次才险过（7 分 26 秒里 7 分钟耗在"生图→判不过→重生"上，占 94%）。**处置**（用户裁决"把留白从求模型改成自己做"）＝生成侧模板去掉留白带（imagegen `0d05400`）、render2d 自己造版面（`3bad51f`）——画面**原样**贴进更高的纸（不缩放/不拉伸/不裁切），白边高度由文案按页宽排完的实测高度算出来，**100% 放得下、重试恒为 0**。**量出来的数**：纸色按**那一侧**边缘取中位（顶 249,248,242 / 底 247,244,236，差 7 级是画的落影，整张一个色不对）；纸纹镜像铺到接缝处、往外铺 4 倍段高后淡尽（纯色版放大三倍看得见"有颗粒"变"死平"；整条都铺会出周期 58px 竖条纹——两头都试过才定）。真跑样张两张（92㎡ 与 138㎡ 真几何，链路每段都真：母版→网关出画面→真文案→叠字），留 `~/.ishome/samples-2026-09-07-叠字白边自己造/`；顺手修一件＝标题从前一行画上去、长标题会顶出页边被裁。经过 render2d `_iteration/run-2026-09-07-caption-band-self-made/run.md`。**在做**：中文折行把词拆开（样张底部"种点绿/植刚刚好"）、三个执行侧 worker 补日志（9-07 全天零输出、失败无从复盘）。**等拍一件**：重启后可能重说一次"我这就为你做设计"——`design_start_told` 随重启丢，业主只说句"在吗"也会收到它，**而那一轮没有活派下去＝说假话**；执行者建议把触发判据从"两样齐那一轮"改成"业务侧这一轮真派了活"（`created_task_ids` 非空）。**记账两件**：`lifestyle-notebook` 模板仍写着留白要求（今天没有派发方用它，判据＝这张图会不会被叠字，没消费方就没判据，等它真被派上用场再拍）；genpipe 那个 `max_caption_retries` 循环按用户指示留着，但 render2d 不再抛版面错，`is_caption_layout_failure` 恒为假＝**它已是死代码**，留着只为"万一将来又有人把版面交给模型"，产物里 `caption_regenerations` 从此应恒为 0，下一跑真机验的就是这个数。本条只记获客线，不影响同版本行里别的线。）
> v2.46 · 2026-09-07（**报告线的缺口清单落档**——§三 末尾新增「离整册交付还差什么」一节，是对着《方案/装修报告AgentGraph设计说明》v0.2 与《规范/装修报告生成规则规范》逐条扫出来的差额，**不是新裁决、不改任何口径**。**同日一册真跑作证**：齐全档 fixture（58 条落点、0 缺口）走生产那条缝 `POST /api/v1/genpipe/reports`，75 秒、`verdict=ok`、**1/1 成册**，册在私有桶 `reports/01MOCKFULL01M1XJD3Q54V41TX/book.html`；五章各触发 1 轮章内重写、storage 一次过、整章重开 0 次；出口自检零 `lkp-` 泄漏、六章都挂齐了「本页依据」。**清单里三件是本轮新查实的**：①**里程碑派不出报告**——`on_enter` 动作类型只有 `CREATE_TASK`，而 `ReportDispatchAppService` 的唯一调用方是 REST 控制器，所以「自动派报告」不是没配置是**配不出来**；②**润色（`gen-polished`）没有载体**——它按规则 2.5/4.5 只作用于清单条目的 `rationale_display`，而清单类 art- 一条都还没做；③排版那件的名字叫 **pt- 页型库**——`Page.page_type` 字段留着恒为 `None`、装配层注释自陈「编译后接管」，渲染层一套 CSS 六章一个模子；与它绑在一起的页级/册级 cr- 判据一条未实现。**顺带订正一处旧说法**：v2.22 记的「138 母版外圈闭合 67% 被拦」已被 9-05 的几何修复覆盖（0.671→0.987），获客线现在的坎是 survey 认房间、不是闭合率；按《纪律·改口径要在版本行留对照》，历史版本行不回改，对照标在这儿。本条只记报告线，不影响同版本行里别的线。）
> v2.45 · 2026-09-07（**业主自己从飞书发图一路收到三张图，今天第一次成立**——17:04 业主重发户型图触发补派，17:12 三张图连同三句系统文案发到他飞书（`第一张：你家的样子` / `第二张：每间房怎么用` / `第三张：手账版`），artifacts 表 6 行、任务 COMPLETED；假设那五条按 8-31 裁决在图发完之后才说、没有提前漏出来。**这条线从 8-23 拆分落地算起，到今天走通。****同日修完四件、全部已推已部署**：①**重发就重跑**——两道门都拆（会话侧判据改成"这一轮业主又给了没有"、业务侧加"该出的图没出来就补派"四道闸 + Flyway V7），真机验证通过（`advanced=False` 但 `tasks` 非空＝补派接住）；②**两样齐那一轮不提问**——结构性堵死（那一轮不调编排模型），判据是 9-01 就写死的"真机再出现一次就做"，今天成立；③**那一轮只说一句**（原先 5 条收成"我这就为你做设计，请稍等。"），提示词补下压约束；④**已给的信息从业务侧取**（用户裁决"好，从业务侧取吧"）——`find_or_create_project` 回执带上已有槽位、算缺口前先还原，**不引 Redis**（那个 TODO 射程是会话态缓存，槽位真相本来就唯一在 `svc_project.slots`）。话术从一轮 9 条降到 2~3 条。**还欠三件**：**第三张图叠字放不下**（16:00 那跑栽在这儿、17:04 那跑过了＝临界靠运气，未根治，是现在挡在"每次都拿得到三张图"前面的唯一一件）；**三个执行侧 worker 全天零日志**（失败除 `generation_tasks.result` 无从复盘）；**重启后可能重说一次"我这就为你做设计"**（`design_start_told` 也随重启丢，执行者建议把触发判据改成"业务侧这一轮真派了活"，等拍）。经过《交接/交接文档-2026-09-07-重发就重跑.md》，裁决全文《拍板清单》§〇 末四条。本条只记获客线，不影响同版本行里别的线。）
> v2.45 · 2026-09-07（**家具资产表返工完成，两条裁决落到库里，全仓 178 测试绿**。契约仓 `ca64e8d`、backend 分支 `catalog-furniture-assets` `a06a553`，均**未推**、backend 那支**未合 main**。**毫米**：24 行值 ×1000 全整数（`Decimal` 逐个验、不走浮点），列类型 `double precision`→`integer`，字段 `width_mm`/`depth_mm`/`height_mm`；**§4.1 那条偏离是真消掉了**——checkstyle 的量纲规则认 `Mm` 后缀、连 warning 都不再有；§三 那行米制例外仍标作废留对照。来路栏里逐字引用的代码注释保留米制原文（引用不改写），表里写明免得看着像漏改。**卫浴拆开**：正规粒度＝`toilet`/`vanity`/`shower`；`bathroom-fixture` 词留着标退役（品类 21＝未退役 20＋退役 1），资产表那一行撤下不留行（25→24）——理由＝没有行是结构堵死，不依赖谁记得过滤标记位。**两条新不变量分两层落**：①退役品类零资产行**落在库里**（品类表恒等生成列 `is_retired` + `UNIQUE(category,is_retired)`，资产表恒 false 生成列，复合外键指过去；生成列谁都写不成 true，退役品类的行插不进来）——**白拿一条反向堵死：还留着资产行的品类退不了役，撤行必须先于退役**；②未退役品类均有行**落在灌库跑批**（PG 声明式约束表达不了"必须存在"，仓内零触发器先例）。灌库脚本因此多了唯一一条删行路径：契约标了退役就连同资产行一起撤并报出撤了几行；"常规档位的行不删"仍成立、例外只此一种且由契约授权。**迁移改 V1 不加 V2**：V1 从没在任何常驻库上跑过（正式 `svc_catalog` 至今没建），加 V2 等于留一份"先建米制列再改毫米"的假历史，而没有任何库经历过那一步。**新形态一处**：复合外键 + 生成列，仓内零先例（既有约束语汇只有列 CHECK、唯一键、部分唯一索引），已在真 PG 上实测三种走向再落进迁移。**仍未成立**：正式 `svc_catalog` 没灌（本机是多 session 共用库）、**布局求解仍没接这张表、零消费方**、24 行里 19 行无定源。**执行层假设新增一行**（§三）：`size_tier` 枚举取小写、偏离《开发规范》§2.1，射程只到这一个枚举。本条只记家具资产线，不影响同版本行里别的线。）
> v2.44 · 2026-09-07（**「重发不重跑」修完上线了，等真机验**。获客线免费闭环上业主唯一够得着的重试入口——生成失败后他重发户型图，系统原先什么都不做。**根因取证后订正成两道门**（9-06 追记一只说了一半、且把它记成槽位层不写，不成立）：**门一**在会话侧（chat-svc 判据是"槽位值变没变"，而户型图槽位的值就是内容寻址对象键 `uploads/{sha256}/original.png`，同一张图恒等于同一个值 → 一个 HTTP 都不打、一行日志都不留），挡的是失败话术里"过一会儿再发一遍"那句；**门二**在业务侧（project-svc 的 `on_enter` 挂在里程碑**迁移这条边**上，项目已在 M0.5 且三件产物一件没 PRESENTED → 无迁移 → 永不再派），**换一张全新的图照样不派**，挡的是"换一张更清楚的户型图"那句。两道门恰好各挡住系统亲口给业主的一条出路。**架构 §2.3 早写了失败路径「告知 + 改期」，只做了告知那半**——20:00:34 那两句话真发出去了，"改期"那半从来没有承载物；红线禁人工介入，重来只能由业主动作触发。**已改已推已部署**：chat-svc 判据改成"这一轮业主又给了没有"（`3dec3ba`，260 测试）、project-svc 加"该出的图没出来就补派"四道闸 + Flyway V7（`6982f20`，109 测试含真 PG）；服务器两个 unit 已重启、schema 6→7、十一个 unit 全 active 零重启、七个生产邻居未受影响。**契约一个字段没加**（`source_event_id` 本来就是幂等锚点，只是业务侧没用过），**不加任何运维重试接口**。**主要风险面＝幂等从隐式变显式**：原先靠"里程碑只迁一次"这个结构性事实兜着、任务表除主键外零约束，补派放开后改由三层显式幂等 + 在途唯一索引接住。**等你**：在飞书重发一次那张 138 ㎡ 的图——这一跑同时是"业主自己从飞书发图一路收到三张图"第一次有希望成立。**新照出的部署坑**：服务器上 chat-svc 的 `pyproject.toml` 被就地改过（contracts 指本地路径），整仓 rsync 会冲回 git 依赖；chat-svc 是 editable 装的，改 .py 只需 restart。经过《交接/交接文档-2026-09-07-重发就重跑.md》。本条只记获客线，不影响同版本行里别的线。）
> v2.43 · 2026-09-07（**用户又拍两条，家具资产表要返工一轮**。①**卫浴在资产库里拆开**（*"拆开卫浴的内容拆开摆"*）——正规粒度＝`toilet`/`vanity`/`shower` 三件，合并那个词按只增不改留着标退役；代价＝几何给不出给排水点位，摆位置有猜的成分，替代路径＝摆放仍归布局求解、依据不足时产出上标明不静默。②**量纲统一毫米，家具资产表不例外**（*"被都改成毫米吧"*）——**推翻执行者同日提的米制例外**（§三 那行已标作废留对照）；实质＝那份契约是草案零实现，没有协议可破。三字段改 `width_mm`/`depth_mm`/`height_mm`，整数毫米、值 ×1000。中控仓的契约草案已改（4 处），`heights.ceiling_height_m` 未动、留给三维线另说。**契约仓与 backend 两侧待返工**：品类闭集加退役标记、25 行值换算、DDL 与实体与灌种子脚本跟着改、测试跟着改。）
> v2.42 · 2026-09-07（**家具资产表建起来了**，契约到库两侧通了。契约仓新起两张登记表——家具品类词表（闭集 21 词，两处真数据去重、一个自造的都没有）与家具资产尺寸表（25 行种子＝21 品类，`bed` 与 `wardrobe` 各三档，其余只有 standard 一行，md 叙事权威 + json 投影照 anchor_items 分工）；backend estate-svc 在分支 `catalog-furniture-assets` 建了 `svc_catalog` 两张表 + 灌种子脚本（**读契约不复制契约**，25 行不进 DDL、改契约仓重跑即重灌）+ 只读查询端口，全仓 175 测试绿。**成立的事实**＝摆家具那一步"这个品类的家具多大"有地方回答了，且**归属域对了**——资产库落 catalog 域，不再硬编在渲染仓（这正是用户 9-07 推翻硬编码提案的理由）。**没成立的三件**：①正式 `svc_catalog` 没灌（本机是多 session 共用库，等 estate-svc 首次启动跑完 Flyway 再灌；灌库路径本身已在真 PG 上跑通）；②**布局求解还没接这张表，表今天零消费方**；③**25 行里 19 行无定源**（7 行是代码常量没写依据、12 行是测试夹具的拟真填充），来路栏如实存着、看得见。**新等拍一件**：资产库的卫浴粒度（合并一件 vs 拆三件），两个词都在闭集里、正规粒度没定，直接影响布局求解摆一件还是摆三件（全文《拍板清单》§〇）。**执行层假设一条**：这张表用米不用毫米，偏离《开发规范》§4.1，射程只到这张表（《拍板清单》§三）。顺带订正：estate-svc 数据源默认端口原写死 5432（首批脚手架遗留、连不上本项目的库），改成与其它服务一致的 15432。三仓提交全部**未推**、backend 那支**未合 main**。）
> v2.41 · 2026-09-07（**排期定了两件、一件在查**。①**业主重发同一张图就重跑**（用户裁决，全文《拍板清单》§〇 末条）——获客线上唯一一件真业主今天会撞上的缺口：生成失败后业主自己没有任何办法触发重试，把重发当成重试入口。**这是下一件，用户 9-07 指派给另一个 session 做**（本 session 不碰，避免撞车）。②**家具常规尺寸接线**（用户 9-07：*"家具可以按常规尺寸接线"*）——它是布局求解那一段，9-06「空房子图是内部中间产物」把"业主看到的第一张效果图＝带家具的那张"的时点钉在这一步之后，所以它是三维线对业主成立的前置。同批起 sub agent 查过"常规尺寸的家具样板和数据能不能进一步扩充"，结论＝**能扩但都没定源**：现有两处全是硬编码（三维仓代码常量 13 组、测试夹具 23 件 18 品类，后者已含三档床与三档衣柜但按房间名硬写、无"档"语义），**资产库域零表零迁移**；人体工程学域另有 12 项通道净距（床侧 ≥750、衣柜前 ≥900、主通道 ≥900 等）可补摆放约束，但整域 draft 未发版、源头待定。**用户 9-07 裁决先建资产表、不走硬编码**（推翻执行者路 A 提案留对照，全文《拍板清单》§〇 末条）；**前置未定＝档位给点值还是给区间**，它决定表结构，与收纳尺寸库"品类名对不上型号级尺寸"那件合并拍。**另照出一个断点**：人数推断只产出不消费——会话侧从面积算出"这房子一般住 4~5 人、我按 4 个人来安排"并**已发给业主**，但这几个字段出了会话侧就没了（不进槽位、不进契约、四仓 grep 零命中），摆家具那一步不知道这家住几个人；业主听见了承诺、后面没兑现，这条不在任何人的计划里。③**门洞当边界 + 房间身份那套改动**：用户在考虑用 sub agent 做，**尚未拍板动手**——执行者已说明它动三到四个仓、地基假设（洞的识别能否脱离房间标签先算）未验且无备选。④纪律侧同日两条：新增《纪律·汇报要说前因后果和系统位置》、《纪律·去掉修饰词》补"干练直接"（纪律 v1.5）。本条只记排期与纪律，不改任何线的技术状态。）
> v2.40 · 2026-09-06 深夜（**服务器上三张图出来了**：render2d 修复（`72bae7a`）部署后原样重放上一跑入参，`verdict: ok`、六件产物齐（母版/功能说明图 5 条注释/风格图/手账写字版文案重生成 1 次过关/几何/解析），约 5 分钟，两个 worker 零报错——**服务器上从几何到三张图整条生成链跑得通**。**照出第四件**：业主重发同一张图**系统不重跑**（项目已存在、槽位无增量，事实上报那步认为无变化），后果＝生成失败后业主自己没有任何办法触发重试，**这是获客线现在最要紧的一件**。**仍未成立**：业主自己从飞书发图一路收到三张图。经过《交接/交接文档-2026-09-06-免费闭环上服务器.md》追记一。）
> v2.39 · 2026-09-06 晚（**三维线换 session：接手先读《交接/交接文档-2026-09-06晚.md》正文 + 追记一**。用户又拍三条（全文《拍板清单》§〇 末三条）：①**门洞当漫延边界**（*"门洞也当边界，我觉得 OK"*）——房间漫延不再只按墙占比加权，已识别的门洞直接拦住；**要调执行顺序**（今天洞是根据房间标签算出来的，得反过来先算洞），这是整条改动的地基、走不通得另想；补种子留着但职责只剩管覆盖率，**不再靠它解卫生间**（实测是碰运气：那 50 格无名团是走廊+餐厅+客厅边缘连成的开放空间，种子丢哪儿决定成败）。三件改动分工至此清楚——**门洞解"吃走廊"、连通分量分团解"同名合并"、补种子解"无名无主"，缺一不可**。②**空房子效果图是内部中间产物、不交业主**（*"房子我理解是内部中间产物"*）——连带四件：「模型自己渲的开关插座算不算陈设」**不再需要用户拍**（验收标准执行者按"能不能看出几何画对了"定）；《方案/深度设计入口与数据契约》§六「设计存在前叫空间氛围图、挂说明文案交业主」那半**作废留对照**，连带作废 `NOTE_RENDER_SHELL`；**cream v3"不建议转正"的前提没了**（那条是按"要交业主所以标准要严"推的），接手要重看那份 run.md；替代路径写死＝**业主看到的第一张效果图是带家具的那张**，时点＝布局求解产出家具并进三维输入包之后。③**家具常规档位尺寸可以进生产**（*"家具常规尺寸可以进生产"*）——**推翻 2026-09-01「家具 mock 不进生产路径」留对照**；射程只到尺寸、**摆在哪儿仍归布局求解**（9-04「陈设的有无/位置/尺寸算设计」不变，模型仍不得自摆）；产出上必须标"常规档位不是选定单品"（照 `heightsSource` 先例）；已知待修＝摆场脚手架给每个阳台都塞洗衣机，而连着客厅的是生活阳台。同批用户对上游数据的口径＝**测试验收期可以 mock**、**基于户型推断**（8-31 已拍且会话侧已实现，只是没往三维线接线）、**上游要给由上游服务保证**。**类别清单粒度仍待拍**：用户问"为什么一定要限制"，执行者已答并落档——**开集的东西给人看、闭集的东西给代码用**，不限制则下游写不出规则／没法当场校验／同一间房两跑变两个词／半年后说不清；代价已兜住＝清单外退到不带类别是看得见的降级、清单按真数据长大。粒度本身未表态。本条只记三维线，不影响同版本行里别的线。）
>
> v2.38 · 2026-09-06 晚（**免费闭环上服务器 + 真机三跑，每跑照出一件**，接手读《交接/交接文档-2026-09-06-免费闭环上服务器.md》：dev 新增五个服务、ishome 服务十一个；真机整条走通了收图→拆富文本→建项目→填槽位→派发→六个活动→回流→告知业主，**唯独没出图**。三跑照出并修掉三件：①服务器 project-svc 落后一笔缺建项目入站口（已重建部署，顺带补了它没配的会话侧地址 19101）；②图文一起发被整条跳过，飞书富文本渠道不认（channel-svc `409556c` 已修已部署，富文本拆两条、派生 id 保幂等，【执行者提议·非裁决】）；③**头号结论＝跨仓字段新增没有端到端回归护栏**：9-05 上游给几何加了洞口类型三字段，render2d 严格模型不认，31 个校验错误把母版拦掉（render2d `72bae7a` 已修已推已部署，58 测试全绿）。**等你**：再发一次图跑第四跑验证能不能出图——这是这条线唯一还没消掉的那件。**新等拍**：母版要不要按洞口类型画门扇窗框（代码里写死的触发条件已成立，画法改判要拍）。）
> v2.37 · 2026-09-06 晚（**换 session：接手先读《交接/交接文档-2026-09-06晚.md》**。较 v2.36 新增三件——①**房间身份两组实测数回来了**（aipipe 分支 `room-identity-evidence`，5 次勘测）：中心点跨重跑最大挪 **663 千分位**、一份产物内同类两间最近隔 182 千分位＝**千分位一份产物内够用、跨重跑完全不够**（裁决"重跑算新一轮"接住了，带出硬约束＝下游不许拿上一轮身份查这一轮）；**5 轮里 3 轮报 9 间、2 轮报 8 间**（书房丢了、右侧那间被叫成玄关）；补种子原型把两卫分开 5/6、走廊独立 12 格、覆盖率 98.14%→99.45%，**但失败那次暴露是碰运气不是机制**（那 50 格无名团是走廊+餐厅+客厅边缘连成的开放空间，种子丢哪儿决定成败）——执行者提的替代方向＝**用已识别的门洞当漫延边界**（要调执行顺序，未拍）；次卧复核订正＝不是 4+6 两团是 **4/2/4 三团**，勘测框向上多罩了一间玄关、分团切得对。②**《方案/房间类别词表-选型材料.md》落档**（执行者提案未拍）：全仓 116 份产物按图源指纹判，**只跑过 2 套真户型、10 个中文写法**；推荐粗版八类（`bedroom` `living` `dining` `kitchen` `bathroom` `study` `balcony` `entry`，10/10 全覆盖），**不细分主次卧**——理由是家具尺寸不由房间类别定、细分今天一件东西也多拿不到，"哪一间"是排序不是种类。③**推送状态订正**：本 session 全程未 push，但 **9-06 15:37–15:39 有一次批量推送**（contracts/imagegen/aipipe/infra 四仓两分半内全推，应是别的 session 执行"全部推送"），把今天的提交一并推上去了；**render3d 36 个提交与本仓 3 个提交至今未推**，contracts v0.4.0 **tag 未打**。）
>
> v2.36 · 2026-09-06（**三维线：主链合了、生成侧端到端跑通、房间身份重新设计**。
> **端到端跑通**（aipipe 分支 `space-render-e2e` `70772cc`，2 张 0.28 元）：138 ㎡ 真户型输入包 → 派发 → 场景包 → 底渲五路 + 控制稿 → 写实化（万相经常驻 :4000）→ 阿里云私有桶 → 回调，**两跑两成、40 秒一次**。核对六件：桶里 9 个对象齐、8/8 键对模板（机位段整条键只出现 1 次）、回调对契约 49/49、同 seed 重跑七件逐字节相同（写实图像素相同但 PNG 字节差 334 B——万相 XMP 里的号，**拿 sha 判"是不是同一张图"对写实图会给错答案**）。**照出构件级失效一条**：右下外墙控制稿画了 3 处窗，出图整面白墙、3 处全丢。没验到：室内机位、门禁重派、单机位失败不拖垮整户、project-svc 真收。
> **主链四路**：contracts **v0.4.0** 合并发版（`c93bd65`，三维线三张表全部"已登记"，写实图键去重复段；tag 留给用户打）；imagegen 键模板与守门测试同改（`3993bad`）；aipipe 派发入口并入 main（`5c0f079`，259 测试）；backend `services/project-svc` 三维派发做完（分支 `space-render-dispatch` `8ac609f`，133 测试，**未合 main**）——设计上游缺的四样收在一条缝后面、痕迹落到每件产物的血缘上。**backend 照出一个真缺口**：`confirmArtifact`/`registerArtifact` 在生产代码里**没有调用方**，业主今天走不到"看效果图"那一步。
> **默认门窗画法已换**（render3d `79d0379`，228 测试，守门测试验过）：`diagonal-cross` → `frame-handle`，旧方案留作历史样本复现依据。
> **今日裁决六条**（全文带问法/选中项/备选在《拍板清单》§〇）：①正式走控制图通路（参考图通路作废留对照）；②:4000 切万相**已执行**，顺带把常驻网关从半个月前的旧配置重启到最新、认的逻辑名 **7→11**，**获客线"不认 floorplan-* 逻辑名、本机靠临时 4001"那条旧账消掉**；③写实图键去掉重复机位段；④**整图错位不算几何错、门禁按配准后的分判**（依据：65 张位移中位 1.4 px / 最大 17.7 px，人工五批一次都没记成错）；⑤换默认门窗画法；⑥**几何重跑当作新的一轮**。
> **房间身份重新设计（第⑦条裁决，多轮对话定型）**。起因＝室内效果图卡住，查出来的是更深的东西：**房间用中文名当身份**，业主真图上勘测认出 9 间、产物里只剩 7 间——两个卫生间、两个次卧被按名字合并；合并后"次卧"的中心点落在客厅里；门记录"通向卫生间"指代不明；图上没写名字的空间不进产物（漏多了整份几何被拒，报错"房间拼不满户型"）。**画出格子分区图之后**又照出一件：两个卫生间不是"两团被合并"，是**连成了一整片 26 格——中间那条走廊没名字、没种子，被它们的漫延吞了**（代码注释里记的"卫生间吃掉 8.7% 户型面积"那个老问题的残留形态）。**拍定：身份 / 区域 / 名字三样拆开**——身份＝`room-{类别}-{x}-{y}`（类别认不出退到少一层，是明写的降级）；类别＝**闭集**，模型只做受限映射不做自由翻译，清单外的计数、攒够真实数据再加行；名字＝中文原名另存、可空可重可改带来源标记。命名空间四条规矩：每层受控、词表进 contracts 只增不改、表外拒收、类别词必须单段。机位顺下来＝`cam-{视角类别}-{被拍对象}[-{取景变体}]`。**未定**：类别清单粒度（取决于家具布局要不要分主次卧）、取景变体位现在留不留。
> **顺带查实两件旧账**：①"我们没有设计能力"这个说法不成立——报告线有整套设计规则库，缺的是**把设计结论变成"这件家具多大、放哪个坐标"**那一环（布局求解），一行代码都没有；②家具库"没数据"也不准确——**常规档位尺寸表已经写了**（床/衣柜/沙发/餐桌/椅/橱柜/卫浴，各带依据），卡在 9-01"家具 mock 不进生产路径"那条旧顾虑上；真单品那层三条路（自建/采购/厂商）关键数字全标"未询"＝还没人去问过。**人数推断早已拍板且已实现**（会话侧从建筑面积算人数区间与采用值），只是没往三维线接线。
> **在跑**：房间类别词表选型材料（中控仓）、房间身份两个数实测（中心点跨重跑抖多少、"给无名空间自动补种子"能不能同时解掉卫生间/走廊/覆盖率三件）。
> **今日万相共 43 张 6.16 元。**）
>
> v2.35 · 2026-09-06（**免费闭环整条上了服务器**，按裁决"真机走 ECS、本机不起"，用户授权执行部署与重启：dev 新增五个服务 `ishome-genpipe-http`(:8104)/`ishome-genpipe-workflow`/`ishome-genpipe-worker`/`ishome-render2d`/`ishome-imagegen`，ishome 服务从六个变十一个；chat-svc 已用最新代码重启。**验证到的**：四条 Temporal 队列（genpipe-workflows / genpipe-activities / render2d-activities / imagegen-activities）poller 全部挂上、genpipe `/healthz` 通、五服务零重启、七个生产邻居全程 active、余量 8.0G 磁盘 4.4G 内存。**没验的**：真机整条还没跑过一次——业主在飞书发图到收图这一遍**等你发一条消息触发**；服务器 project-svc 的 jar 停在 9-02，backend 自那以后无新提交、未重装。五仓已推 GitHub（ishome/contracts/aipipe/infra/imagegen），render3d 在另一 session 手里没动。**坑落档**：服务器装 Python 依赖必须走阿里镜像，直连 pypi 官方 CDN 挂住不动、两个仓各卡 20 分钟零进展（《仓库拓扑.md》v1.9）。）
> v2.34 · 2026-09-06（**三条裁决**：①**门窗符号换默认**——原话*"门窗的画法换"*，射程＝控制稿不再用门斜线窗十字；`frame-sill` 与 `frame-handle` 门侧逐像素相同、本条未收窄到具体哪个，落档时 render3d 工作区（别的 session 在途未提交）默认已是 `frame-handle`，谁当默认由执行侧定不必回头再拍；已知例外＝揭顶宽窗十字仍占优、入户门机位站不出。②**错误分类由模型归纳、不由人工**——原话*"基于评判之后，通过模型来做，而不是人工来做"*，承接"标注当口不给分类选项"的下半截，判官台 rollup 按此形态建，归纳结果只线下用不做运行时答案表。③**真机方式＝走 ECS、本机不起**——原话*"地服务停掉吧，直接走ECS的"*；只射真机跑法，**不射本机开发用的 :4000/:4001/:4002**（有别的 session 在用，未停任何本机进程），待办＝服务器起三个 worker 与网关、chat/project 追平，systemctl 由用户执行。**「下一件」仍未定**——本轮用户答的是四件里三件的处置（换符号、分类交模型、真机走 ECS），**没说先做哪一件**；执行者建议 survey 房间识别（只有它在把错的东西送到业主眼前，且三件套的重跑 harness 正是它的验收工具），【执行者提议·非裁决】。**用户新问未答完**：改完之后能不能用判官结合人工批评来验——见本轮回答，要先量人机一致率才算数。）
> v2.33 · 2026-09-06（**三维线三条拍了、主链开工、五路证据一次收齐**。**裁决**（带问法与选中项在《拍板清单》§〇 末三条）：①**正式走控制图通路**（*"定下来，只走新办法"*）——写实图几何由控制稿定、模型只上材质与光，**参考图通路整条作废留对照**（成因＝同输入三跑出三个不同的家，与"图上是他家"这句承诺相撞）；②**:4000 切万相**（*"现在换"*）——**已执行**，且顺带把它从半个月前的旧配置重启到最新，认的逻辑名 **7→11**，`floorplan-*` 三个与 `image-judge.default` 现在都认，**获客线"常驻网关不认 floorplan-* 逻辑名、本机靠临时 4001"那条待办由此消掉**；③**写实图键去掉重复机位段**（*"现在删掉"*）——`realism-{camera_id}-{style_template_id}` → `realism-{style_template_id}`。**主链四路在跑**（contracts 两草案合并发版 / imagegen 键模板与守门测试同改 / aipipe 派发入口合并 / backend `services/project-svc` 加派发五件），各自分支、全部未 push。
> **五路证据一次收齐**（各自分支未合，全在 `_iteration/`）：④**揭顶符号证据补齐**（render3d `sketch-symbol-birdview`，13 调用 1.82 元；控制稿 sha 去重后判读 21 张 49 个门格）——`frame-sill`/`frame-handle` 残影 **0/49**、符号实体化 **0/49**、地面被符号切色 **0 处**，对 `diagonal-cross` 的 **21/49、12/49、5 处**；`frame-handle` 的把手在没被遮挡的三个洞长出 16/21 且无一例变差。**换默认符号的两半证据（室内 + 揭顶）现在齐了，建议换 `frame-handle`——待拍**。顺带照出 **modern-minimal"不摆陈设"在揭顶 seed 5/6/7 不听话（21 张里 9 张有残留，与符号无关，是 seed 的事）**——前几批只跑 seed 1/2/3 所以一直没照出来。⑤**尺子 v3 跨风格分布**（imagegen `metric-v3-style-distribution`，81 张离线、零花费）：**揭顶够了**——23 张跨五种风格全落在 0.3919–0.4508，错配最高 0.0588，间隙 +0.333、**间隙/极差 5.66**，风格与符号的子带互相盖住；**室内定不出**（间隙/极差 0.47，散布来自房间不是风格：主卧中位 0.6206 对书房 0.7950）；**最强一条＝v2 在室内 42 张上间隙已经为负（−0.075，正确与错配两群重叠），v3 原位 +0.232、配准 +0.169 仍为正**——这是 v3 该取代 v2 的实证。附带一条不用阈值的判据候选：正常行最优位移贴 ±24 搜索边界 0/81，错配行 23/81。⑥**检测器候选**（imagegen `detector-candidates-b4-b5-c2`，65 张真值表、零花费）：B4 **"洞内亮度"在室内窗上真阳 4/4、真阴 20/20、间隙 +126**，但四个正类同出一扇窗（书房），样本弱；B4 **残影检测器分不开且不必做**——换默认符号就没了；B5 **只有 1 个真阳，验不了**，先补样本；C2 室内真阳 0.667 / 真阴 1.000、揭顶分不开——**只能当看板计数器，当不了门禁**；**整图错位那条待拍现在有数了**：65 张位移中位 1.4 px、max 17.7 px（集中在主卧一台机位），**人工在五批 run.md 里一次都没把它记成几何错**，且位移与人工 B4 计数无关。**另照出一件**：judgebench 那个三维线 31 张样本集 **`labels` 是空的**——今天所谓"人工真值"是执行者当时的判读，不是 9-04 裁决说的"用户自己标"。⑦**小房间机位**（render3d `room-camera-pitch`，底渲扫描 + 真跑 4 张 0.56 元）：**俯仰不要单独进候选**——厨房站不出来不是"对着墙"，是**看不到自己的地板**（眼高 1.55 m + 张角 65° 下地板要 2.43 m 外才进画面，厨房退到底只有 2.8 m）；只放开俯仰+眼高时厨房 24480 格里在带内 11 格、且全要眼高 ≤1.20 m，玄关 0 格；手算下界 `D > 2.80/(tan(v−p)+tan(v+p))`——**俯仰只买 5 cm，张角每宽 10° 买 30–40 cm**。建议默认（眼高 1.55 m、平视）不变，**要加维就加张角**，在 {65,75,85} 里由候选评估自己挑。玄关**上游根本没有这间房**（地板被标成次卧），替代形态＝交给揭顶那张（已证 3/3 读成深色门板），或做"按洞口取景"的构件机位站到洞外侧（阈值今天定不了，样本只有一扇门）。真跑照出**新失效：玄关正对入户门那张，洞外没有几何、被模型补成一条通向里面的走廊，而尺子没抓住**（v3 0.4064 落在室内正常带 0.3958–0.8150 内）。⑧**cream v3**（render3d `realism-style-cream-v3`，5 张 0.70 元）：只删句首"奶油风格"四个字、材质与光的词一个不改，控制稿 sha 与 v2 逐字节相同——**v1（4 处）/v2（2 处）带出来的软装（绿植、织物、木平台、矮平台）五张里一件都没有**，剩下的是贴地色块与开关面板状小件；**不建议转正**（判据是所跑 seed 全零残留），且照出一条**口径缺口：开关插座这类算不算"陈设"没有裁决**——按《失效清单》C2 含则不干净、只按软装算则 5/5 干净，**这条不定，补多少张都说不清算不算干净**。另记一条：揭顶 seed3 整图带黑描边、把尺子 v2 分顶到 0.2490 冲出同稿基线带——又一个"分数高不等于几何好"的样本。
> **等拍新增三件**：开关插座这类算不算"陈设"（挡着 cream 转正与 C2 口径）；换默认符号方案（证据两半齐了，建议 `frame-handle`）；整图错位算不算几何错（现在有数了）。**9-06 万相共 41 张 5.88 元。** **待办**：中控仓 `.claude/worktrees/` 六个残留工作树（都无改动、提交都在 main 里）删除被权限拦下，未删。）
>
> v2.32 · 2026-09-06（**标注分类射程拍定**：用户裁决——*"对话册标注，也先去掉分类选项。分类选项，我觉得都在最后产出了知识之后再去分。标注的时候只关注于标注。"* 射程＝**一切标注当口，不分图侧对话侧**；《评审/stage2-mock-2026-09-05/》spec.md §4 与 annotation-shape.md 的"闭集 12 类、标注人只从里面选"作废，建对话标注台时以裁决为准、12 类只作归纳起点（评审件冻结不就地改，对照留在《拍板清单》该条下）。**连带待拍**：那份 spec 是"应该怎样"却归档在 `评审/`（"当时发生了什么"，写完不改），按《纪律·一份文档只回答一个问题》放错了类，要不要 `git mv` 进 `方案/` 变成可改的活规范——【执行者提议·非裁决】建议移。**等你定「下一件」**：survey 房间识别 / 三维线换门窗符号默认 / 迭代机制三件套 / 免费闭环剩两件，四选一后本页「下一件」段就地改。）
> v2.31 · 2026-09-06（**三维线换 session，接手先读《交接/交接文档-2026-09-06.md》**：最后两批合入 render3d main 5d85094——符号合成方案 15 张：门换掉斜线证据够（两批 33 张 0 残影对 10/33），`frame-handle` 对 `frame-sill` 不差，入户门机位在 1.2 m 进深玄关站不出，**换默认符号待拍**；奶油风 v2 去氛围词 3/5 零残留不转正。各仓 main：render3d 5d85094（228 测试）、imagegen f75c126（142）、infra 60bc46a、aipipe c0145df（另一 session 之后有提交）；未合草案分支：contracts 两支（dispatch 支要 rebase 到 c4b6b18）、aipipe `space-render-dispatch`、judgebench `render3d-label-set`。全部未 push。测试网关 :4003/:4004 已停，:4000 仍 Seedream。9-05 至 9-06 万相共 94 张 13.16 元）。
> v2.30 2026-09-06（**接手链补齐，无新进展**：把"C 人拉仓 → 交接文档 → 本页 → 拍板清单 → Stage-2 spec"这条读法接上——①《拍板清单》9-05 四条裁决原只有一条写来路，另三条补《交接/交接文档-2026-09-05-判官台与获客线闭环.md》；②该交接文档发**追记一**：§5 指的 `/private/tmp/.../stage2-mock/` 已失效，三份文件在 `评审/stage2-mock-2026-09-05/`（`311a721`，逐字节一致）；③本页尾部索引表补两行（这份交接文档、Stage-2 归档），此前按文件名搜不到归档。**等你拍**：对话侧标注集要不要跟"标注去掉分类选择、类事后归纳"那条裁决走——归档 spec §4 与 annotation-shape 仍写"闭集 12 类、标注人只从里面选"，图侧已改、对话侧没改（【执行者提议·非裁决】：按裁决字面该一起改）。**待办**：138 母版闭合证据图还在临时目录（9.2 M，会随会话清掉），按惯例应进 `ishome-aipipe/_iteration/<run-日期-事项>/`，未做。仓内旧账两笔（引用叫错名字，条目都在）：《纪律·正文冻结》真名《纪律·日志正文不改》、《纪律·描述逻辑要完整闭环》真名《纪律·不许只说不做什么》。）
> v2.29 · 2026-09-05 深夜（**三维线第四波**：①门窗符号形态实验（render3d 069c8a4，方案开关默认仍门斜线窗十字）：`frame-sill`（窗双框+窗台线、门门扇线）窗 6/6 门 11/12 斜线残影 0，十字方案窗 4/6——**B4 在符号形态上有解，换默认待拍**，《失效清单》追记三；合成方案 + 揭顶 + 入户门机位在跑；②两把尺子分数分布（render3d `run-2026-09-05-score-distribution/`，50 张 157 行）：n≥5 只有三个室内档，**揭顶全部 n<5 不能定线**；v2 分得开 Seedream 与送我们线稿的万相（差 0.068）、分不开万相自抽线稿；③门禁方案 v0.5（量尺限制③分数不能跨风格共用）；④《交接文档-控制图通路落地》追记一（第二三波，等拍十二件表）。今日万相 74 张、10.36 元）。
> v2.28 2026-09-05 深夜（**判官台演进 + 获客线母版闭合修掉**：①获客线几何闭合修复（aipipe `994e66c`）——墙厚上限改随图实测（`WALL_THICKNESS_STROKE_MULTIPLE`×中位墙宽）且拆"松界圈墙像素/紧界定墙线"两用 + `MAX_CELL_WALL_RATIO` 0.25→0.50，**138 母版外圈闭合 0.671→0.987、92 0.937→0.966、98 floorplan 测试全过**；138＝模块技术债预登记的第二批样本。**138 五张图全出得来**（母版/零字/手写/注释版/功能说明，同一母版；生活物件派发不在正式 workflow）。②判官台长成"标注+定位+对话+资产"底座并搬 ECS 公网（`ishome-judgebench` 7 笔未 push：登录+TLS `d2ba9a6`、拆分判官接网页 `1ea0c81`、对话标注 `e8b134c`、demo138 落库 `b4e9d16`、逐条定位 `04f3bd6`、点图为主 `5c066d4`、去分类改点+批评 `c4895e4`）：公网 `https://121.89.85.150:8105/`（自签 TLS+登录 bai，用户已放行安全组 8105），三集合 demo138/stage2-consult/demo，**本地 :8790 停用、一切上 ECS**；拆分判官两类 handler（check 错字 / judge 人脸涂鸦·编外物件），判官模型 `image-judge.default`（qwen3-vl-plus，本机 :4002）。③**下一个坎＝survey 房间识别**：几何修好了但 VLM 在彩色渲染图上认房间不准（138 实证：书房/儿童房/餐厅/两卫全错），下游图全跟着错——模型活，要改 survey 读图口径 + 攒真实户型样本。④**迭代机制议出骨架**（反馈→迭代→验证→资产）：错分层（代码错→回归测试真·不再错 / 模型错→回归门禁降错率）、样本集=回归集、check＝重跑同集看目标错消+不回归+总错率降且防刷指标、反馈资产化五样、**红线＝正确答案只线下用、绝不做运行时答案表（=人预设/运营后台）**；判官台待加三件（归类频次 rollup / 从批评抽正确答案 / 重跑对比错误率 harness）。stage2 咨询能力尚未建、stage2-consult 是用例定形。经过《交接/交接文档-2026-09-05-判官台与获客线闭环.md》。**等你**：修 survey 房间识别 · 迭代三件套建否 · 这批 commit push 否 · 物件派发进流水线否 · imagegen 命令行被别 session 弄坏修否 · 旧本地 5 条 demo 标注迁否）。
> v2.27 · 2026-09-05 深夜（**三维线第三波收口**：①render3d 读上游洞口类型、控制稿按类型画符号、基准换 16 洞产物（e48254a，206 测试）；②imagegen 门禁回执记尺子 v3 原位分/配准分/位移、判仍按 v2 下限未定，北欧模板转正（3e71e13，142 测试）；③三维线派发入口与回流草案两分支未合（aipipe `space-render-dispatch` e379469：`POST /space-renders`、回调装配，259 测试；contracts 同名分支 6d8775f：路径、九个产物词、artifact_type 建议名）——project-svc 要加五件，其中三维输入包谁填、修订号谁铸今天无数据源；④**16 洞控制稿 12 张真跑**（1.68 元）：主卧门窗类型分开了，书房窗十字 2/3 被读成柜格，门斜线 1/3 实体化，射灯小件 3/12——B4 剩的是符号形态，《失效清单》追记二。测试网关 :4003 已停。今日万相 56 张、7.84 元）。
> v2.26 2026-09-05 晚（**三维线第二波并行收口**：①尺子 v3（imagegen `fidelity_metric_v3.py`，0166a42）：分母改从线稿几何取走向后竖直墙线全进分母（室内 52%→100%），加 ±24 px 配准后室内正确配对最低 0.46 对错配最高 0.20、间隙/极差 0.105→1.079，可作室内门禁量分器候选；"非等比缩放造成错位"假设按数不成立；②本机放大器（render3d `run-2026-09-05-upscaler-check/`）：Real-ESRGAN x2plus 8 s/张、2336×1760、确定性、叠线墙门窗不动、逐像素差小于万相超分，建议作独立放大 activity；③换风格只换文字成立（`run-2026-09-05-realism-styles/`，5 张 0.70 元）：控制稿跨风格逐字节相同、叠线全压合；奶油风词带出 4 处小陈设不进模板；**尺子跨风格不可比，阈值不能按一种风格定**；④合并后清理（render3d 删两档与旧请求模型，a3ca3a4；imagegen README 键与机位 id 字符集待拍，119e83f）；⑤三维线 31 张样本按判官台格式做成集，留 `ishome-judgebench` 分支 `render3d-label-set`——**判官 qwen3-vl-plus 与万相同厂商，撞规则 4.17，要拍**；⑥《交接/交接文档-控制图通路落地.md》落档。测试网关 :4003 已停。⑦**洞口类型真值落地**（aipipe c9fc00c，几何侧确定性规则：门弧/跨洞平行线/外轮廓/入户门唯一）：真户型当前产物 16 洞 15 给类型、两图 31 洞 0 判错；**照出真户型基准存档 19 洞里 4 个是假洞**（994e66c 之前的墙线错位产物），render3d 侧读 kind 替代档位猜、基准换 16 洞产物两件待做。**等拍新增**：整图错位口径（v3 两个数都报）、判官厂商、放大 activity 落点）。
> v2.25 2026-09-05 下午（**三维线控制图通路整段接通、九个 agent 并行一日**：①render3d 第五路控制稿（不画地面分界线与天花交线、门斜线窗十字）+ 小房间取景候选评估（厨房响亮失败）+ 两个 activity 实装落桶，186 测试全绿（98358d2/d140380）；②imagegen realism-pass 从存根换实装（线稿控制通道、无陈设提示词、尺子搬入纯库、门禁只记录不判，e859aa6）；③infra LiteLLM custom handler 接万相线稿生图，同 seed 经网关与直连逐像素零差、缺 is_sketch 400（60bc46a；**:4000 未切，测试网关 :4003**）；④aipipe 三维工作流 scene-compile→base-render→逐机位 realism-pass 草案（6784faf 已合 aipipe main，无派发入口与回流）；⑤contracts 登记草案分支 render3d-realism-registry 未合；⑥**首次端到端 15 张**（5 机位×3 seed，2.10 元）：台阶 0/12、斜顶 0/12、灯槽 1/3、家具 0/15——构件级三条系统性失效靠改线稿消掉，《失效清单》追记一；⑦尺子透视自证＝现版不能直接当室内门禁（正确 vs 错配只差 0.023，6–22 px 整图错位），v3 在做；⑧万相超分不放大、送大线稿不放大，出图封 1168×880；⑨《方案/深度设计入口与数据契约》v0.1、《评审/自部署控制通道与跨视角一致性评估》落档。**下一件等你**：看追记一定检测器；拍键形态/入口形态/资产库来源/整图错位口径/厨房俯仰/机位 id 字符集）。
> v2.24 2026-09-05（**判官台建成并部署到 dev**：新仓 `ishome-judgebench`（FastAPI+SQLite+静态页，真跑样本逐张标注人判错没错 + 出图判官 `image-judge.default`＝qwen3-vl-plus 与出图 Seedream 不同厂商 + 人机一致率真阳/真阴分开报）已部署 dev `127.0.0.1:8105`（systemd `ishome-judgebench`，SSH 隧道访问、不开公网端口），网关加 image-judge 已重启生效、四生产邻居未受影响；首版 demo 五验证点（结构读图/结构跟母版/汉字/物件/批注），判官全跑、抓到的点与执行者记录对上（厨宫错字、人脸涂鸦、厨房次卧标反）。**内容缺口**：用户上传的 138 渲染图母版闭合率 67% 出不了三张图（获客线同一个坎），故 demo 只有解析层那张是 138、后四张用 92㎡，用户标注时发现此拼图不一致、待定统一到哪套户型。判官链路一课：喂它我们的房间表就盖章、让它对着原图独立数才抓错。入口《方案/挑图与保真度门禁方案.md》v0.4、记忆 judgebench-service。）
> **活文档** · v2.23 · 2026-09-04 下午（**验收形态讨论收口**：对照 hamel.dev《LLM-as-a-Judge》扫仓——文章七步本仓已建成大半（报告线判官层/反例库/种子集回归/观察态，三维线 9-04"先列失效清单再定检测器"就是这套方法），缺三块＝三图线失效清单与逐张标注、检测器/判官对人标的一致率（两线都没量过，报告线判官只在事故里被校过）、异常物件判官。**两件拍了**（从选项选定，带问法与备选在《拍板清单》§〇 末两条）：标注人＝用户自己判"错没错"；看板＝自己迭代用、挂裁决⑨计数载体、不做网页、升网页事件＝出现第二个看的人。**三图线下一件＝用户逐张标失效清单**（样本＝业主已收真机图 + 三跑记录）；代码侧标注集/判官/计数载体另起，触发事件＝标完清单或拍"做计数载体"。入口《评审/场景库与看板讨论-2026-09-04.md》；《方案/挑图与保真度门禁方案.md》升 v0.4。**同日晚：30 个案例的标注台已发**——claude.ai 私有 artifact「三图线标注台」https://claude.ai/code/artifact/cc94b08b-2865-4f48-8faa-0fa60baf82dd （图内嵌、标注存 artifact db、只判错没错）；30 张全是真跑产物（风格图 18 / 功能说明图 3 / 母版 1 / 蓝图样张 3 / 模型评估 5），原图 + 派发数据从三个 session 的 /private/tmp 归档到 ishome-imagegen `_iteration/run-2026-09-04-label-set-30/`（`1d8ff31`，未 push）。**户型只有 92 ㎡ 样例一套**——业主真图至今没出过一张三图成品。**等你标**；标完读回落 `labels.json`。）
> v2.22 · 2026-09-04 中午（**串联做完了：免费闭环三张图那条腿本机真派发走通**——会话侧 → 业务侧 → 编排 → 三张图 → 回业务侧 → 会话侧，八跑一成、前七跑各照出一件并当场修掉。**成立的事实**（本机，假会话侧收图）：填两样槽位（户型图对象键 + 建筑面积）→ 业务侧自动迁 M0.5、铸任务、派编排 → 勘测/几何/事实/批注/文案/母版/两张风格图/叠字 → 结果按回调回业务侧 → 六件产物登记、outbox 事件、中继 4 秒内到会话侧 → 三张图按顺序送达、置 PRESENTED、项目迁 M1，填槽位到收图 169 秒。契约 contracts **v0.3.0**：design.v1 `PresentDeliverables`、新 `project.v1`、genpipe.v1 `/floorplan-visuals`、activity #15–#19。**五仓全部已推（9-05 用户拍“全部推送”）**，aipipe 的 uv.lock 已重锁到远端 tag。**七跑照出并修掉的**：常驻网关不认 floorplan-* 逻辑名（另起 4001）；uv 父子进程 kill 不净；勘测框回千分网格（换算）；注释递了没物件清单被拒（注释开关默认关，清单生成步落地再开）；叠字放不下（重生成底图再叠 ≤2）；文案贴士超字（带上一稿与原因重写 ≤2）。**真跑样本一条**：业主 9-01 真机传的 138 ㎡ 户型图（图上无房间名）勘测只认六间、母版外圈闭合 67% 被拦——获客线的题，不是接线的。**没做的**：第三张的注释与物件清单、真机（本机起渠道会抢服务器连接，等你定：上服务器还是本机短起）、报告那半（解析产物已进桶、回流带 layout_features，报告派发未接）。**等你**：常驻网关 :4000 重启认新逻辑名、对象键派生物进表、真机怎么跑。经过与判据《交接/交接文档-用户发图到拿到报告.md》追记六。）
> v2.21 · 2026-09-04（**三维线三条拍了 + 失效清单出来了**：①渲染两档合一档（架构对齐 V1.6 §3.1 留对照）；②门禁重推顺序＝先列失效清单再定检测器；③陈设有无/位置/尺寸算设计、样式/材质算观感（门禁方案 v0.3 §七，三条带原话《拍板清单》§〇 末）。**失效清单《评审/失效清单-控制图通路-2026-09-04.md》**：16 张万相真跑 + 7 台室内机位——户型级失效 0/16；构件级四条系统性且来源在线稿画法（地面分界线成台阶、天花线成灯槽、透视线成斜顶、门窗类型错）；陈设默认必摆、"不摆"基本听话；取景两条在模型之前（小房间机位对墙）；尺子在透视视角未自证。**下一件等你看清单定检测器**；执行者建议先改线稿画法再重跑同批样本。万相今日 10 发 10 成 1.40 元）。
> v2.20 2026-09-02 部署、2026-09-03 晚收口（**服务器追平到 main 落成（用户拍"部署吧"）**：①channel-svc 新 jar 上线（渠道出站发桶图那版），飞书长连接已重连——真机发图从此可直接走服务器，不用本机短起抢连接；②chat-svc 四文件更新并重启（假设挪到 deliverables_delivered、系统文案分条），:19101 正常；③project-svc 新 jar 上线（重启那步被本机权限分类器拦、由用户 9-03 晚亲手执行）：**Flyway 四笔迁移全过（svc_project v1→v5，rulebook 建表）**，随后隧道对服务器库 import_seeds（131 条、calibrated 16）+ publish_release 六域全发＝服务器库自己的 `*@v1`（内容＝main 当前快照，含禁词洗净数据）。**欠一小步**：服务器 project-svc 起在发布之前，报告线在服务器真用之前再重启它一次（按"重启否则读旧 release"先例）。生产邻居 aidcp×3/isales×4 全程 active；12/12 服务 active 终验过。旧 jar/旧 .py 均留 .bak.20260902*，回滚一句 install + restart。绘图/报告各 worker 服务器上本就没有、无派发方，未部署（另拍）。**遗留小账**：chat 四个 .py 属主变 501（644 可读不阻塞）、chown 被分类器拦，顺手时 `chown ishome:ishome` 归位）。
> v2.19 同日中午（**控制图通路调研收官＝有，两条真控制通道都验出来了**——百炼万相线稿生图（`wanx2.1-imageedit` doodle，凭证已在网关、0.14 元/张、18–50 s）三判据全过：同 seed 逐像素 100% 相同、线稿转 90° 出图跟着转、三 seed 极差 **0.0040** 对噪声底 0.1718、叠线看图同一套房；本机 SD1.5+ControlNet（depth+mlsd 双通道）同三条也过（极差 0.0136），证机制不证画质。火山方舟 Seedream 官方参数表 `image`＝"参考图片"、无结构控制参数、连 seed 都不在表里；火山视觉智能图生图组只有指令编辑/涂抹/人像/角色保持。**建议（执行者判断，未拍）＝拆分走"换到真控制图通路"，近路万相 doodle 接进 realism-pass，自部署留第二形态；Loop 门禁不撤**。评估记录《评审/控制图通路调研-2026-09-02.md》，证据 render3d `_iteration/run-2026-09-02-control-path-survey/`）。
> v2.18 同日中午（**禁词自检开关开完了**：「数据面不许含本域禁词」进 `verify_seeds.py` 正式核验（扫描面照立案扫法；judgment_style 的 bad 示范刻意不在面内），黑盒正反例六条全绿；存量 6 处照"改说法不改义"改写（通道宽度是否够用／重点照明比环境亮几倍／身份句去「优异」）；**三域重发 release＝ergonomics@v11 / lighting@v14 / material@v11**，新快照按立案扫法复扫零命中；project-svc 已重启读新 release（worker 代码未动无需重启）；考卷夹具同步三处、reportgen 227 测试全过。落点名「重点照明照度倍数」不在裁决射程、照旧（prompt 逐行点名兜着）。提交＝backend 3e7e80b、reportgen acdd9e8，未 push。**整册首跑失败的头号来源就此从源头掐掉**。**同批：扩展名裁决也执行完**——imagegen 键模板改 `.{ext}` 按字节判（`b0b8d1e`，73 测试与 ruff/mypy/import-linter 全绿），三件 9-02 拍的只剩"降档"无事可做（本来就是维持现状））。
> v2.17 同日上午（**一口气拍了三件**：①**扩展名跟字节走**——风格图键改叫 `.jpg`，与 registry"扩展名由字节判"先例合流，执行落点 imagegen 写桶处；对象键前缀形态那半仍未拍。②**生图尺寸先不降档**——维持 0.60 元/张与召回 97.2%，再拍时点未钉（执行者建议钉"门禁跑出真实重画率后"）。③**禁词自检开关开、存量一起改**——检查进 `verify_seeds.py`，存量 6 处改写、三域重发 release。三条带原话《拍板清单》§〇 末三条；③动工中）。
> v2.16 同日上午（**AI 生成标识拍了＝先不打**——替代形态＝报告内说明＋用户协议说明，法律问题统一收敛到最后交法务；合规十问整批冻结、评估文档降为届时法务输入。裁决带原话《拍板清单》§〇 末条；收敛时点"最后"未钉事件名，执行者建议钉"正式对外投放前"。同批用户口头定分工：三维写实化 Loop/换接口归三维线那个 session，本线不动；挑图门禁进入"怎么做、怎么保证效果"讨论，未动工）。
> v2.15 同日上午（**三维线交接给下一个 session：控制图通路调研**——根因定在接口层（参考图≠控制图），Loop 是过渡不是终态，调研回答"当前可达生态里有没有真控制通道的接口"。交接文档《控制图通路调研》含基准数据（真户型 138 ㎡，已进 render3d `_iteration/真户型-基准/`）、可观测判据、候选起点与射程。同批：真户型按真值重算收口，0.951 不变、内门实际 0.86 m）。
> v2.14 同日（**真户型基准数据拍了＝建筑面积 138 ㎡、得房率 80% → 套内 110.4 ㎡**——v2.5 挂起的尺子锚收口：门宽反推 114 ㎡ 偏小约 17%、落在自报敏感度射程内，此后尺子以真值为准、门宽/墙厚锚降为交叉验证，render3d 入包值重算归三维线；裁决带原话在《拍板清单》§〇 末条。**同批：门禁方案升 v0.2**——并入三维线 9-02 两条：量尺混杂实测（底渲自量自家线稿只得 0.0607＝分数只能同输出形态横向比，**阈值不许按裸壳定**）与"根因在接口层、Loop 是过渡形态不是终态"前提（拆流程还是走 Loop 在议未拍；证据 render3d `_iteration/run-2026-09-02-metric-confound/`）。噪声底 0.1718 改判为尺子噪声不纯是模型噪声）。
> v2.13 同日深夜（2026-09-01）（**挑图与保真度门禁立项落档**＝《方案/挑图与保真度门禁方案.md》v0.1 草案（执行者提案，通篇非裁决）：形态收口"生成→量分→不过线重出"——"记 seed 可复现"那半已被三维线实测排除（同 seed 两跑 72.7% 像素不同，render3d `_iteration/run-2026-09-01-variance-knobs/`）；两把尺子配两级失效（三图侧物件级＝清单核对＋异常物件检出／三维侧户型级＝结构边重合度），互不借用；阈值一个都没定（唯一已量事实＝噪声底 0.1718，《纪律·阈值有数据才定》）。**待拍板三件在方案 §六**：房间功能核对进不进门禁（即 v2.2 攒下那批的③）、重出上限 K 与预算（与生图降档联动）、门禁物理落点（若按建议放需回改《架构对齐》表））。
> v2.12 同日深夜（**方差压不下去，三维这条线的门禁形态定了**＝生成→量分→不过线重出／转兜底；`seed`/`guidance_scale`/提示词约束/`strength` 四个旋钮全探过，一个都不管用。**阈值仍未定**，要先在真实户型分布上跑够样本。留两条线索给下轮：`strength` 是唯一方向有利的旋钮、双模态假说（裸壳疑似比富装保真，有反例）。与获客线的挑图门禁是一件事的两面）。
> v2.11 同日（**换 session**：接手先读本页，再读《交接/交接文档-2026-09-01晚.md》——三图侧等业主三件（蓝图挑版/注释版 v2/合规十问）、在途物（蓝图分支未合并、挑图门禁必须做——与 v2.10 三维"生成→量分→不过线重出"是同一形态）、真机发图操作要点都在那份里；六仓推送已平）。
> v2.10 同日晚（**方差量出来了：噪声底 0.1718**——同一条件图连跑三次保真度分数 0.2197/0.1201/0.0479，而六种条件图组合彼此的差距同一量级，**测不出哪种组合更能锁住几何**。连带确认：单张生成不能直接发给业主，要走"生成→量分→不过线重出"的门禁形态。度量的仪器与证据存 render3d `_iteration/run-2026-09-01-condition-matrix/`）。
> v2.9 同日（**三图这边四路并行全部收官**（《交接文档-三张图出得来.md》追记五）：①槽位改全集——小孩房床 3/3、错字三跑零，新失效＝人脸涂鸦与小房间清单挤不下，注释版 v2 已发业主——**挑图门禁由此必须做，与 v2.8 三维"单张不能直发业主"是同一件事的两面**；②蓝图精修三样张已发业主等挑（墙体淡芯/剖面纹 × 版面排底/排侧），页脚与旧裁决相撞已订正（旧裁决赢）；③墙线按段给厚度贯通两仓——54 段全 ≤2px、896 像素欠账清、玄关交界收真值，闭合率 -0.0003 属"不用假墨换指标"的取舍。**等业主三件**：蓝图挑版、注释版 v2 认不认、合规十问（v2.7 那份）。）
> v2.8 同日晚（**三维这边照出一件影响产品形态的事：写实化同一份输入的输出方差极大**——三跑三个样，一次分毫不差、一次凭空加一整间房、一次把房间并成大开间。**单张生成不能直接发给业主**，要加保真度门禁；连带把当天早些时候"摆成目标构图几何就锁住"那条降级成待验证——单样本不作数。另落地两件：底渲观感提一档 + 室内机位退到墙边、家具摆场 mock（测试脚手架，不进生产路径）。经过见 ishome-render3d `_iteration/run-2026-09-01-fidelity-variance/run.md`）。
> v2.7 同日下午（**AI 标识合规调研收官**：义务清单带条文出处、两档方案、十个拍板问题落《交接文档-AI生成标识合规评估.md》；关键实测＝火山返图自埋 C2PA 但不顶我们的义务、显式标识国标字高≥最短边 5%。其余三路并行未归）。
> v2.6 同日下午（**功能说明图拍了＝制图蓝图方向**，附三条改进：墙体不要实心粗带、左右留白、补信息层（标题栏/图例/占比表这类确定性元素）；裁决带原话在《拍板清单》§〇 末条。实施用隔离工作树起 agent，与"按段给厚度"并行；默认输出切换时点＝改进版样张业主点头时）。
> v2.5 同日下午（**三维底渲变成了效果图**：7 次真调用，揭顶模型图达可发业主水平；用户真户型全程走通；**挂了一天的 0.737 结案**——是 fixture 补的矩形外轮廓撑大了分母，真数据是 0.951。新长出三件没解，头一件＝上游不给家具、底渲是光壳、条件图越弱模型改得越多。尺子锚（内门 0.80 m 反推约 114 ㎡）**等用户确认**。经过见《交接文档-三维这条线出得来.md》追记一）。
> v2.4 同日下午（**注释版真跑成了、偏移画线二次修复**：注释与槽位成派发数据，坐标钉位两跑房间名/注释落点全对、编外物件归零，跑二 105 字零错；线稿白缝块 10→2、闭合率不降，质感样张三张等挑；业主今天共收到十张真机图。新等拍：线稿质感挑版、小孩房没床那类"槽位当全集"的改法。经过见《交接文档-三张图出得来.md》追记四）。
> v2.3 同日上午（**第三张拍了＝手账·写字版，且图上要有注释**——注释内容从户型事实来、位置用归一化坐标钉，不许模型自造，裁决进《拍板清单》；线稿"多次划线/偏移"业主再次反馈，定罪与质感样张进行中）。
> v2.2 同日上午（**三图质量一轮并行收官**：次卧宽墙定罪修复、闭合率不降；彩铅与手账风两版真跑落桶，四张真机发到业主飞书；出图模型评估＝保 Seedream 5.0 pro；攒下四件等拍——第三张用哪版、生图尺寸降档减半价、挑图门禁加功能核对、AI 生成标识合规）。
> v2.1 同日早（**桶里的图真的发到业主飞书上了**——绘图两仓成服务、产物真进私有桶，渠道取桶传飞书发出，真机两张都收到并正常显示）。
> v2.0 同日（**比率改百分数全部收口**：裁决落《拍板清单》、报告规范升 v2.10 订正照抄样板）。
> v1.9 同日（**"送得到业主手里"这半通了**：render2d 与 imagegen 成服务、产物真进私有桶；channel-svc 能把我们自己桶里的图经飞书发出去。差最后一步真机验）。
> v1.8 同日（2026-08-31 夜）（**三维这条线出得来**：render3d 从只有骨架到出四路图；排期改判＝力量都投三维，接线那股的另两段本轮撤下未做。「三条线」因此改称「四条线」）。
> v1.7 同日（**比率改百分数已执行并量过**：全书属该批的裸小数清零，四域重发 release）。
> v1.6 同日（**假设说明的时点钉死在"图发到业主手里之后"**，系统写死的文案也归分条管）。
> v1.5 同日（比率怎么印评估完了：印百分数、走改源、不撞红线，等用户拍）。
> v1.4 同日（**三张图都出得来**，会话侧改成只要面积与户型图、其余推断）。v1.3 同日（失败章重开 + 报告里第一条真的量与第一笔真的钱 + 三档质量考卷）。v1.2 同日（整册第一次量出来：四跑一成）。v1.1 同日（母版落地；几何补参照系与外轮廓）、v1.0 同日建档。内容取自《规范/落地假设与拍板清单.md》末两条裁决、
> 《交接文档-整册能不能出得来.md》、《交接文档-用户发图到拿到报告.md》追记一~四、
> 《交接文档-户型图解析与出图启动.md》追记七~十一、《交接文档-2026-08-30.md》追记十一——**五处状态合并为一处**。
> **就地更新，不发追记**（《纪律·活文档就地改》）。进度事实只写这儿，日志里不再写状态（《纪律·状态写在状态页》）。
> 拓扑事实（仓、服务、端口、队列、凭证）在《仓库拓扑.md》；规矩在《规范/》；来路与证据在《交接/》日志。

## 目标

**尽快让真人用上**（用户裁决 2026-08-30 晚）：真人发一张户型图 → **拿到三张图** → 拿到一份能打开的报告。

## 下一件

**串联（9-04）做完了、本机真派发通了**——上面那两段"撤下没做"的（会话收到图上报业务侧、业务侧铸任务派发）现在都有了，三张图那条腿在本机从填槽位到收图全程自动。**剩三件才算"业主自己走通"**：①真机（本机起 channel-svc 会抢服务器的飞书连接——上服务器还是本机短起，等你定）；②常驻网关 :4000 重启认 `floorplan-*` 三个逻辑名（本机现靠临时 4001）；③~~contracts push + tag v0.3.0~~——已推（9-05）。**第三张的注释暂不递**（物件清单生成步没有，递了被 imagegen 门禁拒），裁决"图上要有注释"不变，时点写死＝清单生成步落地。**报告那半仍未接**：解析产物已进桶（`floorplan-reading.json` 带 layout_features）、回流已带到业务侧，差"自动派报告 + 链接回会话"两段。

**先做三张图那条腿**（用户裁决 2026-08-31，排期改判）：通过 chat 收集用户信息 → 生成三张免费的图 →
**飞书直接把图回给业主**。报告那半不废，已通的出口原样保留，接线时点＝三张图这条腿走通之后。

**排期又改判一次（2026-08-31 夜，用户裁决，最新）：力量都投三维**。上面这条腿不废，
但「会话收到图、上报业务侧」与「业务侧铸解析任务并派发」两段**本轮撤下、没做**
（另一个 session 在做「渠道出站把图发回业主」与 render2d/imagegen 服务化，不受影响）。
三维那条见下面**第四条线**；两条裁决（含"服务之间只有数据通信"这条通则）在
《规范/落地假设与拍板清单.md》§〇 末两条。

**三维这条线：控制图通路整段接通（9-05）**。链路＝render3d 五路（第五路控制稿）→ imagegen realism-pass（无陈设、尺子 v2 只记录）→ LiteLLM custom handler（测试网关 :4003）→ 万相线稿生图；首次端到端 15 张：构件级三条系统性失效（地面台阶、天花灯槽、斜顶）改线稿后 0/12、1/3、0/12，家具 0/15；残留＝洞口类型（上游补洞全标门，真值缺）、凭空加窗 1/12、残留小件、出图封 1168×880、尺子透视不能直接当门禁、多视角陈设一致性无解、厨房站不出机位。
**等你定**：①看《失效清单》追记一定检测器（剩 B4/B5/C2 与整图错位口径）；②对象键形态与写实图键重复段、camelCase/snake、机位 id 字符集（contracts 分支 `render3d-realism-registry`）；③网关 :4000 切不切万相（`cp config.control-test.yaml config.yaml` + 重启，LiteLLM 部署级重试默认 2 次那条一起定）；④深度设计入口形态与承诺、资产库来源（《方案/深度设计入口与数据契约》§七）；⑤室内机位俯仰进不进候选（厨房）；⑥自部署第二形态触发与否（《评审/自部署控制通道与跨视角一致性评估》）。
做完的（9-05 晚）：尺子 v3 可作室内量分器候选（imagegen 0166a42）；Real-ESRGAN x2plus 放大不动墙（render3d `run-2026-09-05-upscaler-check/`）；换风格几何不动但尺子跨风格不可比；判官台三维样本集 31 张（judgebench 分支）；交接文档《控制图通路落地》。洞口类型真值已落地 aipipe（c9fc00c）并被 render3d 消费（e48254a：先读后猜、猜了计数、基准换 16 洞）；符号实验两批 33 张：门 `frame-sill`/`frame-handle` 0 残影对斜线 10/33、书房窗 3/3——**换默认符号方案待拍**；入户门机位站不出（玄关太浅）。分数分布已攒：室内三档 n≥5，揭顶不够。奶油风 v2 不转正。**换 session：接手先读《交接/交接文档-2026-09-06.md》**。派发入口与回流草案在 aipipe/contracts 同名分支 `space-render-dispatch`，未合。待拍：`unknown` 洞怎么画/起体、`bands` 三维要不要消费、管井洞要不要起体。来路：《评审/控制图通路调研-2026-09-02.md》、《评审/失效清单-控制图通路-2026-09-04.md》、《交接/交接文档-控制图通路调研.md》。

**"送得到业主手里"这半 9-01 早通了**（用户裁决 2026-08-31 夜："让图片可以通过飞书发出去"、"现在没有明确阻塞就把它起为服务"）：绘图两仓成服务 + 渠道能发我们桶里的图，三处提交都**未 push、未上服务器**。
**真机验过了**：业主给机器人发一条消息 → 从 `svc_channel.inbound_messages` 取到 open_id → 把桶里两张真产物发过去，都收到、都正常显示；飞书 `im:resource`（上传图片）权限业主确认已开通。
**本机起 channel-svc 会和 dev 服务器抢同一个飞书应用的入站事件**（长连接与发送挂在同一个开关上，没有"只发不收"的模式）：验出站时业主别同时发消息，验完立刻停。

**一件等你拍（我提的默认，不是裁决）**：生成图的对象键形态＝落在源图同一前缀下，`uploads/{content_sha256}/plan-master.png` / `plan-walls.png` / `plan-rooms-mask.png` / `plan-rooms.json` / `plan-brief.png` / `atmosphere-{template_id}.png`。判据照 contracts `registries/object_keys.md` 开头写死的三条（确定性派生、不铸新流水号、不含用户身份与渠道方言）。**表里还没写**（只增不改、新增走 PR），点头之后一起进表；三维那条线的场景包与底渲四路等这条落了照抄形态。
**~~同一处还有一个小分歧要一起拍~~——扩展名那半已拍**（9-02，用户裁决：*"标签跟着内容走吧"*）：
**扩展名由字节首部判定**，与 registry `uploads/{sha}/original.{ext}` 先例合流——风格图键改叫 `.jpg`，
现行"键写死 `.png`"作废；三维底渲深度/遮罩不受影响（我们真写 16 位 PNG，名实本来就符）。
**执行已落**（同日，imagegen `b0b8d1e`，未 push）：键模板改 `atmosphere-{template_id}.{ext}`、
`{ext}` 按字节首部判（jpg/png，词面照注册表词表）、与 Content-Type 同源同判，73 测试与三件门禁全绿；
**前缀形态那半（上一段的执行者默认）仍等拍**，进表一起。

**三图质量与成本这批又攒了四件等拍**（9-01 上午，经过与证据在《交接文档-三张图出得来.md》追记三）：
①~~**免费第三张风格图用哪版**~~——**已拍**（9-01 上午，用户裁决）：**手账·写字版，且图上要有注释**。
注释内容从户型事实与假设来、位置用 pro 原生归一化坐标（`<bbox>`/`<point>`）钉在房间上、
文本不含数字（数字上图走叠印，未建），不许模型自造。裁决带原话在《拍板清单》§〇 末条，实装进行中；
②~~**生图尺寸降不降档**~~——**已拍**（9-02，用户裁决）：**先不降档**，维持 0.60 元/张与召回 97.2%；
再拍时点未钉事件名（执行者建议钉"门禁观察态跑出真实重画率后"）。显式像素尺寸与此无关、已在用；
③~~**挑一张发出去的门禁要不要加房间功能核对**~~——零字版在餐厅凹处凭空画了个卫生间，墙没动、
外圈闭合率拦不住这种功能编造。**问题未拍，但已并入《方案/挑图与保真度门禁方案.md》§六待拍板**
（连同重出上限 K、门禁物理落点一起，v2.13）；
④~~**AI 生成内容标识怎么落**~~——**已拍**（9-02，用户裁决）：**先不打**。替代形态＝报告内加说明＋
用户协议加说明，法律问题统一收敛到最后交法务处理；十个拍板问题整批冻结不逐拍，调研结论
（《交接文档-AI生成标识合规评估.md》）继续有效、届时作法务输入。裁决带原话在《拍板清单》§〇 末条
（收敛时点"最后"未钉事件名，执行者建议钉"正式对外投放前"）。

**两股并行**（用户裁决 2026-08-31：母版可与共用段并行）：

- **接线那股（有先后）**：会话收到图、上报业务侧 → 业务侧铸解析任务并派发 → 渠道出站把我们桶里的图
  发回业主（触发条件"生成的图要送回会话时"**已到期**，这条腿要用）。
- **画图那股（不等接线）**：母版绘制 → 三张图。**这一股已经做完**（8-31，见《交接文档-三张图出得来.md》）
  ——三张图都出得来，情绪图与功能说明图是完整成品。**剩的不是"画得出来"，是"送得到业主手里"**，
  那要接线那股。

**~~另有一件挂着：量整册跑一次~~——已做完**（8-31，换 session 执行）：**整册出得来，四跑一成**。
经过、判据与新暴露的三件事见《交接文档-整册能不能出得来.md》追记二。**报告那半下一步不是继续磨成品率**
（那要按章修、逐条啃），排期仍以三张图那条腿为先。

**~~评估完一件等你拍~~——已拍已做完**（用户裁决 2026-08-31：*"我觉得没问题，可以做"*）：
**比率改百分数**。业主看到的从「不低于 0.9 比率」「封闭收纳占比就用 0.8」变成「不低于 90%」
「就用 80%」。做法＝**改源不动渲染层**：7 条落点值 ×100、补 `unit: "%"`（6 条真种子 + 1 条考卷
里的 MOCK），核验白名单撤「比率」加「%」，两仓各一行把 `%` 改成紧排（排版不是换算）。
**不撞渲染层禁换算红线**：那条管的是渲染层不许自己造数字，本次渲染与成文逻辑一行没动。
**量过**：三档考卷 `full` 两跑都一跑成册、六章零失败；**全书裸小数 10 → 4**，剩下 4 处出自
`lkp-budget-confidence-width`（`±比例`，本就不该给业主看，另立）与 `lkp-storage-density-baseline`
（`0.25–0.35 米/㎡`，真有量纲、本来就读得懂），**都不属该批射程**。
经过、判据与两处踩坑见《交接文档-比率怎么印给业主.md》追记一、追记二。
**规范侧也落完了**（9-01）：《落地假设与拍板清单.md》§〇 补上该裁决（带原话与射程边界）；
《装修报告生成规则规范.md》**升 v2.10**——规则 1.9 值形态表那一行的例由 `{min:0.2,max:0.35}`
订正为 `{min:20,max:35}`（**那张表是后来人新增落点时照抄的样板**，不改它，白名单拦得住假单位
「比率」、拦不住照着样板写小数），审计节 §14.15。**这一件到此全部收口。**

**~~追记二里留了一件要你拍的~~——已拍**（9-02，用户裁决：开、存量一起改）：把「prompt 自己不许写
它禁的词」从测试夹具挪进 `verify_seeds.py` 正式核验；存量 6 处（ergonomics 净宽 3 · lighting 照度 2 ·
material 优异 1）改写、三域重发 release、worker 重启。裁决带原话《拍板清单》§〇 末条。**动工中（本轮）。**

## 四条线

### 一、图从会话进来（五段，顺序即依赖）

| 段 | 状态 |
|---|---|
| 渠道把图取下来落私有桶 | **真机通了**（8-31 上午）：飞书发来的真户型图落桶，键由内容哈希推得、取回重算相等，无签名直取 403，取图到落桶约 1 秒 |
| 会话收到图，上报业务侧 | **做了（9-04，单测 + 进程内 gRPC 全链，未真机）**：图片入站记对象键；每轮回话前把新到的三样报业务侧（户型图键、建筑面积、得房率——业主给的 observed、没给按面积推 inferred 80），属主只问一次、项目 id 缓存在会话快照；上报失败对业主如实说、下一轮补报。出站边缘 `chat/project_client.py`（contracts project.v1）；会话侧仍不判里程碑不建任务 |
| 渠道出站发**我们自己桶里**的图 | **真机通了**（9-01 早，`ishome-backend 17881eb`）：统一消息带 `object_key` → 按键取桶字节 → 传飞书 `im/v1/images` 换回 image_key → 按原路发。**两张真产物（功能说明图 118KB、风格图 677KB）都发到业主飞书上并正常显示**，三行日志齐（`private object read` / `uploaded to feishu` / `message sent`）——只发飞书自家的图时前两行不会出现。两条路的判据在一处：有对象键走取桶上传、带 `feishu-image://` 走原路、**两个都没有当场抛**（此前任何不带 scheme 的串都被当 image_key 直接发，业主收到一条打不开的图）。发出去之后要调 `chat.service.deliverables_delivered`——按什么假设做的那套话挂在这个时点上 |
| 业务侧铸解析任务并派发 | **通了（9-04，本机真派发八跑一成）**：两样槽位齐 → M0 判据满足迁 M0.5 → on_enter 建 `vision_image` 任务当场派编排（`/floorplan-visuals`，回调地址由业务侧注入）→ 编排跑勘测/几何/事实/批注∥文案/母版与说明图/两张风格图/叠字 → 结果回调 → 六件产物登记、outbox → 中继 gRPC 到会话侧 → 三张图送达置 PRESENTED → 迁 M1。派发失败/生成失败都写失败事件让业主知道（不挂 PENDING）。`floorplan-parse` 从存根实装（存档 `floorplan-reading.json`）与主链并行、不致命 |
| 解析产物回流，报告变成"你这套户型"的 | **半**：解析存档已进桶、回流报文带 `layout_features`、业务侧按 `floorplan_reading` 登记为产物；报告派发时从它取画像**未接**（属报告那半） |
| 链接送回会话 | 未做（属报告那半） |

**会话侧同日改了形态并上线**（8-31，真机验过）：业主开头**只需要给面积与户型图两样**，其余
（人数/得房率/装修倾向/年龄）由代码从面积推——给了就用、不给就算、不追问。另有四处修复：
不再逼业主拿卷尺量房、不问图里已经写着的东西（小区名/几室几厅）、回话按思维停顿分条发、
内部字段名不再递给模型。**经过与判据见《交接文档-三张图出得来.md》§八、§九。**

**当晚又改两处，只到提交为止**（aipipe `bb61180`，**未 push、未上服务器——服务器上跑的仍是旧形态**）：
①**假设说明说早了**：裁决原话是"产出结果之后也告诉用户"，代码却落成了"两样输入齐了就说"，
业主图还没影就先收到"我按 4 个人来安排、得房率按 80% 算"。现在**收到图那一轮只说开始设计**，
假设那套挪进 `chat.service.deliverables_delivered`——**今天没有调用方，接线时点＝上表第三段接通时**；
②**分条那条规矩此前只管住模型写的回复**，系统自己写死的文案（假设说明 161 字一整段、结构说明 86 字）
不在射程内——现在系统文案一律若干条短消息、与回话共用同一个出站口，并加了一道按 60 字判的自动门禁
（不是再写一条禁令）。**经过见该文追记一；裁决见《规范/落地假设与拍板清单.md》§〇 末三条**
（末三条含补档的「回话按思维停顿分条」——它此前只写在日志里）。

**未拍板一件**：会话侧自抄一套槽位，与业务侧流程定义对不上，而业务侧那个分发接口会话侧一次没调过
——同日三次"系统在问它自己能算的东西"都是从这条缝里长出来的。做"会话收到图、上报业务侧"那一段时
一并提请。**9-04 处置（执行既有裁决，不是新裁决）**：业务侧流程定义 M0 改为只要 `floorplan` + `building_area_sqm`
两样（8-31"开头只给两样"那条），城市/预算/家庭结构降为可选；会话侧报的三个槽位名与之逐字对上。
分发接口会话侧仍没调（槽位名现在是两边各持一份逐字副本）——两边合一由谁分发，仍未拍。

### 二、图变成几何、特征与三张图

| 段 | 状态 |
|---|---|
| `floorplan-parse`（图 → 户型特征 + 依据） | **可用**：分区读 11 次调用、约 38s；厨房形态 3/3，阳台设备位 2/3，朝向 3/3 |
| `floorplan-geometry`（图 → **外轮廓**/墙线/门窗洞/房间遮罩） | **可用**：**零模型调用**、约 2.3s；三份存档 61 段墙逐字完全相同，自证覆盖率 97.3%。8-31 补两处（都是母版一消费它就撞上的）：**产物带上自己的参照系**（x 按图宽归一、y 按图高归一，不知道原图多宽多高就画不出正确形状）、**补外轮廓**（网格投票表达不了飘窗那种台阶，而飘窗那几条边在图上是细窗线、被去家具线的开运算抹光，一个墙像素不剩） |
| 特征进报告 | **链路通且已见成品**：某册一张卡逐字写着"你家阳台画了洗衣机和柜体虚线框，说明它不是晾衣服的地方，而是要天天动手干活的家政区" |
| 母版绘制 | **做完**（8-31）：几何 → 母版 + 墙体图层 + 房间遮罩索引图 + 房间锚点；确定性、零模型调用，同一份几何画两次**逐字节相同**。**已成服务**（9-01，`ishome-render2d c0d86fa`）：`plan-2d-render` 不再是存根，worker 真起、activity 真派、五件产物真进私有桶（母版/墙体图层/房间遮罩/房间锚点/功能说明图），回读与本地纯库画的**逐字节相等**、重派两次键与字节都相同、无签名直取 403；几何清空外轮廓重派 → 响亮失败且桶里一件没写。CLI 不废（本地迭代入口，两条路共用同一份纯库）。外圈闭合率 **94%**（自证数，低于 0.90 整张不出——上线当天就拦下一张 64% 的）。**9-01 上午修掉次卧右墙 51px 宽带**（定罪＝网格投票一条线只给一个厚度、把角上一截真实 22px 套给整条线，不是外轮廓叠加；修后与别处外墙同宽、白缝消失、闭合率不降，`ishome-render2d 58a743f`） |
| 三张图 | **都出得来**（8-31）：情绪图与功能说明图是**完整成品**（含标题/总结/贴士/房间名/批注），彩铅是图形层。**风格图这一路已成服务**（9-01，`ishome-imagegen 8194cda`）：`atmosphere-visual` 真跑出图并写进私有桶（读母版那条键、房间表取键不内联），九间房功能全对、零文字，无签名直取 403；房间表非填不可与网关回执 `input_images` 两道门禁接进 activity 后仍生效。`realism-pass` 仍是存根，时点写死＝第一张底渲出得来时（三维那条线 8-31 夜已出得来，见第四条线）。换风格＝换一份模板数据，代码不动——**9-01 上午量出这话对文字层不成立**（房间名归谁写只能是代码开关，默认不写）。同日风格图再出三张（彩铅／手账写字／手账零字，`ishome-imagegen 518f032`）并与修好的线稿一起真机发到业主飞书；**手账写字版最接近样例**：我们给的九个房间名零错字全在位，翻车的全是模型自造的注释。**出图模型评估收官＝保 Seedream 5.0 pro**（唯一可实测外部候选召回差一个量级出局；显式尺寸与降档、挑图门禁、AI 标识合规等拍，见《交接文档-三张图出得来.md》追记三）。**挑图门禁三图侧 9-04 定了顺序**：先列失效清单、用户逐张标 pass/fail，再定检测器；检测器验收＝真阳率/真阴率分开报（《方案/挑图与保真度门禁方案.md》v0.4 §四） |
| 户型事实 + 批注 | **可用**（8-31）：几何 → 34 条带 id 的事实（零模型调用）；批注由模型拿事实推理、**每句必须引得到事实**，机检两条。真跑过检 5 条打回 0 条 |
| 空间推理背书 | **第一次实装**（8-31，即上一行的批注那一步）；报告正文那一处仍未接 |

### 三、报告出得来（成文 → 装配 → 册检 → 出册）

| 段 | 状态 |
|---|---|
| 成文到出册 | **单章跑得通**：lighting 最近一批 6 跑出册 **5/6**，过检那几跑平均重写 0.8 轮，一跑一稿过检 |
| 成品质量 | 最近五份成品 26 张卡：**禁词 0、叠字 0、裸标识名 0**，五份全渲到桶 |
| 出口 | **全通、真跑过**：册进私有桶、签名链接 `HTTP 200`、手机上能读；无签名直取 403 实测过 |
| **六章整册** | **出得来**。8-31 上午量得四跑一成（失败散在各章不集中，六章各自八九成连乘即约四分之一）；**下午加了「失败章单独重开」**（`max_unit_retries` 默认 2，只重派失败域、已成的章原样留用），此后三跑三成册，其中两跑**全靠重开救回来**（重开前那几章是失败的）。重开救的是随机失败，救不了一章本身就弱 |
| **报告里的「量」与「钱」** | **第一条量、第一笔钱都出来了**（8-31）：套内面积（建筑面积 × 得房率）→ 全屋收纳总长 22.0–30.8 米；水电改造人工费 **6600–7480 元**（单价 60–68 元/㎡ × 建筑面积 110㎡）。两条都**算在求值线不在报告线**，且带推导原文进包——**算出来的数不带推导，写作步就会编一个**（真跑逮到过一次） |
| **造价章的处境** | **五条单价里只有一条接得起来**：五项的"量"只有水电人工那条有（＝建筑面积），其余四条（拆除面积/墙面展开/柜体投影/点位数）**一个都不存在**，全部指向同一个上游——定稿平面。"有单位不等于有量"：三条单价 unit 字面都是「㎡」但要的是三个不同的量，乘哪个量由数据说（单价资产 `props.quantity_basis`） |
| **质量基线** | **有固定考卷了**：三档拟真上游包（`full` 58/0 · `partial-gaps` 55/3 · `mostly-gaps` 12/46，同一户同一批 58 条落点，只差上游算出来几条），跑法 `uv run python -m tests.quality_baseline --tier X --runs N`。此前每次测都用真求值线当场算的包，release 一变卷子就变，**成品率跨天不可比** |

六域当前 release（8-31 晚比率改百分数后四域跳版）：**`budget@v14`** / `ergonomics@v10` /
`lighting@v13` / **`material@v10`** / **`softdeco@v10`** / **`storage@v12`**。

**两件本次量出来、要用的事实**：①取链接那个接口**出册了也可能 500**——跑着的 project-svc 若起动时没
`source ~/.ishome/oss-local.env`，四个 OSS 配置缺省空串、签名必抛（不是代码回归）；②**跨章漏词只有整册跑
照得出**：成册正文里「延米」出现在 softdeco 章，而它是 storage 的域内禁词——每章只按自己那份表判。

**离整册交付还差什么**（2026-09-07 扫出，来源＝设计说明 v0.2 §4/§7/§9 与规则规范 §3.1 对现状的差额；
每行都注了"在哪儿看得见"，没有证据的不写进来）：

| 缺口 | 现状 | 在哪儿看得见 |
|---|---|---|
| **自动派报告** | `POST /api/v1/reports` 是手工 REST 口，唯一调用方是 `ReportsController`；里程碑 `on_enter` 的动作类型只有 `CREATE_TASK`（建生成任务那类）——**不是没配，是配不出来** | project-svc `application/ReportDispatchAppService.java`、`domain/definition/OnEnterActionType.java` |
| **链接回会话** | 签名口 `GET /api/v1/reports/{id}/link` 有、真跑 200，但没有把链接送回 chat 那一段；outbox 事件里没有报告册 | 本页 §一"链接送回会话"行 |
| **采集** | 数据包要的 `anonymousProfile` ＝身高/最高身高/眼高/电视屏高/城市线级/建筑面积/得房率 ＋ `layoutFeatures`；免费闭环只问户型图＋建筑面积，身高那批**没人问**。规范里 Q-BODY/Q-STORAGE/Q-MAINTAIN/Q-BUDGET/Q-COLOR（色彩必须走 H5 视觉网格、禁文字问）都没建，chat-svc 只有 intake | reportgen `tests/fixtures/upstream-package-full.json`；《交接/交接文档-2026-09-05-判官台与获客线闭环.md》§5 |
| **册里只有字** | 设计 13 个 dom-，出册的只有 6 个（有知识域 release 的那六个）。**图类 art- 一张没有**（户型现状/日照分析/墙体性质/平面比选/平面定稿/天花灯位/水电点位/材质意向/空间效果图），规则 3.3 要的图名编号·指北针·比例尺·图例表·精度标注·免责文案因此全部未兑现；**清单类 art- 一条没有**（报价必含项/水电检查/分阶段验收/尺寸驱动采购），其中报价必含项按规则 3.2 **必须早于 stage-project 主流程可达** | 规则规范 §3.1 art- 表；reportrender `render.py` 只渲 card ＋ locked_text ＋ provenance |
| **布局求解** | 平面那批图与造价四条"量"共同的上游，**一行代码都没有**；家具资产表零消费方也是因为它 | 本页 §三"造价章的处境"行 |
| **润色（`gen-polished`）** | 规范里它只作用于**清单条目的 `rationale_display`**（LLM 只改措辞不许改语义，条目本身纯规则求值、禁 LLM 自由生成）。清单没做 → **润色没有载体**。整册口吻不靠它，靠 persona 四件 ＋ 出口判官，这套已在跑 | 规则 2.5 / 4.5 / 4.13 |
| **pt- 页型库（排版版式）** | `Page.page_type` 字段留着**恒为 `None`**，装配层注释自陈"首版按域成页；pt- 页型库编译后接管"；渲染层一套 CSS、六章一个模子 | reportgen `activities.py:312`；设计说明 §7 手册 1「待编译」、§9 遗留「pt- 页型库首版声明」 |
| **页级/册级判据** | 页级 takeaway / first-visual / one-thesis-per-page、册级 branch-cap(≤2) / set-closure / promise-fulfilled / concept-through **一条未实现**；现在册检只有结构性那几条（该有的域有没有页·空页·锁定文案挂没挂·标注挂没挂·数字引用解没解开） | 设计说明 §4；reportgen `activities.py:349` 注释"随 release 判据编译后物化执行" |
| **判官仍观察态** | 出口过检第二道一律 `observing`，只记录不拦截；反例库只收真跑样本，**人机一致率没量过** | reportgen README |
| **冷启动** | 零条 calibrated，成品每条落点都印「未校准」——**预期状态不是漏**；退出判据＝某域行为信号量达阈值 | 设计说明 §6 |

**这一节只记差额，不记排期**——先做哪件由用户拍，拍了写进「下一件」。

### 四、三维底渲（输入包 → 场景包 → 四路图）

| 段 | 状态 |
|---|---|
| 输入包契约 `DesignPackage` | **定了**：几何那一半是 aipipe `floorplan-geometry` 产物的**逐字对面**；三维要而二维没有的那一半（尺子 = 建筑面积 × 得房率、层高门高窗台高、家具体块、材质、相机）今天由拟真包填。**比例尺不许由模型给**；缺哪块就是上游哪块没做出来，不许在本仓编值补上 |
| `scene-compile`（包 → 米制场景包） | **可用**（纯库 + CLI）：真户型 92㎡ 编出 233 块网格 / 1616 三角形，5 ms，零模型调用，编两次逐字相同。尺子锚＝**外轮廓围合面积**（＝套内建筑面积的几何定义，不引系数） |
| `base-render`（场景包 → 几何/深度/线稿/遮罩） | **可用**（纯库 + CLI）：八台相机 **3.0 秒**全出四路，渲两次逐字节相同。**这一步不需要 GPU 与三维引擎**——四路是给 `realism-pass` 当条件图的，不是写实图（执行者判断，不是裁决，见拍板清单§〇） |
| 只有几何时出不出得来 | **出得来**：`design-package-minimal`（家具材质机位全空）照样出图——这就是"独立闭环"的可执行形态，也是今天上游的真实处境 |
| 接进 activity | **未做**，两个 activity 仍是存根。差三件：①落桶模块（照抄 reportrender `book_store`：只写不签）；②场景包与四路图的键进 contracts `registries/object_keys.md`、产物类型进 `registries/artifacts.md`（**今天两张表里都没有三维的位置**）；③函数体换成"取键 → 调纯库 → 写桶 → 返回键" |

| 底渲 → 写实化效果图 | **通了**（2026-09-01 下午，7 次真调用）：走 imagegen 纯库直调，`apply_realism_pass` 仍是存根、**imagegen 一个字节没改**。揭顶模型图已达可发业主水平，换风格只换一段文字。两条口径：**底渲的构图要先摆成目标构图**（否则模型自己转相机）、**条件图不止一路**（实测一次吃得下多张）。均为执行者判断，**不是裁决** |
| **输出方差**（2026-09-01 晚照出） | **单张生成不能直接发给业主**：同一份输入（两组条件图的线稿外接框逐像素相同、边像素只差 1.3%）三跑出了三个样——一次轮廓分毫不差、一次**凭空多出一整间主卧**、一次把房间**合并成大开间**。要加**保真度门禁**（先例＝render2d 外轮廓闭合率 < 0.90 整张不出）；**噪声底已量出＝0.1718**（同一条件图连跑三次，保真度分数 0.2197/0.1201/0.0479）；条件图六组组合彼此的差距多在 0.01~0.18，逐对核过后除一对压线外**全部小于噪声底＝测不出差别**，故不排名次。**在噪声摸清之前，比较条件图组合是在比噪声。** **旋钮全探过了，一个都不管用**——`seed` 发了换不来可复现性（同 seed 连跑 72.7% 像素差超 10 级）、`guidance_scale` 被网关 400 拒、两条提示词约束改法与 `strength` 都测不出差别。**门禁因此立项——形态与判据的权威处＝《方案/挑图与保真度门禁方案.md》，本页不复述**（《纪律·同一条规矩只写一处》）。归本线的两条线索：`strength` 是唯一方向有利的旋钮（每档 n≥5 再判）、**双模态假说**（裸壳保真度疑似高于富装，但有反例，别当口径）。**本线欠那份方案的**＝`strength` 每档 n≥5 复判、双模态假说证实或证伪（方案 §五把它们划归本线）。经过见 render3d `_iteration/run-2026-09-01-variance-knobs/`**同时推翻当天早些时候那条"摆成目标构图几何就锁住"——那也是单样本，降级成待验证** |
| 底渲观感 / 室内取景 | **提了一档**：超采样抗锯齿、半球环境光 + SSAO、家具投影阴影（**只动 geometry 一路**，三条数据路由"逐个 monkeypatch 8 个观感常量"的测试逐字节锁住）；room 机位从"站地板质心平视"改为退到房间边缘朝屋里看，避墙 0.35m 避家具 0.40m，上游给了 yaw 就听上游的（靠 pydantic `model_fields_set`，契约字段没改）。**室内平视改善有限**——狭长且塞满柜体的房间换机位救不回来 |
| 家具摆场 mock | **有了**，但**只是测试脚手架**：新模块 + CLI 开关默认关，`scene_compile` 一个字节没碰——家具摆放是设计决策，mock 进生产路径就等于服务替业主做设计。自证明说"mock 摆场，N 件（常规住宅档位，非实测/非软装设计）"，摆不下的逐件报出不硬塞 |
| 用户真户型走通全程 | **走通了**：82 段墙 / 59 段真外轮廓 / 19 个洞 / 7 间房 → 底渲 → 效果图，房间在图上一一认得出。建筑面积当时用户没给，**不编**——曾用最窄内墙洞按常规内门 0.80 m 反推（约 114 ㎡）、三锚互证量级可信。**9-02 真值拍了：建筑面积 138 ㎡、得房率 80% → 套内 110.4 ㎡**（裁决带原话在《拍板清单》§〇 末条）；门宽反推偏小约 17%，落在它自报敏感度射程内（0.90 m→144 ㎡），**此后尺子以真值为准、门宽/墙厚锚降为交叉验证**；**已按真值重算（9-02）**：尺子 14.27 m/单位；地板面积 105.0 ㎡，对套内比 **0.951**（该比值只看外轮廓形状、与尺子无关）。**9-05 基准换成 16 洞产物后**（旧 19 洞里 4 个假洞）：地板面积 111.31 ㎡、对套内比 1.008，变的是房间遮罩不是尺子（render3d `_iteration/真户型-基准/README.md` 追记）；最窄那批内门实际 0.86 m（当时假设 0.80，线性偏小 7% → 面积偏小 17%，与裁决记的偏差对上）；墙厚 0.102 / 0.205 m **仍在国标 100/200 mm 档**，墙厚锚的印证不因换尺子失效。真户型的入包 JSON 只在 scratchpad（一次性验证包，不进仓）；仓内 fixture 是 8-30 那套房，与本条无关 |
| **控制图通路调研**（9-02 中午） | **答案＝有**：百炼万相 `wanx2.1-imageedit` doodle（`is_sketch=true` 吃 `line.png`）三判据全过——同 seed 逐像素 100% 相同、转 90° 跟着转、三 seed 0.2396/0.2376/0.2417 极差 0.0040（噪声底 0.1718）、叠线看图同一套房；本机 SD1.5+ControlNet（depth+mlsd）也过（极差 0.0136，画质是 SD1.5 的事）。Seedream/火山视觉智能/百炼 wan2.6/千帆/智谱/MiniMax 都没有结构控制口；腾讯 SketchToImage 与 Stability Control 形态上有、无凭证未跑不判；BFL 的 Depth/Canny 已下线。**换 Seedream 版本也无用**（9-02 下午补）：5.0 pro 已是方舟最高档，4.5/4.0 账号未开通，5.0 lite 三跑叠线仍是三套房（整栋平移、隔墙各异），见 `Seedream-5.0-lite-对照/追记.md`。**拆分走哪条路等拍**（建议见评审记录 §六）。经过与数字＝render3d `_iteration/run-2026-09-02-control-path-survey/run.md` |
| **失效清单**（9-04） | 16 张万相 + 7 台室内机位：A1 墙位漂移 0/16；B1 地面分界线成台阶 2/4；B2 天花线成灯槽 4/4；B3 透视线成斜顶 4/4；B4 门窗类型错；C1 默认必摆家具（揭顶 6/6，室内 0/2）；C2 无陈设残留小件；D1 小房间机位对墙 2/7；F1 尺子透视未自证。构件级全系统性、来源线稿画法。全表《评审/失效清单-控制图通路-2026-09-04.md》，样本 render3d `_iteration/run-2026-09-04-failure-catalogue/` |
| **控制稿重跑 · 首次端到端**（9-05） | render3d 控制稿 → imagegen realism → :4003 handler → 万相，15 张（5 机位×3 seed）：B1 0/12、B2 1/3、B3 0/12、C1 0/15；B4 残留（真值缺）、B5 1/12、C2 1/15；厨房无法取景（响亮失败）。超分/大线稿都不放大（`run-2026-09-05-wanx-superres-batch/`）；尺子透视自证（`run-2026-09-05-metric-perspective-selfcheck/`）。证据 render3d `_iteration/run-2026-09-05-control-sketch{,-realism}/`，判读《失效清单》追记一 |
| 接进 activity | **做了**（9-05，d140380）：`scene-compile`/`base-render` 取键→纯库→写桶→返回键与自证数，内存桶全链测试；键形态草案待 contracts 登记；worker 起动要 `ISHOME_OSS_*`。imagegen `realism-pass` 同日实装（e859aa6）。编排草案已合 aipipe main（6784faf），缺派发入口与产物词表回流 |

**真数据照出来三件并已处置**（合成几何一件都照不出）：真跑 61 段墙里 **5 段零长**（跳过 + 计数）；
**15 个洞落在墙段之间一个源像素宽的空隙里**、不与墙相交（改为补过梁与窗下墙，15/15）；
**尺子锚错了**（外接框比外轮廓大 4.12%，尺寸整体偏小）。

**原"四件没解"结掉一件**：地板面积吻合率 0.737 **不是数据错**——是 fixture 的外轮廓为拟真补的矩形、
把分母撑大了；真户型（59 段真外轮廓）一跑是 **0.951**，剩下 4.9% 是内墙占地。
**仍挂着的**：`CameraSpec` 说不出"站在房间哪儿"（room 机位站在面积质心上平视＝对着墙，取景在改）、
洞两侧 1.4cm 通高的缝、鸟瞰图上的黑区。
**新长出来的**：窗洞被写实化模型放大；洞口种类仍按档位猜（**入户门被画成带窗台的窗**，上游没这个字段）；
**上游不给家具，底渲是光壳，条件图越弱模型改得越多**（真户型内隔墙漂得比 fixture 明显）。
**经过与判据见《交接/交接文档-三维这条线出得来.md》追记一；全部数字与失败见 ishome-render3d
`_iteration/run-2026-09-01-realism/run.md`**。

## 各仓（2026-09-01 早实测）

| 仓 | 领先远端 | 8-31 本轮 |
|---|---|---|
| ishome-aipipe | 0 | **9-04 串联五笔 + 重锁（已推）**：解析一族成 activity + 三张图 workflow + 派发入口与回流；会话侧上报/收图；勘测千分网格换算；注释开关 + 叠字重生成；文案/批注带原因重写。contracts 钉 v0.3.0 |
| ishome-render2d | 0 | **9-04：叠字成 activity**（style-caption-overlay，已推）；**9-01：成服务**（plan-2d-render 实装 + 产物落桶 + 真跑）；8-31 母版绘制 + 外轮廓、功能说明图、叠字、中文字体解析（**已 push**） |
| ishome-imagegen | 1 | **9-01：成服务**（atmosphere-visual 实装 + 读母版/写图进桶 + 真跑）；8-31 那批**已 push** |
| ishome-backend | 0 | **9-04：project-svc 串联**（三个入站口、M0 两样、迁 M0.5 即派发、回流登记、outbox 中继到 chat、V6 迁移，已推）；**9-01：渠道出站能发我们桶里的图**（取桶 + 传飞书换 image_key）；另一 session 的报告求值线 |
| ishome-contracts | 0 | **9-04：v0.3.0（已推）**——PresentDeliverables、project.v1、floorplan-visuals、activity #15–#19、模板/事件注册表加行、远程插件钉版本 |
| ishome-infra | 2 | 网关加两个逻辑名（floorplan-notes / floorplan-copy） |
| ishome-reportgen | 1 | 推导步打回按组给路（另一 session） |
| ishome-reportrender | 1 | 目录不印内部域名（另一 session） |
| ishome-render3d | 1 | 三维这条线（本轮）：输入包契约、场景包、底渲四路、拟真包，纯库 + CLI |
| 其余两仓（ishome / frontend） | ishome 领先 5 | — |

**领先远端那一列 9-01 早重测过**：8-31 那几批多数已 push（aipipe 从 13 降到 1 就是这么来的），数字随时会变，看的时候以自己跑一遍 `rev-list --count @{u}..HEAD` 为准。

**同一批仓上可能有别的 session 在干活**：上面那几处未提交改动可能是在途工作，别动、别 `git add -A`
（《纪律·显式 add 路径》）。push 时机由用户定（《纪律·push 由用户定》）。

## 已知工程缺口（结构没拦住的，2026-08-31 从七份日志的坑单判出）

按《纪律·能自动拦住的不写成纪律》：这些**不是纪律，是缺陷**——每一条都曾被当成"下次记得注意"，
实际该由结构拦住。**都还没修**；**登记在中控仓，不进代码仓**（那几个仓可能有别的 session 在动），
做到哪条线时随手带进去。

| 缺口 | 结构上怎么拦 | 落到 | 证据 |
|---|---|---|---|
| 跨仓／编排的改动，两侧单测全绿也可能整条线卡死 | 门禁加一条真派发的全链冒烟（一次派发跑到出册） | aipipe / backend | 踩过三次；加出册那步时卡在重试到超时才发现 |
| 生产侧加顶层字段，消费侧没同批认下 | contracts 发版跑消费侧契约测试；schema 变更 CI 检查所有消费方 | contracts | 一天两次；第二次成文已过、死在出册 |
| 本地自查与门禁不是同一条命令 | 自查做成一个脚本，门禁跑同一个脚本，没有第二条命令可跑 | 各仓 | 给 mypy 传路径覆盖了配置里的 files，本地绿门禁红 |
| 历史迁移文件可以被改动 | pre-push 检查已应用的迁移在 git 里有没有被改过 | backend | Flyway checksum，动了＝全环境起不来 |
| 改判据语义没强制升版本 | 导入时校验：已发布 release 引用的版本内容不许变 | backend | 就地改 v1 等于改了它在历史 release 里的样子 |
| 改判据没回归立案样本 | 每条判据挂立案样本，改判据自动跑那一组 | backend | 两次栽在过拦／漏拦上 |
| 改 prompt 没跑两侧回归 | 两组回归集（该禁的／不该塌的），改完都跑 | reportgen | 一次纯由措辞引发的回归，六跑里三跑 |
| **投影规则在 Java 与 Python 各写一遍** | 消除重复（**这条是技术债，不是加门禁**——写成规矩等于把缺陷固化） | backend | 改一处必须记得改两处 |
| 手起 gradle 服务会吃到本机全局的 Java 8 | 起服务的入口自己 export JDK21（`.githooks/pre-push` 里已有这一句，但它只管 push），或做成一个起服务脚本 | backend | 8-31 晚重启 project-svc 当场 `BUILD FAILED`（`requires at least JVM runtime version 17`） |
| 改了 worker 的代码不重启 worker，跑的还是旧码 | 同"改 aipipe workflow 要重启两个进程"那条，射程要扩到 reportgen/reportrender-worker；最好由起服务脚本统一管 | reportgen / reportrender | 8-31 晚比率改百分数，两个 worker 都得跟着重启才生效 |

同批还判出**一条真规范**（结构拦不住、属设计取舍）：*示范句越多过检率越低，可抄性才是代价不是样本量*
（四轮 A/B 实证，非对称规则 0/6 更差）——**待并入报告规范**，做报告线时先核对那儿有没有写过。

## 要来路时查哪一份

| 想知道 | 查 |
|---|---|
| 入口五段各要做什么、判据是什么 | 《交接/交接文档-用户发图到拿到报告.md》 |
| **三张图怎么做出来的、判据为什么这么定、踩了什么** | **《交接/交接文档-三张图出得来.md》**（8-31，本线最新一份） |
| **三维怎么做出来的、数据契约长什么样、踩了什么** | **《交接/交接文档-三维这条线出得来.md》**（8-31 夜） |
| **控制图通路有没有、候选逐个怎么判、接入代价、建议** | **《评审/控制图通路调研-2026-09-02.md》**（9-02 中午） |
| **控制通路下还有哪些失效、各是什么性质、对照物在不在** | **《评审/失效清单-控制图通路-2026-09-04.md》**（9-04，追记一＝改线稿后重跑） |
| **三维线 9-02→9-05 怎么做出来的、判据、并行怎么组织、踩了什么** | **《交接/交接文档-控制图通路落地.md》**（9-05） |
| **接手三维线从哪儿继续、在途物、等拍清单** | **《交接/交接文档-2026-09-06.md》**（9-06 换 session） |
| **三维线主链怎么合拢的、房间身份为什么重设计、实测数与在途物** | **《交接/交接文档-2026-09-06晚.md》**（9-06 晚换 session，**接手先读这份**） |
| **免费闭环怎么上的服务器、真机三跑各照出什么、部署坑在哪** | **《交接/交接文档-2026-09-06-免费闭环上服务器.md》**（9-06 晚） |
| **判官台怎么长成标注底座、获客线母版闭合怎么修好的、9-05 四条裁决的来路** | **《交接/交接文档-2026-09-05-判官台与获客线闭环.md》**（9-05，追记一＝接手链自检） |
| **Stage-2 咨询该怎么答（目标行为）、对话怎么变成标注集** | **《评审/stage2-mock-2026-09-05/》**（spec.md 定行为 · conversations.json 14 条 · annotation-shape.md 标注形态；闭集 12 类与"类事后归纳"裁决冲突、射程等拍） |
| 解析、几何、出图的来路与技术债 | 《交接/交接文档-户型图解析与出图启动.md》（追记七＝本线接手总览） |
| 整册怎么跑、怎么判读 | 《交接/交接文档-整册能不能出得来.md》 |
| 报告线怎么从 1/12 收敛到 5/6 | 《交接/交接文档-2026-08-30.md》追记一~十一 |
| 仓、服务、端口、队列、凭证 | 《仓库拓扑.md》 |
| 规矩与判据 | 《规范/协作与文档纪律.md》 |
| 全部裁决 | 《规范/落地假设与拍板清单.md》 |
