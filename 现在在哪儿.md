# 现在在哪儿

> **活文档** · v1.3 · 2026-08-31（失败章重开 + 报告里第一条真的量与第一笔真的钱 + 三档质量考卷）。v1.2 同日（整册第一次量出来：四跑一成）。v1.1 同日（母版落地；几何补参照系与外轮廓）、v1.0 同日建档。内容取自《规范/落地假设与拍板清单.md》末两条裁决、
> 《交接文档-整册能不能出得来.md》、《交接文档-用户发图到拿到报告.md》追记一~四、
> 《交接文档-户型图解析与出图启动.md》追记七~十一、《交接文档-2026-08-30.md》追记十一——**五处状态合并为一处**。
> **就地更新，不发追记**（《纪律·活文档就地改》）。进度事实只写这儿，日志里不再写状态（《纪律·状态写在状态页》）。
> 拓扑事实（仓、服务、端口、队列、凭证）在《仓库拓扑.md》；规矩在《规范/》；来路与证据在《交接/》日志。

## 目标

**尽快让真人用上**（用户裁决 2026-08-30 晚）：真人发一张户型图 → **拿到三张图** → 拿到一份能打开的报告。

## 下一件

**先做三张图那条腿**（用户裁决 2026-08-31，最新，排期改判）：通过 chat 收集用户信息 → 生成三张免费的图 →
**飞书直接把图回给业主**。报告那半不废，已通的出口原样保留，接线时点＝三张图这条腿走通之后。

**两股并行**（用户裁决 2026-08-31：母版可与共用段并行）：

- **接线那股（有先后）**：会话收到图、上报业务侧 → 业务侧铸解析任务并派发 → 渠道出站把我们桶里的图
  发回业主（触发条件"生成的图要送回会话时"**已到期**，这条腿要用）。
- **画图那股（不等接线）**：母版绘制 → 三张图。它吃的是几何提取的产物，几何已能跑，**本地拿一张图
  就能画**，不依赖会话侧那两段；接线一通直接对接。

其中第 3 项的**母版已经做完**（8-31，见下面第二条线）——剩三张图那一步。

**~~另有一件挂着：量整册跑一次~~——已做完**（8-31，换 session 执行）：**整册出得来，四跑一成**。
经过、判据与新暴露的三件事见《交接文档-整册能不能出得来.md》追记二。**报告那半下一步不是继续磨成品率**
（那要按章修、逐条啃），排期仍以三张图那条腿为先。

**待评估一件（已交出去）**：《交接文档-比率怎么印给业主.md》——正文里 10 处把比率印成了裸小数
（「遮光率不低于 0.9 比率」「封闭收纳占比就用 0.8」），业主读不懂。执行者提的改法**撞渲染层红线第 3 条
（数字禁换算，且该红线自己写明"遇到即上报待裁"）**，故按用户裁决交由另一个 session **先评估合不合理、
合理再做**。

**追记二里留了一件要你拍的**：把「prompt 自己不许写它禁的词」从测试夹具挪进 `verify_seeds.py` 自动拦住——
开关一开当场拦下三个域共 6 处（ergonomics 净宽 3 · lighting 照度 2 · material 优异 1），等于三章数据一起动。
是现在一起改，还是各域下次动工时顺手改，按《纪律·按定错了会波及什么取舍》该由你定。

## 三条线

### 一、图从会话进来（五段，顺序即依赖）

| 段 | 状态 |
|---|---|
| 渠道把图取下来落私有桶 | **真机通了**（8-31 上午）：飞书发来的真户型图落桶，键由内容哈希推得、取回重算相等，无签名直取 403，取图到落桶约 1 秒 |
| 会话收到图，上报业务侧 | **未做**——会话侧至今没有通向业务侧的出站客户端，那个接入点仍是 `UNIMPLEMENTED`。**这是下一件的第一段** |
| 业务侧铸解析任务并派发 | 未做 |
| 解析产物回流，报告变成"你这套户型"的 | 未做（属报告那半） |
| 链接送回会话 | 未做（属报告那半） |

会话侧另两处**已改完并上服务器**（8-31）：不再逼业主拿卷尺量房（推算是主路径，实测尺寸照收不逼问）；
回话按思维停顿分条发出（写的模型自己标断点，不另起切分模型）。

**未拍板一件**：会话侧自抄一套"最小必要输入"，与业务侧槽位词汇对不上，业务侧那个流程定义分发接口
会话侧一次没调过——做"会话收到图、上报业务侧"那一段时一并提请。同时一并改的还有得房率追问的台阶
（触发条件写死＝会话侧下一次动工时）。

### 二、图变成几何、特征与三张图

| 段 | 状态 |
|---|---|
| `floorplan-parse`（图 → 户型特征 + 依据） | **可用**：分区读 11 次调用、约 38s；厨房形态 3/3，阳台设备位 2/3，朝向 3/3 |
| `floorplan-geometry`（图 → **外轮廓**/墙线/门窗洞/房间遮罩） | **可用**：**零模型调用**、约 2.3s；三份存档 61 段墙逐字完全相同，自证覆盖率 97.3%。8-31 补两处（都是母版一消费它就撞上的）：**产物带上自己的参照系**（x 按图宽归一、y 按图高归一，不知道原图多宽多高就画不出正确形状）、**补外轮廓**（网格投票表达不了飘窗那种台阶，而飘窗那几条边在图上是细窗线、被去家具线的开运算抹光，一个墙像素不剩） |
| 特征进报告 | **链路通且已见成品**：某册一张卡逐字写着"你家阳台画了洗衣机和柜体虚线框，说明它不是晾衣服的地方，而是要天天动手干活的家政区" |
| 母版绘制 | **做完**（8-31）：几何 → 母版 + 墙体图层 + 房间遮罩索引图 + 房间锚点；确定性、零模型调用，同一份几何画两次**逐字节相同**。**纯库 + CLI**，接进 activity 的时点＝派发链路接通时。外圈闭合率 **94%**（自证数，低于 0.90 整张不出——上线当天就拦下一张 64% 的） |
| 三张图 | **零**——文字层与图形层分离，几何跟随度实测后收口。图像模型已接通（火山方舟 Seedream 在网关，`atmosphere-visual`/`realism-pass` 两个逻辑名，8-23 实测四发全过） |
| 空间推理背书 | 机制已定、**实现零**（声明字段，不与占位符统一） |

### 三、报告出得来（成文 → 装配 → 册检 → 出册）

| 段 | 状态 |
|---|---|
| 成文到出册 | **单章跑得通**：lighting 最近一批 6 跑出册 **5/6**，过检那几跑平均重写 0.8 轮，一跑一稿过检 |
| 成品质量 | 最近五份成品 26 张卡：**禁词 0、叠字 0、裸标识名 0**，五份全渲到桶 |
| 出口 | **全通、真跑过**：册进私有桶、签名链接 `HTTP 200`、手机上能读；无签名直取 403 实测过 |
| **六章整册** | **出得来**。8-31 上午量得四跑一成（失败散在各章不集中，六章各自八九成连乘即约四分之一）；**下午加了「失败章单独重开」**（`max_unit_retries` 默认 2，只重派失败域、已成的章原样留用），此后三跑三成册，其中两跑**全靠重开救回来**（重开前那几章是失败的）。重开救的是随机失败，救不了一章本身就弱 |
| **报告里的「量」与「钱」** | **第一条量、第一笔钱都出来了**（8-31）：套内面积（建筑面积 × 得房率）→ 全屋收纳总长 22.0–30.8 米；水电改造人工费 **6600–7480 元**（单价 60–68 元/㎡ × 建筑面积 110㎡）。两条都**算在求值线不在报告线**，且带推导原文进包——**算出来的数不带推导，写作步就会编一个**（真跑逮到过一次） |
| **造价章的处境** | **五条单价里只有一条接得起来**：五项的"量"只有水电人工那条有（＝建筑面积），其余四条（拆除面积/墙面展开/柜体投影/点位数）**一个都不存在**，全部指向同一个上游——定稿平面。"有单位不等于有量"：三条单价 unit 字面都是「㎡」但要的是三个不同的量，乘哪个量由数据说（单价资产 `props.quantity_basis`） |
| **质量基线** | **有固定考卷了**：三档拟真上游包（`full` 58/0 · `partial-gaps` 55/3 · `mostly-gaps` 12/46，同一户同一批 58 条落点，只差上游算出来几条），跑法 `uv run python -m tests.quality_baseline --tier X --runs N`。此前每次测都用真求值线当场算的包，release 一变卷子就变，**成品率跨天不可比** |

六域当前 release：**`budget@v13`**（8-31：禁词分组、`quantity_basis` 金额投影）/ `ergonomics@v10` /
`lighting@v13` / `material@v9` / `softdeco@v9` / **`storage@v11`**（8-31：收纳总长改由公式求得）。

**两件本次量出来、要用的事实**：①取链接那个接口**出册了也可能 500**——跑着的 project-svc 若起动时没
`source ~/.ishome/oss-local.env`，四个 OSS 配置缺省空串、签名必抛（不是代码回归）；②**跨章漏词只有整册跑
照得出**：成册正文里「延米」出现在 softdeco 章，而它是 storage 的域内禁词——每章只按自己那份表判。

## 各仓（2026-08-31 实测）

| 仓 | 领先远端 | 备注 |
|---|---|---|
| ishome-aipipe | 6 | 8-31 +2：几何参照系、几何外轮廓 |
| ishome-reportgen | 1 | 8-31：推导步打回按组给路（两步同形）+ 造价域两组 |
| ishome-backend | 1 | 8-31：budget persona 不再写自家禁词、域内禁词分组（已发 budget@v12） |
| ishome-render2d | 3 | 8-31 +2：母版绘制（纯库 + CLI）、母版画外轮廓 |
| ishome-frontend | 0 | 工作区 1 处未提交改动 |
| 其余六仓（ishome / contracts / imagegen / infra / render3d / reportrender） | 0 | 已推平 |

**同一批仓上可能有别的 session 在干活**：上面那几处未提交改动可能是在途工作，别动、别 `git add -A`
（《纪律·显式 add 路径》）。push 时机由用户定（《纪律·push 由用户定》）。

## 已知工程缺口（结构没拦住的，2026-08-31 从七份日志的坑单判出）

按《纪律·能自动拦住的不写成纪律》：这些**不是纪律，是缺陷**——每一条都曾被当成"下次记得注意"，
实际该由结构拦住。**都还没修**；**登记在中控仓，不进代码仓**（那几个仓可能有别的 session 在动），
做到哪条线时随手带进去。

| 缺口 | 结构上怎么拦 | 落到 | 证据 |
|---|---|---|---|
| 跨仓／编排的改动，两侧单测全绿也可能整条线卡死 | 门禁加一条真派发的全链冒烟（一次派发跑到出册） | aipipe / backend | 踩过三次；加出册那步时卡在重试到超时才发现 |
| 生产侧加顶层字段，消费侧没同批认下 | contracts 发版跑消费侧契约测试；schema 变更 CI 检查所有消费方 | contracts | 一天两次；第二次成文已过、死在出册 |
| 本地自查与门禁不是同一条命令 | 自查做成一个脚本，门禁跑同一个脚本，没有第二条命令可跑 | 各仓 | 给 mypy 传路径覆盖了配置里的 files，本地绿门禁红 |
| 历史迁移文件可以被改动 | pre-push 检查已应用的迁移在 git 里有没有被改过 | backend | Flyway checksum，动了＝全环境起不来 |
| 改判据语义没强制升版本 | 导入时校验：已发布 release 引用的版本内容不许变 | backend | 就地改 v1 等于改了它在历史 release 里的样子 |
| 改判据没回归立案样本 | 每条判据挂立案样本，改判据自动跑那一组 | backend | 两次栽在过拦／漏拦上 |
| 改 prompt 没跑两侧回归 | 两组回归集（该禁的／不该塌的），改完都跑 | reportgen | 一次纯由措辞引发的回归，六跑里三跑 |
| **投影规则在 Java 与 Python 各写一遍** | 消除重复（**这条是技术债，不是加门禁**——写成规矩等于把缺陷固化） | backend | 改一处必须记得改两处 |

同批还判出**一条真规范**（结构拦不住、属设计取舍）：*示范句越多过检率越低，可抄性才是代价不是样本量*
（四轮 A/B 实证，非对称规则 0/6 更差）——**待并入报告规范**，做报告线时先核对那儿有没有写过。

## 要来路时查哪一份

| 想知道 | 查 |
|---|---|
| 入口五段各要做什么、判据是什么 | 《交接/交接文档-用户发图到拿到报告.md》 |
| 解析、几何、母版与三张图怎么做、技术债在哪 | 《交接/交接文档-户型图解析与出图启动.md》（追记七＝本线接手总览） |
| 整册怎么跑、怎么判读 | 《交接/交接文档-整册能不能出得来.md》 |
| 报告线怎么从 1/12 收敛到 5/6 | 《交接/交接文档-2026-08-30.md》追记一~十一 |
| 仓、服务、端口、队列、凭证 | 《仓库拓扑.md》 |
| 规矩与判据 | 《规范/协作与文档纪律.md》 |
| 全部裁决 | 《规范/落地假设与拍板清单.md》 |
