# 开发规范与代码分层

> 版本：V1.1（2026-08-22；V1.1 增补 §五 变化轴与抽象规则，原 §五-§七 顺延）
> 定位：补足《AI装修效果图产品·技术架构方案》§六。那一节定的是**基础设施级**命名（服务/proto/事件/表/REST/错误码）与工具链；本文定**代码级**：服务内部分层、类命名、函数命名、变量命名、领域术语表。
> 原则沿用技术架构总纲第 3 条：**规范不进 CI 等于不存在**。本文每条规则都标注执行工具；标注"评审"的条目进 code review checklist，其余全部挂流水线。

---

## 一、服务内部分层

三种服务形态各一套分层，**不搞一套通吃**——BFF 套四层是过度设计，Temporal 服务不分 workflow/activity 是事故。

### 1.1 Java 业务服务（identity / estate / content / trade / shelf / channel）

DDD-lite 四层。不引入完整 DDD 战术模式（聚合根事件溯源那套）——8-15 人团队不支持，四层够用：

```
com.{code}.{service}/
  interfaces/       # 入站适配：REST controller、MQ consumer、定时任务入口
  application/      # 用例编排：事务边界在这里，跨模块协调、发领域事件
  domain/           # 实体、值对象、领域规则、repository 接口（port）
  infrastructure/   # repository 实现、Mapper、外部服务 client、MQ producer、缓存
```

依赖方向（ArchUnit 锁死，违规挂 CI）：

| 规则 | 理由 |
|---|---|
| `interfaces → application → domain`，`infrastructure → domain`（实现其接口），禁止反向 | 领域规则不感知技术细节，换存储/换框架不动 domain |
| domain 层禁止 import `spring-web`、`mybatis`、`servlet` 任何类 | 同上 |
| controller 禁止直接调 repository / Mapper | 事务边界必须在 application，绕层=事务和权限校验被绕过 |
| `@Transactional` 只允许出现在 application 层 | 事务边界唯一化 |
| 跨服务调用只允许走 contracts 生成的 SDK client | 禁手写客户端（§2.2 既有规则），ArchUnit 补代码侧执行 |

**豁免区（写死范围）**：RuoYi 基座代码生成的运营 CRUD（小区库、交付日历、选品池维护页）走基座自带 controller-service-mapper 三层，不强套四层。豁免只限 admin 侧基座模块，业务服务内不豁免。

### 1.2 Java BFF（c-bff / admin-bff）

BFF **没有 domain 层**——BFF 里出现业务规则就是分层泄漏，规则下沉到域服务：

```
interfaces/   # controller，按端侧场景组织（不按下游服务组织）
aggregation/  # 编排多个下游 SDK 调用、裁剪拼装端侧 DTO
clients/      # contracts 生成，禁手写
```

ArchUnit 规则：BFF 内禁止出现 `@Transactional`、禁止依赖 mybatis/flyway（BFF 无库，出现即报警）。

### 1.3 Python 生成域（genpipe-svc / genpipe-worker / design-svc）

沿用 §6.3 的领域纵切，把每个 domain 内的横层文件名定死，并补 Temporal 专属形态：

```
src/{domain}/
  router.py       # FastAPI 入站（svc 有，worker 无）
  service.py      # 用例层
  repo.py         # 存取
  models.py       # pydantic 领域模型
  workflows.py    # Temporal workflow：只编排，禁止任何 IO（genpipe-svc / design-svc）
  activities.py   # Temporal activity：所有 IO 收口在这（worker 为主）
```

import-linter contracts（写进 pyproject，违规挂流水线）：

| 规则 | 理由 |
|---|---|
| `workflows.py` 只允许 import `models` 和 activity 存根（签名），禁止 import repo / requests / 任何外部 SDK | workflow 必须确定性可重放，这是 Temporal 硬约束，不是风格偏好 |
| `router → service → repo` 单向 | 同 Java 分层逻辑 |
| 跨 domain 只允许 import 对方 `service`，禁止 import 对方 `repo` | 数据属主唯一 |
| `packages/{scoring,adapters,patch-engine}` 禁止反向依赖 `services/` | 库不感知使用者 |
| 禁止 `utils/`、`common/`、`helpers/` 目录 | §6.3 既有规则，重申并由 import-linter 的目录白名单执行 |

### 1.4 前端（TS）

两条：apps 只做装配，业务逻辑进 packages；API 调用只走生成的 api-client，仓库内 grep 到手写 `fetch(`/`axios` 直连后端即 CI 报警。不再展开——前端细则等 c-app 技术选型（小程序框架）拍板后补。

---

## 二、类命名

### 2.1 Java 类后缀表（Checkstyle 正则强制）

| 角色 | 命名 | 所在层 | 示例 |
|---|---|---|---|
| 入站控制器 | `XxxController`，Xxx=资源复数名词 | interfaces | `FloorplansController` |
| MQ 消费者 | `XxxConsumer` | interfaces | `CandidateScoredConsumer` |
| 用例服务 | `XxxAppService` | application | `ClaimAppService` |
| 领域规则 | 按职责命名 `XxxPolicy` / `XxxCalculator` / `XxxValidator` | domain | `TouchFrequencyPolicy` |
| 仓储接口 | `XxxRepository` | domain | `EstateRepository` |
| 仓储实现 | `XxxRepositoryImpl`（内部调 `XxxMapper`） | infrastructure | `EstateRepositoryImpl` |
| 外部客户端 | `XxxClient`（contracts 生成） | infrastructure | `ContentClient` |
| 渠道适配器 | `XxxChannelAdapter` 实现 `ChannelAdapter` 接口 | infrastructure（channel-svc） | `FeishuChannelAdapter` |
| MQ 生产者 | `XxxProducer` | infrastructure | `DesignEventProducer` |
| 事件类 | `Xxx{PastVerb}Event`，与 CloudEvents type 一一对应 | domain | `CandidateScoredEvent` ↔ `genpipe.candidate.scored` |
| 枚举 | `XxxStatus` / `XxxType` / `XxxState`，值 UPPER_SNAKE 且与 DB 存的字符串逐字一致 | domain | `ReviewStatus.PENDING` |

**禁用后缀（Checkstyle 正则拦截）**：`XxxManager`、`XxxHelper`、`XxxUtil(s)`、`XxxHandler`（MQ/事件入口用 Consumer，其余场景说不出职责就说明类该拆）。兜底名是"不知道该叫什么"的自首。

### 2.2 DTO 家族（按层区分，禁止裸 `XxxDTO`）

| 层 | 入参 | 出参 |
|---|---|---|
| interfaces（contracts 生成为主） | `XxxRequest` | `XxxResponse` |
| application | `XxxCommand`（写）/ `XxxQuery`（读） | `XxxResult` |
| infrastructure 持久化 | `XxxPO` | 同左 |

裸 `DTO` 后缀信息量为零，Checkstyle 拦截。

### 2.3 Python 类

- 类 PascalCase、函数/变量 snake_case、常量 UPPER_SNAKE——PEP 8 由 ruff 管，不重复展开。
- pydantic 领域模型用领域名词**原样**、不带后缀：`ProjectState`、`PatchOp`、`BaseFacts`、`PlanMaster`。ORM 类加 `Record` 后缀（`ProjectStateRecord`）与 pydantic 模型区分。
- Temporal workflow 类：`XxxWorkflow`（`DesignProjectWorkflow`、`GenBatchWorkflow`）。

### 2.4 Temporal activity 命名（进 contracts 级管控）

- 注册名 = 架构文档里的 kebab-case 名，**显式声明**：`@activity.defn(name="floorplan-parse")`；函数名 = 同词 snake_case 动词前置：`parse_floorplan`。
- 文档 activity 清单（floorplan-parse / plan-layout-solve / plan-rule-check / plan-2d-render / atmosphere-visual / scene-compile / base-render / realism-pass / consistency-check / compliance-check）与代码注册名一一对应，**只增不改**——改注册名会破坏历史 workflow 重放，等同于改线上协议。清单放 contracts 仓，新增走 PR 评审。

---

## 三、函数命名

| 类别 | 规则 | 示例 |
|---|---|---|
| 查询 | `get`=必得（取不到抛异常）；`find`=可空；`list`=集合；`count`=计数。Java camelCase / Python snake_case 同规则 | `getFloorplan` / `find_scale_anchor` |
| 命令 | 领域动词原样：`create` / `confirm` / `claim` / `publish` / `rollback`，禁止用 `update` 兜所有写操作 | `confirmBaseFacts`，而非 `updateFacts` |
| 转换 | `to_xxx` / `from_xxx` | `to_cloud_event` |
| 事件处理 | `on{PastVerbEvent}` | `onCandidateScored` |
| 判断 | `is` / `has` / `can` 前缀，返回 boolean；禁止否定式命名（`isNotReady`、`disableCheckSkipped` 这类双重否定） | `canSendProactive` |
| 禁用 | `process` / `handle` / `do` / `execute` 万能动词（说不出动作就说明函数该拆）——工具拦不住语义，**评审 checklist 项** | — |

---

## 四、变量命名与领域术语表

### 4.1 量纲入名（本项目最重要的一条变量规则）

装修域全是尺寸和金额，单位错误是真实事故源。数值变量**必须带单位后缀**，Checkstyle/ruff 自定义规则拦截无后缀的尺寸词：

- 长度 `_mm`（全链路毫米，禁止 cm/m 混用）：`wall_length_mm`
- 面积 `_sqm`：`usable_area_sqm`
- 金额 `_cents`（§6.4 "金额 int 分" 的变量侧执行）：`price_cents`
- 时长 `_ms` / `_seconds`：`render_timeout_seconds`

### 4.2 领域术语中英对照表（唯一真源，放 contracts 仓 glossary）

没有这张表，同一概念会在代码里长出 `huxing` / `apartment` / `layout` / `floorplan` 四种写法。术语表放 contracts 仓（`glossary.md` + 枚举 proto），三语言 SDK 生成，代码中出现表外同义词按 code review 打回：

| 中文（架构文档用语） | 代码用词 | 备注 |
|---|---|---|
| 小区 | `estate` | 与 estate-svc 一致 |
| 户型 | `floorplan` | |
| 母版 | `plan_master` / `PlanMaster` | plan-2d-render 的产物 |
| 比例锚点 | `scale_anchor` | |
| 认领（认领即分叉） | `claim` | |
| 方案套系 | `suite` | content-svc |
| 模板 / 模板库 | `template` / `template_library` | `templateId` 沿用既定值风格（如 `cream-journal`） |
| 交付图集 | `delivery_set` | |
| 确认项 / 确认清单 | `confirmation_item` / `confirmation_checklist` | |
| 认知状态 | `cognitive_state`，枚举 `CognitiveState` | 六值 `observed / inferred / proposed / user_confirmed / measured / verified`，以 Agent 方案 §8 词表为准，**禁止出现同义变体**（如裸 `confirmed`） |
| 尺寸类 / 结构类 | `fact_kind: dimensional / structural` | 信任分轨的代码表达 |
| 硬证据 | `hard_evidence` | 结构类信息的机检依据（V1.3：结构复核概念已废除） |
| 触达 | `touch` | channel-svc 触达策略引擎 → `TouchPolicy` |
| 打分器 | `scorer` | packages/scoring |
| 机检门禁 | `machine_gate` | 发布/交付前的自动检查组合（V1.3：人审概念已废除） |
| 模板验收 | `template_acceptance` | 设计时动作，非运行时环节 |
| 屋主手账（文案voice） | `owner-journal` | 已在模板声明 JSON 中使用，保持 |

### 4.3 通用变量规则

- 集合用复数：`floorplans`，不用 `floorplanList`。
- 布尔变量同函数规则：`is/has/can` 前缀。
- 缩写白名单：`id / url / api / db / mq / im / bff / svc`；白名单外禁止缩写（`cfm`、`tpl` 这类进不了 review）。

---

## 五、变化轴与抽象规则（IM 只是样板）

"IM 多渠道"不是孤例——系统里凡是"同一件事有多个供应方/多种变体"的位置，都是一条**变化轴**。轴处理得好，加变体=加包加配置；处理得不好，变体方言渗进核心代码，每加一个变体全链路开膛。本节把 §六 IM 案例的做法提炼为全系统统一规则，并盘点全部已知变化轴——不在盘点表里的位置默认不建轴。

### 5.1 七条通用规则

**R1 何时建轴（防欠抽象，也防过度抽象）**：满足任一判据才建端口——①第二个实现已写进路线图；②边界对面是我们控制不了的外部协议（IM 渠道、支付、模型 API）。两条都不满足就直接写死，等第二个实现出现时**用它来校验抽象**再重构。"以防万一"的接口是负资产：多一层间接，零次兑现。

**R2 变体优先做成数据，其次才是代码**：判据是"新变体能否不发版、由运营/配置完成"。参数差异 → 数据（交付图模板库：新模板 = 新 JSON 声明 + 素材，0 代码）；协议/行为差异 → adapter 代码（渠道、支付）。能做成数据的轴，禁止做成代码。

**R3 稳定核 + 方言隔离**：每条轴定义一个变体无关的**统一模型**（统一消息模型、BaseFacts、统一支付单），进 contracts、字段只增不删；变体方言只存在于 adapter 包内和 `raw_*` 扩展字段里。核心域代码只说统一模型的语言。

**R4 变体名三处白名单（§6.2 规则的全轴推广）**：变体标识只允许出现在 ①adapter 包 ②配置键 ③数据字段值。其余任何位置——核心服务源码、事件名、统一模型字段名——grep 到变体名字面量即 CI 报警。grep 清单由各轴注册表自动生成，注册表加一行，拦截规则自动多一条。

**R5 按能力分支，不按身份分支**：核心代码禁止 `if (type == FEISHU)` 式身份判断；变体差异用**能力声明**表达，分支写成 `if (channel.supportsCard())`。能力缺失时的降级决策集中在策略引擎一处，不散落在各调用点。新变体接入 = 声明自己的能力，而不是改遍所有分支。

**R6 注册表纪律**：一轴一张注册表（contracts 仓），标识只增不改；**同一现实实体跨轴共用同一标识**——`feishu` 在消息轴和身份轴必须是同一个值。需要区分实例时统一用两级 `type × instance`，不新造 type。

**R7 抽象的验收标准（PR checklist，评审）**：加一个新变体 = 新 adapter 包（或新数据行）+ 配置 + 注册表一行，**既有代码 0 处修改**；删一个变体 = 删包删配置。做不到 = 方言已泄漏，打回。反向约束：一轴只许一层 adapter，禁止 adapter 套 adapter；引入新抽象的 PR 必须指认它对应哪条轴（哪项路线图 / 哪个外部协议），指认不出 = 打回。

### 5.2 本系统变化轴盘点

| # | 变化轴 | 首发实现 | 已知后续 | 形态（R2） | 端口 + 统一模型 | 标识 |
|---|---|---|---|---|---|---|
| 1 | IM / 触达渠道 | 飞书 | 企微、微信系、短信 | 代码 adapter | `ChannelAdapter` + channel.v1 统一消息模型 | `channel_type × instance` |
| 2 | 登录身份源 | 飞书 OAuth | 微信系 | 代码 adapter | `IdentityProviderAdapter` + external_identity 统一身份表 | 复用轴 1 的 channel_type 注册表（R6） |
| 3 | 生成模型供应商 | 首发模型组 | 换模型/多模型是常态 | 配置为主 | LiteLLM 网关（该轴的 adapter 层已存在） | 任务级逻辑模型名 `{activity}.{variant}` |
| 4 | 交付图模板 | 温暖奶油之家等 3 套 | 运营持续上新 | 纯数据 | 模板声明 JSON（Agent 方案 §10） | `templateId` |
| 5 | 规则/检测项 | plan-rule-check、compliance 首批 | 持续增补 | 数据优先，行为型入注册表 | 规则表 + `Checker` 接口 | `rule_id` |
| 6 | 打分维度 | scoring 首批维度 | 增维 | 代码注册表 | `Scorer` 接口（packages/scoring） | `scorer_id` |
| 7 | 支付渠道 | 微信支付 | 支付宝（R1 判据②：外部协议） | 代码 adapter | `PaymentAdapter` + 统一支付单 | `pay_channel × merchant` |
| 8 | 联盟商品源 | 首发联盟 | 多平台 | 代码 adapter | `AffiliateSourceAdapter` + 统一商品/归因模型 | `source_type` |
| 9 | 户型输入源 | 用户上传、户型库检索 | 开发商图纸、量房数据 | 代码（输入归一化层即该轴端口） | 归一化统一产物 BaseFacts | `source_type` |
| 10 | 端侧 | 小程序 + H5 | App | BFF 裁剪 | c-bff 按场景聚合（端差异收口在 BFF，不进域服务） | `client_type` |

两行特别说明：

- **轴 3（模型供应商）**：LiteLLM 已是该轴的 API 层 adapter，`packages/adapters` 只做各模型的提示词/出入参变换，**禁止再造一层 API 适配**（R7 一轴一层）。业务代码只引用任务级逻辑模型名（如 `base-render.default`），逻辑名 → 物理 model_id 的映射在 LiteLLM 配置里——换模型是改配置，不是改代码。
- **新增轴**：入表走 PR 评审，与注册表同一纪律。表外位置想引入 port/interface 的，按 R1/R7 在 PR 里指认判据，指认不出不予合并。

---

## 六、多渠道（IM）命名专项（§五规则的样板案例）

IM 可插拔（技术架构决策五）落到命名上是四条规则，即 §五 R3-R6 在渠道轴上的具体化。核心思想：**渠道名是配置数据，不是代码结构**——渠道名允许出现的位置越少，可插拔越真。

### 6.1 渠道标识两级：type（协议）× instance（接入实例）

- **channel_type**：一种接入协议/平台一个标识，小写 snake_case，注册表放 contracts 仓（枚举 `ChannelType`），**只增不改**。
- **channel_instance**：同一协议下的一个具体接入（一个飞书自建应用、一个公众号主体），格式 `{type}:{instance-slug}`，如 `feishu:ishome-prod`。多主体、多环境靠 instance 区分，不新造 type。

首批注册表：

| channel_type | 对应 | 命名理由 / 坑 |
|---|---|---|
| `feishu` | 飞书（open.feishu.cn） | 用产品通用英文小写。**保留 `lark` 给国际版**（open.larksuite.com）——域名、账号体系不同，是另一个渠道，不是同一个 |
| `wecom` | 企业微信 | 微信生态**禁止统称 `wechat`**：公众号 / 小程序客服 / 企微是三套 API、三种身份体系，一个名字盖三套是未来重构的定时炸弹 |
| `wechat_oa` | 微信公众号（服务号） | 同上，待拍板⑤落地时启用 |
| `wechat_mini` | 小程序客服消息 | 同上 |
| `sms` | 短信触达 | 触达级渠道与会话级渠道同一套注册表，靠能力声明区分，不靠命名区分 |

命名规则：取平台**开放平台的通用英文名**小写；无英文名的用拼音（如 `feishu`——飞书开放平台自己就用 feishu 域名）；禁止自创缩写。

### 6.2 渠道名的"出现白名单"（可插拔的命名侧执行）

渠道名（`feishu`、`wecom`…）只允许出现在三个地方，其余位置出现即违规：

1. **channel-svc 的 adapter 包内**：`com.{code}.channel.infrastructure.adapter.feishu`——一个渠道一个独立包，包=可插拔单元；类名 `FeishuChannelAdapter implements ChannelAdapter`。
2. **配置**：`channel.{type}.{instance}.{key}`，如 `channel.feishu.ishome-prod.app_id`。
3. **数据**：`channel_type` 字段的值、网关路由 `/api/v1/channels/{channel_type}/webhook` 的路径参数。

执行：ArchUnit 禁止 channel-svc 之外的任何服务 import adapter 包；CI grep 规则拦截 design-svc / c-bff 源码中出现渠道名字符串字面量——"design-svc 不许有渠道分支"这条架构规则的代码侧兑现。

### 6.3 事件与消息模型：渠道名不进事件名

- CloudEvents 事件名渠道无关：`channel.message.received`、`channel.message.sent`，**禁止** `channel.feishu.message.received` 这类命名——渠道信息放事件属性 `channel_type` / `channel_instance`。事件名带渠道=每加一个渠道加一族事件=订阅方全得改。
- 统一消息模型（contracts channel.v1）字段名用抽象词：`quick_reply`、`card`，不用渠道方言（飞书的"消息卡片"、微信的"图文"）；渠道方言只存在于 adapter 的转换代码里。

### 6.4 外部身份字段：通用名 + 原始档案

identity-svc 的多渠道身份表用统一字段名，不把某个渠道的专有字段名提拔成通用字段：

- `external_user_id`：该渠道内的用户标识（飞书填 open_id，企微填 userid）
- `external_union_id`：跨应用归一标识，可空（飞书填 union_id，公众号填 unionid）
- `raw_profile`（JSON）：渠道原始字段原样存档，字段名保留渠道方言

唯一键 `(channel_type, external_user_id)`。禁止表字段叫 `open_id`——那是飞书方言，第二个渠道接入时就名不副实。

---

## 七、落地方式

| 规范 | 执行工具 | 违规动作 |
|---|---|---|
| Java 分层依赖、层内禁令 | ArchUnit 测试（随 backend 仓 shared/kernel 提供基础规则集） | 挂 CI |
| Java 类后缀 / 禁用后缀 / DTO 家族 | Checkstyle 自定义正则 | 挂 CI |
| Python 模块依赖方向、目录白名单 | import-linter（pyproject 内声明） | 挂流水线 |
| 量纲后缀 | Checkstyle / ruff 自定义规则（尺寸关键词 + 无后缀 = 报警） | 挂 CI |
| activity 注册名只增不改 | contracts 仓清单 + PR 评审 | 阻塞合并 |
| 术语表、认知状态词表 | contracts 仓 glossary + proto enum，三语言生成 | 同义词评审打回 |
| 变体名三处白名单（R4，全轴） | CI grep 规则，清单由各轴注册表自动生成 | 挂 CI |
| 新变体 0 修改验收、新抽象指认变化轴（R1/R7） | PR checklist | 评审打回 |
| 变化轴盘点表新增轴、各轴注册表增项 | PR 评审 | 阻塞合并 |
| 万能动词、语义类命名 | code review checklist | 评审打回 |
| **新服务/新 domain 脚手架** | backend 出 Gradle 模板、aipipe 出 copier 模板，**分层靠模板生成，不靠人记** | — |

---

## 八、与既有文档的关系

- 技术架构方案 §六保留基础设施级命名与工具链，本文是其代码级细则；两处冲突时以**后修订者**为准并回改另一处（对齐文档 §八 的既定纪律）。
- 本文术语表与 Agent 方案 §8 认知状态词表、§10 模板声明 JSON 字段保持逐字一致；任何一侧改词表，另两侧同步回改。
