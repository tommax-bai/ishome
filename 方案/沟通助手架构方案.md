# AI 装修设计助手 · 架构描述文档 v0.3

> **收录说明（2026-08-23，收录进 ishome 中控仓）**：本文档在独立上下文中产出，作为**会话域机制基线**收录（三层架构、封闭动作集、产物换反馈、修订收敛、记忆/承诺体系、指标函数均采纳）。**服务拓扑以《架构对齐-设计Agent×技术架构.md》V1.5 §二 为准**——两项拍板（同日用户裁决）：①design-svc 一拆为二 = chat-svc（Python 会话引擎）+ project-svc（Java 项目状态机）；②里程碑引擎采用本文 §7 事件驱动方案，长周期 Temporal workflow 作废、Temporal 收缩至任务层。另有五处归位修订（本文正文未改，以对齐文档为准）：任务队列 D11 的 gen.tasks/gen.results 由 **Temporal** 承接（真相在表/队列只唤醒/消费幂等三原则保留，事件通知走既定 outbox+RocketMQ 规划）；追踪用 **Langfuse** 不引入 LangSmith；消息进出走 **channel-svc** 网关（chat 定投递策略，channel 按能力声明执行；首发飞书，微信=拍板⑤）；**支付与权益归 trade-svc**；M0 户型获取补**户型库命中（优先）/上传解析（兜底）**双路径。本文 §4 的"设计生成服务"对应 genpipe（编排+解析求解）+ render2d/imagegen/render3d 三绘图服务（V1.4 拆分）；slot 表需带 contracts 已定的认知状态六值。

---

> **文档目的**：供独立 review 与落地实施使用，自包含，不依赖原始讨论上下文。
> **版本脉络**：v0.1 通用对话助手设计 → v0.2 任务形状修正为"长程共创"、三层架构 → v0.3 补齐服务拓扑、数据模型、配置分发与消息机制（2026-08-23）。
> **文内标注**：〔已定〕= 已确认决策；〔草案〕= 待验证；〔开放〕= 未决,欢迎 review 挑战。

---

## 1. 产品定义

**一句话**：AI 驱动的装修设计助手,通过微信等对话渠道,引导**不专业的用户**逐步完善信息,按里程碑产出全套**参考级**装修设计图纸——愿景效果图 → 平面布局 → 空间渲染 → CAD 初稿 → 家具/材质/尺寸明细。

**关键性质**：
- 任务形状是**长程共创**（跨天/周、有终点、逐步产出中间产物）,不是一问一答咨询;
- 助手定位为**有情商的任务型助手**：做情感智能（识别情绪→匹配语气→先回应情绪再解决问题→守边界）,不做情感关系（无亲密人设、不挽留、不用愧疚/FOMO做留存）;
- **全自动闭环**：无转人工、无异步人工补答、无任何依赖人力的服务环节;
- 图纸定位**参考级**,不涉入现场施工环节;
- 商业模式挂点：渲染解锁费等（付费权益属项目域,见 §4）。

## 2. 决策记录（review 前提,挑战请针对决策本身）

| # | 决策 | 状态 |
|---|---|---|
| D1 | 有情商的任务型助手,不做情感关系 | 已定 |
| D2 | 不做转人工,降级阶梯替代 | 已定 |
| D3 | 不做异步人工补答;系统完全自闭环 | 已定 |
| D4 | AI 身份明示但不过度强调（静态位常驻+开场一次+问了就认） | 已定 |
| D5 | 极端情绪危机兜底不入 V1（保留意见已记录,复议成本低） | 已定 |
| D6 | "系统好用"为核心目标函数;责任/合规考量入 backlog | 已定 |
| D7 | chat 管线为独立 Python 服务;编排用 LangGraph,仅用静态图+追踪工具链,不用 Command/Send 等动态导航与框架级挂起 | 已定 |
| D8 | 三层架构：项目层纯静态 / 会话层受限动态 / 任务层异步 | 已定 |
| D9 | ①产物换反馈优先于提问 ②里程碑体验顺序可编排（愿景图前置）③修订循环结构化收敛 | 已定 |
| D10 | 配置单一来源：流程定义归业务域,项目服务权威分发,版本固化到项目 | 已定 |
| D11 | V1 消息中间件用 Redis Stream（或 PG SKIP LOCKED）,不上 Kafka;真相在表,队列只做唤醒,消费幂等 | 已定 |

## 3. 三层架构总纲〔已定〕

```
项目层  里程碑状态机 · 纯静态 · 零模型自由度 · 归项目服务(Java)
  └ 会话层  轮内工作流 · 受限动态(封闭动作集) · 归chat引擎(Python)
      └ 任务层  异步生成管线 · 内部可全动态 · 归设计生成服务(Python)
```

**路由裁决依据（六因素框架）**：①路径可枚举性 ②错误暴露面 ③结果可验证性 ④延迟成本预算 ⑤回归测试需求 ⑥分支维护成本。逐层评估结论：
- 项目层：里程碑迁移必须确定性——项目不能被模型"聊丢方向";
- 会话层：路径不可完全枚举(反馈/跑题/情绪/新信息)但错误直面用户且无人兜底 → 动作运行时选、选项集设计时封闭,并受当前里程碑裁剪;**图中不存在模型能自行开辟的路径**;
- 任务层：产物推送前有确认环节,错误不直接暴露 → 管线内部(多步推理/自检/重生成)自由度放开。
- 光谱认知：纯静态 → **受限动态** → 全动态;⑥压力增大时扩充选项集,不跳全动态。

## 4. 服务拓扑与职责〔已定〕

| 服务 | 栈 | 职责 | 明确不做 |
|---|---|---|---|
| **chat 引擎** | Python + LangGraph | 对话全流程;自然语言→结构化(槽位抽取/确认识别/反馈映射);产物推送与确认引导;画像与对话记忆;承诺区;主动消息引擎 | 不生成图;不持有产物(只持引用);不判定里程碑;不判定修订预算;无装修领域代码(领域知识全部来自配置) |
| **项目服务** | Java/Spring(并入既有 monorepo 多服务体系) | 项目状态机与里程碑判定;槽位/产物/任务/修订记录的唯一真相;生成任务创建与编排;修订预算判定;流程定义权威分发;支付与权益 | 不理解自然语言;不执行生成 |
| **设计生成服务** | Python | 户型解析→布局求解→出图渲染;**纯函数式**:结构化入→结构化出,无库无会话概念,不知用户是谁;外部依赖(如 AIDCP)封装在内部 | 不读对话/画像;不发用户消息 |
| **对象存储** | — | 图片/CAD 文件本体;产物元数据(版本/参数/血缘)在项目服务 artifact 表 | — |

## 5. 数据模型（核心表）〔已定,字段级为草案〕

**项目服务(PG)**：
```
project         (id, user_id, current_milestone, process_version, status, ...)
slot            (project_id, slot_key, value, source_event_id, confidence, updated_at)
artifact        (id, project_id, milestone, type, version, storage_url,
                 gen_params_json, lineage, status: generated|presented|confirmed|rejected)
generation_task (id, project_id, type, input_snapshot_json,
                 status: pending|running|completed|failed, artifact_id, deadline, ...)
revision_log    (project_id, milestone, round_no, directive_json, task_id)
```

**chat 服务(PG + Redis)**：
```
conversation / message          # 会话与消息
session_state (Redis)           # 轮内与会话态:阶段/情绪轨迹/槽位缓存
user_profile                    # 画像五区,见 §11;跨项目稳定的个人特征
episodic_memory                 # 会话摘要(结构化表 + pgvector)
commitment                     # 承诺区:类型/内容/deadline/履约动作/状态/关联task_id
```

**关键切分**：项目档案(槽位/里程碑/产物版本)∈ 项目对象;画像只存跨项目个人特征。一个用户可多项目。槽位抽取发生在 chat,但 schema 由配置定义、值以项目服务 slot 表为唯一真相,chat 仅缓存用于组装上下文。

## 6. 流程定义配置：单一来源双消费〔已定 D10〕

- 版本化配置制品(YAML/JSON),归**业务域**所有,放 monorepo 随发布走评审;
- 项目服务为权威分发点:`GET /process-definitions/{version}`;chat 拉取+缓存,不落库不注册;
- **`process_version` 在项目创建时固化到 project 表**,全生命周期同一版;配置演进只影响新项目,存量迁移是显式数据操作;
- 双方消费不同切片:

| 配置内容 | 项目服务 | chat |
|---|---|---|
| completion_criteria / 里程碑编排(进入某里程碑创建什么任务) / 修订预算 | ✅ | — |
| 槽位 schema(类型/枚举/抽取提示/提问模板) / 修订维度词表 / 动作白名单 / prompt 片段 | — | ✅ |

- **配置里只放数据,逻辑永远归服务**:completion_criteria 仅允许简单谓词(槽位齐/产物confirmed);判据需要真逻辑时做成项目服务接口,不发明表达式语法;
- 纪律:保留通用接缝(引擎代码+配置解释器),**不建通用平台**(无可视化编辑器/多租户/DSL,除非出现第二个业务)。

配置样例:
```yaml
milestone: M1_需求画像
  required_slots:
    - key: 风格方向
      type: enum[原木, 奶油, 极简, 复古, ...]
      extraction_hint: "用户对整体感觉/风格/喜好的表述"
      question: {form: 选择题配图, options: [...], default: ...}
    - key: 硬性约束
      type: multi
  allowed_actions: [提问收集, 解释说明, 呈现产物征反馈, 安抚降速, 里程碑推进确认]
  completion_criteria:
    all_slots_filled: [风格方向, 硬性约束]
    artifacts_confirmed: [愿景图]
  revision: {dimensions: [布局密度, 动线, 功能分区, 家具尺度], budget: 3}
  on_enter_next: [create_task(type: layout)]
```

## 7. 项目层：里程碑〔草案——M1/M2 边界待对话样例验证〕

| 里程碑 | 产出物 | 完成判据(机器可判) |
|---|---|---|
| M0 建档 | 项目档案 | 户型图已传;面积/城市/预算区间/家庭结构槽位齐 |
| M0.5 愿景图 | 低成本效果图 | 生成并送达(不求准求心动,无需确认) |
| M1 需求画像 | 需求清单 | 风格方向/功能需求/硬性约束确认 |
| M2 平面布局 | 布置方案图 | 用户确认布局 |
| M3 效果呈现 | 各空间渲染图 | 主要空间认可 |
| M4 CAD 初稿 | CAD 图纸(参考级) | 生成+确认 |
| M5 明细 | 家具/材质/尺寸清单 | 覆盖已确认空间 |
| M6 交付 | 全套打包 | — |

**判定机制**〔已定〕:项目服务内里程碑引擎(确定性代码),事件驱动——每处理完影响判据变量的事件(slot_filled/artifact_confirmed)并落库后,执行 checkCompletion:读判据配置→查 slot/artifact 表→布尔求值→true 则迁移并执行 on_enter 编排。**单点判定,chat 永不判里程碑;不搞定时轮询。**

**编排原则**〔已定 D9〕:产物依赖刚性(无布局则无CAD);体验顺序可编排——M0 后立即 M0.5 愿景图,情绪价值前置("想让它成真,还需确认几件事"再补 M1);需求画像是修订的副产品,M1 问最少→快出初稿→修订循环里逼出需求。

## 8. 会话层：轮内工作流〔已定〕

每条用户消息触发一次图运行,秒级结束;**对话连续性由外置状态承载,不用框架挂起**(用户隔小时/天回复是常态)。

```
消息 → [聚合去抖:合并连发短消息] →
 ① 理解(并行): 意图 / 情绪(类别+强度+置信度) / 槽位与反馈抽取
 ② 状态装载: get_project_state → current_milestone + process_version
             → loadDefinition(缓存) → 裁剪动作集/注入槽位schema与prompt片段
             + 画像/情景记忆topK/承诺区临期项
 ③ 决策: 规则优先+模型辅助 → 封闭动作集内选一
 ④ 执行: 生成路径含校验回路(重试≤2,失败落降级/模板)
 ⑤ 写回: 发业务事件 + 更新会话态 → 投递(分段/打字延迟/时段调度)
```

**封闭动作集**:
```
{ 提问收集, 解释说明, 触发生成任务, 呈现产物并征求反馈,
  解读反馈并修订, 追问澄清, 安抚降速, 里程碑推进确认,
  降级L1/L2/L3, 受控模板(价格承诺等高风险意图) }
```

**理解层关键规则**:
- **抽取看全量槽位表,提问只看当前里程碑缺口**(机会性抽取:用户 M1 时顺嘴说预算也要接住);
- "确认"是业务事实:用户"可以,就这个感觉"→ chat 识别为确认→发 artifact_confirmed;
- 反馈映射 = **LLM 受限映射 + 代码枚举校验**:修订维度词表作为枚举约束进 prompt,输出 {target, dimension, direction};代码层校验,LLM 自创值不采纳→改走"追问澄清";映射失败记缺口遥测(词表扩不扩由数据说话);跨里程碑反馈(M2 抱怨颜色)→引导话术("效果图阶段调,先定布局")。

**交互主策略**〔已定 D9-①〕:不专业用户答不好问题但都会挑毛病——能用产物换反馈的不用提问换陈述;提问只用于硬信息,一次一问,选择题配图,默认值优先;图示代替术语。

**修订收敛**〔已定 D9-③〕:反馈约束在有限维度(每次聚焦一两个);每里程碑修订轮数软预算(预算判定在项目服务),接近时话术收束;修订不收敛=后续里程碑永远到不了,全流程最大的坑。

**情绪规则表**:
| 场景 | 信号 | 规则 |
|---|---|---|
| 决策疲劳 | 回复变短/"都行" | 收窄选项/给默认值/暂停提问 |
| 产物失望 | 对图负面反馈 | 预期管理("初稿定方向,细节一轮轮调")+转结构化修订 |
| 术语焦虑 | 追问名词/答非所问 | 语言降维/图示化 |
| 中途流失 | 里程碑间静默超时 | 承诺区驱动温和召回 |

通用:情绪强度连续 N 轮不降→停止推进里程碑目标,切止损模式;情绪识别低置信→退回中性专业语气(错误安抚比不安抚糟)。

## 9. 任务层与服务交互〔已定〕

**消息拓扑**(V1: Redis Stream 三 topic;真相在表,队列只做唤醒,消费按 task_id 幂等):
```
[gen.tasks]    项目服务 → 生成服务   {task_id, type, input_snapshot}
[gen.results]  生成服务 → 项目服务   {task_id, status, artifact_url, gen_params}
[chat.notify]  项目服务 → chat       {event, project_id, artifact_id/...}
```

**时序A·首版布局(里程碑迁移触发,无需对话发起)**:
```
项目服务: M1判据满足→迁M2→按on_enter创建task(pending入表)→投[gen.tasks]
          →通知chat(task_created,预估时长)
chat:     用户在会话中→顺势承诺("布局初稿约15分钟"),承诺挂task_id
          用户离线→静默等产物
生成服务: 消费→求解→图传对象存储→投[gen.results]
项目服务: 落task=completed→INSERT artifact v1(参数+血缘)→checkCompletion
          →投[chat.notify]{artifact_ready}
chat:     主动消息引擎触发图运行(动作=呈现产物征反馈)→分段推送→承诺按task_id核销
```

**时序B·修订循环("餐厅感觉太挤")**:
```
chat:     理解层受限映射→{餐厅,布局密度,降低}→发feedback_received(结构化)
项目服务: 修订预算检查(如M2预算3轮)→有余额:创建revision task(base:v1参数+指令)
          →无余额:返回budget_exhausted→chat选"引导收束"话术
生成服务: 基于v1参数增量求解→v2→[gen.results]
后续同时序A: 登记v2→推送("餐厅这版留宽了走道,您看是不是舒服些")
```

**失败路径**(无人兜底,必须显式):队列层自动重试N次→仍失败发task_failed→chat诚实告知+改期(承诺改期一次为限)→二次失败:边界声明+里程碑不迁移+缺口遥测。**静默死亡防护**:generation_task.deadline+承诺区到期扫描,超时未完成未失败→同改期流程。全自动系统最怕无声无息。

## 10. 对话通用机制(自 v0.1 继承)〔已定〕

- **降级阶梯**:全量回答→保守回答(只说有把握部分)→边界声明+替代方向;每级都必须体面;
- **话术红线**:永不说"转人工/同事联系您";永不说"我去核实"(无人核实即违约);说能力边界不说身份借口("我只是AI"❌);
- **承诺白名单=自动化能力清单**:只承诺机器自兑现事项(发资料/定时跟进/生成任务交付时间);校验层检测承诺意图→白名单外拦截重写;**承诺兑现率恒100%,低于即bug**;
- **校验层为唯一防线**:含数字/承诺/时间的输出全量溯源事实库;高风险意图(价格承诺类)走受控模板+槽位填充;低成本自我审查调用;
- **身份策略**:静态位常驻(昵称/简介"AI助理")+开场一次;问了一句话确认立刻回正事;永不否认不装糊涂;AI身份是中性事实;
- **投递拟人化**:分段(2-4条)、模拟打字延迟、按时段调度(深夜压次日早);
- **承诺区归属**〔开放〕:当前倾向存chat侧(服务于对话信任,含纯对话承诺),挂生成任务的以task_id关联业务侧,兑现由任务回调触发——review时请从业务建模角度挑战此切分。

## 11. 记忆系统〔已定〕

三层:**工作记忆**(当前会话原文,不压缩)/**情景记忆**(会话结束触发摘要→pgvector+结构化表;**不用原始聊天记录做RAG**)/**语义记忆**(画像,PG)。

画像五区:事实区(带来源时间戳)/偏好区/**承诺区**(一等公民,驱动主动引擎)/沟通风格区/情绪模式区(**存模式不存状态**——"谈价时容易急"可存,"今天生气"是瞬态过期作废)。

写:会话结束异步抽取,**宁可漏不可错**(错误记忆被戳穿的信任伤害>没记住);冲突=新值覆盖旧值留痕("预算20万→15万"本身是信号)。
读:人设(prompt caching)+画像(≤几百token,设计约束逼提炼)+情景topK+工作记忆全量+承诺临期强制注入。
遗忘:重要性评分+最后引用时间,低权重定期压缩淘汰。
红线:记忆与训练管道物理隔离;条目可溯源可删净。

## 12. 指标(目标函数)〔已定 D6〕

核心:**里程碑转化漏斗**(各里程碑推进率)/**首图时间**(进入→收到愿景图)/**修订收敛轮数**(对照软预算)/**承诺兑现率**(恒100%)/召回响应率/全程完成率(M0→M6)。会话层过程指标:首答解决率/槽位收集效率。**任何功能对指标无贡献→不做;设计争论以指标为裁判。**

## 13. 技术栈汇总

- chat:Python + LangGraph(静态图+LangSmith追踪;不用动态导航/框架挂起)+ Redis(会话态)+ PG;
- 项目服务:Java 21/Spring Boot 3,并入既有 monorepo 服务体系;PG(建议:pgvector 顺带覆盖向量检索,亦为 Postgres vs MySQL 悬案的一票);
- 生成服务:Python;任务队列 V1 用 Redis Stream 或 PG SKIP LOCKED,消息体带 schema 版本,量起后换中间件协议不动;
- 模型分层:理解层/自我审查用小模型或低价LLM(每消息必过,成本延迟大头);生成层用大模型;人设 prompt caching。

## 14. 风险与技术验证优先级

1. **户型图→户型结构化数据是全产品技术咽喉**〔重点〕:用户上传的是手机拍售楼书/带水印截图/歪斜扫描件;解析质量决定布局求解上限。要求:独立模块最先做技术验证,用真实脏数据测;**解析置信度作为字段贯穿全链路**——低置信度在推送话术中体现("标红两处麻烦您确认下"),用"产物换反馈"让用户帮系统校准,比追求解析全对现实。此链路法律责任问题已另行安排咨询律师,不入本设计讨论。
2. 修订循环不收敛(见§8机制);
3. 全自动无兜底的静默失败(见§9防护);
4. M1/M2 里程碑边界是否合并〔草案〕→ 对话样例验证。

## 15. Backlog

算法备案/安全评估、《人工智能拟人化互动服务管理暂行办法》适用性分析(商业化上量前);危机兜底剧本(D5,复议成本极低——一行人设约束即最小形态);情绪识别精调;动作集扩充机制预案。

## 16. 落地顺序建议

1. 户型解析模块技术验证(咽喉,先行);
2. 项目服务:核心表+里程碑引擎+流程定义分发;
3. chat 引擎骨架:七节点图+配置解释器+封闭动作集(动作先接死3-4个);
4. 生成服务打桩(mock出图)跑通三服务消息闭环+时序A/B;
5. 决策层规则表+对话样例四场景(建档/愿景图/修订/召回)验证;
6. 影子期:自有账号真实运行+每周离线复盘,动作集按周放开。

---

*v0.3 · 2026-08-23 · D1–D11 已确认;〔草案〕〔开放〕两类标注项为 review 重点。*
